# SERVICE AUDIT — AutoX / เงินไชโย Credit-Intelligence Platform

> Emitted by the autonomous **market & service intelligence** loop. Audits the platform *as a
> service*: data freshness per layer, provenance coverage, broken data references, and heavy-JSON
> load weight. Every number here is read from the committed tree — nothing is estimated or invented.
> Regenerate the underlying ledger with `python3 pipeline/build_provenance.py`.

_Audit run: 2026-08-28 · finding: **PEER read shipped — the listed-peer asset-quality board (`#acq`) now joins its `S3 coverage` column to the SET P/BV multiple: the market pays UP for conservative provisioning (the two thickest-covered books, MTC 71.9% & Tidlor 52.3%, are exactly the two richest-valued names, P/BV 1.50×/1.66×; the thinnest-covered, Srisawad 30.6%, trades below the field's median multiple).** The MARKET/SERVICE/DEPLOY/PEER sweep was otherwise GREEN. **The fix.** The `s3_coverage_pct` column already rendered (`app.js:4141`) but no READ interpreted it against valuation. Added a guarded `covLine` to `drawPeerAssetQuality`'s readout (`app.js:~4146`) joining `peer_asset_quality.json` (ECL coverage, MEASURED from IFRS-9 filing notes, as of 30 Jun 2026) to `peer_scoreboard.json` (SET P/BV, MEASURED) by symbol, consolidated filers only (HENG's unconsolidated basis excluded via `fs_type`). Pearson r(coverage, P/BV) = **+0.70** (n=5 consolidated). DISTINCT from the sibling `aqLine` (P/BV × NPL, below-book *discount* × credit quality): coverage (allowance *held*) ≠ NPL (book *cleanliness*), so this reads the *premium* against provisioning conservatism — stated as such in a method note. **Guarded/self-verifying:** fires ONLY when the two thickest-covered books are EXACTLY the two richest-valued AND the thinnest-covered trades below the median multiple — a vintage that reorders the field silences the claim rather than misstating it; `≥4` consolidated rows; null-guarded; order-independent (fetches the scoreboard once + redraws if `PEERSCORE` isn't populated when the card renders, guarded by `peeraqScoreFetch`, no render loop). **No `platform/data` file changed → no `build_provenance.py` regen.** **Headless self-review (Chromium/Playwright, local `#acq`):** renders once with the exact committed values (MTC 71.9% & Tidlor 52.3% = richest P/BV 1.66×/1.50×; Srisawad 30.6% below median 1.02×), method note renders, join order-independent, sibling NPL ladder + `aqLine` unaffected, 0 code-side console errors; `node --check platform/app.js` clean. **Rest of the sweep, GREEN:** deploy verified live — public production alias `competitive-intel-blue.vercel.app` → **HTTP 200** on `/`, `/app.js`, `/data/meta.json`, `/data/peer_province.json`, `/data/competitor_coverage.json`; the `-git-master-…vercel.app` alias → **302 → `vercel.com/sso-api`** = up + SSO-gated-by-design (healthy-not-outage); `site-health.yml` correctly targets the master alias with the documented blue-alias fallback for deep checks — no fix. Gate **151 passed / 0 failed**; provenance/vintage internally consistent; `check_site_health.py` `DATA_FILES` covers 80+ static reads. **Recorded (NOT a defect, for future runs):** the app's headline vintage is `2026M07` while `farmgate_prices.json` (2026-08-26) & `thai_price_history.json` (2026-08-23) carry fresher measured prices — a candidate SERVICE freshness-transparency read next run, to be surfaced WITHOUT touching the market-pulse swarm's refresh cadence. **This IS a visual/app change → shipped via PR + headless self-review, self-merged per the safeguard protocol.** Next recommended intelligence task: the peer-valuation quartet (ROE→ROA×leverage · P/BV over ROA · P/BV × NPL · P/BV × ECL-coverage) is complete; pivot to the freshness-transparency read above. · against `platform/app.js` (drawPeerAssetQuality coverage × P/BV join)._

_Audit run: 2026-08-27 (b) · finding: **PEER cross-read shipped — the listed-peer scoreboard (`#acq`) now JOINS its P/BV column to the IFRS-9 NPL layer and answers the question run (v) teed up: the market's below-book discount on Saksiam is NOT a credit-quality discount — it runs the 3rd-cleanest book of the field yet trades cheapest, so the discount is leverage appetite, not asset risk.** The MARKET/SERVICE/DEPLOY sweep was otherwise GREEN. **The fix.** Two committed MEASURED layers had never been joined: `peer_scoreboard.json` (SET P/BV, `drawPeerScore`) and `peer_asset_quality.json` (each peer's own IFRS-9 Stage-3 NPL from reviewed filing notes, as of 30 Jun 2026, `drawPeerAssetQuality`). Added an `aqLine` to `drawPeerScore`'s readout (`app.js:~4024`) that joins them by SET symbol (consolidated filers only; ⚠U HENG excluded, mirroring the existing `levLine`/`valLine`) and states the pattern: at the top valuation DOES track asset quality (Ngern Tid Lor — richest multiple 1.62× AND lowest NPL 1.5%), but at the bottom it breaks — Saksiam trades cheapest (0.98×, below book) on a **2.7% NPL, cleaner than Srisawad (3.9%, 1.00×) and Ngern Turbo (3.5%, 1.16×) which trade richer** — so its below-book discount reflects leverage appetite (lowest leverage of the field, D/E 1.28), not asset quality. The line is fully guarded: it fires ONLY when a worse-book peer trades at a richer multiple than the cheapest name (so it can never claim a discount is un-explained by credit quality when the numbers don't show it); each clause (top "reward-clean-books", "below book", "lowest leverage") is asserted only when its own committed value holds; silent if `peer_asset_quality.json` is absent — no stale claim survives a vintage that reorders the field. Join is order-independent: `drawPeerAssetQuality` re-invokes `drawPeerScore` once the NPL layer lands (no render loop — `drawPeerScore` never calls back). A method-box note documents the P/BV × NPL join + source. **No `platform/data` file changed → no `build_provenance.py` regen** (both layers keep their existing MEASURED provenance). **Headless self-review (Chromium/Playwright, local `#acq`):** the cross-read renders once with the real committed values (Tidlor 1.62×/1.5%; Saksiam 0.98× below book on 2.7% NPL, cleaner than Srisawad 3.9% & Ngern Turbo 3.5%; D/E 1.28), the method note renders, join works regardless of fetch order, 0 code-side console errors; `node --check platform/app.js` clean. **Why the prior run's teed-up "#exposure collateral-vintage" remainder was DECLINED (recorded so no future run re-derives it):** the `#overview` collateral-vintage line shipped in the immediately-prior collateral run (commit `7c32f14`, verified live this run — `collateral_book.json` meta carries `mob_anchor=2026-07` + `dlt_fleet_vintage=2025`, both fields present so the guarded line renders real values). The teed-up task also named `#exposure`, but the only DLT-fleet-based collateral read lives on `#overview` (the `#exposure` book-vs-auction check reads a different collateral valuation — book appraisal vs auction recovery), AND that `#exposure` block's auction side (`collateral_census.json`) is anchored to `anchor_date=2026-09-22`, a **forward-scheduled** Union Auction booking, not a capture date — so a naive "auction data as of …" vintage line there would MISSTATE currency (an exec would read a future date as stale-in-reverse). The `#exposure` collateral-vintage remainder is therefore correctly NOT shippable as posed; declining it is honest, not incomplete. **Rest of the sweep, GREEN:** deploy health verified live — SSO-exempt public production alias `competitive-intel-blue.vercel.app` → **HTTP 200** on `/`, `/app.js`, `/data/meta.json`; the `-git-master-…vercel.app` alias → **302 → `vercel.com/sso-api`** = up + SSO-gated (healthy-not-outage); `site-health.yml` correctly targets the master alias — no fix. **0 broken data refs** (104 static `data/*.json` reads across `app.js` + all pages all resolve on disk). Freshness clean — only the known `UPSTREAM_CAPPED` government vintages (DLT/PICO/DBD/SFI) are old-by-cadence, not stale-by-neglect. **Also refreshed** the CEO dashboard (`plan_cycle.py` → `AUTONOMY_PLAN.md` + `status_data.json`) — it had drifted from anchor `fe3fce1` to HEAD `84e085c`. **This IS a visual/app change → shipped via PR + headless self-review, self-merged per the safeguard protocol.** Next recommended intelligence task: the peer-valuation triptych (ROE→ROA×leverage · P/BV over ROA · P/BV vs NPL) is complete; the next genuinely-new cross-read is an **ECL-coverage lens** — `peer_asset_quality.json` carries each peer's `s3_coverage_pct` (Stage-3 expected-credit-loss coverage: MTC 71.9% vs Srisawad 30.6%), so surface whether the richly-valued names also provision more conservatively against their cleaner books, or whether the market rewards thin coverage. · against `platform/app.js` (drawPeerScore P/BV × NPL join)._

_Audit run: 2026-08-27 · finding: **PEER-HONESTY FIX shipped — the listed-peer market scoreboard (`#acq`) now flags HENG's unconsolidated statement basis INLINE at the row, not only in a collapsible method box, so the exec no longer reads its 0.7% ROE outlier as distress.** The MARKET/SERVICE/DEPLOY/PEER sweep was otherwise GREEN with no other fixable gap, and the prior run's teed-up "next task" turned out REDUNDANT (recorded below so no future run re-derives it). **The fix.** `peer_scoreboard.json` gives all six SET peers a uniform `FY2025 (audited)` / Q9-2025 fundamentals vintage, but HENG files **unconsolidated** (`fs_type="U"`) while the other five file **consolidated** (`C`) — so HENG's ROE (0.7%) sits in the same column as 13–17% peers on a **non-like-for-like** basis. That asymmetry was disclosed ONLY in the scoreboard's `METHOD & CAVEATS` box (`m.fs_type_caveat`, `app.js:3993`), invisible to anyone scanning the ROE column. `renderPeerScore`'s row template (`app.js:~3974`) now appends, for `p.fs_type==='U'` only, a compact gold `⚠U` marker with the tooltip _"Company-only (unconsolidated) SET filing — not strictly like-for-like with the consolidated (C) peers; flagged, not blended in."_ — mirroring the **already-shipped** `⚠` convention in the sibling peer-asset-quality table (`app.js:4046`), so it is a consistency alignment, not a new idiom. **Headless self-review (Chromium/Playwright, local `#acq`):** the badge attaches to **exactly one row — HENG** — and to no other peer (per-row DOM assertion: 1× YES, 5× no); tooltip text renders verbatim; table layout unchanged; `node --check platform/app.js` clean. **Why NOT the prior run's proposed task.** The 08-26 entry recommended a "peer-vintage staleness lens" surfacing each peer's own observation age next to its ROE — but this run confirmed all six peers carry the **same** `FY2025 (audited)` vintage (and both the market-price `price_asof=2026-08-25` and the `fundamentals from FY2025` are ALREADY disclosed in the scoreboard footer, `app.js:3989`), so a per-row vintage age would repeat one uniform figure six times and add no information. The real per-row differentiator was the statement-BASIS asymmetry, not the vintage — that is what shipped. **Also recorded (NOT a defect, so future runs don't re-flag it):** Srisawad's `competitor_coverage.json` `coverage_pct=457%` (5,203 found ÷ 1,138 IR-cited) is a **completeness** signal, not a census overcount — 5,203 is Srisawad's OWN official store-locator count (full group retail network incl. เงินสดทันใจ sub-brands) and 1,138 is a narrowly-scoped listed-entity IR figure; this is already handled honestly in the app (row/total tooltips at `app.js:3296/3328/3430` + builder caveats) and needs no change. **Rest of the sweep, GREEN:** determinism gate `bash tests/run.sh check` → **150 passed · 0 failed** (`validate_data.py` 455/455); `build_provenance.py --check` reproduces byte-identically (**149 layers · 0 unlabelled**); `tests/orphan_layers.py` all consumed; `mandate_guard.py` 0 expansion recs across 380 layers; **0 broken data refs** (the 6 naive-grep hits are `source-data/…` comment mentions). **Deploy health (verified live this run):** SSO-exempt public production alias `competitive-intel-blue.vercel.app` → **HTTP 200** on `/`, `/data/meta.json`, `/data/competitor_coverage.json`; the `-git-master-…vercel.app` alias → **302 → `vercel.com/sso-api`** = up + Vercel-SSO-gated (the gate's own `check_site_health.py` SSO detector reads this as healthy-not-outage); `site-health.yml` correctly targets the master alias — no fix. **Also refreshed** the CEO dashboard (`plan_cycle.py` → `AUTONOMY_PLAN.md` + `status_data.json`) — it had drifted 5 commits behind HEAD (anchored `e44f21d`, HEAD `2b051f4`). **This IS a visual/app change → shipped via PR + headless self-review, self-merged per the safeguard protocol.** Next recommended intelligence task: peer/market/service/deploy surfaces are saturated; the next genuinely-new read within CI-reachable committed data is a **collateral-vintage lens** — surface on `#overview`/`#exposure` how current the DLT registered-vehicle collateral base (`vehicle_collateral.json`, the newest `UPSTREAM_CAPPED` government vintage) actually is relative to the live loan tape, so the exec sees the ~one-vintage lag between the book and its measured collateral floor. · against `platform/app.js` (renderPeerScore fs_type marker)._

_Audit run: 2026-08-26 · finding: **HEALTHY CLOSE-OUT (grown layer set re-verified) — full-stack sweep GREEN across all five pillars with no fixable gap; no forced fix shipped, per the 08-21(c) precedent that refuses to invent a fix on a healthy tree.** **Deploy health (verified live this run):** the SSO-exempt public production alias `competitive-intel-blue.vercel.app` returns **HTTP 200** on `/` (AutoX + เงินไชโย wordmark present) and its data plane loads (`/data/meta.json` parses; top keys `board/region/estates/mws/cws/macro/n_agri/updated/meta/branches_fingerprint`, `updated="2026M07 prices · drought 2026-06-21"` = the editorial-macro vintage carried forward by `derive.py`, not a stale pull). `.github/workflows/site-health.yml` correctly targets the master production alias `…-git-master-…vercel.app` (default `base_url`) — no fix needed. **Determinism/provenance clean:** gate `bash tests/run.sh check` → **148 passed · 0 failed** (up from 146 — the two new checks cover the layers that landed since the last audit: `collateral_census` + the rival loan-book structure / asset-quality peer layers). A fresh `build_provenance.py` reproduces **byte-identically** (`diff` clean, `git status` clean after regen): **148 layers · 86 measured / 62 estimated / 0 unlabelled · 379/379 files labelled · 0 without a meta stamp.** **Broken-ref sweep clean:** a fresh fetched-vs-disk re-sweep of **99** real static `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` = **0 missing** (the 6 naive-grep hits — `apple_reviews`/`bot_policy_rate`/`fuel_stations`/`perimeter_counts`/`rayong_trees`/`staging/nso_lfs` — are all `source-data/…` mentions inside comments, not live `data/*.json` reads); `tests/orphan_layers.py` → **141 layers all consumed** (0 true orphans). **Freshness clean, with one NEW recorded nuance (by-design, NOT a defect — logged so future runs don't re-derive it):** provenance `freshness.stale = []` (`stale_over_days=180`; the oldest dated layer `vehicle_models.json` @ 264d is `UPSTREAM_CAPPED` DLT government-vintage, not stale-by-neglect; 5 layers upstream-capped in total). The `freshness.reference_date` is now **2026-09-22**, taken from `collateral_census.json`'s `anchor_date` (the newest auction date in the Union Auction FORWARD book — a legitimately future-scheduled event date, and `_vintage_of` in `build_provenance.py` deliberately reads `anchor_date` as a data-observation vintage, with an honest RELATIVE `reference_note` = "days behind the freshest dated layer"). Consequence to be aware of: because the yardstick is a forward auction booking, every other dated layer's `age_days` reads ~4 weeks larger than its true capture recency — but this produces **no false stale flag** (nothing crosses the 180-day cut on this basis) and each layer's per-layer vintage on the Data-room card is still its own true observation date, so it is cosmetically-relative, not a correctness bug. Left unchanged: reversing `anchor_date`'s deliberate inclusion (or clamping the reference) would either override a documented design choice or require a wall-clock read the script intentionally avoids for determinism — not a fix worth forcing on a healthy tree. **Peer/market surfaces confirmed saturated:** `peer_province.json` carries the full per-brand read (Heng 450 · Muangthai 8931 · Srisawad 5203 · Tidlor 1919; per-100k-vehicle saturation, autox_rank, rival concentration, PICO) and is surfaced on `#acq`; `peer_scoreboard.json` carries all 6 SET-listed peers' ROE. **Heaviest JSON (recorded, no action):** the three Overture building catchments (`rayong_catchment` 33 MB · `chiang-mai_catchment` 32 MB · `bangkok_catchment` 29 MB) are each loaded ONLY on their own `rayong-catchment.html?city=…` route (the deliberate map-tech split), never on the SPA — so page weight is bounded per-route by design. **No code/data/app/visual surface altered → docs-only, no provenance regen (verified byte-identical above), self-mergeable per the safeguard protocol.** Next recommended intelligence task: the deploy-health / broken-ref / freshness / peer-consistency categories are all closed and saturated; the highest-value NEW dimension left within CI-reachable committed data is a **peer-vintage staleness lens** — surface, next to each `peer_scoreboard.json` ROE/P-B figure, its own `price_asof` observation age so the exec sees how current the SET-blocked-from-CI scoreboard actually is (a genuinely new read, not more probe-coverage). · no `platform/data` or app file altered._

_Audit run: 2026-08-24 (b) · finding: **HEALTHY CLOSE-OUT — full-stack sweep GREEN with no fixable gap; the deploy-health probe-coverage category the prior run declared closed is CONFIRMED closed, and no forced fix was shipped (per the 08-21(c) precedent that refuses to invent a probe on a healthy tree).** Deploy verified live this run: the SSO-exempt public production alias `competitive-intel-blue.vercel.app` returns **HTTP 200** on ALL entry surfaces — `/` (+ AutoX wordmark present), `/app.js`, `/styles.css`, and the clean-URL routes `/province`, `/branch-explorer`, `/rayong-catchment`, `/status`, `/data` (the `*.html` originals correctly **308**→clean-URL per `vercel.json cleanUrls`) — and **HTTP 200** on 9 critical data files (`meta`/`branches`/`amphoe`/`peer_province`/`competitor_coverage`/`rival_density`/`tape_real`/`provenance`/`snapshots_index`); the master `…-git-master-…vercel.app` alias returns **302** = up + SSO-gated, read as healthy-not-broken. `check_site_health.py --local platform` **368/368 HEALTHY** at baseline. **Determinism/provenance clean:** a fresh `build_provenance.py` reproduces byte-identically (**146 layers · 85 measured / 61 estimated / 0 unlabelled · 377 files · 0 without a meta stamp**, `git status` clean after regen). **Freshness clean:** provenance `freshness.stale = []`; the oldest dated layer (`vehicle_models.json` @ 264d) is `UPSTREAM_CAPPED` DLT government-vintage, not stale-by-neglect. **Broken-ref sweep clean:** a fresh fetched-vs-disk re-sweep of **97** static quoted `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` = 0 missing; **app-fetched-but-unprobed reads = 0** (every static app read is in the `check_site_health.py` `DATA_FILES` registry — the probe-coverage category holds closed). **Orphan analysis (recorded so future runs don't re-derive it):** a naive `grep` for un-referenced `platform/data/*.json` flags ~230 files, but every one is a FALSE positive — the per-province `<slug>_places/_roads/_water/_catchment.json` files are fetched via template-literal URLs (`data/${slug}_…`) by the 3D scenes (not static-grep-visible), and the handful of non-geographic hits (`dbd_formation`→`build_macro_book._load`, `farmgate_prices`→`build_crop_stress`, `pico_census`→`build_peer_province`/`build_amphoe`, `farm_income_impact`/`region_debt`/`province_lfs`/`used_vehicle_value`/`vehicle_brands`) are PIPELINE INTERMEDIATES published to `platform/data` and consumed server-side by `build_*.py`, not dead client reads — no true orphan to remove. **Peer/market consistency confirmed fully guarded (recorded so future runs don't re-open it):** the peer-count chain `competitors_official.json (n) → competitors_census.json → competitor_coverage.json (found) → peer_province.json (by_brand rollup)` reconciles EXACTLY today (Srisawad 5203 · Tidlor 1919 · Muangthai 8931 · Heng 450; official.n == official len(coords) == census official-locator items == coverage.found on all four), and every hop is `--check`-gated in `tests/run.sh` (`build_competitor_census.py` ← official/national/overture; `build_competitor_coverage.py` ← census; `check_peer_rollup.py` coverage↔peer_province cross-path; `check_peer_constants.py` scoreboard constants) — so `official→coverage` is a linear provably-reproducing chain (no different-path divergence like the 08-15 coverage↔peer_province split that warranted its own guard), meaning an extra `official==coverage` guard would be REDUNDANT, not additive. `site-health.yml` correctly targets the master production alias (no fix needed). **No code/data/app/visual surface altered → docs-only, no provenance regen, self-mergeable per the safeguard protocol.** Next recommended intelligence task: the deploy-health probe frontier is exhausted and the peer/market/consistency surfaces are saturated — pivot the next run OFF probe-coverage to a genuinely NEW analytical dimension (e.g. a cross-layer read that turns the already-committed `province_pressure.json` double-pressure join into a sharper single exec signal, or a peer-vintage staleness lens on `peer_scoreboard.json` vs the IR-reported `national_standing` figures), rather than continuing to search a closed category. · no `platform/data` or app file altered._

_Audit run: 2026-08-24 · finding: **DEPLOY-HEALTH — closed the residual sub-13 KB unprobed-reads tail: the last FIVE static `data/*.json` the app fetches but the nightly live-URL probe never covered, each `r.ok?json():null`-guarded so a CDN truncation degrades it SILENTLY with no phone alert. The app-fetched-but-unprobed count is now 0.** Deploy re-verified up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/branches.json`; master `…-git-master-…vercel.app` alias **302** = up + SSO/access-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; `check_site_health.py --local platform` **353/353 HEALTHY** at baseline; **0 broken data refs** — the 5 naive-grep misses (`apple_reviews`/`bot_policy_rate`/`fuel_stations`/`perimeter_counts`/`rayong_trees`) are all `source-data/…` comment mentions, not live `data/*.json` fetches; freshness sweep clean — provenance `freshness.stale=[]` (the oldest dated layer, `vehicle_models.json` @ 264d, is `UPSTREAM_CAPPED` DLT government-vintage, not stale-by-neglect); reliability sweep of `swarm_runs.json` clean of NEW deterministic faults — `oae_yield` (100% recent) was FIXED at 07:01 UTC by PR #599, landed **after** the last swarm run (05:01 UTC), so its record predates the fix; the sibling `pull_oae_farm_economics.py` flagged by the prior "next recommended" as CKAN-migration-fragile was checked and is **NOT** — it is a static PDF puller (`oae.go.th/uploads/…Cai-up-2568.pdf`) with anchor verification, structurally immune to the CKAN catalog migration that broke `oae_yield`; the residual `google_trends`/`google_ads`/`investor_docs` failures are intermittent Google-rate-limit / network flakes, not deterministic bugs.** A fresh fetched-vs-probed re-sweep (**100** static quoted `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` vs the 120-token `DATA_FILES` registry) left exactly **5** surfaced-but-unprobed reads, all sub-13 KB — the exact tail the 08-23 (b) `province_bbox` ship named as next ("`ev_penetration`, `promo_gap`, `nso_wage_anchor`, `tiles_config`") plus `catchments_r2`. Each is a genuinely SILENT-degrading render read: **`ev_penetration.json`** (12 KB, obj #1 — the Overview EV collateral watch; `renderEvWatch` gates on `EVDATA.meta.national` then paints the fuel-split KPI cards + the per-province electrified table, `r.ok?json():null` → self-hides on a fault); **`nso_wage_anchor.json`** (9 KB, obj #1 — the province deep-dive's MEASURED NSO-LFS wage anchor, the real number the ESTIMATED occupation-income line reality-checks against; `province.html` reads `by_app_region[reg][bucket]` + `meta.vintage`, null → the wage line just omits); **`promo_gap.json`** (9 KB, obj #2 — the Competition promo-vs-card verdict; `drawPromoGap` gates on `.operators`/`.card_only`, reads `name_th`/`key`/`card_floor`/`gap_pp`/`undercuts_own_card`); **`tiles_config.json`** (3 KB — the 3D deep dives' streamed-Overture-buildings + `HAS_SCENERY` isochrone/trees-toggle config; a gutted `.buildings`/`.scenery` drops the tile layer + toggles); **`catchments_r2.json`** (2 KB — `rayong-catchment.html`'s per-province building-catchment availability manifest; `CATALOG.meta.baseUrl` is the R2 CDN fallback base for every non-pilot province's `<slug>_catchment.json`, so a gutted `meta.baseUrl` drops the building scene for all but the 3 git-committed pilots). None self-heals a CDN truncation of a repo-correct file (config/manifest/offline-baked, no CI cron), so the live-URL `DATA_FILES` probe is the only safeguard for each. Closed all 5 with `_shape_ev_penetration` / `_shape_nso_wage_anchor` / `_shape_promo_gap` / `_shape_tiles_config` / `_shape_catchments_r2` + 5 `DATA_FILES` entries: each asserts the exact render contract (the display-gate + the fields the app reads) as **SHAPE not values**, with a truncation tripwire wherever the layer carries a count (`meta.n_provinces` / `meta.n_checked` / `meta.n_card_only` / `meta.n`) — robust to any future DLT/LFS/promo/Overture vintage moving the numbers. Verified: all 5 real payloads → **PASS**; **17 injected negatives** rejected across the five (drop display-gate object · type-gut a KPI · truncate a counted list past its tripwire · empty both promo arrays · drop `meta.baseUrl`/`meta.vintage`/`buildings.pmtilesUrl`/`coverageBbox`/`scenery` · non-object list); offline `--local platform` **368/368 HEALTHY** (+15 vs baseline: 5 files × fetch/parse/shape); post-sweep static app-fetched-but-unprobed reads = **0**; determinism gate **passed · 0 failed** (the `check_site_health.py --local` self-test that gates every probe accepts all 5 committed payloads). Probe-script + docs only — no `platform/data`/app/visual surface altered → no provenance regen, no PR/headless render needed → self-mergeable per the safeguard protocol. This closes the deploy-health probe-coverage category: every static `data/*.json` the app fetches now fires a phone alert on a CDN fault instead of degrading in silence. · against `pipeline/check_site_health.py` (5 `_shape_*` fns + `DATA_FILES`)._

_Audit run: 2026-08-23 (b) · finding: **DEPLOY-HEALTH — `province_bbox.json`, the per-province bounding-box index that resolves each branch to its province slug so the per-branch 3D scene can load that province's MEASURED Overture building catchment, was the last STRUCTURAL surfaced-but-unprobed read that silently DEGRADES a scene (not just blanks it) on a CDN truncation.** Deploy re-verified up front (master `…-git-master-…vercel.app` production alias returns **302** = up + SSO/access-gated, read as healthy-not-broken; `check_site_health.py --local platform` self-test **HEALTHY** at baseline; **0 broken data refs** — the 5 naive-grep misses (`apple_reviews`/`bot_policy_rate`/`fuel_stations`/`perimeter_counts`/`rayong_trees`) are all `source-data/…` comment mentions, not live `data/*.json` fetches; freshness sweep clean — `commodities`/`thaiwater_*`/`rival_*`/`fuel_prices` all ≤2d, `macro_indicators` 6d, `search_demand` 14d/35d-TTL, all inside window). A fresh fetched-vs-probed re-sweep (**98** static `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` vs the now-114-entry `DATA_FILES` registry) left **6** surfaced-but-unprobed reads, all sub-20 KB — so the heavy-read blind spots opened by the 08-22..23 3D-scene probes are fully closed and the remaining triage is by BREAKAGE MODE, not weight. Of the 6, **`province_bbox.json`** (MEASURED — 77 provinces, each a `[S,W,N,E]` bbox = the union of that province's amphoe-polygon extents, GeoBoundaries THA ADM2, `pull_overture_buildings.py write_province_bbox`) is the one whose fault is genuinely SILENT: `branch-explorer.html`'s `resolveProvinceSlug()` point-in-boxes the branch to a slug, then `fetchCatchment()` pulls `<slug>_catchment.json` to paint the "buildings: Overture catchment (measured)" layer; the fetch is `if(!r.ok)return null` + try/caught, so a truncated/404 CDN deploy resolves NO slug and the scene silently FALLS BACK from the measured Overture catchment to the coarser live-OSM Overpass source — a different, lower-fidelity building layer — with no error and no phone alert (worse than a blank, because the scene still looks populated). It cannot self-heal a CDN truncation of a repo-correct file (`write_province_bbox` is an offline emit off the static amphoe geometry, no CI cron), so the nightly live-URL `DATA_FILES` probe is the only safeguard. Closed it with `_shape_province_bbox` + a `DATA_FILES` entry: asserts the render contract — the object, the `.provinces` dict, ≥1 province carrying a well-formed 4-number `[S,W,N,E]` bbox (the point-in-box slug-resolve read), and — when `meta.count` is present — that the province total **equals** it (a truncation tripwire) — SHAPE not values, robust to any future amphoe-geometry vintage moving the extents. Verified: real 10,012-B / 77-province payload → PASS; **4 negatives** rejected (non-object list / empty `.provinces` / all-gutted bboxes / province-count-10≠`meta.count`-77 truncation); offline `--local platform` **353/353 HEALTHY** (+3: fetch/parse/shape); determinism gate **passed · 0 failed** (the `check_site_health.py --local` self-test that gates every probe accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → self-mergeable, no PR/headless render needed. **Negative-space note recorded for a future run (not acted on here):** of the residual 5, `vehicle_collateral.json` (~16 KB, the heaviest) is a **DEAD app read** — `loadVehicleCollateral()` has exactly one occurrence in `app.js` (its own definition, never called) and `VCOLL` is never read; its intended renderer `renderDieselCollateral` was RETIRED 2026-08-02 when the diesel-share/brand-mix content moved into `renderCollateralBook` (reads `collateral_book.json`), leaving the fetch an orphan. It should be REMOVED (dead `loadVehicleCollateral`/`VCOLL`), not probed — but that touches app.js behaviour (PR + headless render self-review), so it is logged here rather than folded into this probe-only ship. · against `pipeline/check_site_health.py` (`_shape_province_bbox` + `DATA_FILES`)._

_Audit run: 2026-08-23 · finding: **DEPLOY-HEALTH — `catchment_poi.json`, the nationwide OSM POI fabric (`window.POINAT`) behind EVERY province's 3D catchment scene, was — after the 08-22 `national_places.json`/`occupation_leads.json` multi-MB 3D-scene reads and the 08-22 (d) `province_geo.json` ship — the heaviest surfaced read (~1.2 MB) still absent from the site-health shape registry, so the residual unprobed layer most exposed to a partial/truncated CDN deploy.** Deploy re-verified up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/branches.json`; master `…-git-master-…vercel.app` alias returns **302** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; `check_site_health.py --local` self-test **HEALTHY** at baseline; **0 broken data refs** — all 102 static `fetch('data/*.json')` targets present on disk). A fresh fetched-vs-probed re-sweep (102 static `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` vs the 113-entry `DATA_FILES` registry) left **8** surfaced-but-unprobed reads; applying the corrected probe-worthiness test (08-21 (c): "**is this a load-bearing read whose CDN-fault breakage is silent to `site-health.yml` because it is not in `DATA_FILES`**"), the standout by weight was **`catchment_poi.json`** (MEASURED — OSM establishment coordinates for all **11** scene pin types → **68,946** points, `build_catchment_poi.py`). `rayong-catchment.html` (the primary 3D entry point for all 77 provinces) fetches it in the deferred second wave and exposes it as `window.POINAT` (`d.poi.poi`, keyed by scene pin type) to draw the per-city POI columns; the fetch is `opt()`/null-guarded → a truncated/404 CDN deploy silently drops the commercial-fabric POI columns from every province's 3D scene with **no phone alert**, and it cannot self-heal a CDN truncation of a repo-correct file (`build_catchment_poi.py` is an offline bake of `osm_layers.json` with no CI cron), so the nightly live-URL `DATA_FILES` probe is the only safeguard for that fault class. Closed it with `_shape_catchment_poi` + a `DATA_FILES` entry: asserts the render contract — the object, the `.poi` dict keyed by scene pin type, ≥1 pin type carrying a well-formed `[lat, lng]` point (the column render read), and — when `meta.n_points` is present — that the point total across pin types **equals** it (a strong truncation tripwire) — SHAPE not values, robust to any future OSM vintage moving which points each pin type lists. Verified: real 1,257,493-B / 68,946-point payload → PASS; **4 negatives** rejected (truncated poi point-total≠`meta.n_points` / empty `.poi` / all-gutted points / non-object list); offline `--local platform` **350/350 HEALTHY** (+3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates every probe accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → self-mergeable, no PR/headless render needed. Next by weight: `vehicle_collateral.json` (~16 KB), `ev_penetration.json` (~12 KB) — the residual tail is all sub-20 KB, so the heavy-read deploy-health blind spots are now closed. · against `pipeline/check_site_health.py` (`_shape_catchment_poi` + `DATA_FILES`)._

_Audit run: 2026-08-22 (d) · finding: **DEPLOY-HEALTH — `province_geo.json`, the National-map risk-choropleth boundaries painted under the branch dots for the `hhdti`/`pstress` per-province risk lenses (obj #1), was — after the 08-22 (c) `national_places.json` ship covered the multi-MB 3D-scene POI reads — the heaviest surfaced read (~1.18 MB) still absent from the site-health shape registry, so the residual unprobed layer most exposed to a partial/truncated CDN deploy.** Deploy re-verified up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/`; master `…-git-master-…vercel.app` alias returns **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; `check_site_health.py --local` self-test **HEALTHY** at baseline; **0 broken data refs** — the 5 naive-grep misses are `source-data/…` comment mentions, not live `data/*.json` fetches). A fresh fetched-vs-probed re-sweep (98 static `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` vs the 110-entry `DATA_FILES` registry) left **11** surfaced-but-unprobed reads; applying the corrected probe-worthiness test (08-21 (c): "**is this a load-bearing read whose CDN-fault breakage is silent to `site-health.yml` because it is not in `DATA_FILES`**"), the standout by weight was **`province_geo.json`** (MEASURED boundaries — 77 amphoe-regrouped province MultiPolygons, `build_province_geo.py`). `app.js` `loadProvinceGeo()` fetches it as `r.ok?json():null` and `PGEO` stays `null` on any fault → a truncated/404 CDN deploy silently degrades the National map to **dots-only** (choropleth simply not drawn) with **no phone alert**, and it cannot self-heal a CDN truncation of a repo-correct file (`build_province_geo.py` has no CI cron), so the nightly live-URL `DATA_FILES` probe is the only safeguard for that fault class. Closed it with `_shape_province_geo` + a `DATA_FILES` entry: asserts the render contract — a `FeatureCollection`, exactly **77** province features, and on a sampled feature the `properties.province` **string join key** (the field the choropleth joins the risk-lens values onto; a CDN truncation keeping `features[]` but stripping `properties` would paint an all-grey no-op layer, which this catches) plus a present `geometry` — SHAPE not values, robust to any future boundary-source vintage. Verified: real 1,208,471-B / 77-feature payload → PASS; **3 negatives** rejected (wrong feature count / `properties.province` stripped / not a FeatureCollection); offline `--local platform` **341/341 HEALTHY** (+3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates every probe accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → self-mergeable, no PR/headless render needed. Next by weight: `poi_relevance.json` (~622 KB), `lead_sites.json` (~545 KB). · against `pipeline/check_site_health.py` (`_shape_province_geo` + `DATA_FILES`)._

_Audit run: 2026-08-22 (c) · finding: **DEPLOY-HEALTH — `national_places.json`, the national POI fallback behind EVERY province's 3D building scene, was — after the 08-22 (b) `occupation_leads.json` ship — the heaviest surfaced read (~5.4 MB) still absent from the site-health shape registry, so the residual unprobed layer most exposed to a partial/truncated CDN deploy.** Deploy re-verified up front (public production alias `competitive-intel-blue.vercel.app` was reachable this run; master `…-git-master-…vercel.app` alias returns **302** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; `check_site_health.py --local` self-test **HEALTHY** at baseline; **0 broken data refs** — the 5 naive-grep misses are `source-data/…` comment mentions, not live `data/*.json` fetches). A fresh fetched-vs-probed re-sweep (98 static `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` vs the 109-entry `DATA_FILES` registry) left **12** surfaced-but-unprobed reads; applying the corrected probe-worthiness test (08-21 (c): "**is this a load-bearing read whose CDN-fault breakage is silent to `site-health.yml` because it is not in `DATA_FILES`**"), the standout by weight was **`national_places.json`** (MEASURED — Overture named places, grid-thinned to ~2.22km cells → **264,106** points across **14 buckets**). `rayong-catchment.html` (the primary 3D entry point for all 77 provinces) fetches it as the documented "national fallback for every province's 3D scene" wherever a per-city `<city>_places.json` is absent, and the fetch is null-guarded (`r.ok ? r.json() : null`) → a truncated/404 CDN deploy silently drops the establishment layer from every non-pilot province's building scene with **no phone alert**, and it cannot self-heal a CDN truncation of a repo-correct file (`build_national_places.py` is an Overture pull with no CI cron), so the nightly live-URL `DATA_FILES` probe is the only safeguard for that fault class. Closed it with `_shape_national_places` + a `DATA_FILES` entry: asserts the render contract — the object, a non-empty `meta.buckets` (the scene's bucket legend), the `.places` dict keyed by those buckets, ≥1 bucket carrying a well-formed `[lng, lat]` point, and — when `meta.count` is present — that the point total across buckets **equals** it (a strong truncation tripwire) — SHAPE not values, robust to any future Overture vintage moving which places each bucket lists. Verified: real 5,366,371-B / 264,106-point payload → PASS; **5 negatives** rejected (non-object list / `meta.buckets` removed / empty `.places` / malformed point / point-total≠`meta.count` truncation); offline `--local platform` **338/338 HEALTHY** (+3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates every probe accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → self-mergeable, no PR/headless render needed. · against `pipeline/check_site_health.py` (`_shape_national_places` + `DATA_FILES`)._

_Audit run: 2026-08-22 (b) · finding: **DEPLOY-HEALTH — `occupation_leads.json`, the DATA SOURCE behind the per-branch acquisition lead board ("Who to acquire here — nearest workplaces to call") on TWO nav pages, was the LARGEST surfaced read (~6.8 MB) still absent from the site-health shape registry — so the residual unprobed layer most exposed to a partial/truncated CDN deploy.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`; master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; `check_site_health.py --local` self-test **HEALTHY** at baseline; **0 broken data refs** — all 101 static `fetch('data/*.json')` targets present on disk). A fresh fetched-vs-probed re-sweep (99 live `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` vs the probe registry) left **12** surfaced-but-unprobed reads; the hard-fail BACKBONE indices (`regions.json` 08-21(b), `provinces/index.json` 08-22(a)) are now BOTH probed, so — applying the corrected probe-worthiness test recorded in the 08-21(c) audit ("**is this a load-bearing read whose CDN-fault breakage is silent to `site-health.yml` because it is not in `DATA_FILES`**", NOT "does a `build_*.py` cron exist") — the next tier is the heaviest load-bearing GRACEFUL-degrading read, and the standout is **`occupation_leads.json`**. It is objective #2's acquisition-lead surface: `app.js:8400` reads `OCCL[i].L` (the per-branch `[bucket, name, phone, dist_km]` lead rows, index-aligned to all 2,015 branches; bucket labels from `OCCLB = meta.buckets`) in the `#map` branch popup, and `branch-explorer.html` reads the same layer for its per-branch lead list. `loadOccLeads()` is null-guarded (a fetch reject → `OCCL=null` → the board just doesn't render), so — as the 08-21(c) audit correctly noted — it degrades GRACEFULLY (no hard page-break), which is exactly why it ranked BELOW the hard-fail indices and was left for after they were covered. But graceful degradation is itself the hazard here: a truncated/404 CDN deploy silently **blanks the acquisition lead board on both pages with NO phone alert**, and at ~6.8 MB (the largest of the 12 residual unprobed reads) it is the file most likely to be partially served. It cannot self-heal a CDN-serving truncation of a repo-correct file (a byte-identical `build_occupation_leads.py` rebuild via the weekly `committee-geocode.yml` `for f in build_*.py` sweep only re-commits when the Overture Places source input changes — nothing to redeploy otherwise), so the nightly live-URL `DATA_FILES` probe is the only safeguard for that fault class. Closed it with `_shape_occupation_leads` + a `DATA_FILES` entry: asserts the render contract — the object, the **index-aligned `.branches` list of exactly 2015** (a truncation drops the count), a non-empty `meta.buckets` (the `OCCLB` label read), and ≥1 branch carrying a well-formed `.L` lead row `[bucket, name, phone, dist_km]` — SHAPE not values, robust to any future Places vintage moving which workplaces/phones each branch lists. Verified: real 6,807,683-B / 2,015-record payload → PASS; **4 negatives** rejected (non-object list / length-1000≠2015 truncation / `meta.buckets`-removed / all-empty-`L` gutted layer); offline `--local platform` **335/335 HEALTHY** (+3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates every probe accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → self-mergeable, no PR/headless render needed. · against `pipeline/check_site_health.py` (`_shape_occupation_leads` + `DATA_FILES`)._

_Audit run: 2026-08-22 · finding: **DEPLOY-HEALTH — `provinces/index.json`, the BACKBONE index of the entire 77-province deep-dive, was the single most load-bearing surfaced read still ABSENT from the site-health shape registry, and it cannot self-heal a CDN truncation.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`; master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; `check_site_health.py --local` self-test **329/329 HEALTHY** at baseline). A fresh fetched-vs-probed re-sweep (98 live `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` vs the probe registry) left 12 surfaced-but-unprobed reads; triaged for load-bearing × cannot-self-heal-a-CDN-fault, the standout — the province analogue of the 08-21(b) `regions.json` Data-Book-backbone probe — was **`provinces/index.json`** (built by `build_province.py`). It has **4 fetch sites**, MORE than any other unprobed read, gating **two primary nav routes plus two more consumers**: the **#provinces selector** (`renderProvinces`: `PROV.map(p=>p.region)` builds the region-filter chips, `PLOOK` is keyed on `p.th`, each row drills out on `p.slug` → `rayong-catchment.html?city=<slug>` / `province.html?p=<slug>`), the **#market table** (`renderMarket`, same list + same `p.region` chips), the **SPA boot** province-name lookup (`provLookupByName`, `m[p.th]=p`), and the **branch-explorer** province picker. `renderProvinces` degrades to "Could not load provinces." on a fetch reject, but a truncated/404 CDN deploy blanks the #provinces selector AND the #market table AND the boot name-lookup at once with **no phone alert** — the nightly `site-health.yml` probe only fetched the registered `DATA_FILES`, and this index was never registered. Like `regions.json` it is a structural index, so a byte-identical `build_province.py` rebuild (it rides `data-gov-census.yml` + the weekly `committee-geocode.yml` blanket `for f in build_*.py` sweep) **cannot restore a CDN-truncated-but-repo-correct file** — the live-URL DATA_FILES probe is the only thing that catches that class, exactly the corrected probe-worthiness test recorded in the 08-21(c) audit. Closed it with `_shape_provinces_index` + a `DATA_FILES` entry: asserts the render contract — a **non-empty list** whose first record carries a string `slug` (the drill links), string `th` (the `PLOOK`/`provLookupByName` key), string `region` (the region-filter chips), and a numeric `branches` (the branch-count column / index sort key) — SHAPE not values, robust to any future spatial-join / DLT / census vintage refresh moving the counts. Verified: real 19,295-B / 77-record payload → PASS; **10 negatives** rejected (non-list dict / non-list str / empty list / rec0-not-dict / no-`slug` / blank-`slug` / non-string `th` / null `region` / `branches`-missing / `branches`-is-bool); offline `--local platform` **332/332 HEALTHY** (was 329, +3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates every probe accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → self-mergeable, no PR/headless render needed. · against `pipeline/check_site_health.py` (`_shape_provinces_index` + `DATA_FILES`)._

_Audit run: 2026-08-21 (d) · finding: **DEPLOY-HEALTH — the nightly deep page/data checks have been DARK for 5 days: the 08-16 SSO-bypass fix requires an owner-provisioned secret that was never set, so `site-health.yml` has SKIPPED all 11 page + all data-file shape checks every night since the master alias went behind Vercel Authentication.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`, wordmark present, `competitors_national.json` 2,556 items live == committed; master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; **0 broken data refs** — all 98 live `fetch('data/*.json')` targets present, the 5 naive-grep misses are `source-data/…` comment mentions). The concrete gap: the 08-16 audit correctly detected the SSO wall and shipped the officially-sanctioned deep-probe path — Vercel **Protection-Bypass-for-Automation** via `VERCEL_AUTOMATION_BYPASS_SECRET` — but that path is **dormant until the owner adds the secret**, and (the owner being away) it never was. So `check_site_health.py`'s pre-flight hits `AuthGated`, records "up + protected", and SKIPS the entire deep suite — meaning a CDN truncation / bad deploy of `branches.json`, `meta.json`, or any of the shape-probed data files would fire **no phone alert**, exactly the class of fault every prior `_shape_*` audit added a probe to catch. The whole shape-probe registry has been unenforced against production nightly since 08-16. **Closed it with a zero-config path that needs no owner action:** the SAME build is also served through the SSO-EXEMPT **public** production alias (`competitor-intel-blue.vercel.app`, the URL the owner shares — CLAUDE.md). `check_site_health.py` now takes a `--public-url` / `PUBLIC_BASE_URL` (defaulted to that alias, baked in as `PUBLIC_PROD_ALIAS`); when the primary alias is auth-gated the deep page + data + freshness + R2 checks run against the public alias **instead of being skipped** — restoring full nightly production coverage with no secret. The bypass-secret path still works (and deep-probes the master alias directly when provisioned); both are optional; `--public-url ""` disables the fallback. Refactored the deep suite into a reusable `run_deep_checks(fetcher, record)` so it runs identically against either alias; the fallback only engages for a DIFFERENT-origin public alias (probing the same gated alias twice buys nothing). Verified: offline `--local platform` **329/329 HEALTHY** (unchanged, `public_fetcher=None`); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates every probe accepts the change); **live run against the real SSO-gated master alias → deep checks RAN against `competitor-intel-blue.vercel.app`, 336 checks passed** (2 files hit a transient sandbox-proxy `SSL UNEXPECTED_EOF`, both confirmed HTTP 200 + valid JSON on a direct re-fetch — a cloud-sandbox transport artifact, not a site fault; GitHub-hosted runners have normal internet and would pass all); **4 fallback branches unit-tested offline** (no public fallback → original SSO-skip messaging; public also-gated → skip w/ public-gated note; public unreachable → skip w/ unreachable note; public OK → deep checks run, no SKIP line emitted). Probe-script + workflow-comment only — no `platform/data` file altered → no provenance regen, no visual/app change → self-mergeable, no PR/headless render needed. Restores the nightly deep production coverage the 08-16 SSO wall silently took away, without waiting on an owner-side secret. · against `pipeline/check_site_health.py` (`PUBLIC_PROD_ALIAS` + `run_deep_checks` + `--public-url` fallback) + `.github/workflows/site-health.yml` (env comment)._

_Audit run: 2026-08-21 (c) · finding: **SERVICE — the recurring "verify the lead-scene layers are still surfaced post-consolidation-pivot BEFORE probing them" question (flagged as _next recommended_ in the last three intelligence runs) is now RESOLVED, and the loop's "cannot self-heal" triage rationale is CORRECTED.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`; master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; determinism gate **passed · 0 failed** at baseline). Full fetched-vs-disk integrity re-sweep: **98 / 98** literal `fetch('data/*.json')` targets across `platform/app.js` + all `platform/*.html` are present on disk — **zero broken references** (the five apparent misses in a naive grep — `apple_reviews`/`bot_policy_rate`/`fuel_stations`/`perimeter_counts`/`rayong_trees` — are `source-data/…` path mentions inside comments/strings, not live fetches). Provenance ledger clean: **146 layers · 85 measured / 61 estimated / 0 unlabelled · 377 files · 0 without a meta stamp**, no drift from a fresh `build_provenance.py`. Freshness only the known upstream-capped layers (`ev_penetration`/`vehicle_collateral` 174d DLT-capped; `pico_*`/`dbd_formation`/`peer_npl` on quarterly/monthly IR cadence; `competitors_census` 48d on the deliberately-PAUSED scout). No eager heavy-JSON load: the two largest non-catchment reads (`occupation_leads.json` 6.6MB, `national_places.json` 5.2MB) load lazily — `occupation_leads` only inside the `#map` branch-popup/selection handlers (`app.js:8048/8090`), `national_places` only in the `rayong-catchment.html` 3D scene (opened on demand) — neither is on the SPA boot path. **(1) Lead-layer surface-verification, RESOLVED:** all four flagged layers are STILL surfaced AND consumed — `occupation_leads.json`→`OCCL`/`OCCLB` (branch-popup named-leads block + `rayong-catchment`/`branch-explorer`), `lead_sites.json`→`LSITES` (measured lead-site popup), `national_places.json`→`window.NATPLACES` (rayong-catchment place-source fallback, `:792`), `poi_relevance.json`→`POIREL` (the `poirel` National-map lens, re-badged ESTIMATED in run (a) below). None is dead weight; the consolidation pivot retired sibling _expansion_ features (the `build_expansion_plan.py` board is dormant/unsurfaced; the catchment "acquisition leaderboard" was reframed to a "coverage-gap · competitive-exposure read, not an open-a-branch recommendation") but left these four alive and load-bearing — so they warrant neither a fix nor a rewrite. **(2) "Cannot self-heal" triage rationale, CORRECTED for future runs:** the recent deploy-health probes stated their targets "cannot self-heal — scheduled in no workflow", but `.github/workflows/committee-geocode.yml` (weekly cron `10 3 * * 1`) actually re-runs **every** `build_*.py` in a blanket `for f in build_*.py` sweep — so `build_occupation_leads.py`, `build_occupation_risk.py`, `build_ev_exposure.py`, `build_regions.py` etc. ARE all re-executed weekly. The precise truth is subtler and more useful: a weekly rebuild only restores a file whose SOURCE INPUT changed (byte-identical output → nothing to commit → no redeploy), so it **cannot restore a CDN-serving truncation of a repo-correct file** — the nightly live-URL `site-health.yml` DATA_FILES probe is the ONLY thing that catches that class of fault. So the correct probe-worthiness test is NOT "does a `build_*.py` cron exist" (one always does, via committee-geocode) but "**is this a load-bearing read whose CDN-fault breakage is silent to `site-health.yml` because it is not in DATA_FILES**". By that corrected test the core hard-fail surfaces are already covered (the prior runs' probes remain valid — `regions.json` genuinely hard-fails the whole Data Book); the four lead layers each degrade GRACEFULLY on a bad deploy (branch popup shows no leads / the scene falls back), so they are correctly LOWER-priority than the hard-fail reads already probed — a monitoring nuance recorded, not a defect. **No fix shipped: the tree is genuinely healthy, and forcing a probe on a graceful-degrading read with a now-known-imprecise "no cron" rationale would repeat exactly the honesty error the 08-21 `promo_gap` run refused.** Docs-only close-out of a recurring investigation loop so future runs advance to new work. · no `platform/data` or app file altered._

_Audit run: 2026-08-21 (b) · finding: **DEPLOY-HEALTH — `regions.json`, the province→region rollup that is the BACKBONE of the numbers-first Data Book (`data.html`), was a surfaced core-surface read with NO deploy shape-probe, and it CANNOT self-heal.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`; master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; `check_site_health.py --local` self-test **326/326 HEALTHY** at baseline). A fresh fetched-vs-probed re-sweep (`platform/app.js` + all `platform/*.html` live `fetch()`'d `data/*.json` vs the `check_site_health.py` probe registry) left 26 surfaced-but-unprobed reads; triaged for load-bearing × cannot-self-heal, the standout was **`regions.json`** — the only one BOTH load-bearing on a core exec surface (**3 fetch sites**, the most of the unprobed set) AND unable to self-heal (`grep` confirms **no workflow cron and no `pull_swarm` entry** rebuilds it). It is the backbone of the Data Book: `data.html` reads it into `DATA` and drives (a) the national header + occ-income block off `DATA.national` (`:270/632/685`, an object rollup with `.branches`, `.occ_income`, …), (b) the region cards + the region filter `r.region===key` (`:630`), and (c) the province lookup that walks every region's `.provinces[].slug` (`:673`) to resolve a slug → its summary + parent region. The loader THROWS on `!r.ok` (`:1004`) and paints "**Data book unavailable — could not load data/regions.json**" (`:1015`) — so a truncated/404 CDN deploy of it breaks the WHOLE Data Book. The failure is visible to a human who opens the page, but the nightly `site-health.yml` probe only fetches the **registered** `DATA_FILES`, and `regions.json` was never registered → the break fired **no phone alert**, and no CI job restores it. Closed it with `_shape_regions` + a `DATA_FILES` entry: asserts the render contract — a non-empty `national` object carrying a numeric `branches` (the national-rollup header read), plus a non-empty `regions` list with ≥1 entry that carries a string `region` (the `r.region===key` filter) AND a non-empty `provinces[]` whose members carry a string `slug` (the `.provinces[].slug` walk) — SHAPE not values, robust to any future rollup vintage refresh. Verified: real payload → PASS; **10 negatives** rejected (non-dict / no-`national` / `national`-not-object / no-numeric-`branches` / `branches`-is-bool / no-`regions` / empty-`regions` / region-missing-`region`-key / region-empty-`provinces` / province-missing-`slug`); offline `--local platform` **329/329 HEALTHY** (was 326, +3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates all probes accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → no PR/headless render needed. **Freshness sweep this run (recorded so future runs don't re-derive it):** the two stalest dated layers `ev_penetration`/`vehicle_collateral` (174d, `2026-02-28`) are the KNOWN DLT-upstream-capped pair — `data-gov-census.yml`'s `pull_dlt_fuel.py` fails on the intermittent/geo-blocked DLT host and logs "stay at last vintage this run", and they are consumed as build-INPUTS folded into `collateral_book.json` (which honestly discloses `dlt_fleet_vintage: 2025`), not dead weight; `pico_*`/`dbd_formation`/`peer_npl` are on natural quarterly/monthly IR cadence; `competitors_census` at 48d refreshes only via the deliberately-PAUSED committee scout (on-demand `workflow_dispatch`), a design choice, not a break — noted, not alarmed. · against `pipeline/check_site_health.py` (`_shape_regions` + `DATA_FILES`)._

_Audit run: 2026-08-20 (c) · finding: **DEPLOY-HEALTH — the per-branch occupation-stress triage layer (`occupation_risk.json`) was a surfaced obj-#1 read with NO deploy shape-probe, and it CANNOT self-heal.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`; master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; `check_site_health.py --local` self-test **317/317 HEALTHY** at baseline). Continuing to drain the surfaced-but-unprobed-reads set the 08-20 sweeps opened, the next obj-#1 candidate that both gates a visible surface AND cannot self-heal is **`occupation_risk.json`** (obj #1 — ESTIMATED COMPOSITE: MEASURED occupation shares from Overture Places, a lower bound, weighted by an ESTIMATED "stressed sector" judgement; a triage flag, NOT a measured default rate). It is load-bearing on **two** surfaces: the `#map` occupation-stress lens (`occriskVal(d)=OCCRISK[i].s`, so the `branches` array MUST stay index-aligned to `branches.json`) and the branch popup's "Occupation × stress" block (`occriskPopupHTML` reads `.t` as its render gate, `.s` score, `.d` dominant-bucket key, `.ds` share, `.f` flag). It is a genuine blind spot for the usual two reasons: (1) the client loader sets `OCCRISK=null` on any fetch/parse failure and every reader returns `0`/`''` when the record is missing, so a truncated/404 CDN deploy silently zeroes the lens (`renderLenses` even hides the pill) and drops the popup block with **no phone alert**; and (2) it **cannot self-heal** — `build_occupation_risk.py` depends on `branch_occupations.json` (the Overture pull) and is scheduled in **no** workflow, so a gutted deploy has no CI job to restore it. Closed it with `_shape_occupation_risk` + a `DATA_FILES` entry: asserts the render contract — a 2015-branch index-aligned `branches` array (the `OCCRISK` gate + the `occriskVal OCCRISK[i]` read) whose first record carries a numeric `s` (the `#map` lens val), numeric `t` (the popup render gate `occriskRec.t>0`) and a bool `f` (the FLAGGED callout gate) — SHAPE not values, robust to any future Overture vintage refresh moving the scores. Verified: real payload → PASS; **8 negatives** rejected (non-dict / no-`branches` / empty-`branches` / wrong-length / first-missing-`s` / `s`-is-bool / no-`t` / `f`-not-bool); offline `--local platform` **320/320 HEALTHY** (was 317, +3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates all probes accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → no PR/headless render needed. · against `pipeline/check_site_health.py` (`_shape_occupation_risk` + `DATA_FILES`)._

_Audit run: 2026-08-20 (b) · finding: **DEPLOY-HEALTH — the Overview collateral board's MEASURED "ICE auto-parts jobs exposed" card (`ev_exposure.json`) was a surfaced obj-#1 read with NO deploy shape-probe, and unlike its self-healing siblings it CANNOT self-heal.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`; master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline; the `check_site_health.py --local` self-test that gates every probe reports 314/314 HEALTHY at baseline). A fetched-vs-probed re-sweep (`platform/app.js` + all `platform/*.html` live `fetch()`'d `data/*.json`, 130 unique, vs the `check_site_health.py` probe registry, 107) left 31 surfaced-but-unprobed reads; triaged for load-bearing × cannot-self-heal × obj#1/#2 impact, the highest-value was **`ev_exposure.json`** (obj #1, MEASURED — DIW `fac-10scurve` automotive-group factory census: n / workers / registered capital per province, 173,380 workers across 1,630 factories in 48 provinces, Eastern-corridor-concentrated). It renders the "ICE auto-parts jobs exposed" card on the shared Overview collateral board — the **borrower-INCOME channel of the EV transition** (jobs the same electrification pressures), the measured companion to the editorial diesel-pickup resale card above it (which reads the collateral ASSET side). It is a genuine blind spot for the usual two reasons: (1) `renderCollatOutlook` GATES the whole card on `EVEXP.meta.national.workers != null` and silently drops it (**no phone alert**) when the file is missing/truncated — masking the breakage; and (2) it **cannot self-heal** — `grep` confirms `build_ev_exposure.py` is scheduled in **no** workflow (its DIW s-curve input `source-data/scurve_by_province.json` rides no cron of its own — the recurring `data-gov-census.yml` pulls the SAME DIW CKAN host but for the `factype3` category-3 census, a distinct dataset), so a truncated/404 CDN deploy that guts `ev_exposure.json` has no CI job to restore it (distinct from the self-healing siblings in the unprobed set that ride the DIW/DLT/rival crons). Closed it with `_shape_ev_exposure` + a `DATA_FILES` entry: asserts the render gate + rendered-card shape — numeric `meta.national.workers` (the gate + lead figure), numeric `meta.national.factories` (the "across N factories" clause), numeric `meta.n_provinces` (the "in NP provinces" clause), and a non-empty `provinces` list whose rows carry a string `th` + numeric `workers` (the top-3-by-workers concentration clause the render sorts on) — SHAPE not values, robust to any future DIW s-curve vintage refresh moving the counts. Verified: real 4,283-B payload → PASS; **11 negatives** rejected (non-dict / no-or-non-dict `meta` / no-or-non-numeric `meta.national.workers` / no-or-non-numeric `factories` / no `n_provinces` / empty `provinces` / non-dict row / blank `th` / non-numeric `workers`); offline `--local platform` **317/317 HEALTHY** (was 314, +3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates all probes accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → no PR/headless render needed. · against `pipeline/check_site_health.py` (`_shape_ev_exposure` + `DATA_FILES`)._

_Audit run: 2026-08-20 · finding: **DEPLOY-HEALTH — the Overview's MEASURED informal-borrower-base macro KPIs (`labour_context.json`) were a surfaced obj-#1 read with NO deploy shape-probe, and unlike its neighbours they CANNOT self-heal.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`; master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, read as healthy-not-broken; determinism gate **146 passed · 0 failed** at baseline). A fetched-vs-probed sweep (`app.js` live `fetch()`'d `data/*.json`, 111, vs the `check_site_health.py` probe registry, 106) left 18 surfaced-but-unprobed reads; triaged for load-bearing × cannot-self-heal, the highest-value was **`labour_context.json`** (obj #1, MEASURED — ILOSTAT mirror of Thailand's NSO LFS, overlaid with NSO quarterly LFS cross-tabs). It renders **three MEASURED national KPIs on the shared Overview macro board** — informality `63.2%` ("no payslip — the title-loan borrower base"), self-employed `50.24%`, and Agriculture jobs `27.32%` +660k YoY ("the agri-PD demand backdrop") — the informal-borrower base every segment score on the platform rests on. It is a genuine blind spot for the usual two reasons: (1) `renderLabourContext` GATES the block on a non-empty cards list and, per its own comment, `absent file → nothing extra renders` — it silently drops the three KPIs with **no phone alert**; and (2) it **cannot self-heal** — `grep` confirms `build_labour_context.py` is scheduled in **no** workflow, so a truncated/404 CDN deploy that guts it has no CI job to restore it (distinct from the self-healing `promo_gap`/`imf_weo` siblings in the same unprobed set, which ride the rival/IMF crons). Closed it with `_shape_labour_context` + a `DATA_FILES` entry: asserts the three rendered-card shapes — numeric `informality.rate_pct` (lead card) + numeric `self_employment.self_employed_pct` (second card) + an Agriculture row in `employment.sectors` carrying a numeric `share_pct` (matching the render's `/agri/i` find) — SHAPE not values, robust to any future ILOSTAT/NSO-LFS vintage refresh moving the percentages. Verified: real 2,784-B payload → PASS; **9 negatives** rejected (non-dict / no-or-non-numeric `informality.rate_pct` / no-or-missing `self_employed_pct` / no `employment` / empty `sectors` / no-Agriculture-row / non-numeric agri `share_pct`); offline `--local platform` **314/314 HEALTHY** (+3: fetch/parse/shape); determinism gate **146 passed · 0 failed** (the `check_site_health.py --local` self-test that gates all probes accepts the committed payload). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → no PR/headless render needed. · against `pipeline/check_site_health.py` (`_shape_labour_context` + `DATA_FILES`)._

_Audit run: 2026-08-19 (b) · finding: **DEPLOY-HEALTH / SERVICE — the "Live board" nav page (`live_board.json`) was a real, frequently-refreshed surfaced read with ZERO site-health shape-probe coverage.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/` + `/data/meta.json`; determinism gate **140 passed · 0 failed** at baseline). With the market/peer pillars saturated (per-province per-brand peer comparison ships in `peer_province.json` + `competitor_coverage.json`, cross-consistent) and the freshness-guard set complete over every frequent live upstream, a render-path re-scan of surfaced reads found `live_board.json` — the service-freshness registry rendered by `live.html`, **linked in the shared nav on every page** — had `grep live_board pipeline/check_site_health.py` = **0**. It is a genuine blind spot: `build_live_board.py` re-runs on every market-pulse swarm pass (live-facing), `live.html` GATES the whole page on `LB.feeds` (`if (!LB || !LB.feeds)` → the calm "live_board.json could not be loaded" note, **no phone alert**), then the pulse strip runs `freshness(f)` over every feed (`f.fresh_days`/`f.aging_days`/`f.stamp_iso`) and reads `M.n_with_history`/`M.history_points` — so a truncated/404 CDN deploy would silently blank the entire Live board while every existing check stayed green, and unlike a CI-pulled layer it **cannot self-heal** (build_live_board.py has no cron of its own; it rides the swarm). Closed it with `_shape_live_board` + a `DATA_FILES` entry: asserts the `feeds` gate (non-empty list) + first feed `key`/`label` + the `fresh_days`/`aging_days` bucket keys + ≥1 feed carrying a string `stamp_iso` + the numeric `meta.n_with_history`/`history_points` counters — SHAPE not values, robust to any feed vintage/roster refresh. Verified: real 23-feed payload → PASS; **8 negatives** rejected (non-dict / no-or-empty `feeds` / feed-missing-`label` / feed-missing-`fresh_days` / no-`stamp_iso`-anywhere / meta-missing-`n_with_history` / meta-non-numeric-`history_points`); offline `--local platform` **311/311 HEALTHY** (+3: fetch/parse/shape); determinism gate **144 passed · 0 failed**. Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → no PR/headless render needed. · against `pipeline/check_site_health.py` (`_shape_live_board` + `DATA_FILES`)._

_Audit run: 2026-08-19 · finding: **DEPLOY-HEALTH — the R2 catchment store that now serves 74 of 77 province 3D building scenes had NO liveness monitoring, so an R2 outage would silently break them all with no phone alert.** Deploy re-verified green up front (public production alias `competitive-intel-blue.vercel.app` **HTTP 200** on `/`, `/app.js`, `/data/meta.json`; the master `…-git-master-…vercel.app` alias correctly **302→`vercel.com/sso-api`** = up + SSO-gated, handled by `check_site_health.py` as healthy-not-broken; determinism gate **144 passed · 0 failed**, data-integrity **455/455**; **0 broken data refs** — all **102** live `data/*.json` reads across `app.js`+all `*.html` resolve to committed files; freshness clean — 40 layers carry a vintage, live inputs fresh to 2026-08-19 [farmgate/fuel/thaiwater/social/rival-ads all 08-19], no stale live-input layer). The concrete gap fixed is a **monitoring blind spot the 2026-08-18 R2 offload (`abcb79a`) opened**: that commit `git rm`'d the 74 non-pilot `<slug>_catchment.json` files (~2.2 GB) now served ONLY from Cloudflare R2 (`pub-…r2.dev`, local-first→R2 fallback, keeping just the 3 pilots `bangkok`/`chiang-mai`/`rayong` in git). The nightly `site-health.yml` probes the **deployment's own origin** (4 pages + 7 critical data files + freshness); it never touched R2 — a DIFFERENT host — so a deleted bucket / revoked public-access / DNS change on R2 would break all 74 R2-only 3D scenes while every existing check stayed green and no GitHub issue / phone alert fired. Closed it with `run_r2_catchment_checks` in `check_site_health.py`: a **LIVE-ONLY** probe that reads the committed `catchments_r2.json` manifest (`meta.baseUrl` + `provinces` list − `git` pilots), HEADs a **deterministic even-spread sample of 4 R2-only catchments**, and records **PASS** on 2xx / **FAIL** on a clean non-2xx (bucket/object gone) / **not-evaluated** on a transient DNS/TLS/timeout blip (false-alarm-proof, matching the file's freshness-guard philosophy). It runs regardless of the alias auth gate (R2 is its own origin — called in BOTH the SSO-gated early-return path and the normal live path), and is gated to `HttpFetcher` so the **offline `--local` gate path never touches the network** → determinism gate byte-unchanged. Verified: `_r2_sample_slugs` deterministic + excludes the git pilots (unit-tested); the probe PASSes/​FAILs/​not-evaluates correctly against mocked 200/404/timeout openers; **live probe against real R2 = 4/4 sampled catchments served 200** (amnat-charoen, lamphun, phangnga, samut-songkhram); spot-checked 10 more R2-only provinces all 200; `--local platform` emits **0** R2 lines (network-free). Probe-script-only — no `platform/data` file altered → no provenance regen, no visual/app change → no PR/headless render needed. Closes the last un-monitored single-point-of-failure the R2 migration introduced. · against `pipeline/check_site_health.py` (`run_r2_catchment_checks` + two call sites, live-only)._

_Audit run: 2026-08-16 · finding: **DEPLOY-HEALTH — the master production alias is now behind Vercel Authentication (SSO), and the nightly deep checks lost their way past it.** Every prior *service* audit (through 08-15) reported the master alias `…-git-master-…vercel.app` as **HTTP 200** on `/`, `/app.js`, `/data/*`; it now **302-redirects to `vercel.com/sso-api`** — a Vercel Deployment-Protection SSO wall, NOT the app's own `middleware.js` Basic Auth (which answers 401). (An intelligence run earlier on 08-16 already logged the same 302 as steady-state; this run confirmed the project setting behind it and closed the CI monitoring gap it opened.) Confirmed via the Vercel project API: `ssoProtection.enabled = true`, `deploymentType = **all_except_custom_domains**`; `passwordProtection.enabled = false`; the project has **no custom domain** (only the two `*.vercel.app` aliases, both SSO-walled). Read as the owner completing the one OPEN backlog item **`dep-access`** (protect sensitive branch-level PD) via Vercel's native SSO rather than the app middleware — so the site is UP + correctly protected, not down. But it has two real consequences: (1) **owner-side** — the vercel.app alias is no longer openable with the `SITE_PASSWORD`; a plain link / phone view now needs a Vercel team login (surfaced to the owner; the fix — add an SSO-exempt custom domain, narrow the SSO `deploymentType`, or accept Vercel-login-only — is his call given the sensitive-PD trade-off, NOT auto-changed here); (2) **CI deep-check gap** — `check_site_health.py` already detects the SSO redirect and reports *healthy-but-gated* (no false alarm, verified live: pre-flight `index.html` → `AuthGated` "HTTP 200 → vercel.com/login…"), but its docstring said the only way to deep-probe past SSO is "an SSO-exempt custom domain", so the 7 data-file shape checks + 4 page checks silently stopped running against production. Closed the CI gap with Vercel's officially-sanctioned mechanism — **Protection Bypass for Automation**: `HttpFetcher` now sends the `x-vercel-protection-bypass` header (+ `x-vercel-set-bypass-cookie`) when a `VERCEL_AUTOMATION_BYPASS_SECRET` is supplied (new `--bypass-token` arg / env var, defaulted like `SITE_PASSWORD`), making Vercel serve real content so the deep checks run again — **without weakening the site's protection for anyone else**, and **dormant until the owner provisions the secret** (added to `site-health.yml` env). A stale/wrong bypass secret is reported as gated-not-broken (probe-secret mismatch), never a false outage. Verified: header injected iff token present (absent → not leaked, live probe still `AuthGated`); env-var default wired; `--local` gate path untouched → determinism gate byte-unchanged (**HEALTHY 278/278** offline). Probe-script + workflow-env only — no `platform/data` altered → no provenance regen, no visual/app change → no PR/headless render needed. · against `pipeline/check_site_health.py` (automation-bypass support) + `.github/workflows/site-health.yml` (secret wiring)._

_Audit run: 2026-08-15 · finding: deploy re-verified green up front (master production alias **HTTP 200** on `/`, `/app.js`, `/data/meta.json`, `/data/branches.json`, `/status.html`, `/status_data.json`; determinism gate **138 passed · 0 failed**, data-integrity **455/455**; autonomy plan 98% — 49 done / 1 owner-side open, no autonomously-completable backlog item left). With the shape-probe, freshness-guard, cross-consistency (book-total), source-consumption and dead-code backlogs all exhausted, this run added a NEW cross-layer-consistency dimension the prior peer-consistency audit (08-11(b)) named but did not assert: **the two obj-#2 peer surfaces must agree per brand.** `competitor_coverage.json` (de-dups `competitors_census.json` DIRECTLY → national `found`/brand) and `peer_province.json` (census → `rival_density.json` district PIP → province rollup, `by_brand` summing back to a national figure) both count the same big-4 field by DIFFERENT paths, and each is `--check`-gated — but a `--check` only proves a file reproduces from ITS OWN input, never that the two agree with EACH OTHER, so a one-sided de-dup/district-assignment change would silently diverge the two exec boards with a green gate. Verified they reconcile EXACTLY today (Muangthai 8931 · Srisawad 5203 · Tidlor 1919 · Heng 450 · all-rival total 16,503 · AutoX anchor 2,015 — `competitor_coverage` == `peer_province` rollup on all 6). Closed it with `pipeline/check_peer_rollup.py` — a PURE cross-consistency assertion (nothing written; derives every expected figure from the live files so a census refresh that rebuilds both stays green, a one-sided change trips it; exit 1 on mismatch), wired into `tests/run.sh` after `build_peer_province.py --check` and auto-discovered by `rederive_drift.py` (`--selftest` 140 → **141** gated scripts). Negative-tested a real tripwire (one-sided +5 to Muangthai `found` → flags `['Muangthai']`; province truncation moves the rollup). Test-harness/pipeline-only — no `platform/data` altered → no provenance regen, no visual/app change → no PR/headless render needed. · against `tests/run.sh` (gate wiring) + `pipeline/check_peer_rollup.py` (new cross-surface guard)._

_Audit run: 2026-08-13 · finding: deploy + integrity re-verified green up front (master production alias **HTTP 200** on `/`, `/app.js`, `/data/meta.json`; `build_provenance.py --check` byte-exact — **142 layers, 0 stale** (threshold 180d), **0 unlabelled**, only the 2 KNOWN DLT-upstream-capped layers `vehicle_collateral`/`ev_penetration` at 164d/`2026-02-28` (DLT's freshest resource); **0 broken data refs** — all **107** live `data/*.json` reads across `app.js`+all `*.html` resolve to committed files; `site-health.yml` correctly targets the master alias; peer board re-confirmed honest — Srisawad's `found` 5,203 vs 1,138 IR (457%) is CORRECTLY caveated in 3 places as store-locator group footprint vs listed-entity IR, not a mislabel). With the freshness-guard, shape-probe and cross-consistency backlogs exhausted, this run audited the **dimension the last PROGRESS_LOG entry named as the next pass: source-data consumption** — extend the `tests/orphan_layers.py` producer-aware idea from `platform/data/*` (all consumed) to `source-data/*`, i.e. does every committed source layer feed a downstream builder? **Finding #1 (a warning for the next run): a naive orphan-source gate is UNSAFE — it false-positives 63/76.** The `platform/data` gate works because a leaf there is only ever WRITTEN by its builder; but a `source-data` layer is READ by pipeline builders that ALSO write their own JSON output, so a write-sink→producer classifier misreads a genuine consumer (e.g. `build_amphoe.py` reading `vehicles_by_province.json`, 13 refs) as that layer's producer and drops it. So this cannot be shipped as a hard gate the way `orphan_layers.py` was — a correct one needs open-then-parse dataflow (which JSON each `open(..,'r')`/`load()` actually reads), not a same-file co-occurrence heuristic. **Finding #2 (the one genuinely-dormant source layer, hand-verified): `source-data/bot_policy_rate.json`** is referenced by nothing but its own puller `pull_bot_policy_rate.py` (scheduled in no workflow) — every other low-ref candidate traced to a real consumer. It is **BENIGN, not a defect:** the app's live `policy_rate` (Overview macro card + `regional_outlook`) sources the SAME MEASURED value **1.0% (period 2026-06)** from BIS via `pull_macro.py`, verified IDENTICAL to the BoT-native latest MPC decision in the dormant file (meeting `ครั้งที่ 3/2569`, 2026-06-24, Hold, `rate 1.0`). So `bot_policy_rate.json` is a richer dormant *reference* (full 203-decision MPC history), not a stale/mislabelled live layer; wiring it into `build_macro_indicators.py` would change only the source attribution (`BIS`→`BoT MPC`) for **zero numeric gain** at real gate + rendered-card risk in an unattended run → correctly NOT done. Recorded so future negative-space runs don't re-investigate it and so a naive source-orphan gate isn't shipped. **Outcome: no concrete data/deploy/consistency defect — the honest state is a fully-healthy tree**, and the deliverable is this recorded source-data-consumption audit (a new standing dimension + a documented "don't build the gate this way" landmine) plus a `plan_cycle.py` dashboard refresh. Docs-only — no `platform/data`/pipeline file altered → determinism gate byte-unchanged, no provenance regen, no PR/headless render needed. · against the committed tree (`source-data/*` consumption sweep across all `pipeline/*.py`+`committee/*.py`+`platform/*`; cross-checked `bot_policy_rate.json` ⇄ `macro_indicators.json` policy_rate)._

_Audit run: 2026-08-11 (c) · finding: deploy re-verified green up front (master production alias **HTTP 200** on `/` + `/data/meta.json`; `build_provenance.py --check` byte-exact — 142 layers, only the 2 KNOWN DLT-upstream-capped layers `vehicle_collateral`/`ev_penetration` >90d at the freshest available 2026-02-28 vintage; determinism gate **132 passed · 0 failed**; `site-health.yml` correctly targets the master alias). The concrete gap fixed is a **dead-code / audit-integrity** defect the fetched-vs-probed sweep surfaced: a diff of SPA-`fetch()`'d `data/*.json` (114) vs the health-probe registry flagged `dbd_formation`/`region_debt`/`sfi_credit` as live-but-unprobed reads — but tracing each render path found they are **NOT live reads at all**. On 2026-08-02 four Overview macro cards (`province_lfs`, `region_debt`, `dbd_formation`, `sfi_credit`) were retired from `#overview` and folded SERVER-SIDE into `macro_book.json`; their client loaders/renderers were left as **dead code** (zero call sites anywhere in `platform/`, mount-point anchors absent from `index.html`) still carrying never-fired `fetch()` calls — which is exactly why the sweep mis-classified three layers as live SPA reads, and why a prior `check_site_health.py` comment had already tripped on the same confusion (correctly excluding `dbd_formation` while its equally-dead siblings `region_debt`/`sfi_credit` retained "eager Overview card" probe rationale). Removed all 8 orphaned functions + their module globals from `app.js` (−283 lines net), rewrote the retirement comment to state the code is gone and the MEASURED signal survives on the drill (business-formation → `macro_book.national.new_biz_n`, already asserted by `_shape_macro_book`; `region_debt`/`sfi_credit` remain probed as committed macro_book INPUTS, not live reads — corrected their rationale). Verified: every removed global was confined to the dead ranges (the many stray `LFS` grep hits were the label string "NSO LFS", not the variable); `node --check` OK; gate 132/0, data-integrity 455/455, `check_site_health --local` 236/236; **headless-render self-review (Chromium) confirmed `#overview`/`#home`/`#acq` render BYTE-IDENTICALLY to the pre-change baseline** — same console errors, zero page errors, same body-text length, `macro_book` drill present (dead-code removal is behaviour-neutral by construction). No `platform/data` file altered → provenance byte-unchanged, no regen. This makes the service audit's fetched-layer set HONEST so future sweeps stop re-tripping on the phantom fetches. · against `platform/app.js` (8 orphaned loader/renderer pairs) + `pipeline/check_site_health.py` (stale probe rationale comment)._

_Audit run: 2026-08-11 (b) · finding: deploy + gate re-verified green up front (master production alias **HTTP 200** on `/`, `/data/meta.json`, `/data/competitor_coverage.json`, `/data/peer_province.json`, AND today's freshly-pulled `/data/commodities.json` — **78,068 B live == committed**, so the daily NABC price cron's auto-merge reached the CDN intact; determinism gate **130 passed · 0 failed**, data-integrity **455/455**; `build_provenance.py --check` byte-exact; freshness clean — the only >30d vintages are the KNOWN-upstream-capped DLT layers `vehicle_collateral`/`ev_penetration` at 164d, still at DLT's freshest resource `…25690228` (2026-02-28); 77/77 province catchments present; **0 genuine broken data refs** (the 4 `data/*`-shaped grep hits — `apple_reviews`/`fuel_stations`/`perimeter_counts`/`rayong_trees` — are all `source-data/…` comment tails or a guarded optional 3D scene input); **0 genuine orphan layers** (the 235 name-unreferenced files are 231 slug-interpolated `<prov>_{places,roads,water}` scene inputs + gated build intermediates `pico_census`/`collateral_census`/`farmgate_prices`/`provenance_sidecar`); `site-health.yml` correctly targets the master alias). With the freshness-guard and shape-probe backlogs exhausted (see the 08-11(a) note below), this run audited a **dimension no prior SERVICE run had verified: cross-layer numeric consistency of the peer-comparison board** (obj #2's whole point — the figures the exec reads off `#acq` must trace to one measured/reported source, not diverge silently across layers). Verified end-to-end, all green: (1) **AutoX's ฿46.572bn book** reproduces exactly along `tape_real.json:bucket_ladder.book_total.os_sum` (46,572,023,587) → `/1e9` in `build_competitor_coverage.py:_autox_book_bn()` → `competitor_coverage.json:national_standing.book_intensity[AutoX].book_bn` (46.572, ฿23.1m/branch) — and this auto-derived link is **already drift-protected**: `build_competitor_coverage.py --check` sits at `tests/run.sh` line 339, and `rederive_drift.py` discovers the gate's `--check` set by PARSING `tests/run.sh`, so a future owner-side tape refresh that moved the book without rebuilding the peer board would turn the gate **red** and block the commit — no silent stale-book class exists here; (2) the **peer NPL ladder** `peer_npl.json` (TIDLOR 1.5 · MTC 2.53 · SAWAD 3.55) matches `docs/RESEARCH_DIGEST.md §B` FY2025 IR figures verbatim; (3) the **peer branch/book counts** in `competitor_coverage.json` (MTC 8,673 br / ฿183.222bn · Tidlor 1,873 / ฿109.586bn · Srisawad 1,138 / ฿93.155bn) match their cited §B IR lines. **Data-bot drift posture re-confirmed structurally sound:** every direct-to-master data job (NABC/fuel/thaiwater/macro) runs `rederive_drift.py` + full gate before auto-merge; the 3 committers that skip the gate (`data-collateral-census`, `data-set-filings`, `deck-freshness`) all push to a **data branch + draft PR** (never straight to master) and touch only self-`--check`ed or non-derived files (`source-data/set_filings`, `docs/decks`), so none can land gate-drift on master. **Outcome: no concrete data/deploy/consistency defect found — the honest state is a fully-healthy, internally-consistent tree**, and the run's deliverable is this recorded cross-consistency verification (a new standing audit dimension) rather than a fabricated micro-fix. Docs-only + `plan_cycle.py` dashboard refresh — no `platform/data` file altered → gate byte-unchanged, no provenance regen, no PR/headless render needed. · against the committed tree (cross-layer read of `tape_real.json` / `competitor_coverage.json` / `peer_npl.json` / `RESEARCH_DIGEST.md`; verified gate-protected via `tests/run.sh` L339 + `rederive_drift.py`)._

_Audit run: 2026-08-11 · finding: deploy re-verified green up front (master production alias **HTTP 200** on `/` + `/data/meta.json`; `build_provenance.py --check` byte-exact; live health **231/231**; every guarded live-pull layer fresh — commodities 0d, fuel 1d, thaiwater 1d, macro 2d, peer_scoreboard 4d; peer layers cross-consistent (`peer_province.per_brand_total` == `competitor_coverage.found`, 16,503 rivals / 2,015 AutoX); `site-health.yml` correctly targets the master alias). The concrete gap fixed reopens a premise the freshness-guard backlog had marked "exhausted": every prior audit treated **`search_demand.json`** (obj#2 brand share-of-search, live-`fetch()`'d into `#acq`) as a **monthly** Google-Trends layer — honestly labelled stale, therefore deliberately **excluded** from `FRESHNESS_LAYERS` (a lag was ambiguous). That is no longer true: **`data-swarm.yml` (4×/day)** now carries a `google_trends` feed that re-pulls + re-derives the board — `meta.pulled_at_utc` advanced **3× in an 18h window** (08-08 21:58 → 08-09 10:08 → 08-09 16:01 UTC), live vintage 2 days old — so it became a **frequently-refreshed live read with a shape probe but no freshness guard**: a dead Trends pull would freeze it and ship **green forever** with no phone alert. Added `("data/search_demand.json", "pulled_at_utc", 35, …)`. **Why TTL 35:** both refresh paths (the 4×/day swarm + the monthly `data-search-demand.yml` backstop) call the SAME rate-limited `pull_google_trends.py` from the same CI IP range, so a Google block freezes them together and `pulled_at_utc` (stamped fresh on every *successful* pull) stops advancing; a >35-day lag is past both the monthly backstop cadence AND any rate-limit cluster (days) — an unambiguous "the Trends pull is dead" signal. Unit-tested against the real payload: PASS 2d, PASS 35d edge, FAIL +42d, absent→"not evaluated"; `pulled_at_utc` carries a `T..Z` tail that `_parse_iso_day` handles via `[:10]`. Live: `search_demand.json` now freshness-evaluated (2d, TTL 35, PASS), **231 → 232 HEALTHY**. Live-only → the `--local` gate path skips it, so `tests/run.sh check` is byte-unchanged (**224/224 HEALTHY offline**); determinism gate **130 passed · 0 failed**. Probe-script-only — no `platform/data` file altered → no provenance regen, no PR/headless render needed. · against `pipeline/check_site_health.py` (`FRESHNESS_LAYERS`, live-only)._

_Audit run: 2026-08-10 · finding: deploy re-verified green up front (master production alias **HTTP 200** on `/`, `/data/meta.json`, `/data/competitor_coverage.json`; determinism gate 130/0). With the shape-probe and freshness-guard backlogs exhausted, this run closed a **TEST-B (frozen-value) coverage gap** in `check_feed_liveness.py` — the one guard that catches a dead-but-present feed (stamp advancing, value frozen; the expired-key class). A fetched-vs-accumulated cross-check found the **YouTube feed itself had no frozen-value canary**: `append_history.py` accumulates 26 series but none for `rival_youtube.json`, even though the checker's own docstring cites `youtube_comments.json` freezing for **9 days behind an expired key** as its origin story — TEST A (stale stamp, via `live_board`) is blind to that class by design. Fixed by adding a `rival_youtube_views` canary = summed `channels[].views_total` (verified moving 915.9M → 920.0M across the 07-30 → 08-09 pulls; goes flat only if the API stops returning fresh stats, so no false-alarm risk — `n_videos` was rejected as it sat flat at 1,543). Backfilled real git history (`--from-git`), re-projected (`build_feed_history.py --check` byte-exact), regenerated provenance (layer set unchanged 142=142, 0 unlabelled). New series is `chartable:false` at 2 points → `live.html` renders no new row and `build_live_board.py --check` reproduces byte-exact → no visual/app change. Negative-tested the guard fires on 4 identical in-window values and stays clean on 4 moving ones; live it reports "too few to judge" (thin, exit 0). Gate **130 passed · 0 failed**. · against `pipeline/append_history.py` (`_youtube_views_total` + registry spec) + regenerated `source-data/feed_history.json` / `platform/data/feed_history.json` / `provenance.json`._

_Audit run: 2026-08-09 · finding: deploy re-verified green up front (master production alias **HTTP 200** on `/`, `/data/meta.json`, `/data/peer_province.json`; `build_provenance.py --check` byte-exact; freshness sweep clean). With the shape-probe and freshness-guard backlogs exhausted, a negative-space sweep found a **structural refresh gap** serving obj #1: `ev_penetration.json` + `vehicle_collateral.json` (MEASURED per-province EV/diesel penetration + diesel-share collateral watch, rendered in the Macro collateral book) were the two **stalest** layers in the tree — frozen at the **2026-02-28** DLT vintage — because their raw input `source-data/dlt/raw/dataset_1_1_04/*.csv` is landed only by `pipeline/pull_dlt_fuel.py`, a light any-IP CKAN puller **scheduled in NO workflow** (nor in `pull_swarm.py`) — the "unscheduled puller aging in silence" case. The fix wires ONE tolerant `pull_dlt_fuel.py` step into `.github/workflows/data-gov-census.yml` (which already pulls the SAME `dataset_1_1_04` weekly from the same CKAN host for type-counts, already runs the gate-driven `rederive_drift.py`, and already opens a draft PR — so the raw CSV was the only missing link; both builders are in `tests/run.sh`'s `--check` set at 278/288, so the existing re-derive picks them up automatically). Verified: DLT's newest resource is still `…25690228` (2026-02-28), so the layers are already at the freshest available vintage — staleness is upstream, but the refresh path is now live for the next snapshot. The raw CSV was pulled locally this run so the determinism gate actually RAN (not SKIPped) `build_ev_penetration.py --check` + `build_vehicle_collateral.py --check` — both **byte-exact**; gate **131 passed · 0 failed**. Workflow-config-only, no `platform/data` altered → no provenance regen, no PR/headless render needed. · against `.github/workflows/data-gov-census.yml` (+ `pipeline/pull_dlt_fuel.py`, previously orphaned)._

_Audit run: 2026-08-08 (c) · finding: freshness coverage over the frequent live upstreams is complete, so this run turned to the **shape/deploy-probe** gap — the live-fetched obj-#1/#2 reads that render but have **no `_shape_*` deploy probe**, so a truncated/404 CDN deploy of one silently blanks its card with no phone alert. Cross-referenced every `data/*.json` live-`fetch()`'d in `app.js` (109) against the `check_site_health.py` shape registry (56 probed): the highest-value unprobed read was **`data/province_stress_index.json`** (obj #1, MEASURED NSO legs) — the combined **structural-stress index** (household DTI + unemployment) that renders the exec **front-door** "Structurally riskiest · household DTI + unemployment" card (`PSTRESS_LIST[0]`, app.js:6099) AND a `#map` lens, and is the **parent** whose composite the already-probed `province_pressure` join consumes. `loadProvinceStress` GATES every render on `PSTRESS_LIST` (built by filtering `.provinces` on `composite_stress != null`) being non-empty and degrades SILENTLY when the file is missing/truncated (PSTRESS stays empty, lens self-hides, no error) — and it **cannot self-heal** (its DTI + unemployment legs come from NSO SES/LFS folded by a pipeline build, not a CI pull). Added `_shape_province_stress_index` + its registry entry: asserts the composite-bearing rows the render gate is built from + the `province` name each row renders, mirrors the builder's honest `meta.absent` guard (absent → OK, not an alert, matching the `household_risk`/`search_demand` siblings), and asserts shape not values (robust to a future SES/LFS vintage). Live+local probe path (no wall-clock dependency) → the `--local` gate path exercises it: **161/161 HEALTHY offline** (was 158, +3 fetch/parse/shape); determinism gate **129 passed · 0 failed**. Probe-script-only — no `platform/data` file altered → no provenance regen, no PR/headless render needed. · against `pipeline/check_site_health.py` (`_shape_province_stress_index` + `CHECKS`)._

_Audit run: 2026-08-08 (b) · finding: with the daily/weekly price·weather·macro upstreams already freshness-guarded, this run closed the last **frequent** live-pull layer with no vintage guard — the **listed-peer SET market scoreboard** (`data/peer_scoreboard.json`, obj #2). It is live-`fetch()`'d into the #acq peer scoreboard (app.js:3816), refreshed by the **WEEKDAY** `data-set-peers.yml` cron (Mon–Fri) via an autonomous SET in-browser Playwright pull, and — like `macro_indicators` and the assist radars — **CANNOT self-heal**: if the Playwright/SET-API pull breaks, the last-good file is structurally valid, so every `_shape_peer_scoreboard` probe stays GREEN while a **frozen** market cap / valuation / peer-ROE scoreboard ships forever with no alert. `meta.price_asof` is the SET **trading date**, which advances every business day the pull succeeds, so it is a reliable vintage signal. `FRESHNESS_LAYERS` now carries `("data/peer_scoreboard.json", "price_asof", 12, …)` — a **12-day** TTL = ~7 missed trading-day pulls, past any weekend + Thai market-holiday cluster (validated tolerant of the current 2-day weekend lag: `price_asof` 2026-08-07 reads 2d old, PASS), so a trip is an unambiguous "the SET pull stuck", not a market lull. Live-only (a function of wall-clock now) → the `--local` gate path skips it, so `tests/run.sh check` is byte-unchanged (**158/158 HEALTHY offline**; determinism gate **129 passed · 0 failed**). Probe-script-only — no `platform/data` file altered → no provenance regen, no PR/headless render needed. Freshness coverage now spans every independent frequent-cron live upstream (NABC farm-gate, Bangchak fuel, ThaiWater flood/rain, thai price history, weekly BIS/WB macro, and now weekday SET peers). · against `pipeline/check_site_health.py` (`FRESHNESS_LAYERS`, live-only)._

_Audit run: 2026-08-08 · finding: data room re-confirmed healthy up front — live master production alias **HTTP 200** on `/` + `/data/meta.json`; `build_provenance.py --check` byte-exact; every daily-CI price/weather layer fresh (commodities farm-gate 0d, fuel 1d, thaiwater flood/rain 0d, thai_price_history 6d); the only lagging vintages are the KNOWN owner-side/monthly constraints (`search_demand` 35d monthly Google-Trends cron, `rival_pulse` promos 20d Thai-IP pull, both honestly labelled). The concrete gap fixed **extends the live-only freshness guard** (`check_site_health.py::FRESHNESS_LAYERS`): it covered the 5 daily price/weather layers but **missed `macro_indicators.json`** — the MEASURED, live-`fetch()`'d (app.js:1788) **obj-#1** national borrower-leverage read (household debt-to-GDP · policy/lending rate · CPI · USD/THB), refreshed by its OWN **weekly** CI cron (`data-macro.yml`, Mondays) from a **distinct keyless cloud upstream** (BIS + World Bank + ECB/Frankfurter FX) that none of the guarded price/weather layers touch. So the macro pull can freeze while every other layer stays fresh, every `_shape_*` probe still passes, and the frozen macro backdrop **ships green forever with no alert** — the exact missed class the freshness guard exists to close. `FRESHNESS_LAYERS` now carries `("data/macro_indicators.json", "pulled", 21, …)`. **Why `pulled` is a reliable weekly signal:** `pull_macro.py --stamp` writes `meta.pulled = today` every run AND the `usd_thb` leg moves daily, so the folded file genuinely changes → commits every weekly run; a >21-day lag (3 missed Mondays) is an unambiguous "the macro pull stuck" signal (TTL looser than the 14-day daily-layer TTL to match the weekly cadence). Verified: `_freshness_result` PASSes at 5d and the 21-day edge, FAILs under +40d, and leaves junk/absent vintages "not evaluated". Live-only (freshness is a function of wall-clock now) → the `--local` gate path skips it, so `tests/run.sh check` is byte-unchanged (**152/152 HEALTHY offline**; determinism gate **127 passed · 0 failed**). Probe-script-only — no `platform/data` file altered → no provenance regen, no PR/headless render needed. This completes the freshness-coverage set over the repo's independent CI data upstreams. · against `pipeline/check_site_health.py` (`FRESHNESS_LAYERS`, live-only)._

_Audit run: 2026-08-07 (b) · finding: the data room was re-confirmed healthy up front this run — live master production alias **HTTP 200** on `/` + `/app.js` + `/data/meta.json` + `/data/tape_real.json`; `build_provenance.py --check` reproduces byte-exact (**138 layers · 76 measured · 62 estimated · 0 unlabelled** — the shame board has held at 0 through the tree's growth to 521 git-tracked JSON files); freshness clean (**0 layers >180d stale**; oldest `vehicle_collateral.json` at 160d, freshest `fuel_prices.json` age 0); `peer_npl.json`'s MEASURED AutoX anchor still reproduces byte-exact against the current real tape (`build_peer_npl.py --check` OK); `site-health.yml` correctly targets the master production alias. A render-path re-scan for the highest-value surfaced-but-unprobed `data/*.json` reads closed the **proactive-assistance radar PAIR (obj #1)** — the two NEWEST surfaced exec reads, both live-`fetch()`'d into the Proactive-assist (#assist) surface and both with **no deploy probe**: **`assist_price_radar.json`** (the PRICE LENS — healthy farm book × MEASURED Thai farm-gate price direction: which provinces hold Current/X-bucket accounts riding on a falling crop, "the slice to call before collections turn") and **`assist_branch_radar.json`** (the BRANCH DRILL — the falling-crop radar taken from province down to branch, "a call list, not a map", shipped in the most recent commit `f86e075`). They are notable blind spots for the usual two reasons: (1) each `renderAssist…Lens` GATES its whole section on a non-empty array (`.crops` / `.provinces`) and silently drops to a calm _"not built for this vintage"_ placeholder with **no phone alert** when its file is missing/truncated; and (2) **neither self-heals** — `build_assist_radar_price.py` / `build_assist_radar_branch.py` have no cron, so a truncated/404 CDN deploy that guts either has no CI job to restore it. `check_site_health.py` now probes both (`_shape_assist_price_radar`: the `.crops` gate + a crop label + ≥1 numeric `n_current_x` [the "healthy accounts riding on it" column] + the `.provinces` call table + `meta.trigger` + the `.tripped` list — and it stays GREEN in the legitimate "nothing tripped" state where `.tripped` is empty; `_shape_assist_branch_radar`: the `.provinces` gate + a province `th` label + ≥1 province carrying a non-empty `.branches` list whose first row has a `name` + the numeric `meta.n_branches_ranked`/`n_provinces` the lead renders — SHAPE not values, and deliberately NOT requiring `exposed_crop_ha`/`n_farm` to be numeric since both are legitimately null under the tape's ≥30 floor). Verified: the real 127 KB + 263 KB payloads accepted; **thirteen negatives** reject non-dict / empty-or-non-list `crops` / blank crop label / no-numeric-`n_current_x` / empty `provinces` / missing `tripped` / missing `meta.trigger` (price) and empty-or-blank `provinces` / no-province-with-branches / branch-missing-`name` / missing-`meta.n_branches_ranked` (branch); the nothing-tripped state stays green. Offline `--local platform` reports **149/149 HEALTHY** (143 → 149, +6: fetch+parse+shape for each of the two files). Determinism gate **green** (probe-script-only — no `platform/data` file altered, so no provenance regen, and no visual/app change, so no PR/headless render needed; `--local` runs the SAME validators inside the gate). WHY LIVE freshness is unaffected: these are shape probes, not vintage guards; the assist radars are deterministic-rebuild layers with no daily cron, so they are (correctly) not in `FRESHNESS_LAYERS`. This closes the Proactive-assist surface's remaining obj-#1 deploy-health blind spot — the two newest actionable "call list" reads now fire a phone alert instead of silently blanking. · against the committed tree (2 probe validators + `DATA_FILES` entries in `pipeline/check_site_health.py`)._

_Audit run: 2026-08-07 (a) · finding: the data room was re-confirmed healthy up front this run (live master production alias **HTTP 200** on `/` + `/app.js` + `/data/meta.json`; `build_provenance.py --check` reproduces byte-exact; local `check_site_health.py --local` **137/137 HEALTHY**; every daily-CI price/weather layer fresh — fuel `pulled` 0d (today), commodities `farmgate_vintage` 3d, thai_price_history 5d, thaiwater flood/rain 3d; the only lagging vintages are the KNOWN owner-side/monthly constraints — `search_demand` `pulled_at_utc` 2026-07-04 (monthly Google-Trends cron, next fires the 9th) and `rival_pulse` promos 2026-07-19 (Thai-IP pull), both honestly labelled). The concrete gap fixed extends the **structural** freshness guard the 2026-08-06 (c) run added, not another shape probe: that guard's `FRESHNESS_LAYERS` set covered `commodities.json`, `thai_price_history.json` and the two ThaiWater layers, but **missed `fuel_prices.json`** — a DAILY, CI-refreshed, CI-reachable, cloud-pull (Bangchak retail oil-price API, `data-fuel-prices.yml`, cron `40 22 * * *`), MEASURED, live-`fetch()`'d (`app.js:11315`) layer. It is a genuine blind spot because `commodities.json` keys its freshness on `farmgate_vintage` (the **NABC** farm-gate upstream), a **DISTINCT source** from Bangchak: if the Bangchak fuel API drops or `pull_fuel_prices.py` starts erroring while NABC keeps working, `commodities.json` stays fresh, every `_shape_*` probe still passes, and `fuel_prices.json` **silently freezes and ships green forever** with no phone alert — exactly the missed class the freshness guard exists to close, one upstream short of full daily-CI coverage. `check_site_health.py`'s `FRESHNESS_LAYERS` now carries `("data/fuel_prices.json", "pulled", 14, …)`. **WHY `pulled` IS A RELIABLE SIGNAL (not a flat-price false alarm):** `pull_fuel_prices.py` writes `meta.pulled = <today>` on **every** run (`build(_fetch(), a.stamp)`), and the daily workflow re-derives + commits `platform/data/fuel_prices.json` whenever that changes — so `pulled` advances every successful daily cron (verified: live vintage today, and the master log shows fuel commits on 08-05 and 08-07, cadence well under the 14-day TTL), which means a >14d lag is an unambiguous "the Bangchak pull stuck" signal, not a legitimately-flat-price week. Verified: the pure `_freshness_result` proves the guard is real, not a no-op — against the LIVE production payload it PASSes today (`2026-08-07`, 0 days old) and FAILs under an injected +40-day clock (40 days > 14-day TTL → "the refresh cron may be stuck"); the full live `run_freshness_checks` PASSes all 5 layers (fuel included) against the deployment right now. WHY LIVE-ONLY (unchanged from the parent guard): freshness is a function of wall-clock "now", so it stays out of the deterministic repo gate — the `--local` path (`LocalFetcher`) skips the whole block, so `tests/run.sh check` output is **byte-unchanged** (still 137/137 HEALTHY offline). Determinism gate **green** (probe-script-only; no `platform/data` file altered → no provenance regen, no PR/headless render needed). This is strictly higher-leverage than another shape probe: it completes the daily-CI freshness-coverage set, closing the one remaining daily price upstream whose independent failure the shape probes structurally cannot see. · against `pipeline/check_site_health.py` (`FRESHNESS_LAYERS`, live-only)._

_Audit run: 2026-08-06 (c) · finding: the ~40 deploy probe validators guard every surfaced read's **shape**, but **not one asserts its VINTAGE** — so the one class of breakage they structurally cannot catch is a **silently frozen data-refresh cron**. If an upstream API drops, a workflow secret rotates, or a pull script starts erroring, the last-good file keeps serving, still passes every `_shape_*` probe, and ships **green forever** — the deploy looks healthy while the numbers quietly rot, with no phone alert. The data room was re-confirmed healthy up front this run (live master production alias **HTTP 200** on `/` + `/data/meta.json`; `build_provenance.py --check` reproduces byte-exact; **0 broken data references** — the 4 `data/*`-shaped grep hits all resolve to `source-data/…` comment tails or a guarded optional 3D scene, 0 real `fetch()` misses; **0 genuine orphan layers** — the unreferenced files are per-province slug-interpolated catchment/scene inputs + deterministic build intermediates like `pico_census.json`; `site-health.yml` correctly targets the master production alias). Internal data vintages were swept (git commit-date is uniform-2d from a bulk 08-04 commit and so useless as a freshness signal — the real signal is each layer's own `meta` vintage): the daily-CI price/weather layers are fresh (commodities `farmgate_vintage` 2d, thai_price_history 4d, thaiwater flood/rain 2d), and the only lagging vintages are the **known owner-side / Thai-IP constraints** — `rival_pulse` promos `promos_pulled_at` 2026-07-19 (18d, Thai-IP pull, **honestly labelled** in the UI: "Refreshed … (Thai-IP pull)") and `search_demand` `pulled_at_utc` 2026-07-04 (33d, monthly Google-Trends cron) — not breaks.
The concrete gap fixed is **structural, not another single probe**: `pipeline/check_site_health.py` now carries a **live-only** freshness guard (`run_freshness_checks` / `FRESHNESS_LAYERS`) over the DAILY, CI-refreshed, CI-reachable price/weather set — `commodities.json` (NABC farm-gate + fuel), `thai_price_history.json`, `thaiwater_flood.json`, `thaiwater_rain.json` — where a lagging vintage is an unambiguous "the cron broke" signal. A layer FAILs (→ nightly GitHub issue → phone alert) **only** when its `meta` vintage parses cleanly AND exceeds a generous **14-day TTL** (~2 weeks of missed daily runs, well past any weekend/holiday upstream gap); any fetch / parse / missing-key case records a non-fatal "not evaluated" note, so the guard is **false-alarm-proof** — it can only newly-fail on genuine staleness, and the fetch + shape probes already own "does the file serve". **WHY LIVE-ONLY (HttpFetcher, never `--local`):** freshness is inherently a function of wall-clock "now", so it CANNOT live in the deterministic repo gate (`tests/run.sh` runs `--local` and must reproduce byte-for-byte on any date); the nightly probe already runs against the real deployment with a real clock, which is exactly where it belongs. The `--local` path skips the block entirely, so the gate's output is **byte-unchanged** (still 131/131 HEALTHY offline; `run.sh` only requires `--local` exit 0). Verified: the pure `_freshness_result` helper unit-tested (fresh 2d PASS · 20d/15d stale FAIL · 14d edge PASS · junk/None/with-time all "not evaluated"); the live runner against the committed tree PASSes all 4 today and FAILs all 4 under an injected +40-day clock (proving it is a real guard, not a no-op). Determinism gate **green** (`check_site_health.py --local` accepts the committed payloads; no `platform/data` file altered — probe-script-only, so no provenance regen and no PR/headless render needed). This is strictly higher-leverage than the prior "one more shape probe per run" cadence, whose backlog had reached its low-value tail: it closes an entire missing class of deploy-health coverage (data going stale) that no amount of shape probing could reach. · against `pipeline/check_site_health.py` (`run_freshness_checks` + `FRESHNESS_LAYERS`, live-only)._

_Audit run: 2026-08-06 (b) · finding: the ~40 deploy probe validators had **no repo-gate guard** — now they do. Data room re-confirmed healthy up front this run (live master production alias **HTTP 200** on `/` + `/data/meta.json`; `build_provenance.py --check` reproduces byte-exact; freshness clean, **0 layers >180d stale**, oldest `vehicle_collateral.json` at 158d; **0 broken data references** across the 131 static `data/*.json` refs; 77/77 province catchments present; every `--check`-capable builder already gated; `site-health.yml` correctly targets the master production alias). The concrete gap fixed is **structural, not another single probe**: `pipeline/check_site_health.py`'s ~40 `_shape_*` validators — every one hand-authored by this intelligence loop to guard a surfaced exec read at deploy time — were **never invoked by `tests/run.sh check`**, exactly as every prior audit note states ("the gate does not invoke `check_site_health.py`, so this probe-script-only change is outside its scope by construction"). So a future edit that broke a validator against the real committed payload (or a page that lost the AutoX wordmark, or a probed data file gone missing) would ship **silently**, surfacing only in the nightly LIVE run as a filed GitHub issue — never in the pre-commit gate. `tests/run.sh` `phase_check` now runs the SAME validation code path against the local committed tree — `python3 pipeline/check_site_health.py --local platform` (`LocalFetcher` = filesystem only, pure stdlib, **no network**, so it stays inside the gate's offline/deterministic contract). Verified all 41 probed data files AND the probed entry pages are git-tracked, so the self-test reproduces on a clean checkout; it FAILs only on a genuine probe/payload regression. Gate green **122 passed · 0 failed** (was 121 — the self-test is the +1, reports `[PASS]`). Negative-tested it is a real guard, not a no-op: temporarily dropping `meta.json`'s `updated` key drove the self-test to exit 1 (gate red); `git checkout` restored exit 0. This is strictly higher-leverage than the prior "one more probe per run" cadence, whose backlog had reached its low-value tail (only secondary graceful-degrading reads remained) — it protects all ~40 existing validators and every future one at once. No `platform/data` file altered — test-harness change only, so no provenance regen and no PR/headless render needed. · against `tests/run.sh` (`phase_check`) + `pipeline/check_site_health.py` (`--local`)._

_Audit run: 2026-08-06 (a) · finding: data room re-confirmed healthy up front — provenance `build_provenance.py --check` reproduces byte-exact (**136 layers · 75 measured · 61 estimated · 0 unlabelled**), freshness clean (**0 layers >180d stale**; oldest `vehicle_collateral.json` at 158d, freshest `fuel_prices.json` age 0), and the live master production alias **HTTP 200** on `/` and `/data/meta.json`. A render-path re-scan for the highest-value surfaced-but-unprobed `data/*.json` read closed the **last surfaced obj-#2 PEER read on the Competition surface with no deploy probe** — the one explicitly named as the next target by the 2026-08-05 audit below: **`peer_npl.json`** (obj #1 + #2, the peer LOAN-QUALITY benchmark — the listed title-lenders' OWN reported NPL ratios [Tidlor 1.5% / MTC 2.53% / Srisawad 3.5–3.6%, docs/RESEARCH_DIGEST.md §B, FY2025 / 2025 IR] shown next to AutoX's MEASURED own-book NPL from the real loan tape [NPL-live 6.06% OS, strict-90+ 12.21%]). It is a notable blind spot for the same two reasons as its `peer_scoreboard` sibling: (1) it is a load-bearing exec read that `drawPeerNpl` GATES the whole `#acq` board on (a non-empty `.peers` array — else it silently drops to a calm _"Peer NPL benchmark not available"_ placeholder with **no phone alert**), rendering per row `.name`/`.ticker` + `.npl` (the bar, the colour band, the label AND the readout's best/worst spread clause) + `.collateral` + `.source`, plus the distinct MEASURED `.autox` self-anchor row (`.name` + `.npl_live_os_pct` [`.toFixed(2)`, bar + readout] + `.npl_90plus_os_pct` [`.toFixed(1)`, strict-90+ readout figure]); and (2) **unlike the census siblings it cannot self-heal** — its peer figures come from an off-repo research doc (`RESEARCH_DIGEST.md §B`) and its AutoX anchor from the owner-side loan tape, neither of which any CI job re-pulls, so a truncated/404 CDN deploy that guts it has no job to restore it. `check_site_health.py` now probes it (`_shape_peer_npl`: fetch + parse + render-shape — asserts the `.peers` gate [≥2 rows], a row `name`/`ticker` label, ≥1 numeric `npl`, the `.autox` anchor dict carrying a non-blank `name` and numeric `npl_live_os_pct` + `npl_90plus_os_pct` — shape not values, robust to a future RESEARCH_DIGEST / tape-vintage refresh moving the ratios). Verified: the real 2,571-byte payload accepted; **twelve negatives** reject non-dict / missing-or-short-or-non-list `peers` / row-not-dict / row-missing-`name`&`ticker` / no-numeric-`npl` / missing-or-non-dict `autox` / anchor-missing-`name` / non-numeric-`npl_live_os_pct` / non-numeric-`npl_90plus_os_pct`. Offline `--local platform` reports **131/131 HEALTHY** with `peer_npl.json` served + shape-sane (128 → 131 checks, +3: fetch + parse + shape). Determinism gate **121 passed · 0 failed** (data integrity 455/455 — the gate does not invoke `check_site_health.py`, so this probe-script-only change is outside its scope by construction). No `platform/data` file altered — probe-script-only, so no provenance regen and no PR/headless render needed. Next probe targets: the remaining lower-value secondary reads (the graceful-degrading rival-pulse promo/review boards, the province deep-dive fetches). · against the committed tree (probe validator + `DATA_FILES` entry in `pipeline/check_site_health.py`)._

_Audit run: 2026-08-05 · finding: data room re-confirmed healthy up front — provenance `build_provenance.py --check` reproduces byte-exact (**136 layers · 75 measured · 61 estimated · 0 unlabelled**), freshness clean (**0 layers >180d stale**; oldest `vehicle_collateral.json` at 158d, freshest `thaiwater_flood.json` age 0), and the live master production alias **HTTP 200** on `/` and `/data/meta.json`; `site-health.yml` correctly targets the master production alias. A render-path re-scan for the highest-value surfaced-but-unprobed `data/*.json` read closed the **last unprobed obj-#2 PEER read on the Competition surface**: **`peer_scoreboard.json`** (obj #2, MEASURED — SET listed-peer market scoreboard: market cap / valuation / ROE / net profit for the 3-4 listed title-lenders — MTC ฿70.5bn … — with AutoX's 25% ROE TARGET as the reference line). It is a notable blind spot for two reasons: (1) the render code itself calls it _"the sharpest external benchmark we have"_, yet every sibling obj-#2 competition read (`peer_province`, `competitor_coverage`, `rival_reputation`, `rival_threat[_region]`, `pico_district`) was already probed and this one was not; and (2) **unlike the census siblings it cannot self-heal** — SET (`set.or.th`) is Akamai-blocked from CI (owner-side / Thai-IP refresh only, per the §-below note), so a truncated/404 CDN deploy that guts it has no CI job to re-pull it. `drawPeerScore` GATES the whole `#acq` board on a non-empty `.peers` array and silently drops to a calm _"Listed-peer scoreboard not available"_ placeholder with **no phone alert** when the file is missing/truncated — masking the breakage — while per row it renders `.name`/`.symbol` + `.market_cap_bn` (the bold "Mkt cap" column) + `.roe` (the ROE column, its bar, AND the readout's "would sit above/below" clause vs the 25% target), and the readout leads with `.headline` and benchmarks against `.autox_roe_target`. `check_site_health.py` now probes it (`_shape_peer_scoreboard`: fetch + parse + render-shape — asserts the `.peers` gate, a row `name`/`symbol` label, ≥1 numeric `market_cap_bn`, ≥1 numeric `roe`, a non-blank `headline`, and the numeric `autox_roe_target` reference line — shape not values, robust to a future SET price/quarter pull moving the figures). Verified: the real 2,360-byte payload accepted; **eleven negatives** reject non-dict / missing-or-short-or-non-list `peers` / row-not-dict / row-missing-`name`&`symbol` / no-numeric-`market_cap_bn` / no-numeric-`roe` / blank-`headline` / missing-or-non-numeric-`autox_roe_target`; a symbol-only row is still accepted. Offline `--local platform` reports **125/125 HEALTHY** with `peer_scoreboard.json` served + shape-sane; the live alias serves it **HTTP 200** (2,360 bytes, matches local). Probe coverage **37 → 38** exec data layers (122 → 125 checks incl. fetch+parse). Determinism gate **121 passed · 0 failed** (data integrity 455/455 — the gate does not invoke `check_site_health.py`, so this probe-script-only change is outside its scope by construction). No `platform/data` file altered — probe-script-only, so no provenance regen and no PR/headless render needed. Next probe targets: the remaining lower-value secondary reads (`peer_npl.json` reported-NPL benchmark, the graceful-degrading rival-pulse promo/review boards, the province deep-dive fetches). · against the committed tree (probe validator + `DATA_FILES` entry in `pipeline/check_site_health.py`)._

_Audit run: 2026-08-04 · finding: deploy re-verified green up front (master production alias **HTTP 200** on `/` and `/data/meta.json`; `build_provenance.py --check` reproduces exactly). A render-path re-scan for the highest-value surfaced-but-unprobed `data/*.json` read surfaced **`rival_reputation.json`** (obj #2, MEASURED sample) — the rival service-reputation board (review-count-weighted Google rating by brand: Tidlor 4.94★ … Krungsri 3.08★, 547 rated rival branches / 4,414 reviews). It is a notable blind spot because it is the shared **PARENT of the two already-probed threat layers** (`rival_threat` + `rival_threat_region` both consume its ratings) yet was itself **unprobed** — and its two children are **pre-built, committed** files, so they keep rendering even if `rival_reputation.json` is gutted at deploy. `drawRivRep` GATES the whole `#acq` board on a non-empty `.by_brand` array and reads `.brand` + `.rating_wavg` per row + `.headline` in the readout; a truncated/404 CDN deploy silently drops it to the calm "Rival reputation not yet computed" placeholder with **no phone alert**, masking the breakage — the same "broken demo" blind spot the `peer_province` / `competitor_coverage` / `rival_threat` probes closed for the sibling competitive reads. `check_site_health.py` now probes it (`_shape_rival_reputation`: fetch + parse + render-shape — asserts the `by_brand` gate, the `brand` name each row renders, at least one numeric `rating_wavg` (the weighted-rating column + colour scale), and the non-blank `headline` the readout reads — shape not values, robust to a future rating-vintage refresh). Verified: the real payload accepted; **nine negatives** reject non-dict / missing-or-non-list-or-short `by_brand` / row-not-dict / row-missing-`brand` / no-numeric-`rating_wavg` / blank-or-missing-`headline`. Offline `--local platform` reports **122/122 HEALTHY** with `rival_reputation.json` served + shape-sane; the live alias serves it **HTTP 200** (1,561 bytes, matches local). Probe coverage **36 → 37** exec data layers. Determinism gate **121 passed · 0 failed** (the gate does not invoke `check_site_health.py`, so this probe-script-only change is outside its scope by construction). No `platform/data` file altered — probe-script-only, so no provenance regen needed. Next probe targets: the remaining lower-value secondary reads (the graceful-degrading rival-pulse promo/review boards, the province deep-dive fetches). · against the committed tree (probe validator + `DATA_FILES` entry in `pipeline/check_site_health.py`)._

_Audit run: 2026-08-03 (c) · finding: with the data room re-confirmed clean this run — `build_provenance.py --check` reproduces exactly, the **live master production alias HTTP 200** on `/`, `/data/meta.json` and `/data/deltas.json` (one transient TLS-handshake blip on the first `deltas` fetch cleared on immediate retry — network flake, not a regression), and `site-health.yml` correctly targets the master production alias — a render-path re-scan of the surfaced-but-unprobed `data/*.json` reads (75 total, most secondary or graceful-degrading) closed the newest wave still uncovered: **`vehicle_models.json`** (the #275/#276 Macro "nameplate" wave, obj #1 collateral context — explicitly flagged as unprobed by the 2026-08-02 (c) audit below). It is load-bearing on **two** MEASURED render paths, not one: (1) the "which models, and which are growing" nameplate panel (`cb-nameplates`) GATES on `V.plates_last12` then renders the pickup/ppv `.top[]` boards (`plate/units/share_pct/yoy_pct`); (2) the **collateral pickup-definition verdict** (`renderYearTable`) takes this layer as the AUTHORITATIVE pickup count on AutoX's own nameplate rule (pickup+PPV nameplates in any class), falling back to the registrar's รย.3 truck class only when it is absent. The client loader itself sets `VMODELS=null` unless `Array.isArray(v.annual)`, so a truncated/gutted CDN deploy **silently** reverts BOTH surfaces to their fallback with no phone alert — the same "broken demo" blind spot the `collateral_book`/`macro_book`/`deltas` obj-#1 probes closed for their siblings. `check_site_health.py` now probes it (`_shape_vehicle_models`: fetch + parse + render-shape — asserts the `annual` array gate + a `plates_last12` dict carrying non-empty `pickup` AND `ppv` groups whose first `.top` row has a string `plate` and numeric `units`/`share_pct` — shape not values, robust to a future DLT-vintage refresh moving the counts). Verified: the real 21,829-byte payload accepted; twelve negatives reject non-dict / missing-or-empty-or-non-list `annual` / missing-or-empty `plates_last12` / missing pickup-or-ppv group / empty `.top` / row-missing-`plate` / non-numeric-`units` / missing-`share_pct`. Offline `--local platform` reports **116/116 HEALTHY** with `vehicle_models.json` served + shape-sane; the live alias serves it **HTTP 200** (21,829 bytes, matches local). Probe coverage **35 → 36** exec data layers. Determinism gate **121 passed · 0 failed** (data integrity 455/455 — the check phase does not read the probes). No `platform/data` file altered — probe-script-only, so no provenance regen needed. Next probe targets: the two sibling reads from the same nameplate wave still uncovered — `vehicle_brands.json` (`cb-vbrands`, gates on `national.by_type.ry3 && .ry1`, ESTIMATED province split) and `vehicle_mix.json` (`cb-mix`, gates on `national.stock && .new && types.length`, MEASURED). · against the committed tree (probe validator + `DATA_FILES` entry in `pipeline/check_site_health.py`)._

_Audit run: 2026-08-03 (b) · finding: with the data room otherwise clean this run — **0 broken data references** (all 3 `data/*`-shaped hits in `app.js` on a fresh scan resolve to `source-data/…` in comments, not missing SPA fetches), **0 surfaced layers >180d stale** (oldest ISO-dated `vehicle_collateral.json`/`ev_penetration.json` at 156d), and the **live master production alias HTTP 200** on `/`, `/data/meta.json` AND `/data/deltas.json` — a render-path re-scan of the FRONT-DOOR (#home) reads found the **last surfaced command-center read with no deploy probe**: `deltas.json` (the TIME dimension, obj #1 — which segments/branches are getting riskier between vintages). It drives BOTH the command-center "Movers" card (`renderHomeMovers` off `DELTAS.region` + `DELTAS.branches`) AND the whole Risk-trend (#trend) tab (`.board` YoY re-ratings + the region/branch mover rows). Its degradation is MORE insidious than a silent blank: a missing/truncated/404 file drops both surfaces to the CALM string _"Baseline captured — trends appear after the next data refresh"_ — **masquerading a broken deploy as the normal single-vintage baseline state**, hiding real obj-#1 risk movement with no phone alert. `check_site_health.py` now probes it (`_shape_deltas`: fetch + parse + render-shape — asserts the `baseline` gate + a blank-safe `to` vintage label, and when NOT in baseline mode a non-empty `branches` movers list carrying `n/comp`, a non-empty `region` list carrying `r/d_agri`, and the `board` YoY list the #trend tab reads; it stays GREEN in a legitimate single-vintage baseline so it can't false-alarm if the snapshot history is ever reset). Verified: ten tests — the real 80-branch/5-region payload AND a legitimate `baseline:true` file both accepted; eight negatives reject non-dict / missing-`baseline` / blank-`to` / empty-`branches` / branch-missing-`comp` / empty-`region` / region-missing-`d_agri` / non-list-`board` shapes. Offline `--local platform` reports **113/113 HEALTHY** with `deltas.json` served + shape-sane; the live alias serves it **HTTP 200** (16.3 KB). Probe coverage +1 exec check. Determinism gate **121 passed · 0 failed** (data integrity 455/455). No `platform/data` file altered — probe-script-only, so no provenance regen needed. · against the committed tree (probe validator + `DATA_FILES` entry in `pipeline/check_site_health.py`)._

_Audit run: 2026-08-02 (c) · finding: a render-path re-scan of the recently-landed **#258/#261 macro/collateral wave** found several new SPA-fetched layers still with **no deploy site-health probe** — `collateral_book`, `macro_book`, `farm_book`, `used_vehicle_value`, `vehicle_models`/`vehicle_brands`/`vehicle_mix`, `flood_hazard`, `thai_price_history` are all live-`fetch()`'d in `app.js`/`index.html` yet unprobed. The highest-value of them is **`collateral_book.json`** — the Overview/Macro tab's **section-leading collateral read** (obj #1): `renderCollateralBook` GATES the whole "Collateral value — what the titles are worth, and what we hold against them" section on `j.national && j.types` (else `host.style.display='none'`), and the load-bearing verdict sentence reads `N.os / N.core_share_pct / N.ltv_proxy_pct / N.ticket / N.eval_avg` plus the 8-row collateral-type table. MEASURED (real loan tape × DLT registrations); it live-degrades SILENTLY (a missing/truncated file just hides the primary obj-#1 collateral-value screen with no phone alert). `check_site_health.py` now probes it (`_shape_collateral_book`: fetch + parse + render-shape — asserts the display gate `national` KPI block with numeric `os/ltv_proxy_pct/ticket/eval_avg/core_share_pct` AND a non-empty `types` table whose rows carry `type/tier/os_share_pct` — shape not values, robust to a future tape/DLT vintage refresh). Verified: eight negative tests reject non-dict / missing-`national` / non-numeric-KPI / missing-or-empty-`types` / missing-`type` / missing-`tier` shapes while accepting the real payload; the offline `--local platform` path and the **live master production alias** both report **98/98 HEALTHY** with `collateral_book.json` served HTTP 200 (632 KB) and shape-sane. Probe coverage 97 → **98** exec checks. The rest of the data room re-confirmed clean this run: provenance `--check`-reproducible (**135 layers · 0 blank-vintage · 0 unlabelled**); live alias HTTP 200 on `/`, `/app.js`, `/data/meta.json`, `/data/branches.json`, `/data/competitor_coverage.json`; `site-health.yml` correctly targets the master production alias. Next probe targets: the sibling wave reads `macro_book` (`renderMacroBook` gates on `.national && .provinces`) and `farm_book` / `flood_hazard`. · against `platform/data/provenance.json` (**135 layers** · 0 unlabelled)._

_Audit run: 2026-08-02 (b) · finding: the live site-health probe covered 27 of the ~88 SPA-fetched `data/*.json` layers, and a full render-path re-scan this run found **two surfaced exec reads still with no deploy probe**, both live-degrading SILENTLY when their file is missing/truncated (no phone alert): (1) `contested_pop.json` — the command-center (#home) "MOST CONTESTED GROUND" front-door lead (`renderHomeWhitespace` reads `CPOP.top`) PLUS the National-map contested lens (index-aligned `.rows[i]=[pop10, contested_pop]`, obj #2); (2) `exit_whitespace.json` — the Competition (#acq) rival-fragility board under the Q1-2026 BoT registration deadline (`drawExitWhitespace` reads `.districts` + `meta.competitor_census`, obj #2, ESTIMATED), the last surfaced #acq competitive read with no probe. `check_site_health.py` now probes both (fetch + parse + render-shape: `contested_pop` asserts the `.top` leaderboard shape AND the exact 2015 index-aligned `.rows` length that a truncated build would silently misalign; `exit_whitespace` asserts a non-empty ~928-amphoe `.districts` board carrying the sort key + component split + the meta census block the readout headline reads — shape not values, robust to census growth). Verified: negative tests reject empty/short/mis-aligned shapes; the offline `--local platform` path and the **live master production alias** both report **95/95 HEALTHY** with both new files served HTTP 200 and shape-sane. The rest of the data room re-confirmed clean this run: provenance `--check`-reproducible (**130 layers · 435 files · 70 measured · 60 estimated · 0 unlabelled**); **0 broken data references** across 124 distinct `data/*.json` fetch refs; **0 genuine orphan signal layers** (the 3 non-geometry unreferenced files are legitimate build inputs / a retired stub); live alias HTTP 200 on `/`, `/app.js`, `/data/meta.json`, `/data/branches.json`, `/status_data.json`. Probe coverage 27 → **29** exec layers. · against `platform/data/provenance.json` (**130 layers · 435 files** · 70 measured · 60 estimated · 0 unlabelled)._

_Prior audit run: 2026-08-02 (a) · finding: the #248 macro/agri data wave landed three MEASURED, live-fetched survey/registry layers — `debt_source` (NSO household debt-by-source), `vehicle_fleet` (DLT registered-vehicle stock) and `farm_household` (OAE farm-household cash P&L) — that each stamp their freshness ONLY under a layer-specific key the extractor did not scan (`latest_year_ce` as an **integer** calendar year on the first two, `span` as a BE crop-year window on the third), so all three showed **blank** in the exec Data-room card despite carrying a real measured data-vintage. `_vintage_of()` now scans `latest_year_ce` (with int→str coercion) and `span`, placed LAST so any proper ISO/observation key still wins; `_parse_vintage` leaves each age-blank (a bare year / BE label is never coerced into a false age), exactly like the `vintage_individual='2025'` precedent. Verified the regenerated ledger changes **only these three** vintage cells (`'' → 2023`, `'' → 2025`, `'' → 2562/63..2566/67`) — counts (68 measured · 59 estimated · 0 unlabelled), labels, sources, files, and the freshness block (n_dated 24 / n_undated 103, correct — coarse labels are not ISO-dated) are byte-identical, and `build_provenance.py --check` reproduces exactly. `crop_mix` (the fourth new layer) correctly stays vintage-blank — it is a first-order DERIVED layer inheriting freshness from its measured inputs, the honest ABSENT state. Gate 113 passed · 0 failed, data integrity 448/448. · against `platform/data/provenance.json` (**127 layers · 432 files** · 68 measured · 59 estimated · 0 unlabelled)._

_Prior audit run: 2026-07-31 · finding: the live site-health probe's `_shape_competitor_coverage` validator asserted `.brands` + `meta.totals.found` but **not** `meta.national_standing` — the exec headline peer claim ("AutoX runs the 2nd-largest title-loan branch network"), which `drawCompCoverage` gates the whole readout on (`ns.autox_rank` + `ns.ranking`). A truncated deploy that dropped that block would silently vanish the headline with no phone alert; the probe now asserts it (shape, not counts). This run's fresh scan re-confirmed the rest of the data room is clean: provenance **118 layers · 60 measured · 58 estimated · 0 unlabelled** (`--check`-reproducible); freshness — 0 genuine dropped data-vintages across all 84 undated layers (the 7 date-shaped candidates are all the accepted pull-stamp / methodology-param blank set); **0 broken references** across 103 distinct `data/*.json` fetch refs; live master alias HTTP 200; determinism gate 100 passed · 0 failed. · against `platform/data/provenance.json` (**118 layers · 423 files** · 60 measured · 58 estimated · 0 unlabelled)._

_Prior audit run: 2026-07-30 · finding: `scenarios.json` (the LIVE/stress scenario engine, #sim) stamps its freshness only under `board_vintage` (= `2026M06`, the commodity/macro board month its MEASURED live drivers observe), which the extractor did not scan — so it showed **blank** in the exec Data-room card despite carrying a real measured vintage; the extractor now scans `board_vintage` and the cell surfaces (§1) · against `platform/data/provenance.json` (**115 layers · 420 files** · 57 measured · 58 estimated · 0 unlabelled — `build_provenance.py --check` reproduces exactly)._

_Prior audit run: 2026-07-28 · finding: the R2 catchment migration left **~2.2 GB of R2-duplicated building catchments committed to git** — the deployed data-room is **10× the size §4 last recorded** (§4, corrected that run) · against `platform/data/provenance.json` (114 layers · 419 files · 57 measured · 57 estimated · 0 unlabelled)._

**Deployment health (verified live this run, 2026-07-28):** the master production alias
(`competitive-intel-git-master-kaustav-bagchis-projects.vercel.app`) serves **HTTP 200** on `/`,
`/app.js`, `/status`, and the key data layers (`branches.json`, `meta.json`, `peer_province.json`,
`competitor_coverage.json`). Determinism gate **96 passed · 0 failed**; data integrity **446/446**;
all **77/77** province catchments present; **0** broken data references. No regression.

## Headline

The data room is healthy. **No broken data references, no missing fetches; provenance intact
(52 measured · 51 estimated · the single standing catchment family).** The one concrete gap this
run **fixed**: `peer_scoreboard.json` — the MEASURED SET listed-peer market scoreboard (obj #2, the
sharpest external competitive benchmark AutoX has) — stamps its real freshness date under the key
`price_asof` (`= 2026-07-17`, the SET price-observation date), which `build_provenance.py::_vintage_of()`
did not scan, so its vintage showed **blank** in the exec-facing Data-room card. Exactly the same
class of bug as the 2026-07-17 (6 keys), 2026-07-19 (3 keys) and 2026-07-24-am (`snapshot`) §1 fixes;
a full re-scan of every committed analytical layer's `meta` this run found `price_asof` to be the one
remaining **data-vintage** key still unscanned — so the extractor now scans it and the scoreboard
vintage surfaces like the rest. (Four other blank-vintage layers — `amphoe_crops`, `crop_margin`,
`region_debt`, `rival_universe` — carry only a **pull/verify** stamp, `retrieved` / `cost_ingested` /
`verified`, which the standing convention deliberately deprioritizes vs a data-observation vintage
[see the 2026-07-24-am note below], so their blank cells are the accepted honest ABSENT state, not a
bug.)

**Verified this run — the SET scoreboard cannot auto-refresh (a structural staleness, not a gap to
fix with a CI job):** every other live-pull layer family in the repo now has a scheduled `data-*.yml`
refresh job (the ThaiWater omission was closed 2026-07-24). The listed-peer scoreboard is the last
family **without** one — but unlike ThaiWater it is **genuinely blocked from CI**: `set.or.th`'s API
403s every external request (Akamai bot-protection) and even a headless-browser same-origin fetch from
this datacenter sandbox got `ERR_CONNECTION_RESET` (verified this run). So SET belongs with the
competitor corporate sites in the **owner-side / Thai-IP-only** refresh set, not the CI-refreshable
set — building a `data-set.yml` job would build a job that cannot pull. Surfacing the layer's own
`price_asof` observation date (this fix) is therefore exactly how the exec sees how current the
scoreboard is; a future refresh is owner-side (`pull_set_peers.py` from a browser that clears Akamai).

_Tree grew 96 → 104 layers since the last audit (drought_district, crop_drought, the two thaiwater
live-pulse layers, set/valuation and credit-anchor market layers, …). Re-verified this run: the
ledger reports 104 layers (52 measured · 51 estimated · 1 unlabelled = the standing 3D building-
catchment family, 77 files under one `family:true` row — structurally meta-less, an accepted standing
state, not a regression); every analytical layer is wired into a live `fetch()` or is a pipeline
input; and no `data/*.json` path referenced in `platform/*.html` + `app.js` fails to resolve._

## 1. Freshness per layer (the fix this run shipped)

**2026-08-02 (this run):** a full re-scan of every currently blank-vintage labelled layer's `meta`
for date-shaped keys *outside* the extractor's list found **three** MEASURED, live-fetched layers
(all new since the last audit, from the #248 macro/agri wave) still dropping a real **data-vintage**
from the Data-room card:

| Layer | Freshness key (was dropped) | Value now surfaced | Class |
|---|---|---|---|
| `debt_source.json` | `latest_year_ce` (int) | 2023 | MEASURED — NSO household debt-by-source survey, newest wave year |
| `vehicle_fleet.json` | `latest_year_ce` (int) | 2025 | MEASURED — DLT registered-vehicle stock, newest registry year |
| `farm_household.json` | `span` (BE crop-years) | 2562/63..2566/67 | MEASURED — OAE farm-household cash P&L survey, observation window |

`_vintage_of()` now also scans `latest_year_ce` (with int→str coercion — it is a bare calendar year,
not a date string, exactly the `vintage_individual` NSO-year precedent, just stored as an int) and
`span`, both placed **last** in the priority list (coarse, non-ISO labels — any proper ISO/observation
key still wins). `_parse_vintage` leaves all three age-blank, so the freshness pulse never coerces a
bare year or a BE crop-year window into a false age; the row's vintage cell simply surfaces the layer's
own committed label. A diff of the regenerated ledger confirms the change touches **only these three
vintage cells** — the 127-layer counts (68 measured · 59 estimated · 0 unlabelled), labels, sources,
the files block, and the freshness block (n_dated 24 / n_undated 103) are byte-identical, and
`build_provenance.py --check` passes on the recommitted ledger. No date is invented — each value is
read from the layer's own committed `meta`. `crop_mix.json` (the fourth new layer this wave) correctly
stays vintage-blank: it is a first-order DERIVED layer (province area × Thai farm-gate YoY × NSO
income) whose freshness is inherited from its measured inputs, so its blank cell is the honest ABSENT
state, not a bug.

---

**2026-07-30 (prior run):** live deployment re-verified green — the master production alias
(`competitive-intel-git-master-kaustav-bagchis-projects.vercel.app`) serves **HTTP 200** on `/`,
`/app.js`, `/data/branches.json`, `/data/meta.json`; the `site-health.yml` cron correctly targets that
alias. Determinism gate **96 passed · 0 failed**, data integrity **446/446**, provenance
`--check`-reproducible, **0** broken data references (the three unresolved `data/*.json` strings are a
code-comment path and two substring artifacts of `source-data/*`, none a live `fetch()`). A full
re-scan of every blank-vintage labelled layer's `meta` for date-shaped keys *outside* the extractor's
list found **one** layer still dropping a real **data-vintage** from the Data-room card:

| Layer | Freshness key (was dropped) | Value now surfaced | Class |
|---|---|---|---|
| `scenarios.json` | `board_vintage` | 2026M06 | MEASURED commodity/macro board month the LIVE scenarios observe (#sim scenario engine) |

`_vintage_of()` now also scans `board_vintage`, placed among the data-observation vintages (right
after `farmgate_vintage`, ahead of any pull timestamp — it is the board month the scenario engine's
own label commits to showing per card, a MEASURED data-vintage exactly like `price_vintage` /
`farmgate_vintage`, not a pull time). scenarios.json is fetched live (`app.js` `tmliFetch('scenarios')`,
rendered in `#sim`) so it appears in the exec Data-room card; it stamps freshness *only* under this
key, so without it the row showed **blank** despite a real measured vintage. Verified the change
touches **only this one** vintage cell (`'' → 2026M06`); a diff of the regenerated ledger confirms
every other field — the 115-layer counts (57 measured · 58 estimated · 0 unlabelled), labels, sources,
the files block — is byte-identical, and `build_provenance.py --check` passes on the recommitted
ledger. No date is invented — `2026M06` is read from the layer's own committed `meta.board_vintage`.
The standing accepted-blank set held: `amphoe_crops` (`retrieved`), `crop_margin` (`cost_ingested`),
`region_debt` (`retrieved`), `rival_universe` (`verified`) carry only a **pull/verify** stamp (the
convention deprioritizes those vs a data-observation vintage), and `tape_real`/`tape_geo_occ`'s
`mob_anchor` is a months-on-book methodology parameter, not a freshness date — so their blank cells
are the honest ABSENT state, not a bug. `board_vintage` was the one genuine dropped data-vintage.

---

**2026-07-25:** the tree grew 104 → **109 layers** (414 files) since the prior audit (new
market layers landed: `commodities`, `dbd_formation`, `credit_anchor`, `peer_npl`, `napprang`,
`vehicle_registry`, `province_lfs`, `nso_wage_anchor`, …). A full re-scan of every currently
blank-vintage labelled layer's `meta` for date-shaped freshness fields *not* in the extractor's key
list found **one** layer still dropping a real **data-vintage** from the Data-room card:

| Layer | Freshness key (was dropped) | Value now surfaced | Class |
|---|---|---|---|
| `commodities.json` | `farmgate_vintage` | 2026-07-24 | MEASURED Thai farm-gate price observation date (global Pink Sheet × Thai farm-gate × book-exposure board) |

`_vintage_of()` now also scans `farmgate_vintage`, placed among the data-vintage keys (right after
`price_asof`, ahead of any pull timestamp — it is the Thai farm-gate price *observation* date, a
MEASURED data-vintage exactly like `price_vintage` / `price_asof`, not a pull time). The commodities
board carries only this price date plus a divergence note in its `meta`, so without it the exec's
Data-room card showed the layer **blank** despite a fresh measured farm-gate vintage. Verified the
change touches **only this one** vintage cell (`'' → 2026-07-24`); a diff of the regenerated ledger
confirms every other field — the 109-layer counts (52 measured · 57 estimated · 0 unlabelled),
labels, sources, the files block — is byte-identical, and `build_provenance.py --check` passes on the
recommitted ledger. No date is invented — `2026-07-24` is read from the layer's own committed
`meta.farmgate_vintage`.

The same re-scan re-confirmed the standing accepted-blank set: `amphoe_crops` (`retrieved`),
`crop_margin` (`cost_ingested`), `region_debt` (`retrieved`) and `rival_universe` (`verified`) carry
only a **pull/verify** stamp, which the convention deliberately deprioritizes vs a data-observation
vintage, so their blank cells are the honest ABSENT state, not a bug; `brand_trends.note_be_to_ce`
is a BE→CE explainer note, not a vintage; `tape_real.mob_anchor` is a months-on-book methodology
parameter, not a freshness date. `commodities.farmgate_vintage` was the one genuine dropped
data-vintage this run.

**Freshest reachable inputs today** (read from the regenerated ledger): thaiwater flood/rain
2026-07-25 05:20/05:00, commodities farm-gate 2026-07-24, fuel prices 2026-07-22, macro indicators
2026-07-20, rival-pulse promo watch 2026-07-19, credit anchor 2026-07-18, SET peer scoreboard
2026-07-17. No stale live-input layer detected.

---

**2026-07-24 (pm, prior run):** a full re-scan of all analytical layers' `meta` blocks for date-shaped
freshness fields *not* in the extractor's key list found **one** layer still dropping a real
**data-vintage** from the Data-room card:

| Layer | Freshness key (was dropped) | Value now surfaced | Class |
|---|---|---|---|
| `peer_scoreboard.json` | `price_asof` | 2026-07-17 | MEASURED SET listed-peer market scoreboard (obj #2) |

`_vintage_of()` now also scans `price_asof`, placed among the data-vintage keys (right after
`price_vintage`, ahead of any pull timestamp — `price_asof` is the SET market-price observation date,
a data-observation vintage exactly like `observed_to` / `price_vintage`, not a pull time). Verified
the change touches **only this one** vintage cell (`'' → 2026-07-17`); a diff of the regenerated
ledger confirms every other field — the 104-layer counts (52 measured · 51 estimated · 1 unlabelled),
labels, sources, the files block — is byte-identical, and `build_provenance.py --check` passes on the
recommitted ledger. **Headless render self-review** of `index.html#home` (`data-errors:[]`, 0 JS
errors) shows the Data-room `peer_scoreboard.json` row now reads `2026-07-17 · 2 KB` (was blank). No
date is invented — `2026-07-17` is read from the layer's own committed `meta.price_asof`.

---

**2026-07-24 (am, prior run):** a full re-scan of all 104 layers' `meta` blocks for date-shaped
freshness fields *not* in the extractor's key list found **one** layer still dropping a real vintage
from the Data-room card:

| Layer | Freshness key (was dropped) | Value now surfaced | Class |
|---|---|---|---|
| `drought_district.json` | `snapshot` | 2026-06 | MODELLED OAE-SPEI per-amphoe drought (freshly surfaced, #141) |

`_vintage_of()` now also scans `snapshot`, placed among the data-vintage keys (after `price_vintage`,
ahead of any pull timestamp — `snapshot` is the SPEI reference month, a data vintage, not a pull
time; the layer's own `retrieved: 2026-07-20` pull date is intentionally **not** surfaced as the
freshness cell, matching how `observed_to` / `price_vintage` prefer the observation window over the
pull stamp). Verified the change touches **only this one** vintage cell (`'' → 2026-06`); a diff of
the regenerated ledger confirms every other field — counts, labels, sources, the files block — is
byte-identical, and `build_provenance.py --check` passes on the recommitted ledger. No date is
invented — `2026-06` is read from the layer's own committed `meta.snapshot`.

---

**2026-07-19 (prior run):** a full re-scan of all 96 layers' `meta` blocks for date-shaped freshness
fields *not* in the extractor's key list found **three** layers still dropping a real vintage from
the Data-room card:

| Layer | Freshness key (was dropped) | Value now surfaced | Class |
|---|---|---|---|
| `rival_pulse.json` | `promos_pulled_at` | 2026-07-19 | live rival promo/sentiment watch (freshest) |
| `pico_competitors.json` | `pico_vintage` | 2026-05-22 | FPO PICO-finance licence registry |
| `occupation_income_individual.json` | `vintage_individual` | 2025 | NSO LFS individual-income |

`_vintage_of()` now also scans `pico_vintage, vintage_individual, promos_pulled_at`. Verified the
change touches **only these three** vintage cells (`build_provenance.py --check` byte-exact; a diff
of the regenerated ledger confirms counts/labels/sources unchanged). No date is invented — each is
read from the layer's own committed `meta`. `rival_reputation.json` / `rival_threat.json` carry an
explicit `vintage: null` and correctly stay blank (no rating vintage was captured).

---

**2026-07-17 (prior run):** `_vintage_of()` previously scanned only `updated / vintage / as_of /
updated_to`. Six layers stamp their freshness under **other** real keys and were therefore showing
blank in the Data-room card despite carrying a date:

| Layer | Freshness key (was dropped) | Value now surfaced |
|---|---|---|
| `thaiwater_flood.json` | `observed_to` | 2026-07-11 08:40 |
| `thaiwater_rain.json` | `observed_to` | 2026-07-10 02:00 |
| `search_demand.json` | `pulled_at_utc` | 2026-07-04T00:45Z |
| `fuel_prices.json` | `pulled` | 2026-07-05 |
| `macro_indicators.json` | `pulled` | 2026-07-05 |
| `macro_sensitivity.json` | `price_vintage` | 2026M06 |

Extraction now scans (in priority order): `updated, vintage, as_of, updated_to, observed_to,
price_vintage, pico_vintage, vintage_individual, pulled_at_utc, pulled, promos_pulled_at`. A note
field that merely mentions a year
(`brand_trends.json::note_be_to_ce = "พ.ศ. − 543 = ค.ศ. …"`) is correctly **not** treated as a
vintage — the extractor keys off known freshness fields, not any date-shaped string.

Result: **23 of 96** layers now carry a captured vintage (was 20 before this run's three-key fix;
11 of 78 after the 2026-07-17 fix). The remaining 73 are derived/geometry layers whose freshness is
inherited from their inputs (no independent vintage); their vintage-blank state is honest, not a bug.

**Freshest reachable inputs today** (all measured except the rival watch, all recent): rival-pulse
promo pull 2026-07-19, credit anchor 2026-07-18, fuel prices 2026-07-17, thaiwater flood 2026-07-11,
thaiwater rain 2026-07-10, search demand 2026-07-04. No stale live-input layer detected.

## 2. Provenance coverage

- **115 layers · 420 files** (as of 2026-07-30). 57 measured · 58 estimated · **0 unlabelled** — the shame board is **clear**; `build_provenance.py --check` reproduces the ledger byte-for-byte. _(The narrative below records the 2026-07-17 run that first cleared the board at 83 layers · 314 files; the mechanism has held the board at 0 unlabelled through every layer added since — the tree has grown 83 → 114 layers with no regression.)_
- **Update 2026-07-17 (a), intelligence loop:** the board was **cut 6 → 2**. Four structural layers
  gained an honest self-declared `meta` stamp at the generator so they stay `--check`-reproducible:
  `meta.json` (`derive.py::build_meta` — MIXED, classifies ESTIMATED), `deltas.json` +
  `snapshots_index.json` (`timeseries.py::targets`), and the orphaned curated pilot aggregate
  `rayong_province.json` (hand-stamped — verified **no live `fetch()`**).
- **Update 2026-07-17 (b), intelligence loop — the sidecar mechanism (this run):** the last **2**
  unlabelled files — `branches.json` and `provinces/index.json` — are **top-level JSON arrays** that
  structurally cannot carry an inline `meta` block. `build_provenance.py` now consults a hand-authored
  **sidecar manifest** (`platform/data/provenance_sidecar.json`, keyed by relpath) for exactly those
  array-shaped layers, supplying the same `label/source/provenance` an inline block would. `branches.json`
  → **ESTIMATED** (MIXED: measured location/context + derived segment scores), inheriting meta.json's
  live vintage via a `vintage_from` key; `provinces/index.json` → **MEASURED** (a directory/index with
  measured rollups). The sidecar carries **provenance text only — zero data, zero numbers**; a file is
  upgraded only when an honest committed entry names it. **Shame board 2 → 0.** Honest by mechanism, not
  by fabrication — the board reports 314/314 labelled.

## 3. Broken data references — NONE

Cross-checked every `data/*.json` path referenced in `platform/*.html` + `app.js` (66 distinct)
against the committed tree. The only two that don't resolve —`perimeter_counts.json`,
`rayong_trees.json` — appear **exclusively in code comments** (describing a source path and an
optional scenery layer), never in a live `fetch()`. No functional break. The optional per-city
scenery layers (trees/rail/isochrone) are already allowlist-gated (`SCENERY_CITIES`, empty today)
so they issue no 404s — see PROGRESS_LOG 2026-07-12 (ux-loop).

## 4. Heavy-JSON load weight

**Corrected 2026-07-28 (was badly stale — the old figure read "242 MB across 309 files"):** the
git-tracked `platform/data` tree is now **2.5 GB across 497 JSON files**, overwhelmingly the 3D
building catchments — **77 `*_catchment.json` files totalling 2.28 GB** (avg **30.3 MB** each). The
five heaviest, all catchments, all lazy:

| File | Size | Load path |
|---|---|---|
| `chon-buri_catchment.json` | 34.6 MB | lazy — only on `rayong-catchment.html?city=chon-buri` |
| `chachoengsao_catchment.json` | 33.6 MB | lazy — only on that province's 3D scene |
| `saraburi_catchment.json` | 33.1 MB | lazy — only on that province's 3D scene |
| `khon-kaen_catchment.json` | 32.9 MB | lazy — only on that province's 3D scene |
| `rayong_catchment.json` | 32.6 MB | lazy — the Rayong pilot scene |

None load on the SPA's default routes — each is fetched only when its own heavy WebGL scene opens
(the deliberate one-route-per-GL-context split, CLAUDE.md), so command-center / overview / map stay
light. **Not a runtime regression.**

**Concrete finding this run — the R2 catchment migration is half-done, leaving ~2.2 GB redundantly
committed.** `catchments_r2.json` states the design plainly: _"Every province's `<slug>_catchment.json`
is served (R2, with the 3 pilot provinces also in git). The 3D scene fetches local-first then R2."_
The manifest lists only **3 provinces** as intended-git (`bangkok`, `chiang-mai`, `rayong`) with the
other **74 meant to be R2-only** — but on disk **all 77 catchments are still git-tracked** (2.28 GB).
So the repo and the Vercel deploy carry ~**2.2 GB of catchment data that R2 already serves**. Verified
live this run: R2 returns **HTTP 200** for the non-pilot catchments spot-checked
(`chon-buri`, `khon-kaen`, `roi-et`, `amnat-charoen`), and the scene's documented local-first→R2
fallback means removing the 74 git copies would shrink the repo/deploy by ~2.2 GB while scenes fall
back to the (verified-live) R2 objects. This is the biggest available deploy-weight win by far.

> **Update 2026-08-19:** §4's recommended R2 catchment migration **SHIPPED** in `abcb79a` (2026-08-18)
> — the 74 non-pilot `<slug>_catchment.json` files (~2.2 GB) are now `git rm`'d and served from R2
> only, keeping the 3 pilots (`bangkok`/`chiang-mai`/`rayong`) in git; `catchments_r2.json` `git`
> list == on-disk (3 pilots). The migration's one side-effect — 74 3D scenes now depending solely on
> R2 with no git fallback — is now **monitored**: `check_site_health.py::run_r2_catchment_checks`
> spot-probes R2 liveness nightly (see the 2026-08-19 audit entry at the top). R2 verified live this
> run (10/10 sampled non-pilot provinces HTTP 200).

## Next service task (recommended)

**Done 2026-07-17 (b)** — the sidecar mechanism shipped (see §2): `build_provenance.py` now reads
`provenance_sidecar.json` for the two array-shaped layers, taking the board **2 → 0 / 314 files
100 % labelled**. Provenance coverage is now complete and self-sustaining (any future array-shaped
layer just adds a sidecar entry).

The next standing service target is now **§4's confirmed finding: finish the R2 catchment
migration** — untrack the **74 non-pilot `*_catchment.json` files (~2.2 GB)** that R2 already serves,
keeping only the 3 pilots (`bangkok`, `chiang-mai`, `rayong`) in git. Evidence is in place: R2 returns
200 for the non-pilot catchments (verified this run) and the scene fetches local-first→R2, so the
fallback is already wired. **This intelligence loop deliberately did NOT execute the removal** — a
~2.2 GB `git rm` changes the data source of 74 3D scenes (git → R2) and is an architecturally
significant, owner-reviewable change, not a small autonomous edit (and the owner is currently driving
the repo manually — see the 2026-07-28 cron-pause commit). Recommended owner-side action: `git rm`
the 74 R2-served catchments, keep `catchments_r2.json` + the 3 pilots, and headless-render two
non-pilot 3D scenes to confirm the R2 fallback renders before merging.

Aside from that one weight item, the data room is healthy: provenance 114/114 labelled and
`--check`-reproducible, 0 broken references, 77/77 catchments reachable, and the live deployment
green (see the deployment-health note under **Headline**).
