# WHAT'S NEXT — AutoX / เงินไชโย credit-intelligence roadmap

## 1. Bottom line
The single highest-value buildable-now move is to **fix the risk number the director actually acts on**: `branch_risk.json` drives Home, Risk and Map (7 app.js refs) yet ships as an abstract 0–100 that "is NOT a measured default rate," fused from only 4 estimate-heavy legs — while every measured layer we added this wave (napprang second-crop, truck_flow SME churn, drought flag, district DTI) is invisible to it. Anchoring that score to the **measured** peer-NPL ladder (TIDLOR 1.5% → SAWAD 3.55%) and re-fusing it with the wave's measured legs is network-free, turns a proxy into a measured-weighted early-warning, and makes it legible ("reads like a ~X% NPL book") instead of an index the director tunes out. In parallel, only one owner-side unlock matters — the **real loan tape** — and it should become a dated commitment, not a passive "when it lands."

## 2. Do next (ranked)

1. **Anchor the composite to the measured peer-NPL band** — *risk · S.* Map each branch's estimated 0–100 linearly onto peer_npl's real floor→ceiling (TIDLOR 1.5% / MTC 2.53% / SAWAD 3.55%), labelled *ESTIMATED-calibrated-to-MEASURED-peers*. **Why now:** the two tables already render adjacent in app.js (3120–3140) but never link; one small change converts the abstraction the director ignores into a decision-grade readout. Ship it flat (not "by collateral tier" — AutoX is overwhelmingly vehicle-title so tiers barely vary).

2. **Re-fuse the branch composite with this wave's measured legs** — *risk · M.* build_branch_risk.py fuses only household 0.35 / agri 0.25 / occupation 0.2 / segment 0.2, and the household leg *inherits the PROVINCE value* when measured district DTI already sits in `amphoe.json`. Add napprang second-crop + truck_flow SME churn + drought flag, and re-join household to district DTI. **Why now:** it's the platform's most-consumed risk object and every measured layer we shipped is currently invisible to it — no new data pull, byte-checkable via the gate. (Keep labour informality as a flat national backdrop, not a per-branch discriminator.)

3. **Make the Road-to-3,000 sequencer competition-aware** — *acquisition · M.* build_expansion_plan.py ranks on `demand·(1−0.25·risk)` and cannibalizes only against our own branches — it has **zero competitor term**, so it sequences net-new sites into ground already saturated by Muangthai/Srisawad. Re-point the contested_pop geometry to score every amphoe (incl. 0-branch white-space) against the 16,503-point measured big-4 census and net rival-covered population out of demand. **Why now:** it's the single highest-leverage acquisition fix — turns "open next" from a demand map into a true white-space map, using data we already hold and ignore.

4. **Ship a branch-level collections-priority tier** — *risk · M.* Fuse `branch_peers` (≥2σ twin outliers, ESTIMATED) + `rival_pressure` siege flag (25 besieged branches, MEASURED geometry) into one ranked list the regional collections head can action weekly — today these render as two disjoint lists and decision_queue.json is only an 8-item editorial synthesis. **Why now:** it's an actionable objective-#1 surface from measured geometry *now*, and it becomes the concrete consumer for the real tape's 90+ balances — de-risking the owner ask by giving the tape somewhere to plug in. Label the fused tier ESTIMATED.

5. **Pull a measured sub-scale-lender presence layer** — *acquisition · M.* The competitor census is 100% big-4 (Muangthai 8,931 / Srisawad 5,203 / Tidlor 1,919 / Heng 450) — zero informal operators — and exit_whitespace self-labels "NOT a measurement... inferred from big-4 scarcity." committee/scout.py's Places textsearch runs **from any IP**; add โรงรับจำนำ / เต็นท์รถ / สินเชื่อ terms. **Why now:** upgrades exit_whitespace from inference to a measured-floor anchor; reframe as *post-deadline capture* (the Q1-2026 BoT cutoff has passed) — where independents that just retreated leave share on the table. Label measured-floor, not census (Places under-counts unregistered lenders).

## 3. Then (next tier)
- **Extend the OAE crop-cut detector to cassava / rubber / oil-palm / maize** — *risk · M.* napprang is rice-only; crop_stress names these crops but carries no measured production for the Isan/South/North majority borrower base. Clone the builder against the reachable sibling OAE datasets — but reframe perennials (rubber, palm) as YoY yield-vs-trend decline, not "abandonment," and resolve package ids via package_show (the hardcoded stub ids disagree).
- **Attach unit-economics to each Road-to-3,000 site** — *acquisition · M.* expansion_plan carries no baht/payback; join AUM-ramp + payback months from branch_roi_proxy so the queue reads as an investment case for the 25% ROE / IPO story. Label *synthetic-tape ESTIMATED* until the real export lands (do not call it "empirical from the book").
- **Collapse the National-outlook's 5 regional cards behind one expander** — *trust/ux · S.* The regional grid is ~2,122px of always-open cards above the first table on a 390px screen; reuse the shipped collateral-wave `<details>` pattern.
- **Headline labour_context as the borrower-base thesis** — *trust/ux · S.* 50.4% self-employed / 63.2% informal / agri −300k YoY is measured but buried in an #overview tile; one headline row on Home reframes it as the title-loan TAM + segment risk.
- **Add an OAE+DLT monthly-refresh CI job** — *both · S.* vehicle_flow / ev_penetration derive from a DLT mirror dated Feb-28-2026 (~4.5mo stale), refreshed by no cron. Scope to the DLT raw mirror + OAE napprang only (rain and farm-gate prices already have crons); add the INTERMITTENT-host retry loop and the >500-row guard.
- **Add the #acqjump chip nav to Risk→Outlook** — *trust/ux · M.* Cheap papercut fix for a long section; a few `<h2>`s need ids. Convenience, not insight — keep it low.

## 4. Owner-side asks (only Kaustav can unlock)
- **Real no-PII loan-tape export** (contract ready at `pipeline/loan_tape_schema.md`; ingest validated at 100% join on 202,586 synthetic loans, so platform-side cost ≈ 0 — the block is purely organizational). **What it flips:** 4 SYNTHETIC/measured:false outputs → measured; calibrates every estimated risk score to real defaults; gives the collections tier (move #4) real 90+ balances; enables a **measured** payback ranking of the 985 net-new sites (regress real payback on the catchment features we already hold). Turn it into a **dated commitment**, backed by a one-page "what the tape buys" brief.
  - **One framing correction for that brief:** do **not** pitch it with the synthetic calibration-divergence table. The "actual 90+" numbers in loan_tape_derived are themselves fabricated by make_synthetic_tape.py, so proxy-vs-"actual" divergence is a construction artifact that proves nothing about the real book — showing it to the director as evidence the live risk ranking fails would be misleading. The honest, sufficient pitch is: **the 2027 IPO needs an auditable measured PD / vintage / ROI trail, and today objective #1 runs entirely on proxies with no anchor to real defaults.**

## 5. Sequencing
Start with the two S/M risk-credibility fixes that need no data pull — **peer-NPL anchor (1)** then **re-fuse the composite (2)** — because they upgrade the object the director touches every day and can land within the current gate this week. Run the **competition-aware sequencer (3)** and the **sub-scale-lender Places pull (5)** as the parallel acquisition track (5 feeds 3's white-space accuracy, so kick 5 off first since it's a network job that can run while you build 3). Fold the **collections tier (4)** in next as the surface that will consume the real tape, and file the **loan-tape brief + dated ask** immediately so the owner clock starts running behind all of it. Treat the crop detector, unit-economics, and the two UX collapses as fast fillers between the bigger builds; the CI refresh job is a one-sitting maintenance task to slot in before any layer ages further.
