# QA/UXUI audit — 2026-07-12

Automated Playwright audit of every route (desktop 1440×900 + mobile 390×844), served locally.
**Counts:** 1 blocker · 2 major · 3 minor · 3 polish. All 13 routes loaded; no page-level horizontal
scroll at 390px. This is the intake for the recursive UX-improvement loop.

## Findings (severity-ranked)

| # | Sev | Route | Category | Issue | Fix |
|---|-----|-------|----------|-------|-----|
| 1 | **BLOCKER** | branch-explorer.html | functional | Basemap dead (flat void) + 150+ `ERR_NAME_NOT_RESOLVED` console errors — deck.gl `TileLayer` doesn't expand the `{s}` subdomain token | Hardcode `a.basemaps.cartocdn.com` (as province/rayong pages do) — **✅ FIXED + verified (0 console errors)** |
| 2 | major | rayong-catchment.html | functional | `rayong_isochrone.json` + `rayong_trees.json` 404; reach-rings & trees toggles silently no-op | Isochrone toggle already self-hides when absent; now gate the optional-scenery fetches (iso/trees/rail) behind a per-city allowlist so no request fires until a file ships — **✅ FIXED (0 console 404s from scenery layers)** |
| 3 | major | index.html `#map` | mobile | Lens pills overlap the Leaflet zoom +/− at top-left (legibility + tap accuracy) | Move zoom control bottom-right; collapse lenses into a sheet on narrow viewports |
| 4 | minor | province.html | mobile | Top stat-chip toolbar clips off the right edge; chips unreachable | `overflow-x:auto` scroller |
| 5 | minor | (global) | consistency | SPA defaults light ("Paper Console"); 3D pages default dark; convention is dark-console | Persist one theme choice across pages |
| 6 | minor | (global) | accessibility | Light-theme `--dim #7A8598` on `#F4F6FA` ≈ 3.4:1 (below WCAG AA) on 10–11px microcopy | Darken to ~`#5B6678` |
| 7 | polish | index routes | functional | favicon 404 | Add a favicon |
| 8 | polish | index.html `#branches` | clarity | Opens straight into a search box, no headline/lead line (unlike every other route) | Add a header + one-line lead |

## Verdicts
- **Mobile-readiness: strong.** SPA reflows to single column, nav is a scrollable ~41px-target strip,
  healthy 3D pages init non-blank with bottom-sheet controls. Main blemishes: #map lens/zoom overlap
  and (now-fixed) branch-explorer.
- **"Leads with the answer": excellent** on content routes (`#home`/`#overview`/`#trend`/`#acq`/
  `#exposure` each open with a bold one-line answer + KPIs + ranked source-tagged actions; `#sim` is
  genuinely interactive). Only gap: `#branches`.

Fix the blocker (done) + the two majors and the platform is in very good shape.

## Fix log — recursive UX loop (2026-07-12, branch claude/ux-catchup)

Each item below is a REAL, surgical fix matching the dark instrument-console theme (accent `#5B7CFA`,
IBM Plex fonts). IDs match `committee/plan_cycle.py`'s uxui block (which flips `state` to done off these
lines) so the CEO `/status` dashboard reflects the progress.

- ux-favicon — ✅ FIXED 2026-07-12 — added `platform/favicon.svg` (dark rounded square + accent "AX" monogram) and `<link rel="icon" href="/favicon.svg">` in index.html, rayong-catchment.html, province.html, branch-explorer.html, status.html. Stops the favicon 404 (#7).
- ux-contrast — ✅ FIXED 2026-07-12 — light-theme `--dim` darkened `#7A8598` → `#5B6678` in styles.css (≈3.4:1 → ≈5.4:1 on `#F4F6FA`, clears WCAG AA for microcopy) (#6).
- ux-map-overlap — ✅ FIXED 2026-07-12 — added a `@media(max-width:430px)` rule in styles.css moving the Leaflet zoom `+/−` to the bottom-right on the `.maphero` map, clear of the floated lens pills (#3).
- ux-province-overflow — ✅ FIXED 2026-07-12 — province.html `#strip` stat-chip toolbar made a proper touch horizontal scroller: added `-webkit-overflow-scrolling:touch` + `white-space:nowrap` (already `overflow-x:auto`); chips no longer clip off the right edge on mobile (#4).
- ux-theme-persist — ✅ FIXED 2026-07-12 — every page already reads the persisted `autox-theme` from localStorage on load; province.html + branch-explorer.html defaulted dark while the SPA front door (index.html) and rayong-catchment.html default light — aligned those two to the SPA's light default so the first-load theme is consistent across pages (a saved choice still wins everywhere) (#5).
- ux-branches-lead — ✅ FIXED 2026-07-12 — `#branches` (index.html) now opens with a header + lead ("All 2,015 AutoX branches — search, sort, and open any branch's 3D scene.") like every other route (#8).
- ux-search-a11y — ✅ FIXED 2026-07-12 — the three SPA search boxes (`#branches`, `#provinces`, `#market`) were placeholder-only `text` inputs with no accessible name (WCAG 4.1.2); added `type="search"` (native × clear button on mobile) + `aria-label` to each, matching the scene search (`ssInput`) pattern already used in rayong-catchment.html.
- ux-theme-comment (NEW, polish) — the pre-paint theme-init comment on line 4 of index.html/rayong-catchment.html/rayong-province.html/status.html still reads "else saved, else dark" but the code now defaults `'light'` (post ux-theme-persist). Stale comment only, no user-facing effect — one-word fix "dark"→"light" when next touching those files. (Partly closed by ux-theme-persist-status below: index.html + rayong-catchment.html + status.html comments corrected; rayong-province.html still stale.)
- ux-theme-persist-status — ✅ FIXED 2026-07-12 — `status.html` (linked from the nav "Status ↗") still defaulted **dark** while index / province / branch-explorer / rayong-catchment default **light** — a leftover the earlier theme-persist fix missed. Aligned its pre-paint init + catch fallback to `'light'` so a first-time visitor gets the same theme on every page (a saved `autox-theme` choice still wins). Also corrected the stale "else dark" comments in index.html + rayong-catchment.html whose code already defaults light (follow-up to #5).
- ux-isochrone-guard — ✅ FIXED 2026-07-12 — rayong-catchment.html no longer requests `<slug>_isochrone.json` / `<slug>_trees.json` unless the province is registered in `tiles_config.json`'s `scenery` list, so absent files no longer 404 in the console; the isochrone toggle already self-hides when `window.ISO` is empty (also hidden by default now), so no dead toggle (#2).
- ux-map-overlap-tablet — ✅ FIXED 2026-07-13 — the #3 zoom/lens-overlap fix only relocated the Leaflet zoom to bottom-right at ≤430px (phones); headless renders at 500/600/760px showed the lens pills still wrap to 2–3 rows and crowd/overlap the top-left zoom across the whole 431–760px band (foldables, tablets, landscape phones, split-screen). Extended the relocation `@media` breakpoint `430px → 760px` in styles.css so the zoom sits bottom-right (always clear) across the full phone+tablet range; desktop (>760px, verified clean at 1440px — pills fit one row) keeps the conventional top-left zoom (#3, tablet band).
- ux-skip-link — ✅ FIXED 2026-07-13 (NEW, a11y) — the `role="tablist"` nav has ~12 focusable stops on every route with no bypass (WCAG 2.4.1 Bypass Blocks, Level A gap). Added a "Skip to main content" link as the first `<body>` child in index.html (off-screen until keyboard focus, then overlays the fixed nav in accent `#5B7CFA`), gave `<main>` `id="main-content" tabindex="-1"`, and a tiny focus handler that `preventDefault`s so the SPA hash-router (unknown hash → falls back to `#home`) never fires. Zero visual change for pointer users; keyboard/SR users can now jump the nav.
- ux-color-scheme — ✅ FIXED 2026-07-16 — no page declared `color-scheme`, so native UA surfaces (scrollbars, form controls, the pre-paint canvas, iOS rubber-band overscroll) rendered in the OS default scheme — e.g. dark-console users got OS-default LIGHT scrollbars against the dark chrome. Added a CSS-only, theme-tracking pair in `styles.css`: `color-scheme:dark` on the canonical `:root` + `color-scheme:light` on `html[data-theme="light"]`. Keys off the `data-theme` attr the pre-paint script sets, so it applies from the first frame and auto-follows the toggle across all 5 styles.css pages (index/province/rayong-catchment/branch-explorer/status). Verified: computed `color-scheme` = light on default load, dark on `?theme=dark`; both themes render clean (0 console errors).
- ux-theme-color — ✅ FIXED 2026-07-16 — no page set `<meta name="theme-color">`, so the mobile browser UI bar / notch area didn't match the app chrome. Added a `theme-color` meta to all 5 styles.css pages (index/status/province/rayong-catchment/branch-explorer) and folded a one-line updater into each page's existing `sync()` (which runs on load AND on every toggle), so the tag TRACKS `data-theme` — light `#F4F6FA` / dark `#0a0e17` (= `--bg`) — rather than `prefers-color-scheme` (which the app ignores). Verified: meta present, content=`#F4F6FA` on default light load and `#0a0e17` under `?theme=dark`.
- ux-databook-filter-sort — ✅ FIXED 2026-07-16 (NEW, functional) — data.html national table: filtering the 77-province list to one region (region chips), then clicking a column header to re-sort, silently reverted to all 77 provinces — the th sort handler closed over the stale full `allProv` array while the intended shared `#ptbl.__rows` field was set but never read. Made `#ptbl.__rows` the single source of truth: `applySort()` with no arg re-sorts the table's current (filtered) set; region-filter and initial render pass a new set. Filter + sort now compose. (node --check clean; default render unchanged.)
- ux-search-empty-state (NEW, clarity) — ✅ FIXED 2026-07-17 — the three SPA search tables (`#branches`, `#provinces`, `#market`) rendered only a lone header row when a query matched nothing — no feedback, so a mis-typed search reads as "broken/blank". Added an escaped `.cc-empty` "No branches/provinces match "<query>" — clear the search…" fallback row to each (colspan 9/9/7; region-qualified where a region chip is active; query run through `dqEsc` so it can't inject markup). Populated path unchanged (headless #branches render clean, 215 rows). node --check clean.
- ux-map-overlap-residual — ✅ FIXED 2026-07-13 — headless renders of `#map` confirmed the residual: at 900px the lens row wraps to 2 rows and the Leaflet zoom `+/−` (56px top-left offset clears only 1 row) overlaps the 2nd-row "More lenses ▾" pill; it settles to one clean row by ~1050–1100px. Extended the zoom bottom-right relocation `@media` breakpoint `760px → 1080px` in styles.css so the zoom sits bottom-right (clear of any wrap) across the full phone+tablet+small-laptop band, with a cushion above the headless one-row transition; >1080px keeps the conventional top-left zoom (verified clean at 1100/1200px). Closes the last open `#map` overlap item (#3 residual band).
- ux-msheet-handle-keyboard — ✅ FIXED 2026-07-17 (NEW, a11y) — the mobile branch-detail bottom sheet's close handle (`#msheet-handle` in index.html) is `role="button" tabindex="0" aria-label="Close branch detail"`, so it is keyboard-focusable and announced as a button — but `wireBranchSheet()` in app.js only wired a `click` listener, and a `<div role="button">` (unlike a native `<button>`) does NOT fire click on Enter/Space. A keyboard/switch user could focus the visible "Close" control but not activate it (only the global Escape closed the sheet — no visible affordance). Added a `keydown` handler on the handle for Enter/Space (`preventDefault` + `closeBranchSheet()`), matching WCAG 2.1.1 (Keyboard). No pointer-user or visual change; node --check clean. (NEW backlog item spotted while here: the `#nav role="tablist"` interleaves non-tab children — the "Data book" plain link, the "More ▾" button, and the theme-toggle button — inside a tablist that ARIA says should contain only `role="tab"`; a proper fix wraps the tabs in their own tablist without disturbing app.js's `#nav a[data-v]` routing.)
- ux-nav-tablist — ✅ FIXED 2026-07-17 (a11y, ARIA) — index.html's `#nav` was `role="tablist"` but (a) interleaved non-tab children (brand `<span>`, "Data book" plain link, "More ▾" button, theme-toggle button) which ARIA forbids in a tablist, and (b) never implemented the rest of the tab pattern — the `.view` sections have no `role="tabpanel"`/`aria-controls` and there is no roving-tabindex/arrow-key handling, so screen readers announced "tab N of 6" for a widget that isn't one. This is hash-route SITE navigation, not a tab widget. Dropped `role="tablist"` from `#nav` (it is already a `<nav aria-label="Main views">` landmark) and `role="tab"`/`aria-selected` from the 6 links; app.js `showTab()` now toggles `aria-current="page"` on the active link instead of `aria-selected`. This matches the CORRECT pattern data.html + status.html already use, so all three navs are now consistent. `.on`-class styling (the only visual driver) is untouched — zero visual change; node --check clean.
- ux-nav-more-keyboard — ✅ FIXED 2026-07-17 (a11y, keyboard) — the "More ▾" nav dropdown declares the ARIA menu-button pattern (`aria-haspopup="true"` + `role="menu"`/`role="menuitem"`) but the menu is re-parented to the END of `<body>` (so its `position:fixed` escapes the nav's backdrop-filter containing block), which pushed its items to the very end of the tab order — a keyboard user tabbing off "More ▾" skipped straight past Simulator/Provinces/Market/Branches/Status to the page body, and no arrow-key roaming or Escape-to-restore-focus was ever wired (WCAG 2.1.1 Keyboard / 2.4.3 Focus Order). Added the standard menu-button keyboard handling to index.html's nav script: ArrowDown/ArrowUp on the button open the menu and land focus on the first/last item; ArrowUp/Down/Home/End roam the items (wrapping); Escape and Tab close and return focus to the trigger. Also tightened the outside-click-to-close so it ignores clicks inside the re-parented menu. Pointer users unaffected; node --check clean, headless #home render unchanged. (NEW backlog item spotted: the "More ▾" menu items have no distinct `:focus-visible` outline — keyboard focus reuses the hover background, which is subtle against the popover; a dedicated `.nav-more-menu a:focus-visible` accent outline would make the roaming focus clearly visible.)
- ux-expocards-clamp-title — ✅ FIXED 2026-07-18 (clarity) — the `#exposure` KPI cards' `.n` subtext is deliberately `-webkit-line-clamp:2` (styles.css `#expocards .mcard .n`) so the 2×2 row scans as one set — but on mobile 2-col that clips the measured/estimated **provenance caveat** off the tail (e.g. "…(World Bank YoY < −10%, direction proxy)", "(HDX proxy)", "(OSM/price-based, not measured)") with no way to recover it, cutting against the project's honesty-of-provenance principle. Added a `title` carrying the full note to all four `#expocards .mcard .n` divs (HHI + 3 stress cards) in app.js's `renderExposure`, escaped with the house `dqEsc` helper (matches the existing `title="…${dqEsc()}"` pattern). Full note now recoverable on hover / to screen readers; the deliberate visual clamp and 2×2 scan are untouched — verified zero visual change (headless #exposure mobile render identical, title attrs present, visible text unchanged). node --check clean; `tests/run.sh check` 65-passed/0-failed.
- ux-navmore-focusvisible-noop (INVESTIGATED, not a bug) — the "More ▾" `:focus-visible` item spotted above is already covered: the global `a:focus-visible` rule (styles.css ~L469, a bare `a:focus-visible`, not scoped to `#nav`) reaches the re-parented menu links and paints the 2px accent outline on keyboard roam. No dedicated rule needed. Closed to stop future runs chasing it.
- ux-sim-slider-labels — ✅ FIXED 2026-07-18 (NEW, a11y) — the `#sim` simulator's four range sliders (Crop-price shock, Rainfall/drought, Used-vehicle value move, Factory/manufacturing slowdown) had visible `<label>` elements but NO association to their `<input type="range">` (no `for=`, input not nested), so a screen reader announced each as an unnamed "slider" and the visible label wasn't a click target (WCAG 3.3.2 Labels / 4.1.2 Name-Role-Value). The BoT rate-cap checkbox just below already wraps its input correctly — the sliders were the gap. Added `for="sim-price|sim-rain|sim-veh|sim-factory"` to the four labels so each slider gets its visible label (incl. the live value span) as its accessible name; zero visual change, pointer users unaffected. node --check clean.
- ux-rayong-province-stub-residuals — ✅ FIXED 2026-07-18 — `platform/rayong-province.html` (RETIRED redirect stub → `/rayong-catchment`) was the last page with no `<link rel="icon">` (favicon 404 if the instant redirect ever stalls) and still defaulted theme **dark** + carried the stale "else dark" pre-paint comment while every live page defaults light. Added `<link rel="icon" href="/favicon.svg">`, flipped the pre-paint default + catch fallback `'dark'→'light'`, and corrected the comment to "else light" — closing the very last favicon/theme residuals across all pages (a saved `autox-theme` choice still wins; the ~0ms redirect is untouched). `tests/run.sh check` 65-passed/0-failed; headless render confirms the stub still redirects to `/rayong-catchment`.
- ux-doc-lang-consistency — ✅ FIXED 2026-07-18 (a11y, consistency) — `index.html` (the front door) and `branch-explorer.html` declared `<html lang="th">` while the other 5 pages (data/province/rayong-catchment/status/rayong-province) declare `lang="en"` — yet all UI chrome is English prose ("Command center", "The two questions, on one screen…", every nav label), so a screen reader on those two pages announced the entire English interface with Thai pronunciation rules (WCAG 3.1.1 Language of Page). Set both roots to `lang="en"` (matching the majority + the actual content) and wrapped the discrete Thai-only spans that remain — the `AutoX·เงินไชโย` brand on both pages and the three inline `<span class="mono">` loan-type terms in index.html (จำนำทะเบียนรถ · สินเชื่อรถแลกเงิน · พิโกไฟแนนซ์) — in `lang="th"` so they keep correct Thai announcement rather than trading one mispronunciation for another. Zero visual change (IBM Plex Sans Thai renders both scripts); `tests/run.sh check` 0-failed; headless index/branch-explorer renders identical.
- ux-noscript-fallback — ✅ FIXED 2026-07-19 — the app is a heavy Leaflet/deck.gl JS SPA, so a visitor with JS disabled (or a fully-blocked map/font CDN) got a blank `<main>` with no explanation. Added a themed `<noscript>` banner (console vars: `var(--panel)` card, `var(--accent)` left rail, IBM Plex) as the first child of `index.html`'s `<main>` — "This dashboard needs JavaScript enabled" + one line on why. Renders only when JS is off (zero change for normal users), so verified by DOM/grep (present in source, inside `<main>`) not PNG; headless render with JS ON is identical. `tests/run.sh check` 0-failed. (Front door only for now; the other pages could get the same one-liner in a follow-up.)
- ux-noscript-fallback-3d — ✅ FIXED 2026-07-19 — followed up the `index.html`-only banner above: the three deck.gl/WebGL pages (`rayong-catchment.html`, `province.html`, `branch-explorer.html`) render entirely into a canvas, so JS-off (or a blocked map/font CDN) left a fully blank scene with no explanation — arguably worse than the SPA's blank `<main>`. Added the same themed `<noscript>` banner as the first `<body>` child of all three, using each page's own console vars (`--card`/`--cardln` on the two catchment pages, `--panel`/`--line`/`--accent` on branch-explorer; `#5B7CFA` left rail; `z-index:9999` above the empty canvas), with a link back to `index.html`. JS-off pages now explain themselves and offer the standard dashboard. Renders only with JS off (zero change for normal users), verified by grep/DOM + JS-on headless render unchanged. `tests/run.sh check` 0-failed.
- ux-overview-headline-mandate — ✅ FIXED 2026-07-19 (content, mandate-alignment) — the `#overview` "Bottom line" (the "answer up top" hero, `national.headline` from `regional_outlook.json`) recommended **branch expansion** — "eases borrower risk — **a window to grow selectively**. Priority: **expand where white-space is thin-competition** (most in X)" — directly contradicting the core mandate (CLAUDE.md: the network is *consolidating/rationalising, not expanding*; the platform makes *"no open / close / where-to-open recommendations"*). A leftover the two-risk-storyline repositioning (#83) missed. Reframed the generator (`pipeline/build_regional_outlook.py`, lines ~300–305) to a pure risk lens on the *existing* footprint: backdrop now "eases borrower risk **across the existing book**" and the priority triad now "**defend the branches under heaviest rival pressure** (most in X), lead with vehicle-title products where collateral density is high (most in Y), and de-risk agri-stressed branches (most in Z)" — same data variables (`top_reg("defend"/"collateral"/"agri_stress")`, swapped the expansionary `acquire` leg for `defend`). Regenerated `regional_outlook.json` + `provenance.json` (byte-size bump). `build_regional_outlook.py --check` byte-exact; `tests/run.sh check` 71-passed/0-failed; headline verified in the headless `#overview` render (layout otherwise identical). NOTE: the builder edit is in `pipeline/` (not `platform/`) because `regional_outlook.json` is gated by `--check` and cannot be hand-edited without failing the determinism gate.

- ux-databook-theme-color — ✅ FIXED 2026-07-19 — the `ux-theme-color` fix added a `data-theme`-tracking `<meta name="theme-color">` to "all 5 styles.css pages" but missed `data.html` (the "Data book" nav route, which also uses styles.css) — so on mobile its browser chrome bar rendered the OS default instead of the app chrome, inconsistent with every other route. Added the same `theme-color` meta (after the favicon `<link>`, matching index.html) and folded the identical one-line updater into data.html's existing `sync()` (runs on load AND every toggle) so the tag tracks `data-theme` — light `#F4F6FA` / dark `#0a0e17` (= `--bg`). Zero body-visual change; verified meta present with `content="#F4F6FA"` on default light load. `tests/run.sh check` 0-failed.

- ux-databook-sort-keyboard — ✅ FIXED 2026-07-20 (a11y, keyboard) — `data.html`'s sortable province-table column headers (`.db-tbl th`, national + region levels) were **mouse-only**: `wireTable()` wired a `click` listener but the `<th>`s weren't focusable, had no keyboard handler, and exposed no sort state — so a keyboard/switch user could not sort at all (WCAG 2.1.1 Keyboard, Level A) and a screen reader announced no current sort (4.1.2). Made each sortable header `scope="col" tabindex="0" aria-sort="none"` with a `title="Sort by …"` hint; refactored the click logic into a shared `doSort(th)` and added an Enter/Space `keydown` handler (`preventDefault`); `applySort()` now maintains `aria-sort` (`ascending`/`descending`/`none`) alongside the existing sort-arrow class; and added a `.db-tbl th:focus-visible` accent outline (2.4.7 Focus Visible). Pointer users unaffected, zero visual change on load; node --check clean. (NEW backlog item spotted while here: `districtTable`'s static `<th>`s — District/AutoX/Rivals/Working-age/DIW workers — and other one-off tables carry no `scope="col"`; harmless for a simple table but worth a sweep for consistent header semantics.)

- ux-table-scope-districttable — ✅ FIXED 2026-07-20 (a11y) — first slice of the `ux-table-scope-sweep` backlog item: `data.html`'s `districtTable` header row (District/AutoX/Rivals/Working-age/DIW workers) rendered bare `<th>` with no `scope`, so a screen reader on this wider table couldn't reliably associate each data cell with its column header (WCAG 1.3.1 Info and Relationships). Added `scope="col"` to all five `<th>` — matching the sortable `#ptbl` header (already `scope="col"` since ux-databook-sort-keyboard). Zero visual change (scope is non-presentational); node --check clean. The app.js SPA tables' bare `<th>`s remain — tracked below.

- ux-doctitle-per-route — ✅ FIXED 2026-07-20 (clarity, history/a11y) — the `index.html` SPA never updated `document.title` on hash-route change, so all 10 routes (`#home`/`#overview`/`#map`/`#trend`/`#acq`/`#exposure`/`#sim`/`#provinces`/`#market`/`#branches`) shared the single static tab title "AutoX · เงินไชโย — Credit Intelligence" — browser history entries, bookmarks and multiple open tabs were all indistinguishable, and SPA route changes were silent to screen readers (no page-title announcement). Added a `TAB_TITLES` route→label map and one line in `app.js`'s `showTab()` setting `document.title` to `"<Route> · AutoX · เงินไชโย"` (brand suffix kept so the tab stays recognisable); unknown/`home` route falls back cleanly. Runs on every route switch (nav click, hashchange, content `→` links, deep-link on load). Zero visual/body change; node --check clean.

- ux-chip-aria-pressed — ✅ FIXED 2026-07-23 (a11y, WCAG 4.1.2 Name/Role/Value) — the SPA's single-select filter `.chip` toggle-buttons conveyed their selected state ONLY visually (the `.on` class): the four region filters (`#acqchips`, `#ampchips`/`#amprchips` via `ampChips()`, `#provchips`, `#mktchips`) and the `#riskSub` risk-proxy metric selector rendered `<button class="chip">` with no `aria-pressed`, so a screen reader announced every option identically and gave no cue which region/metric was active. Only `#sortchips` (branches) already followed the correct pattern. Aligned the five other groups to it: container `role="group"` + `aria-label` ("Filter by region" / "Risk proxy metric"), `aria-pressed` on each button, and the click handlers now sync `aria-pressed` alongside the `.on` class (`c.setAttribute('aria-pressed',String(on))`). Zero visual change (only ARIA attrs added); node --check clean. All six SPA chip groups now expose selected state consistently.

- ux-msheet-focus-management — ✅ FIXED 2026-07-23 (a11y, WCAG 2.4.3 Focus Order + ARIA modal-dialog practice) — the mobile branch-detail bottom sheet (`#msheet`, `role="dialog" aria-modal="true"`) opened without ever moving focus INTO the dialog and closed without RETURNING focus to its opener. A keyboard / screen-reader user who opened a branch was left with focus stranded on the now-inert map behind the `aria-modal` backdrop (nothing to read from the top of the dialog), and on close was dropped at the top of the document instead of back where they were. Added `tabindex="-1"` to `#msheet` in index.html so it's programmatically focusable; `openBranchSheet` now captures `document.activeElement` (skipping `<body>` / non-focus openers like a map-marker tap) and `s.focus()`es the dialog after the open animation; `closeBranchSheet` restores focus to the remembered opener if it's still connected. Zero visual/pointer-user change; node --check clean. (NEW backlog item spotted while here: the sheet still has no focus TRAP — Tab from the last focusable element inside escapes to the page behind the modal; noted below.)

- ux-msheet-focus-trap — ✅ FIXED 2026-07-23 (a11y, WCAG 2.4.3) — completing the mobile branch-detail sheet's modal semantics: `ux-msheet-focus-management` moves focus in on open and restores it on close, but the sheet (`#msheet`, `aria-modal="true"`) still had no focus TRAP — Tab from the last focusable control (or Shift+Tab from the first) escaped to the map/nav behind the backdrop, contradicting `aria-modal="true"`. Added a `keydown` Tab handler on `#msheet` in `wireBranchSheet()` (app.js) that cycles focus among its visible focusable descendants (the handle is first, the body's links follow): forward Tab off the last wraps to the first, Shift+Tab off the first (or the dialog container itself) wraps to the last; interior tabbing stays native (only the boundaries are intercepted), and an empty body keeps focus on the handle rather than letting it escape. Sits alongside the existing swipe/close handlers without touching them. Zero visual/pointer-user change; node --check clean.

- ux-sim-slider-valuetext — ✅ FIXED 2026-07-23 (a11y, WCAG 4.1.2 Name/Role/**Value**) — the `#sim` simulator's four range sliders show a **formatted** value to sighted users via an adjacent span (`"normal"`, `"drier -10%"`, `"+5%"`, `"wetter +10%"`) but a screen reader announced only the raw slider number (`0`, `-10`, `5`) — the rainfall slider worst of all, where `0`≠"empty" but "normal" and negative means *drier*. `ux-sim-slider-labels` fixed the *name* (`for=`) but not the *value*. Added `aria-valuetext` to all four inputs in index.html (initial values mirror the visible spans) and kept it in sync with the visible label in app.js's three update paths: `wireSim`'s `oninput` bind, `simReset`, and the factory-hide branch. Now AT announces the same human-readable value sighted users see. Zero visual change (SR-only attr — verified identical headless `#sim` render, `aria-valuetext` present in settled DOM, `data-errors=[]`); `tests/run.sh check` 87-passed/0-failed; node --check clean.

- ux-cc-jumplink-href — ✅ FIXED 2026-07-24 (a11y, WCAG 2.1.1 Keyboard + 4.1.2 Name/Role/Value) — the command-center card-header jump links (`Competition →`/`Exposure →`/`Overview →`/`Risk trend →` — six static `<a class="cc-link no-print" data-v="…">` in index.html plus the JS-rendered decision-queue `open →` in app.js's queue renderer) were anchors with a `data-v` but **no `href`**, so they were not in the tab order and not announced as links: a keyboard/switch/SR user could see the "→" affordance but could not focus or activate it (only the pointer `#main-content a[data-v]` click handler reached them). Every other content jump link (`story-next`, `cc-hero-card`, `pill`) already carries `href="#<slug>"`. Added `href="#<data-v>"` to all seven so they are real focusable links; the existing delegated click handler still `preventDefault()`s and routes via `showTab()`, so SPA behaviour is unchanged and the href is a graceful JS-off fallback. Zero visual change; `node --check` clean.

- ux-tabtitles-pillar-drift — ✅ FIXED 2026-07-24 (clarity, history/a11y) — `ux-doctitle-per-route` gave each SPA hash route its own `document.title` via a `TAB_TITLES` map (app.js), but a later owner IA change (the five-pillar journey, 2026-07-24) renamed the nav labels and added a NEW `#assist` route without updating the map: `#assist` had **no entry** at all (tab title fell back to bare "AutoX · เงินไชโย" — indistinguishable in history/bookmarks/tabs, silent to screen readers), and three labels drifted from what the user now clicks (nav says **Macro/Acquisition/Risk**; the map still said Overview/National map/Exposure). Realigned `TAB_TITLES` to the current pillar labels (overview→Macro, map→Acquisition, exposure→Risk), added `assist:'Assistance'`, and reordered to match nav order; `home:'Command center'` (the page's own H2), `acq:'Competition'` and the five More-menu labels already matched and are unchanged. Zero visual/body change (SPA content untouched); node --check clean.

- ux-cc-tape-table-overflow — ✅ FIXED 2026-07-24 (mobile, layout) — the command-center "Assistance radar" table (`#cc-tape`, rendered by `renderHomeTape` in app.js) had `min-width:520`-class columns but, unlike EVERY other app table (which scrolls inside `.tblwrap`) and unlike the dataroom table (`.dr-tblwrap`), was emitted as a bare `<table class="tbl">` with **no scroll container**. When the real loan tape is present (i.e. in production), the 415px table widened the whole `#v-home` page past the mobile viewport → true horizontal page scroll at 390px (measured `scrollWidth 482 > 390`), clipping ALL command-center content — the hero verdict, the header lead, even the "Print / PDF" button — off the right edge unreadably. Wrapped the table in the house-standard `<div class="tblwrap">…</div>` (the same `overflow-x:auto` guard rail the rest of the app uses), so it now scrolls inside its own bordered box and never widens the page. Verified with a true mobile viewport (Playwright, 390px isMobile/DSF2): `scrollWidth == clientWidth == 390`, `over:false` (was `over:true` before). Zero desktop change (headless 1100px render identical — the table has room and never scrolls). `tests/run.sh check` 87-passed/0-failed; node --check clean.

- ux-cc-hero-card-narrow-overflow — ✅ FIXED 2026-07-24 (mobile, layout) — at a narrow 360px viewport a `.cc-hero-card` overflowed its grid column by ~7px (`right=367` vs `clientWidth=360`): the card is a grid item (default `min-width:auto`, so it couldn't shrink below its content) and a long unbroken token in `.cc-hero-big`/`.cc-hero-sub` (a Thai crop/province name + big number) refused to wrap. Added `min-width:0` to `.cc-hero-card` (lets the flex/grid item shrink to its column) and `overflow-wrap:anywhere` to `.cc-hero-big` + `.cc-hero-sub` (lets a long token break rather than push the box). Zero desktop change; node --check clean.

- ux-share-metadata — ✅ FIXED 2026-07-25 (polish/clarity) — no page carried a `<meta name="description">` or any social-share (Open Graph / Twitter-card) metadata, so when the production URL is shared internally (board / colleagues — a core use of an exec tool) the link unfurled BARE (raw URL, no title or summary) — a poor first impression before the app even loads. Added a static, self-contained (no image/CDN ref) share block to `index.html`'s `<head>` after `<title>`: `description` + `og:type`/`og:site_name`/`og:title`/`og:description` + `twitter:card=summary`/`twitter:title`/`twitter:description`, all honest to the mandate ("competitive-risk and portfolio-risk readouts across all 2,015 title-loan branches … numbers tagged measured or estimated"). Pure metadata — zero body/visual change (headless `#home` render identical), no JS touched; `node --check` unaffected. Front door only this run; the other routes tracked below.

- ux-share-metadata-other-pages — ✅ FIXED 2026-07-25 (polish/clarity) — followed up the `index.html`-only `ux-share-metadata` block: the other five shareable routes (`data.html` "Data book", `province.html`, `rayong-catchment.html`, `branch-explorer.html`, `status.html`) still had no `<meta name="description">` / Open Graph / Twitter-card tags, so a deep-linked share of any of them unfurled BARE (raw URL). Added a page-appropriate static share block after each page's favicon `<link>` — `description` + `og:type`/`og:site_name`/`og:title`/`og:description` + `twitter:card=summary`/`twitter:title`/`twitter:description` — each honest to the mandate (risk lens on the 2,015-branch existing footprint; "numbers tagged measured or estimated"). Self-contained (no image/CDN ref), zero body/visual change (headless renders identical), no JS touched; `tests/run.sh check` 0-failed. Closes the share-metadata gap across every shareable route.

- ux-impact-drill-aria-expanded — ✅ FIXED 2026-07-25 (a11y, WCAG 4.1.2 Name/Role/**Value**) — the impact-card drill buttons (`.ic-drill`, rendered by `icCard` in app.js and mounted on the command-center front door plus the Assistance/Exposure/Competition strips via `renderImpactStrip`) expand/collapse each region's province table, but conveyed their open/closed state ONLY visually (the ▸/▾ chevron swap): the `<button>` carried no `aria-expanded`, so a screen reader announced "N provinces — press to drill" identically whether the panel was open or shut. Every other disclosure in the app already exposes state (`lensMoreBtn`, `navMoreBtn`, the `.chip` groups per ux-chip-aria-pressed). Aligned it: the button template renders `aria-expanded="false"` and the click handler now syncs `aria-expanded` (`String(open)`) alongside the existing chevron/`pane.hidden` toggle. It's a native `<button>`, so Enter/Space already worked — only the value was missing. Zero visual change (SR-only attr); node --check clean. (NEW backlog item spotted while here: the `.ic-prow` province rows one level down are click-to-expand `<tr>`s with no `role`/`tabindex`/keyboard handler — mouse-only, and not focusable at all — noted below.)

- ux-pillar-assist-wrong-route — ✅ FIXED 2026-07-25 (navigation, broken affordance) — the command-center five-pillar band's ③ **Assistance** card (`renderHomePillars` in app.js) carried the foot label "**Assistance radar →**" but linked to `data-v="exposure"`/`#exposure` (the ④ Risk view) — which does NOT contain the assistance radar (`#assist-radar` is rendered ONLY by `renderAssist()` into `#v-assist`). So a user clicking the "Assistance radar →" affordance landed on the Risk/Exposure page with no radar in sight, contradicting the card's own label AND the nav's Assistance pillar (correctly `data-v="assist"`). Pointed both `pillCard(3,'Assistance',…)` calls (the tape-present branch + the null-safe fallback) at `'assist'` so the card jumps to the dedicated Assistance detail page it names. Verified by headless click-through: click → hash `#assist`, `#v-assist` active, `#assist-radar` populated (13 rows), title "Assistance · …". Zero visual change (byte-identical home render); pillars ② ("National map →"→`#map`) and ④ ("Risk trend →"→`#trend`) are internally consistent (label matches destination) and were left untouched. node --check clean; `tests/run.sh check` 0-failed.

- ux-pillar-risk-wrong-route — ✅ FIXED 2026-07-26 (navigation, IA drift) — a sibling of `ux-pillar-assist-wrong-route`: the owner's 2026-07-25 five-pillar nav re-IA points the ④ **Risk** tab at `data-v="exposure"`/`#exposure` (nav title: "where the book sits in stress: live book vs the 180+ legacy stock") and demoted `#trend` to a nav-retired route ("Risk trend — embedded in the impact cards"), but the command-center pillar CARD ④ (`renderHomePillars` in app.js) still routed to `data-v="trend"`/`#trend` with the foot label "Risk trend →". So a user clicking the front-door ④ Risk pillar — whose preview shows NPL-live 90–179dpd + the ฿-bn/N-acct 180+ legacy stock — landed on the retired time-dimension `#trend` page, NOT the page that actually renders that exact bucket-ladder content (`renderExposureTape()` → `#v-exposure`, H2 "Portfolio exposure · The real book"). Pointed both `pillCard(4,'Risk',…)` calls (tape-present branch + null-safe fallback) at `'exposure'` and relabelled the foot "Risk trend"→"Risk exposure" so the card jumps to the same Risk destination the nav names and the card previews. Verified by headless click-through: click → hash `#exposure`, `#v-exposure` active, bucket-ladder present; default home render byte-identical (SPA content untouched). node --check clean; `tests/run.sh check` 0-failed. (NEW backlog item spotted while here — the SIBLING ② Acquisition mismatch, noted below.)

- ux-impact-prow-keyboard — ✅ FIXED 2026-07-26 (a11y, WCAG 2.1.1 Keyboard, Level A) — the impact-card drill-down province rows (`.ic-prow` `<tr>` rendered by `icProvTable` in app.js, on the command-center front door plus the Assistance/Exposure/Competition strips) navigate from a region's province list into a province's booking-branch rows, but were **mouse-only**: bare `<tr>` with no `role`/`tabindex`/keydown handler, so a keyboard/switch user could neither focus nor activate them (only the pointer path via the mount's click delegation worked). The sibling `.ic-drill` (region→province) and `.ic-back` (breadcrumb) controls are native `<button>`s and already keyboard-activatable. Aligned the rows to the house clickable-row convention (`tr[role="link"] tabindex="0"`, used in ~7 other app.js tables — reuses the existing `tr[role="link"]:focus-visible` inset outline in styles.css L480 and the `.ic-prow{cursor:pointer}` rule, zero CSS change) and added an Enter/Space `keydown` handler to the impact mount's existing local delegation (the global `tr[role="link"][onclick]` handler doesn't reach these — they route via delegation, not inline `onclick`), mirroring the click branch exactly; `icFocusLevel` already moves focus to the back button after the drill, so keyboard continuity holds. Zero visual/pointer-user change; node --check clean.

- ux-reducedmotion-3dpages — ✅ FIXED 2026-07-26 (a11y, WCAG 2.3.3 Animation from Interactions) — the three deck.gl pages (`province.html`, `rayong-catchment.html`, `branch-explorer.html`) carry their own inline `<style>` and, unlike the shared `styles.css` (which has honored `@media(prefers-reduced-motion:reduce){*{transition:none!important}…}` since ux-color-scheme), had **no** reduced-motion guard — so a vestibular-sensitive user with the OS "reduce motion" preference still got the full `transition:transform .3s cubic-bezier` panel slides (on mobile the `#left`/`#right`/`#panel` cards slide in via `translateX` and the `#ctl`/`.ctl` control bar via `translateY` on every open/close) plus province's infinite `nspin` loader spinner. Inconsistent with every `styles.css` route and against the animation-reduction guidance. Added the house rule to each page's inline `<style>` (`*{transition:none!important}`; province also `.notice .sp{animation:none}` to still the spinner), mirroring `styles.css:522` exactly. Zero visual/behavioral change for users without the preference (default renders identical); node --check unaffected (CSS-only). Closes the reduced-motion gap across all deck.gl pages.

- ux-pillar-acquisition-wrong-route — ✅ FIXED 2026-07-26 (navigation, IA drift) — completes the pillar-routing trio (siblings ux-pillar-assist-wrong-route + ux-pillar-risk-wrong-route). The owner's 2026-07-25 re-IA points the nav's ② **Acquisition** tab at `data.html` (the data book: "browse & drill all 2,015 branches … every measured feature") and demoted the Leaflet national map to a "More ▾ · Map view" item, but the command-center pillar CARD ② (`renderHomePillars`, both the tape-present branch + null-safe fallback) still routed `data-v="map"`/`#map` with the foot "National map →" — so the front-door ② Acquisition pillar sent users to the demoted spatial map, not the data-book Acquisition surface the nav's Acquisition tab (and the card's own label) opens. The backlog flagged this as bigger-than-surgical because it needs a CROSS-PAGE href, but the `#main-content` click delegation (app.js ~L1310) only intercepts `a[data-v]` — a link with a real `href` and no `data-v` navigates natively. Taught `pillCard` to detect a cross-page `.html` target (regex `\.html?(\?|#|$)`) and emit `href="<url>"` with NO `data-v` (hash routes still emit `data-v`/`#hash` unchanged); pointed both ② calls at `'data.html'` and relabelled the foot "National map"→"Data book" to match the destination + nav. The card's "collateral by branch" preview reads correctly against the data book (collateral type/brand/age are measured branch features it lists). Verified by headless `#home` render + `node --check`; zero visual change to the card layout. (NEW backlog item spotted while here, noted below.)

- ux-branches-cap-count — ✅ FIXED 2026-07-27 (clarity, honesty-of-scope) — `renderBranches` (app.js) hard-caps the `#branches` table at `rows.slice(0,150)` but showed **no** indication the list was truncated: on the default no-search view a user saw 150 of 2,015 branches with the other ~1,865 silently dropped, and a broad search (e.g. a populous province) could exceed 150 and lose matches with no cue — directly against CLAUDE.md's "no silent caps: log what was dropped" operating principle, and against the `#branches` static lead that promises "All 2,015 AutoX branches". Captured the pre-slice `total` and appended a muted `.cc-empty` footer row — "Showing the top 150 of N (matching) branches — refine the search or change the sort to reach the rest." — that renders ONLY when `total > CAP` (query-aware: "matching" only when a search is active; count via `toLocaleString()`). Reuses the existing empty-state row style, `no-print`, colspan 9; the populated ≤150 and empty-result paths are untouched. `provinces`/`market` tables render all matches (77 max) so need no equivalent. node --check clean; verified headless (#branches settled DOM shows the footer with "top 150 of 2,015 branches" on default load).

- ux-noscript-fallback-databook-status — ✅ FIXED 2026-07-27 — closes the follow-up flagged by `ux-noscript-fallback` ("the other pages could get the same one-liner in a follow-up"): the two remaining JS-rendered routes `data.html` (the "Data book" — branch tables + Leaflet map built via `db-root.innerHTML` + fetches) and `status.html` (autonomy plan rendered into `#app`/`#foot` from `status_data.json`) had **no** `<noscript>` banner, so a JS-off visitor got a blank `<main>` with no explanation while `index.html` + the 3 deck.gl pages already explain themselves. Added the same themed banner (console vars `var(--panel)`/`--line`/`--accent` left rail/`--txt`/`--hi`, all defined for both themes in styles.css) as the first child of each page's `<main>`, with a link back to the `index.html` front door. Renders ONLY with JS off (zero change for normal users — JS-on render identical, verified by headless render of both pages); the noscript gap is now closed across ALL 6 shareable routes. (NEW backlog item spotted while here: the 3 deck.gl pages disable pinch-zoom via the viewport meta — noted below.)

- ux-thesis-bar-dead-css — ✅ FIXED 2026-07-27 (mandate-alignment, dead-code) — the command-center "board thesis" band's progress bar (`.cc-thesis-bar`/`.cc-thesis-fill`/`.cc-thesis-barlab`) was a **branch-EXPANSION "Road-to-3,000" gauge** that the consolidation pivot already removed from the render path (`app.js` `renderHomeThesis`, which now emits only the `.cc-thesis-line` sentence — see its "REMOVED (strategy pivot)… no growth target to track" comment). But `styles.css` still carried the three dead bar rules + their mobile `@media` + three print rules, and **three comments still named the forbidden "Road-to-3,000" branch-growth target** (CLAUDE.md is emphatic: no IPO, no branch-growth target, the network is consolidating and the platform makes no open/close recommendations). Removed the dead selectors (live block lines ~743–749 + print block ~793–795) and rewrote the three stale comments to describe the current risk-only band / competition board (dropped "Road-to-3,000" and the retired "white-space leaderboard" framing). Pure dead-CSS removal (net −8 lines) — the selectors match nothing in the rendered DOM, so **zero visual change** on every route/theme (verified by headless `#home` render, identical); `node --check`/`tests/run.sh check` unaffected (CSS-only, no JS touched). Closes the last "Road-to-3,000"/expansion residue in `platform/`; the two remaining app.js references are intentional "REMOVED — strategy pivot" documentation comments and correctly stay.

- ux-acq-promo-mobile-overflow — ✅ FIXED 2026-07-27 (mobile, layout) — the whole **Competition (`#acq`) route scrolled horizontally on a real phone** (measured Playwright 390px, `isMobile`/DSF2: `scrollWidth 909 > 390`, `over:true`), clipping the header, jump-nav and every section off the right edge. Two root causes, both in `drawRivalPulse` (app.js), which injects the "Live rival promotions" landscape + raw-feed tables into `#pulsepromolandscape`/`#pulsepromolist` via `innerHTML` AFTER boot — so the boot-time `wrapTables()` never reached them: (1) **8 of 28 acq tables were unwrapped** (no `.tblwrap`), a wide `.tbl` pushing the page sideways (the other 20 are wrapped); (2) the promo-type **chip row overflowed to 903px** — the `.tag` chips are `white-space:nowrap` and joined with no whitespace, so an inline row has NO soft-wrap opportunity and ran as one unbreakable line. Fix: (a) re-run the idempotent `wrapTables()` at the end of `drawRivalPulse` so every promo table scrolls inside its own `.tblwrap` box (reuses the canonical wrapper + its role/tabindex/aria-label a11y attrs); (b) made the chip-row container `display:flex;flex-wrap:wrap;gap:6px;align-items:center` (dropped the now-redundant per-chip `margin-right:6px`) so each chip gets its own wrap point. Verified: `#acq` now `over:false, scrollWidth==390`, uncontained acq tables 8→0; chip row wraps cleanly; promo tables scroll inside bordered boxes (mobile screenshot self-reviewed). Full-route SPA sweep confirms every other route was already `over:false`. `tests/run.sh check` 96-passed/0-failed; headless index render `data-errors=[]`. Only `platform/app.js` touched (10+/2−).

- ux-databook-search-aria-label — ✅ FIXED 2026-07-28 (a11y, WCAG 4.1.2 Name/Role/Value) — `data.html`'s "All branches" search box (`#brsearch`, on the primary "② Acquisition" nav route) was placeholder-only with **no accessible name** — a placeholder disappears on input and isn't a reliable accessible name, so a screen reader announced it as an unnamed "search". This was the sibling gap `ux-search-a11y` missed when it aligned index.html's three SPA search boxes (`#search`/`#provsearch`/`#mktsearch`) to `type="search" + aria-label`. Added `aria-label="Search branches by name, province, or district"` to the `#brsearch` input (it was already `type="search"`), matching the index.html pattern. Zero visual change (SR-only attr); node --check clean. (NEW backlog item spotted while here: `rayong-province.html`'s viewport lock — noted below.)

- ux-viewport-stub-maximum-scale — ✅ FIXED 2026-07-28 (a11y, WCAG 1.4.4 Resize Text) — `rayong-province.html` (the RETIRED redirect stub → `/rayong-catchment`) carried `maximum-scale=1` in its viewport meta, blocking pinch-zoom for low-vision users. Unlike the three deck.gl pages tracked in `ux-viewport-user-scalable-3dpages` (deferred because they need `touch-action:none` on the map canvas + device testing), this stub has NO map/canvas/WebGL — it just redirects — so dropping `maximum-scale=1` is a safe, surgical one-attribute fix with no gesture conflict. Aligned its viewport meta to the exact SPA-page convention (`width=device-width, initial-scale=1, viewport-fit=cover`, matching index/data/status.html). Zero body-visual change (the stub redirects in ~0ms); the last `maximum-scale` residual on a non-canvas page is now closed. `node --check` unaffected (no JS touched).

- ux-skiplink-print-leak — ✅ FIXED 2026-07-28 (print, layout) — the exec-facing **Print / PDF** of the command center (the "🖨 Print / PDF" + "⬇ Download brief (CSV)" buttons on `#home`) rendered the accessibility skip-link as a **visible blue "Skip to main content" button overlapping the printed brief** (bottom of page 1, on top of the first region card's "N provinces — drill in ›" link). Root cause: the `.skip-link` (added by ux-skip-link) hides off-screen on screen via `position:fixed;transform:translateY(-180%)`, but Chrome's print engine flattens `position:fixed` + transforms into the page flow, so the ~30px pill landed in-content instead of above the viewport — and it was NOT in the `@media print` `display:none` list (unlike `nav`/`#themeToggle`/`.no-print`/`.cc-actions`). Added `.skip-link` to that print-hidden selector list in styles.css (one selector added to the existing rule). A skip-link is a keyboard-nav affordance with zero meaning on paper, so hiding it in print is unambiguously correct. Verified by print-to-PDF before/after: the blue button is gone and the region-drill link is no longer occluded; ZERO on-screen change (the edit is inside `@media print` only, so the keyboard skip-link still works). CSS-only, no JS touched; `node --check`/`tests/run.sh check` unaffected.

- ux-cc-jumplink-nav-vocab-drift — ✅ FIXED 2026-07-30 (clarity, IA drift) — the command-center jump-links still carried the PRE-five-pillar nav vocabulary: three static card-header links in index.html read "Exposure →" (`#exposure`) and "Overview →" (`#overview`, ×2), the JS-rendered `renderHomeHero` verdict-card CTA read "National map →" / "Overview →", and the ① MACRO **pillar-band** card foot (`pillCard(1,'Macro',…)` in `renderHomePillars`) read "Overview →" — but the owner's 2026-07-24 five-pillar re-IA renamed those nav tabs (overview→**Macro**, exposure→**Risk**, map→**Map view**) and `TAB_TITLES` already matches, so a user saw the "Risk"/"Macro"/"Map view" pillar in the nav yet a card pointing to the *same* destination labelled "Exposure →"/"Overview →"/"National map →". Same drift class as ux-tabtitles-pillar-drift + the ux-pillar-*-wrong-route trio, but here the destination was already correct — only the LABEL was stale. Aligned all six affordances (all in `platform/`) to the live nav labels: index.html card headers "Exposure →"→"Risk →" and both "Overview →"→"Macro →"; app.js hero CTA "National map →"/"Overview →"→"Map view →"/"Macro →"; and the ① pillar foot `'Overview'`→`'Macro'`. Routing untouched (`data-v`/`href`/`v`/`tab` unchanged) — label-only. Zero layout change; `node --check` clean; verified in the headless `#home` render (data-errors=[]; the ① pillar foot + card headers now read "Macro →"/"Risk →", the hero CTA "Map view →"). The other pillar feet (② "Data book", ④ "Risk exposure") were deliberately set by prior ux-pillar-*-wrong-route fixes and correctly match their destinations — left untouched. (Two NEW nav-vocab residuals spotted while here — one data-layer, one prose — noted in the backlog.)

- ux-provenance-prose-nav-vocab — ✅ FIXED 2026-07-30 (clarity, IA drift) — the `renderProducts` provenance paragraph (app.js ~L7126) read "IMF WEO macro backdrop is now wired (**Overview** → IMF macro outlook)", still naming the `overview` route by its pre-five-pillar label — but the owner's 2026-07-24 five-pillar re-IA renamed that nav tab **Overview → Macro** (index.html nav link, the two `.cc-link` card-header jump-links, and `TAB_TITLES` all already say "Macro"; ux-cc-jumplink-nav-vocab-drift closed the clickable affordances). This descriptive-prose straggler pointed the reader at an "Overview" route that no longer exists by that name. Label-only copy fix: "Overview →"→"Macro →" in the one user-facing string (all other "Overview" hits in app.js are non-user-facing code comments, left untouched). Zero layout/behavior change; node --check clean. Closes the descriptive-prose half of the nav-vocab drift; the pipeline-side `ux-decision-queue-golabel-nav-vocab` (gated data file) remains.

- ux-decision-queue-golabel-nav-vocab — ✅ FIXED 2026-07-30 — the "This week — do these first" decision-queue rows (index.html `#cc-queue`, rendered from `platform/data/decision_queue.json` via app.js ~L7196) labelled the jump to `#overview` "**Overview →**" — the pre-five-pillar name (nav route is now **Macro**). The label is a builder constant: `pipeline/build_decision_queue.py:73` `GO_LABEL`. Since `decision_queue.json` is `--check`-gated (tests/run.sh:338), the file can't be hand-edited — followed the `ux-overview-headline-mandate` precedent: changed the builder constant `"overview":"Overview →"`→`"Macro →"` (kept `"trend":"Risk trend →"`, still `#trend`'s `TAB_TITLE`) and regenerated the gated file. `build_decision_queue.py --check` reproduces byte-exact; the rank-6 crop-watch row now shows "Macro →" (1 row), the 5 defend/audit rows keep "Risk trend →". Closes the last clickable "Overview →" nav-vocab straggler on the exec front door (siblings ux-cc-jumplink-nav-vocab-drift + ux-provenance-prose-nav-vocab fixed the card/hero/pillar/prose affordances).
- ux-provenance-prose-nav-vocab — ✅ ALREADY RESOLVED (verified 2026-07-30) — the `renderProducts`/`renderExposureTape` provenance paragraph (app.js L7139) already reads "IMF WEO macro backdrop is now wired (**Macro** → IMF macro outlook)"; the earlier line-121 fix (same 2026-07-30) closed this string. This backlog entry was a stale duplicate of that fix — no open work; closed to stop future runs chasing it.

- ux-acq-ads-msgtables-unwrapped — ✅ FIXED 2026-07-30 (mobile, layout) — the new #217 rival paid-media pulse's per-operator **ad-copy sub-tables** (`<table class="tbl">` injected into `#pulseadsmsgs` inside each brand's `<details>` by `drawRivalAds` in app.js) were emitted via `innerHTML` AFTER boot, so the boot-time `wrapTables()` never reached them — they were wrapped in the house-standard `.tblwrap` scroll box ONLY if `drawRivalPulse`'s own `wrapTables()` happened to win the race between the three independent `#acq` fetches (rival_pulse / rival_ads / rival_youtube), a non-deterministic gap. Left unwrapped, a wide ad-copy sub-table can push the Competition (`#acq`) page sideways on mobile — the exact failure `ux-acq-promo-mobile-overflow` closed for the promo tables. Added a trailing idempotent `wrapTables()` to `drawRivalAds` (mirroring `drawRivalPulse`), so every ad-copy sub-table now scrolls inside its own bordered box regardless of fetch order. The main `#pulseadstbl`/`#pulsevidtbl` are static in index.html and already wrapped at boot; `drawRivalVideo` injects no dynamic tables, so neither needed a change. Zero visual change on default load (sub-tables live in collapsed `<details>`); `node --check` clean, `tests/run.sh check` 0-failed.

- ux-csv-utf8-bom — ✅ FIXED 2026-07-31 (clarity, i18n/data-integrity) — all four client-side CSV exports (`acqCSV` → `autox_catchment_coverage.csv`, the district-coverage export → `autox_district_coverage.csv`, `#mktcsv` → the market CSV, and `ccBriefCSV` → `autox_command_center_brief.csv`) built their Blob as `new Blob([csv],{type:'text/csv;charset=utf-8;'})` with **no UTF-8 BOM** — but every one of them carries Thai text (`province_th`, measured Thai district/branch names incl. `สาขา…`, Thai crop labels, e.g. `อำนาจเจริญ`). Microsoft Excel on Windows (the exec's tool in Thailand — Kaustav/board download these briefs) does NOT auto-detect a BOM-less UTF-8 CSV and decodes it with the legacy locale codepage, so all Thai columns open as mojibake. Prepended the UTF-8 BOM as a separate leading Blob array element (`new Blob(['\ufeff',csv],{…})`) at all four export sites — Excel now auto-detects UTF-8 and renders Thai correctly; Google Sheets / LibreOffice / `csv` parsers already strip/ignore a leading BOM so nothing else regresses. Verified: the produced Blob now begins with bytes `EF BB BF` (Node `Blob.arrayBuffer()` check) and headless `#home` render is byte-identical (behavioural export change only, `data-errors=[]`). `tests/run.sh check` 99-passed/0-failed; `node --check` clean.

- ux-tblwrap-region-unique-name — ✅ FIXED 2026-07-31 (a11y, axe landmark-unique / WCAG 1.3.1) — `wrapTables()` (app.js) gave EVERY horizontal-scroll table wrapper the SAME `aria-label="Scrollable data table"`. Each wrapper is `role="region" tabindex="0"` (a named landmark), so on table-dense routes (`#acq` ~28 tables, `#overview`, `#exposure`) a screen-reader user navigating by region/landmark heard "Scrollable data table" dozens of identical times with no way to tell which table they'd landed on (axe `landmark-unique` failure). Added a `tableSectionLabel()` helper that walks back/up to the table's nearest preceding heading (h1–h6; the last heading in a preceding sibling block is the closest) and names each region `"Scrollable table: <section heading>"`, with a de-dup counter (`(2)`, `(3)`…) seeded from already-labelled wrappers so names stay unique even across `wrapTables()`'s multiple idempotent calls (boot + post-`drawRivalPulse`/`drawRivalAds`). Verified in the headless `#home` DOM dump: 77 wrappers, every `aria-label` unique and descriptive (e.g. "Crop-household stress · worst provinces", the repeated ad-copy sub-tables correctly disambiguated to "…newest first (2)…(5)"); `data-errors=[]`, screenshot visually identical (SR-only attr). `tests/run.sh check` 100-passed/0-failed; `node --check` clean. (NEW backlog item spotted while here — noted below.)

- ux-static-tblwrap-missing-a11y — ✅ FIXED 2026-07-31 (a11y, WCAG 2.1.1 Keyboard / axe scrollable-region-focusable + landmark-unique) — the two ThaiWater tables in `index.html` (`#thaiwater-flood-tbl` / `#thaiwater-rain-tbl`, L508–509) shipped pre-wrapped in a STATIC `<div class="tblwrap">`, so `wrapTables()`'s idempotent "already wrapped" guard skipped them and they never received the `role="region" tabindex="0" aria-label="…"` attrs every JS-wrapped scroll region gets — their scroll region was unlabelled and not keyboard-reachable, the lone gap after ux-tblwrap-region-unique-name named all the dynamic ones. Added the three attrs to both static wrappers in the HTML with unique descriptive names ("Scrollable table: Live flood pulse" / "Scrollable table: Live rain pulse"), matching `wrapTables()`'s `"Scrollable table: <heading>"` convention exactly; `wrapTables()` already seeds its de-dup set from `.tblwrap[aria-label]` static wrappers, so these names stay collision-free across its idempotent calls. Zero visual change (attrs only, no layout/JS touched); `node --check` clean, `tests/run.sh check` 0-failed. Closes the last unlabelled scroll region.

- ux-pillar-narrow-overflow-wrap — ✅ FIXED 2026-08-01 (mobile, layout) — the command-center five-pillar summary band's `.pill` cards (`.pill-big`/`.pill-read`, `renderHomePillars` in app.js) render measured Thai branch/province names (e.g. the Acquisition card's "densest branch book: <b>NAME</b>", the Competitor read, the Assistance radar #1 province) — long *unbreakable* Thai tokens (Thai script has no inter-word spaces). Each `.pill` is `overflow:hidden` (styles.css L643), so such a token would be silently CLIPPED at the card's right edge on a narrow phone rather than wrapping — the sibling `.cc-hero-card` verdict card already guards this exact case with `overflow-wrap:anywhere` on `.cc-hero-big`/`.cc-hero-sub` (ux-cc-hero-card-narrow-overflow), but the five-pillar band, which shows the same class of content, was never given the treatment. Added `overflow-wrap:anywhere` to `.pill-big` + `.pill-read` (both five-pillar-only classes; the map lens `.pill.lens` shares the base `.pill` but not these text children, so lens pills are untouched). Space-separated prose is unaffected, so zero visual change on the current data; CSS-only, no JS touched. `tests/run.sh check` 102-passed/0-failed; headless `#home`/`#assist`/`#acq` renders identical.
- ux-branch-explorer-doctitle — ✅ FIXED 2026-07-31 (clarity, history/a11y) — `branch-explorer.html` (the per-branch `?lat=&lng=&n=` deck.gl scene) was the one 3D page that never updated `document.title` — it shipped a static `<title>Branch 3D Explorer — AutoX · เงินไชโย</title>`, so every branch opened in the explorer shared ONE tab name, making browser history entries, bookmarks and multiple open tabs indistinguishable and giving screen readers no page-title cue (the exact gap `ux-doctitle-per-route` closed for the SPA, and that `province.html`/`rayong-catchment.html` already handle for their scenes). Set an initial title from the URL `?n=` name right after `NM` is parsed (`NM+' — AutoX · เงินไชโย'`, so the tab reflects the branch before the fetch resolves), then refined it inside `loadPanel()` once `branches.json` matches the focal record to the measured name + province (`(best.n||NM)+' · '+best.v+' — AutoX · เงินไชโย'`, mirroring province.html's `province_en · region — …` pattern). Both wrapped in `try/catch` like the other pages; when `?n=` is absent the static title stands. Zero visual/scene change; `node --check` clean.
- ux-table-scope-primary-search-tables — ✅ FIXED 2026-08-01 (a11y, WCAG 1.3.1 Info and Relationships) — first slice of `ux-table-scope-sweep-appjs`: the three PRIMARY interactive SPA search tables — `#branches` (all 2,015 branches, `renderBranches` app.js L7173), `#provtbl` (77-province selector, `drawProv` L7219) and `#mkttbl` (market assessment, L7332) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so on these wide tables a screen reader couldn't reliably associate each data cell with its column header. These are the exact same trio already hardened by `ux-search-a11y` (accessible search names), `ux-search-empty-state` and `ux-branches-cap-count`, so the primary search surface is the natural first slice. Added `scope="col"` to every `<th>` in the three header rows (matching `data.html`'s already-scoped `#ptbl`/`districtTable` convention); all are column headers, so `scope="col"` is correct throughout. Zero visual change (scope is non-presentational — headless `#branches`/`#provinces`/`#market` renders identical); node --check clean. Remaining app.js tables (`#region`/`#acqtbl`/`#amptbl` families + competition/exposure) still tracked under `ux-table-scope-sweep-appjs`.

- ux-table-scope-competition-tables — ✅ FIXED 2026-08-01 (a11y, WCAG 1.3.1 Info and Relationships) — second slice of `ux-table-scope-sweep-appjs`: the four Competition (`#acq`) route tables — `#searchtbl` (brand share-of-search, `drawSearchDemand`), `#compcovtbl` (competitor census-coverage, `drawCompCoverage`), `#rivdentbl` (districts where AutoX is outnumbered, `drawRivalDensity`) and `#peerprovtbl` (province competitor-exposure, `drawPeerProvince`) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so on these wide multi-column rival tables a screen reader couldn't reliably associate each data cell with its column header. `#acq` is the objective-#2 competitive-risk route and the most table-dense surface, so it's the natural next slice after the primary search trio. Added `scope="col"` to every `<th>` — including the peer-province table's DYNAMIC per-brand / PICO / saturation / district-lost headers (`bh`/`ph`/`sh`/`dh`), all of which are column headers — matching `data.html`'s already-scoped `#ptbl`/`districtTable` convention. Zero visual change (scope is non-presentational — headless `#acq` render identical); node --check clean. Remaining app.js tables (`#region`/`#amptbl` families + exposure) still tracked under `ux-table-scope-sweep-appjs`.

- ux-table-scope-exposure-tables — ✅ FIXED 2026-08-01 (a11y, WCAG 1.3.1 Info and Relationships) — third slice of `ux-table-scope-sweep-appjs`, on the objective-#1 portfolio-risk route: the Exposure render path (`renderExposureTape` + `renderExposure`) built 15 column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`) — the real-tape bucket ladder (`#expo-tape-restr` restructuring, `#expo-tape-ltv`, `#expo-tape-occ` occupation, `#expo-tape-frontier` NPAT frontier, `#expo-tape-coll` collateral-by-age), the concentration tables (`#expoprov`, `#expotbl`, `#expo-contested-tbl` contested catchment, `#expo-brisk-tbl` twin-outlier, the scenario `Δ`-shock table), the vintage/branch-audit tables (`#exposure` "real book"), and the two risk-trend delta tables the same pass builds (`#trendboard`, `#trendbranches`) — so on these wide portfolio tables a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to every `<th>` (incl. the dynamic collateral-brand per-age-band headers), matching `data.html`'s already-scoped `#ptbl`/`districtTable` convention and the primary-search + competition slices. Zero visual change (scope is non-presentational — headless `#exposure` render identical, `data-errors=[]`, 100 `scope="col"` present in settled DOM); `node --check` clean, `tests/run.sh check` 0-failed. Remaining app.js tables (`#region` overview/commodity-board + `#amptbl` district families) still tracked under `ux-table-scope-sweep-appjs`.

- ux-table-scope-district-tables — ✅ FIXED 2026-08-02 (a11y, WCAG 1.3.1 Info and Relationships) — fourth slice of `ux-table-scope-sweep-appjs`, on the National (`#map`) district (amphoe) lens: the two district tables `#amptbl` (coverage/whitespace, `drawAmpTable`) and `#amprtbl` (risk proxy, `drawAmpRisk`) built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`) — incl. the conditional occupation/rival headers (`haveOcc`/`haveComp`) — so a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to every `<th>` in both header rows (all are column headers), matching `data.html`'s already-scoped `#ptbl`/`districtTable` convention and the primary-search + competition + exposure slices. Zero visual change (scope is non-presentational — headless render identical); node --check clean. Remaining bare family: the Overview/commodity-board tables in `renderOverview` (`#region`, DLT vehicle/agri/water) + the `#acq` rival-pulse/ads/YouTube/sentiment tables, still tracked under `ux-table-scope-sweep-appjs`.

- ux-table-scope-overview-tables — ✅ FIXED 2026-08-02 (a11y, WCAG 1.3.1 Info and Relationships) — fifth slice of `ux-table-scope-sweep-appjs`, on the Overview (`#overview`) macro route: the ~14 Overview-family tables (EV-watch electrified/DLT `#region`, drought-district SPEI, amphoe-crops, province-LFS seasonal-idle, DLT vehicle-registration brand-trends, region-debt/household, DBD new-firms, SFI system-NPL, ThaiWater flood+rain, informal-debt-source, and the coverage tables `#estates`/`#mws`/`#cws`) built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to every column `<th>` (and `scope="colgroup"` to the two ThaiWater `colspan="4"` section-title cells — the semantically-correct value for a header spanning a group of columns), matching `data.html`'s already-scoped `#ptbl`/`districtTable` convention and the primary-search + competition + exposure + district slices. Zero visual change (scope is non-presentational — headless render identical); node --check clean. Remaining bare family: the `#acq` rival-pulse/ads/YouTube/sentiment/footprint tables, still tracked under `ux-table-scope-sweep-appjs`.

- ux-table-scope-acq-tables — ✅ FIXED 2026-08-02 (a11y, WCAG 1.3.1 Info and Relationships) — final slice of `ux-table-scope-sweep-appjs`, completing the sweep on the objective-#2 Competition (`#acq`) route: the 11 remaining rival-intelligence tables — SET-listed peer scoreboard (`#peerscoretbl`, `drawPeerScore`), iOS app-sentiment ladder (`drawRivalIos`), rival paid-media pulse + its per-brand ad-copy sub-tables (`drawRivalAds`), YouTube cadence (`drawRivalVideo`), Pantip threads (`drawPantip`), social-theme + call-to-action tables (`drawSocialThemes`), Google Play sentiment ladder (`drawRivalPulse`), operator universe/footprint (`drawRivalUniverse`), rival reputation (`drawRivRep`), and the two threat tables (`drawRivThreat`/`drawRivThreatRegion`) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so on these wide multi-column rival tables a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to all 83 `<th>` across the 11 tables (incl. the empty leading cell in the operator-universe table, `<th scope="col"></th>`), matching `data.html`'s already-scoped `#ptbl`/`districtTable` convention and the primary-search + competition + exposure + district + overview slices. Mechanical + surgical: the edit is confined to lines 3632–4535 (drawPeerScore…drawRivThreatRegion), one `scope="col"` per column header, no other bytes changed. Zero visual change (scope is non-presentational — headless `#acq` render identical); node --check clean, `tests/run.sh check` 0-failed. **Closes `ux-table-scope-sweep-appjs`** — every app.js SPA table header is now `scope="col"`, matching `data.html`.

- ux-live-feed-table-a11y — ✅ FIXED 2026-08-03 (a11y, WCAG 2.1.1 Keyboard + 1.3.1 Info and Relationships) — the newer `live.html` "Live board" page (added after both site-wide table-a11y sweeps, and being its own page — not app.js, not data.html — was missed by both) shipped its "Every feed" table (`#lb-table`) with two gaps every other table on the site had already been hardened for: (1) its `.lb-wrapx` horizontal-scroll wrapper (the page's own scroll container, styles.css-independent) carried NO `role="region"`/`tabindex="0"`/`aria-label`, so a keyboard-only user could not scroll it to reach the clipped right-hand columns on a narrow viewport (axe `scrollable-region-focusable`) — the exact gap `ux-static-tblwrap-missing-a11y` closed for index.html's static ThaiWater wrappers; and (2) its six header `<th>` (built in the render JS' `<thead>` string) were bare with no `scope="col"`, the class `ux-table-scope-sweep-appjs` closed everywhere else. Added `role="region" tabindex="0" aria-label="Scrollable table: Every feed"` to the wrapper (matching the site's `"Scrollable table: <heading>"` convention, keyed to the table's "Every feed" H2) and `scope="col"` to all six column headers. Zero visual change (both are non-presentational a11y attrs — headless render identical); the feed table now matches the site-wide table-a11y convention. node --check clean; `tests/run.sh check` 0-failed.

- ux-live-chart-mobile-label-legibility — ✅ FIXED 2026-08-03 (mobile, legibility) — `live.html`'s five hand-rolled inline-SVG trend charts (System NPL / commodity prices / real-GDP+IMF projection / household debt / accumulated daily feeds) draw all axis, band and last-value labels in fixed viewBox user-units (`.lb-axl` 10px, `.lb-lastv` 11px, `.lb-bandl` 9px) inside a `viewBox="0 0 1000 250"` that renders `width:100%`. On a phone each chart is a single ~260–350px column, so the viewBox scales to ~0.26 and those labels render at **~2.6px — legible on desktop but effectively invisible on a handset** (verified: the y-axis numbers and x-axis quarter/date labels were unreadable smudges at 390px). Added a phones-only `@media(max-width:520px)` block (the band where the scale drops under ~0.5) bumping the three SVG text classes to 18/19/15 user-px, so at 390px the labels now render at ~4.7px (crisp on a retina handset) and at the 500px near-breakpoint ~8.6px — legible and proportionate, with no x-label collision (labels sit ~6 apart, ~148 units) and no clipping (last-value labels stay within the card's `overflow:visible` padding). **Scoped strictly below 520px, so the 520–760 tablet band and desktop are byte-identical** (verified: computed `.lb-axl` font-size stays 10px at ≥1000px; mobile render self-reviewed — all axis + last-value labels ["4.48%", "494", "2.5%", "87.5% GDP", "36.69 ฿/L", the "PROJECTED" band] now readable). CSS-only in the page's inline `<style>`, no JS touched, on-theme (no color/layout change); `node --check` unaffected, `tests/run.sh check` 0-failed.

- ux-nav-active-pill-scroll — ✅ FIXED 2026-08-03 (mobile, orientation/wayfinding) — the `#nav` is a `position:fixed`, `flex-wrap:nowrap`, `overflow-x:auto` horizontal scroll strip (styles.css:213), so on a phone the pillar pills overflow and scroll sideways — but `showTab` (app.js) only toggled the `.on` class + `aria-current` on the active pill and never scrolled it into the visible portion of the strip. So a route reached via a content jump-link (`#main-content a[data-v]` cards / "Next in the story" footers), a deep-link on load, or a `hashchange` could leave its "you are here" highlight scrolled off-screen — the classic mobile-tab wayfinding gap. Added a guarded `t.scrollIntoView({block:'nearest',inline:'nearest'})` on the selected pill inside the existing forEach, scoped with `t.closest('#nav')` so it fires ONLY for pills still in the strip — the re-parented `#navMoreMenu` items (`map`/`sim`, moved to `<body>`, which the selector now also matches for the `.on`/`aria-current` highlight) are correctly excluded (scrolling a `<body>`-level fixed dropdown item would be meaningless). `inline:'nearest'` scrolls only the nav horizontally; `block:'nearest'` is a no-op on the always-visible fixed nav (and the trailing `window.scrollTo(0,0)` resets any vertical nudge); `try/catch` guards the options-object form. Verified headless (390×844): default `#home` unchanged (brand + Home visible at scrollLeft 0, no scroll needed); deep-linked `#acq` scrolls the strip toward the active Competition pill; both `data-errors=[]`; desktop unaffected (nav fits one row → inline:'nearest' is a no-op). `tests/run.sh check` 121-passed/0-failed; node --check clean.

- ux-live-aging-pill-contrast — ✅ FIXED 2026-08-04 (a11y, WCAG 1.4.3 Contrast (Minimum), Level AA) — `live.html`'s "Every feed" freshness pills carry state in shape+text as well as hue, but the **"aging"** pill (`.lb-aging`, `font:600 9.5px`) used `--opp` gold, which in LIGHT theme is `#A67714` and — at 9.5px it is SMALL text needing **4.5:1** — measured only **3.68:1** on the light canvas (`--bg #F4F6FA`) and 3.78:1 on row-hover (`--raised`), a real AA failure on the freshness-board's own "this feed is overdue" flag (the exact state the board exists to surface, so it renders in production whenever a daily feed slips a day). The other three pills pass in their actual table context (fresh 4.73:1, stale 5.02:1, ref 5.37:1 on the canvas; the pulse-strip "Aging" **count** is 26px large text and correctly stays `--opp`). Fix is surgical + scoped: added `html[data-theme="light"] .lb-aging{color:#8A6210}` to live.html's inline `<style>` — darkens ONLY this pill's text in light theme to a gold that clears AA (`#8A6210 ≈ 5.06:1` on canvas / 5.19:1 on hover, still recognizably gold, distinct from the red stale pill). Dark theme is untouched (`--opp #E6B450` already ~8.3:1 on the dark panel). No global `--opp` change, so no other gold surface (fills, lines, the large count) shifts. CSS-only, no JS touched; `node --check` unaffected. Contrast ratios computed with the WCAG relative-luminance formula.

## Open backlog (discovered by the loop)
- ux-live-hhdebt-real-quarter-labels — ✅ FIXED 2026-08-04 (clarity, `live.html`) — the household-debt trend chart was the only one of the five `live.html` charts with cryptic RELATIVE x-axis labels (`q-5`,`q-4`,…`now`, built as `'q' + (i + 1 - tr.length)` — the dead `|| 'now'` never fired since a non-empty string is always truthy); every other chart (SFI NPL, commodity, GDP, daily feeds) shows real time labels. The BIS layer stamps an anchor `period` (`"2025-Q4"`), so real quarter labels are derivable: parse the anchor and back-count one quarter per point → `24Q3 24Q4 25Q1 25Q2 25Q3 25Q4`. Falls back to the old relative labels if `period` is absent/unparseable (axis never blanks); also folded the anchor into the SVG `aria-label`. Against the "concrete, not abstract" mandate the cryptic relative axis was a real clarity gap. `platform/live.html` only; node --check clean.
- ux-live-series-switcher-group-role — ✅ FIXED 2026-08-04 (a11y, WCAG 4.1.2 Name/Role/Value) — `live.html`'s two series-switcher button groups (`#lb-cmdbtns` commodity-price selector in `drawC`'s card, `#lb-fhbtns` daily-feed selector in `drawF`'s card) are single-select control sets whose `.lb-sb` buttons already carry `aria-pressed` (added with the page), but the containing `.lb-seriesbtns` divs had **no `role="group"`/`aria-label`** — so a screen reader announced a bare run of toggle buttons with no cue about what set they belong to. Same gap `ux-chip-aria-pressed` closed for the SPA's `.chip` groups; live.html's switchers were added after that sweep and missed it. Added `role="group" aria-label="Commodity price series"` / `"Daily feed series"` to the two container template strings, matching the house `role="group"`+`aria-label`+`aria-pressed` pattern (`#sortchips`, the region-filter chips). Zero visual change (attrs only); node --check clean.
- ux-nav-active-pill-deeplink-fontreflow — ✅ FIXED 2026-08-04 (mobile, wayfinding) — completing ux-nav-active-pill-scroll's deep-link case: on a DEEP-LINK-on-load to an off-screen route (e.g. `#acq` at 390px), `showTab`'s `scrollIntoView` fires before the async IBM Plex web-font settles, so it scrolls against the narrower fallback-font pill widths and, once the font loads and the pills widen, the active pill can end up only PARTIALLY revealed. Added a one-line `document.fonts.ready` re-scroll of the active `#nav a[data-v].on` pill right after the boot-time `showTab` (in `boot()`, not `showTab` — so it fires ONCE per page load, on the deep-link path, not on every interactive nav click where fonts are already loaded and it'd be a redundant no-op). Reuses showTab's exact `scrollIntoView({block:'nearest',inline:'nearest'})` call (inline-only horizontal nav scroll; block:'nearest' no-op on the fixed nav), double `try/catch`-guarded and feature-detected (`document.fonts&&document.fonts.ready`) so it silently no-ops on browsers without the Font Loading API. Idempotent — when the active pill is already fully visible (interactive nav; short-nav desktop) it's a no-op. Zero visual change for the dominant already-loaded case; node --check clean.
- qa-provenance-gate-drift — ✅ RE-RESOLVED (2026-08-01) — same recurring data-hygiene class as `qa-commodities-gate-drift`: PR #244's live-board daily-feed commit added a new MEASURED data file without regenerating the `--check`-gated `platform/data/provenance.json`, so `build_provenance.py --check` (and validate_data.py's "standalone provenance rows record the real byte size") were red on `origin/master`, unrelated to any platform/ change. Regenerated `provenance.json` deterministically — the ONLY change is the provenance census catching up to the committed tree (122→123 layers, 64→65 measured, 427→428 files, refreshed byte sizes/freshness dates); zero risk/exposure/price VALUES changed. Gate green again. The data bots need a `build_provenance.py` regen step, else every UX-loop run must restore it first.
- qa-commodities-gate-drift — ✅ RE-RESOLVED (2026-07-30) — recurred: the 2026-07-29 NABC/ThaiWater/fuel data-bot commits (#208/#209/#210) again refreshed `build_commodities.py`'s staging inputs without regenerating the committed output, so `build_commodities.py --check` was red on `origin/master` (1 gate failure, unrelated to any platform/ change). Regenerated `platform/data/commodities.json` against the already-committed refreshed inputs — the ONLY change was `meta/farmgate_vintage 2026-07-27 → 2026-07-29` (1 of 203 leaves; zero commodity price/exposure values changed), plus the propagated `provenance.json` meta census. Gate green again. This is a recurring data-hygiene drift, not a platform/ bug — the data bots need a `build_commodities.py` regen step, else every UX-loop run must restore it first. (Earlier: ✅ RESOLVED 2026-07-27 after PR #185's NABC #181 / ThaiWater #182 refresh.)
- ux-pillar-foot-arrow-doubled — ✅ FIXED 2026-08-02 (polish, robustness) — `pillCard` (app.js) appended a literal " →" to the `foot` label (`${foot||''} →`) with no guard, so a future caller passing a foot that already ends in "→" would render a doubled "→ →". Hardened the template to strip any trailing arrow before re-appending the canonical one: `${(foot||'').replace(/\s*→\s*$/,'')} →`. Idempotent — all five current callers pass plain labels (Macro / Data book / Assistance radar / Risk exposure / Competition), so zero visual change today (headless `#home` render identical: each pill foot still shows exactly one "→", `data-errors=[]`), but a stray trailing arrow can no longer double. `tests/run.sh check` 118-passed/0-failed; node --check clean.
- qa-visual-baseline-stale (NEW, test-infra) — the committed `tests/baseline/index.png` (and likely the other `index.html#…` rows) predates the whole five-pillar IA redesign AND the light-theme default: it still shows the OLD dark theme + old nav (Overview/National/Rayong 3D/Exposure) + old command-center ("WHERE TO EXPAND" cards). A fresh render diffs at mean ~202 vs baseline (tol 12), so `qa.yml`'s visual-regression phase has been red on master for weeks, independent of any one change — every UX-loop merge ships on the owner's `tests/run.sh check` gate + headless self-review instead. Fix is a deliberate `tests/run.sh baseline` refresh (large binary churn: several PNGs) once the front door is IA-stable — out of scope for a surgical UX run, but worth doing so the visual gate carries signal again. Not a platform/ bug.
- ux-table-scope-sweep-appjs (a11y) — the app.js SPA builds ~40 table header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`). PROGRESS: the three primary interactive search tables (`#branches`/`#provtbl`/`#mkttbl`) are now `scope="col"` (see `ux-table-scope-primary-search-tables`, 2026-08-01), the four Competition (`#acq`) tables (`#searchtbl`/`#compcovtbl`/`#rivdentbl`/`#peerprovtbl`) too (`ux-table-scope-competition-tables`, 2026-08-01), and the 15-table Exposure render path (`renderExposureTape`+`renderExposure`, incl. `#trendboard`/`#trendbranches`) too (`ux-table-scope-exposure-tables`, 2026-08-01); and the district `#amptbl`/`#amprtbl` family too (`ux-table-scope-district-tables`, 2026-08-02); and the ~14 Overview-family tables (`#region` EV-watch, drought/crops/LFS, DLT vehicle-reg, region-debt, DBD firms, SFI NPL, ThaiWater flood+rain, informal-debt, `#estates`/`#mws`/`#cws`) too (`ux-table-scope-overview-tables`, 2026-08-02); and the 11 `#acq` rival-pulse/ads/YouTube/sentiment/footprint tables too (`ux-table-scope-acq-tables`, 2026-08-02). ✅ **DONE / CLOSED (2026-08-02)** — every app.js SPA table header row is now `scope="col"`, matching `data.html`'s two already-scoped tables. No bare `<th>` remains in the SPA.
- ux-acquire-taxonomy-mandate (content/mandate) — ✅ **RESOLVED / CLOSED + GUARDED (2026-08-20, intelligence loop)**. The description above records the PRE-2026-08-02 state; the user-facing violation it names is gone and was verified this run to no longer exist anywhere the reader sees. **Current code (HEAD):** `build_regional_outlook.py`'s `acquire` kind emits _"Low competitive pressure — N branches (P%) face thin rival presence; least margin pressure in the region."_ (a risk read, not "📈 Expand … lead the region's acquisition here"), and `_top_action`'s `short` maps `acquire → "Low rival pressure"` (not "Expand"); `branch_recommendations.json`'s `acquire` rec reads _"Low competitive pressure: only N rival branch(es) ≤5 km around this branch."_; and `app.js` filters `type==='expand'` out of the decision queue ("the network is consolidating, so branch-'expand' actions are not surfaced"). **Verified this run:** a sweep of every surfaced `platform/data/*.json` string value for branch-open/expand recommendation phrases returned **0 hits** (377 layers), and the rendered app text carries **no** standalone "Expand"/"Acquire" action label — only mandate-compliance code comments and the legitimate CUSTOMER-acquisition surface ("Who to acquire here — top leads", allowed). The only residue is the INTERNAL taxonomy key `acquire` (a dict key + `meta.kinds` entry), which is never rendered to the reader and is therefore not a mandate violation — renaming it is a broad, `--check`-gated cross-file refactor with real regression risk and **zero** user-facing value, so it is deliberately NOT done. **Locked in:** `tests/mandate_guard.py` (wired into `tests/run.sh check`) now fails the determinism gate if forbidden branch open/close/expand recommendation language reappears in any data layer — proven to fire on the exact historical "📈 Expand … lead the region's acquisition here" phrase and to stay quiet on rival-expansion reporting + customer-acquisition copy. No future run needs to re-audit this by hand.
- ux-viewport-user-scalable-3dpages (NEW, a11y, WCAG 1.4.4 Resize Text, Level AA) — the three deck.gl pages disable browser zoom in their viewport meta: `province.html` + `rayong-catchment.html` carry `maximum-scale=1` and `branch-explorer.html` carries `maximum-scale=1, user-scalable=no` — the classic mobile a11y antipattern that stops low-vision users pinch-zooming the HTML control panels/text. The SPA styles.css pages (index/data/status) correctly allow zoom. NOT a surgical drop-in: these pages set NO `touch-action` CSS on the map container, so the viewport lock may be the only thing keeping a pinch on the canvas from zooming the whole page instead of the map. The correct fix removes the lock AND adds `touch-action:none` to the deck canvas/container (deck.gl's mjolnir.js should already set this, but it's unverified), then confirms map pinch-zoom still works on a real device/emulator — gesture behavior is NOT verifiable in the headless render harness. Deserves its own device-tested run, not an unattended auto-merge.
- ux-live-chart-mobile-viewbox-responsive (NEW, mobile, residual of ux-live-chart-mobile-label-legibility) — the phones-only font bump above makes `live.html`'s SVG labels legible at ~4.7px on a 390px handset, but the ROOT limitation remains: one fixed `viewBox="0 0 1000 250"` serves both a ~552px desktop card (scale 0.55) and a ~260px phone card (scale 0.26), so no single label size is optimal at both — the bump is a floor-raise, not a true fix. A cleaner solution renders a NARROWER viewBox (or a `preserveAspectRatio`/JS-measured width) on small screens so geometry and text scale together, giving genuinely desktop-quality labels on mobile. Bigger than surgical (touches `lineChart()`'s coordinate math) — worth its own run.
- ux-live-pulse-tally-gap — ✅ FIXED 2026-08-03 (clarity) — `live.html`'s "lead-with-the-answer" pulse strip showed `Live feeds: 20` then `Current 15 / Aging 0 / Stale 0`, but the three freshness counts only tallied feeds with a `fresh_days` cadence + a parseable stamp — feeds classed `reference` (no cadence, 3) or `no stamp` (2) were counted in NEITHER, so 15+0+0 ≠ 20 and the 5-feed gap was unexplained on the exec strip (undercutting the very honesty-of-numbers the board exists for). Added a fourth freshness bucket: a `tally.lb_ref` counter (the `forEach` `else` branch now captures every non-fresh/aging/stale feed) and a **"Reference"** pulse card right after Stale — `var(--dim)` (matching the existing `.lb-ref`/`.lb-nostamp` badge color, line 47), subtext "no cadence or stamp — don't age". Now Current + Aging + Stale + Reference = Live feeds (15+0+0+5 = 20) so the strip reconciles. `.lb-pulse` is `auto-fit,minmax(132px,1fr))` so the 7th card reflows cleanly; zero change to any other surface. `node --check` clean, `tests/run.sh check` 0-failed.
- ux-font-preconnect-gstatic — ✅ FIXED 2026-08-05 (performance, perceived load) — all 5 pages that load the Google Fonts CSS (index/data/status/province/rayong-catchment) fetch the actual IBM Plex Sans Thai + Mono font FILES from `fonts.gstatic.com`, but **no page preconnected to gstatic** — and the 3 that had a preconnect only warmed `fonts.googleapis.com` (the stylesheet host), not gstatic (the font-file host), while `province.html`/`rayong-catchment.html` had no preconnect at all. Without the gstatic preconnect the browser must run a fresh DNS+TLS handshake to gstatic AFTER the stylesheet arrives — a well-known render-blocking waterfall that delays first text paint, and text (Thai script + the mono numbers) is the entire content of this console, so it's a real perceived-load cost on Thai mobile. Added Google's own recommended pairing `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` (the `crossorigin` attr is required — font files are fetched with CORS) right after the googleapis preconnect on all 5 pages, and added the googleapis preconnect too on the 2 pages that lacked it. Head-only, no body/JS/CSS touched → **zero visual/behavioral change** (headless renders identical); `node --check` unaffected. (NEW backlog item spotted while here: `branch-explorer.html` declares `font-family:'IBM Plex Sans Thai'/'IBM Plex Mono'` throughout its CSS but never loads the Google Fonts stylesheet, so it silently falls back to `system-ui` — it either intends the system-font minimalism or is a missing `<link>`; noted below.)
- ux-font-preconnect-gstatic-live — ✅ FIXED 2026-08-06 (performance, perceived load) — `ux-font-preconnect-gstatic` (2026-08-05) added the `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` font-file-host warm-up to "all 5 pages that load the Google Fonts CSS" — but `live.html` (the "Live board" nav route, a 6th page that loads the same `fonts.googleapis.com/css2?…IBM+Plex…` stylesheet) was NOT in that list and had only the `fonts.googleapis.com` (stylesheet-host) preconnect, so its browser still ran a fresh DNS+TLS handshake to `fonts.gstatic.com` AFTER the stylesheet arrived — the same render-blocking waterfall the sweep fixed everywhere else, delaying first text paint on a page whose content (five inline-SVG trend charts + freshness tables) is entirely text/numbers. Added the identical `fonts.gstatic.com` crossorigin preconnect right after the googleapis one (matching province.html byte-for-byte incl. the explanatory comment). Head-only, no body/JS/CSS touched → **zero visual/behavioral change** (headless render identical); `node --check` unaffected. Now all 6 font-loading pages preconnect to gstatic — closes the last gstatic-preconnect gap.
- ux-branch-explorer-font-not-loaded — ✅ FIXED 2026-08-06 (consistency) — `branch-explorer.html` set `font-family:'IBM Plex Sans Thai'` / `'IBM Plex Mono'` on every text element (33 CSS references: bar, panel, controls) but, unlike the other 5 pages, never included the `fonts.googleapis.com/css2?…IBM+Plex…` stylesheet `<link>`, so its branch-name + panel text rendered in the `system-ui,sans-serif` fallback — inconsistent with the rest of the app and (per CLAUDE.md) the mandated design standard, with Thai branch names losing the IBM Plex Sans Thai shaping the app standardises on. Chose option (a): the page already DECLARES IBM Plex everywhere and the project standard is unambiguous (CLAUDE.md: "Fonts IBM Plex Sans Thai + IBM Plex Mono"), so loading them is the design-correct move, not a toss-up. Added the exact three head lines province.html/data.html use — the `fonts.googleapis.com` + `fonts.gstatic.com`(crossorigin) preconnect pairing (matching `ux-font-preconnect-gstatic`) + the `css2?family=IBM+Plex+Sans+Thai…&family=IBM+Plex+Mono…&display=swap` stylesheet — after the share-metadata block. Head-only, no body/JS/CSS touched; `display=swap` means text paints immediately in fallback then swaps to IBM Plex (no invisible-text flash). Now all 6 shareable pages load the mandated fonts. node --check unaffected; headless render self-reviewed (scene + panels intact, no layout break). Closes the last font-loading inconsistency across the app.
- ux-impact-ladder-bar-aria — ✅ FIXED 2026-08-05 (a11y, WCAG 1.1.1 Non-text Content / 4.1.2 Name-Role-Value) — the command-center impact cards' whole-book **delinquency ladder bar** (`icLadder` in app.js, the stacked proportion bar in the province- and branch-drill tables on `#home` + the Assistance/Exposure/Competition strips) carried its Current/X/30–89/90–179/180+ split ONLY in a `title` on a bare `<span class="ic-lad">` — but `title` on a non-interactive span is a hover tooltip, not a reliable accessible NAME, so the bar was effectively silent to screen readers (and the 30–89 + 180+ buckets it shows aren't in any adjacent numeric column, so that split was recoverable nowhere else). The app's OTHER proportion bar (`rz-bar`, app.js L10508, the brand-concentration bar) already does this correctly with `role="img"` + `aria-label`. Aligned `icLadder` to that precedent: factored the split into a shared `desc` string that now feeds BOTH the `title` (unchanged sighted hover tooltip) and a new `role="img" aria-label="Delinquency ladder — <desc>"`, so AT announces the same distribution sighted users see on hover. Values are measured book percentages (no user input, no injection); the per-segment child `<span title>`s become presentational under `role="img"` but their hover tooltips still render for pointer users. Zero visual change (SR-only attrs — headless `#home` render byte-identical, `data-errors=[]`); `node --check` clean, `tests/run.sh check` 0-failed.
- ux-navmore-menu-offscreen-mobile — ✅ FIXED 2026-08-06 (mobile, functional/BROKEN AFFORDANCE) — on the three `styles.css`-based secondary pages (`live.html`, `data.html`, `status.html`) the "Explore ▾" nav dropdown was **dragged almost entirely off the left edge of the screen on a phone**, making every Explore route (Risk trend / Map view / Provinces 3D / Branches / Market / Simulator / Live board) unreachable there. Root cause: `#nav` carries BOTH `backdrop-filter` (which makes it the containing block for `position:fixed` descendants) AND `overflow-x:auto` (the horizontal swipe strip), and these three pages — unlike `index.html` — never re-parented `#navMoreMenu` out of the nav, so its `position:fixed; right:10px` resolved against the *scrolled* `#nav` box, not the viewport. Since "Explore ▾" is the last nav item, reaching it on a 390px phone REQUIRES scrolling the strip fully right, so the menu was always dragged left by the scroll amount. Measured (Playwright, 390px, `isMobile`, nav scrolled): menu `x=-132` (161px wide → only ~29px on-screen), `elementFromPoint` on the first item = `none` (unclickable). Fix mirrors `index.html`'s nav script exactly with a 2-line change per page: `document.body.appendChild(menu)` (re-parent to `<body>`, whose fixed children anchor to the viewport) + `open()` now toggles `.open` on the **menu** as well as the wrap (so the `.nav-more-menu.open` display selector already in `styles.css` matches once re-parented), plus the outside-click guard gains `&&!menu.contains(e.target)`. After: `x=219` mobile / `x=1269` desktop 1440px, first item `elementFromPoint = menu` (fully on-screen + clickable) on all three pages. The three deck.gl pages (`province.html`/`rayong-catchment.html`/`branch-explorer.html`) were **deliberately left untouched** — they carry their OWN inline nav CSS with `position:absolute` (anchored to the button, so it scrolls *with* the nav and never drifts off the left) and only the descendant display selector, so re-parenting would REGRESS them (verified: menu → `display:none`); their separate right-edge-overflow nuance is backlogged below. `tests/run.sh check` 0-failed; `node --check` clean; PNG self-review of all three pages on load unchanged.
- ux-navmore-3dpages-absolute-overflow (NEW, mobile, spotted while fixing the above) — the three deck.gl pages (`province.html`/`rayong-catchment.html`/`branch-explorer.html`) style `.nav-more-menu` in their own inline CSS as `position:absolute;left:0` (NOT the `styles.css` `position:fixed`), so it stays anchored to the "Explore ▾" button and does not suffer the off-left drag the styles.css pages had — but at 390px with the nav scrolled right, the measured menu (`x≈274, w=160`) spills ~44px past the RIGHT viewport edge and `elementFromPoint` on the first item returned a page element rather than the menu item, i.e. it is partially off-screen / occluded on a phone. These pages also lack the `.nav-more-menu.open` selector, so the same re-parent fix can't be dropped in without also touching their inline CSS, and they mount a WebGL/deck.gl canvas whose pointer/gesture behaviour is not verifiable in the headless harness — so this needs its own device-tested run (same caution as `ux-viewport-user-scalable-3dpages`), NOT an unattended auto-merge.
- ux-fb-h4-tag-nowrap-bleed — ✅ FIXED 2026-08-06 (mobile, layout/BLEED+PAGEX) — on `#overview` (Macro) at 390px the `.fb-h4` sub-headings in the collateral book (`#collat-book`, `renderCollatBook` in app.js) each pair a title text node with a `white-space:nowrap` provenance `.tag` pill inside a `display:flex;align-items:center` container that had **no `flex-wrap`** — so the longest heading ("Which nameplates, and which are growing" + "MEASURED national · DLT nameplate") could not wrap: the un-wrappable tag was forced inline and bled **49px past its box**, pushing the whole document to **393px wide (a 3px sideways page scroll, PAGEX)** on a phone — the exact "text bleeding out of its box" defect `tests/visual_overflow.js` exists to catch. Added `flex-wrap:wrap` to `.fb-h4` (styles.css L1751) so the tag drops to its own line below the title when it can't sit beside it on a narrow viewport; desktop (where title+tag fit on one line) is a no-op by flex-wrap semantics. Verified with Playwright at true mobile 390px/DSF2/isMobile: heading now `right=344` (was bleeding to ~393, contained in the 390 viewport), `document.scrollWidth==clientWidth==390` (PAGEX gone), and the full `visual_overflow.js` audit is **clean across all 11 routes** at mobile width (was PAGEX 1 + BLEED 1 on macro); PNG self-review confirms the tag pill wraps cleanly onto its own line with its rounded `var(--collat)` border intact. `tests/run.sh check` 124-passed/0-failed; node --check clean.
- ux-skip-link-status — ✅ FIXED 2026-08-07 (a11y, WCAG 2.4.1 Bypass Blocks, Level A) — `status.html` was the ONLY `styles.css` nav page missing the "Skip to main content" bypass link that `index.html`/`data.html`/`live.html` all carry: its `#nav` (`role`-free landmark with the brand link + 6 primary nav links + "Explore ▾" menu button + theme toggle = ~9 focusable stops repeated on every visit) had no way for a keyboard/switch/SR user to jump past it to the page body, and its `<main>` was a bare `<main>` (no `id`/`tabindex`) so nothing could receive the skip target. Added the exact compact `data.html` block right after `<body>` — the off-screen-until-focus `<a class="skip-link" href="#main-content">` (styles.css `.skip-link` already paints it accent `#5B7CFA` on focus and hides it in print) + the tiny `preventDefault`+`focus()`+`scrollIntoView` handler — and gave `<main>` `id="main-content" tabindex="-1"` so it's programmatically focusable. Zero visual change for pointer users (skip-link is `transform`-hidden until keyboard focus); node --check clean. Now all four styles.css nav pages expose the bypass link consistently.
- ux-noscript-fallback-live — ✅ FIXED 2026-08-07 — `live.html` (the "Live board" nav route, added AFTER the `ux-noscript-fallback-databook-status` sweep) was the ONLY `styles.css` nav page with no `<noscript>` banner: it builds its entire content (freshness pulse strip, feed tables, five inline-SVG trend charts) in the browser via JS, so a JS-off visitor (or a fully-blocked font CDN) got a blank `<main>` with no explanation — while `index.html`/`data.html`/`status.html` + the 3 deck.gl pages all already explain themselves. Added the same themed banner (console vars `var(--panel)`/`--line`/`--accent` left rail/`--txt`/`--hi`, all defined for both themes in styles.css) as the first child of `<main>`, page-appropriate copy ("The live board needs JavaScript enabled…"), with a link back to the `index.html` front door. Renders ONLY with JS off (zero change for normal users — JS-on render identical); the noscript gap is now closed across ALL 7 shareable routes. `node --check` unaffected (no JS touched).
- ux-themetoggle-aria-label-3dpages — ✅ FIXED 2026-08-07 (a11y, WCAG 4.1.2 Name/Role/Value) — the theme-toggle button on the three deck.gl pages (`province.html`/`rayong-catchment.html`/`branch-explorer.html`) carried only `title="Toggle light / dark"` + `aria-pressed` with the `☾`/`☀` glyph as its content, and **no `aria-label`** — but in the accessible-name computation element *content* beats `title`, so a screen reader announced the button by its moon/sun glyph character (e.g. "last quarter moon"), not a meaningful label; worse, the JS `sync()` flips that glyph on every toggle, so the announced name changed with state. The three `styles.css` pages (`index.html`/`data.html`/`status.html`) already carry `aria-label="Toggle light or dark theme"` for exactly this reason. Added the identical `aria-label` (between `title=` and `aria-pressed=`, matching the canonical markup byte-for-byte) to the button on all three deck.gl pages, giving each a stable, meaningful accessible name that no longer shifts with the glyph (`aria-pressed` already conveys the light/dark state). Zero visual change (SR-only attr — the glyph, styling, and toggle behaviour are untouched); `node --check` clean. All six theme-toggle buttons now expose the same accessible name.
- ux-watchlist-star-aria-label — ✅ FIXED 2026-08-07 (a11y, WCAG 4.1.2 Name/Role/Value) — the watchlist toggle star buttons (`.cc-star`, from `starBtn` in app.js — rendered on the command-center impact/branch/province rows + the watchlist card at L11639) carried only a `title` with the `★` glyph as content and **no `aria-label`** and **no `aria-pressed`** — the same class just closed for the theme toggle (`ux-themetoggle-aria-label-3dpages`): element content beats `title` in the accessible-name computation, so a screen reader announced each star as "black star" not "Add to watchlist", AND the toggle's pinned/unpinned state was conveyed ONLY visually (the gold `.on` class) with no `aria-pressed`; the `title` was also static "Add to watchlist" even when already starred (clicking removes). Gave `starBtn` a state-aware `aria-label`/`title` ("Add to"/"Remove from watchlist") + `aria-pressed`, synced all three in `watchToggle`'s live-refresh loop (was toggling only the `.on` class), and added the matching `aria-label`/`aria-pressed="true"` to the always-on watchlist-card star. Zero visual change (glyph/class/behaviour untouched); node --check clean.
- qa-visual-overflow-not-in-ci (NEW, test-infra) — the bug above was caught by `tests/visual_overflow.js` (the project's standing "text bleeding out of its box" audit), which had gone UNDETECTED because that phase **silently skips** whenever Playwright's node module can't be `require()`d — and the CI/sandbox `node_modules` has no `playwright`, so the overflow gate has effectively never run in the loop's determinism gate. A global install IS present at `/opt/node22/lib/node_modules/playwright` and the audit runs fine against it via `NODE_PATH=/opt/node22/lib/node_modules node tests/visual_overflow.js …` (that's how this bleed was found + verified). Wiring the overflow phase to fall back to the global Playwright (or adding it to the gate's require path) would give the loop a real, automated bleed/clip/page-x gate instead of relying on a lucky manual render. Left for its own run — it touches `tests/run.sh`/`tests/visual_overflow.js` (test infra, not `platform/`), and the audit takes ~2 min for 11 routes × 2 viewports so the gate-time budget needs a look.
- ux-doc-lang-parts-nav-brand — ✅ FIXED 2026-08-08 (a11y, WCAG 3.1.2 Language of Parts, Level AA) — completes `ux-doc-lang-consistency` (2026-07-18, which wrapped the Thai brand `เงินไชโย` in `lang="th"` on ONLY `index.html` + `branch-explorer.html`): the other five `<html lang="en">` nav pages — `data.html`/`live.html` (`<a class="nb">`) and `status.html`/`province.html`/`rayong-catchment.html` (`<span class="nb">`) — carried the same `AutoX·เงินไชโย` nav brand with **no** `lang="th"`, so a screen reader on those pages announced the Thai word `เงินไชโย` with English pronunciation rules (the exact defect the index.html fix closed). Added `lang="th"` to the `.nb` element on all five, byte-matching the established `index.html` pattern (`<span class="nb" lang="th">`). Attr-only, zero visual change (IBM Plex Sans Thai renders both scripts identically), no JS/layout touched; overflow audit clean (11 routes × 2 viewports), `tests/run.sh check` 127-passed/0-failed. All seven nav pages now declare the brand's Thai run consistently.
- ux-spa-missing-h1 — ✅ FIXED 2026-08-08 (a11y, WCAG 1.3.1 Info and Relationships + axe `page-has-heading-one`) — the `index.html` SPA (the front door, the most-used page) had **no on-screen `<h1>`**: every visible route renders its title as an `<h2>` ("Command center", "③ Assistance — who needs help now", the Macro/Risk/Competition section titles), and the only real `<h1>` in the document is the **print-only** masthead (`#print-head`, `class="print-only"` → `display:none` off-print, inserted by `preparePrint()`). So the on-screen document outline started at `<h2>` with no heading-one — and index.html was the ONLY nav page missing an on-screen h1 (data.html renders `<h1 class="db-h1">` per view, status.html/live.html each have a static h1, rayong-catchment.html renders `<h1>Call sheet · …</h1>`). Promoting the 11 view-title `<h2>`s to `<h1>` would mean mirroring all the themed `h2` CSS (sizing, the `::before` accent bar, both theme blocks) onto `h1` — a large, visually-risky change, not surgical — so instead added a single **visually-hidden** `<h1 class="sr-only">AutoX · <span lang="th">เงินไชโย</span> — Credit Intelligence</h1>` as the first child of `<main id="main-content">` (right where the skip-link lands), giving a correct hierarchy (h1 = app, h2 = the current view) with **zero visual change**; the Thai brand run is wrapped `lang="th"` per the `ux-doc-lang-parts` convention, and `document.title` still carries the per-route name (ux-doctitle-per-route). Added a standard `.sr-only` clip utility to `styles.css` (there was none) and folded `.sr-only` into the existing `@media print{…display:none}` list so the sr-only h1 doesn't co-exist with the print masthead's visible h1 during print. Verified: index render **byte-identical** before/after (174,112 B, PNG `cmp` equal — zero visual change), exactly one real `<h1 class="sr-only">` now in the settled DOM; `tests/run.sh check` 0-failed (HTML+CSS only, no JS touched, node --check unaffected). (NEW backlog item spotted while here: `province.html` + `branch-explorer.html` have NO `<h1>` at all — static or dynamic — noted below.)
- ux-deckgl-missing-h1 — ✅ FIXED 2026-08-08 (a11y, WCAG 1.3.1 Info and Relationships + axe `page-has-heading-one`) — the two deck.gl pages `province.html` and `branch-explorer.html` rendered **no `<h1>` at all** (0 static, 0 dynamic), so their document outline had no heading-one — the same gap `ux-spa-missing-h1` closed on index.html. Followed that fix's visually-hidden-h1 approach (chosen over promoting a panel title, which would risk the inline-CSS panel layout): added a `<h1 class="sr-only" id="pageH1">` as the first child of `<body>` on both pages, a page-appropriate static initial text (province: "Province deep-dive — …", explorer: "Branch 3D explorer — …", each with the Thai brand run wrapped `lang="th"` per the ux-doc-lang-parts convention), and — since these pages' identity is dynamic — kept its `textContent` in step with `document.title`: province.html updates it in the same `.then` that sets the tab title once the record resolves (focal province · region); branch-explorer.html updates it at the initial `?n=` URL-name parse AND again in `loadPanel()` once `branches.json` matches (measured branch name · province). Both pages carry their own inline CSS, so added the canonical `.sr-only` clip utility (byte-matching styles.css:130) to each page's `<style>`. Zero visual change (SR-only attr — headless renders identical); each page now has exactly one, correctly-named `<h1>` at the top of its outline. `node --check` unaffected (HTML/CSS + a `getElementById().textContent` line each). (`rayong-catchment.html` already renders `<h1>Call sheet · …</h1>`, so all 7 nav pages now have a heading-one.)
- ux-newtab-link-cue — ✅ FIXED 2026-08-08 (a11y, WCAG G201 / 3.2.5 Change on Request) — the three `target="_blank"` links the SPA renders (the branch-risk method box's provenance "source" link, and the two rival-promo title links on `#acq` — the landscape table + the raw per-brand feed) opened an external site in a NEW TAB with **no cue** to screen-reader/keyboard users that the context would change; a sighted user at least sees the new tab appear, but AT announced only "source"/"<promo title>" with no warning. Every one already carried `rel="noopener"` (security) but none signalled the new-tab behaviour. Appended a visually-hidden `<span class="sr-only"> (opens in a new tab)</span>` inside each of the three anchors so AT announces "…, opens in a new tab" while sighted layout is byte-identical (`.sr-only` is the canonical clip utility already in styles.css:130, print-hidden at :864). All three links live only in the index.html SPA (app.js render paths), which loads styles.css, so the utility resolves everywhere they appear. Zero visual change; `node --check` clean.
- ux-search-results-status-message — ✅ FIXED 2026-08-09 (a11y, WCAG 4.1.3 Status Messages, Level AA) — the three SPA search boxes (`#search`→`#branches`, `#provsearch`→`#provtbl`, `#mktsearch`→`#mkttbl`) replace their result table's `innerHTML` on every keystroke, but the change was **entirely silent to screen readers**: there was NO `aria-live`/`role="status"` region anywhere in the app (confirmed by grep), so an AT user typing a filter got no cue how many rows now match (or that a mistyped query dropped to zero) — the SC 4.1.3 status-message gap, distinct from the sighted-only `.cc-empty` empty-state rows added earlier. Added ONE shared visually-hidden polite live region (`<div id="searchstatus" class="sr-only" role="status" aria-live="polite" aria-atomic="true">`) right after the sr-only `<h1>` in index.html, plus a guarded `setSearchStatus(msg)` helper (skips identical text so a re-sort/tab-open doesn't re-announce) called from all three draw fns with the match count — query-aware, region-qualified, and grammatically correct singular/plural ("1 province match" vs "7 provinces match", "0 branches match"); an empty query sets '' so tab-open and clearing the box stay silent. Verified with Playwright: region is `role=status`/`aria-live=polite`/1px×1px `clip rect(0,0,0,0)`; typing "rayong"→"59 branches match", "zzzzzz"→"0 branches match", clear→"", "chon"→"1 province match", "nakhon"→"7 provinces match"; `pageerror=[]`, `#branches` PNG self-review byte-visually identical (sr-only, zero visual change). `tests/run.sh check` 129-passed/0-failed; `node --check` clean.
- ux-sim-result-status-message — ✅ FIXED 2026-08-09 (a11y, WCAG 4.1.3 Status Messages, Level AA) — the `#sim` scenario simulator (the app's flagship interactive surface) recomputes its verdict/cards/readout/table via `innerHTML` on every slider move, but the RESULT was silent to screen readers: the sliders' `aria-valuetext` announces the INPUT value being dragged, yet the OUTCOME (high agri-stress provinces 0→3, exposed branches X→Y) had no `aria-live`/`role="status"` region — the same SC 4.1.3 gap `ux-search-results-status-message` just closed for the search boxes. Added a dedicated visually-hidden polite live region (`<div id="simstatus" class="sr-only" role="status" aria-live="polite" aria-atomic="true">`) inside `#v-sim` plus a guarded `setSimStatus(msg)` helper mirroring `setSearchStatus`, called from `computeSim()`: announces "Under this shock: high agri-stress provinces A to B (±); exposed branches X to Y (±)." when a crop-price/rainfall shock is set, and '' on baseline/reset (silent on tab-open, matching the search precedent) and when crop-stress data is absent. Scoped to the headline agri outcome (the "lead-with-the-answer" verdict); zero visual change (sr-only). node --check clean; `tests/run.sh check` green. (Follow-up backlog: the used-vehicle + factory sub-card results are announced only via their own cards, not the status region — a broader multi-lever announcement could be a future run.)
- ux-sim-result-status-message-all-levers — ✅ FIXED 2026-08-09 (a11y, WCAG 4.1.3 Status Messages, Level AA) — completes `ux-sim-result-status-message`'s follow-up: that fix announced only the agri (crop-price/rainfall) outcome via the `#simstatus` polite live region, but the `#sim` simulator's other two levers — **used-vehicle collateral** (`renderSimCollat`→`#sim-collat`) and **factory/manufacturing slowdown** (`renderSimFactory`→`#sim-factory-out`) — recompute their sub-cards via `innerHTML` on every slider move and were still SILENT to screen readers (dragging the vehicle or factory slider produced no announcement, since `computeSim` set the status to '' when price/rain were 0). Extended the announcement in `computeSim()` to build a multi-clause status from ALL active levers: an agri clause when price/rain is set (unchanged wording), a used-vehicle clause when `simState.veh!==0` ("used-vehicle collateral ±V% — recovery firming/softening, loss-given-default lower/higher"), and a factory clause when `simState.factory>0` ("manufacturing slowdown: avg occupation-stress B to S (+Δ) across N factory-base branches", reusing the already-computed `simFactoryModel()`, guarded on `fm.mfgBr`); clauses join with "; " under one "Under this shock: …" lead, and an all-zero state stays silent (matching the search/reset precedent). Verified headless (Playwright, driving each slider): baseline "" → veh −10 announces the vehicle clause → +factory adds the factory clause → +price adds the agri clause → reset "" ; `pageerror=[]`. Zero visual change (SR-only live region — `#sim` PNG self-review identical); `tests/run.sh check` 129-passed/0-failed, `node --check` clean. Now all four scenario levers announce their result to AT.
- qa-live-not-in-overflow-audit-routes (NEW, test-infra — spotted while here) — `tests/visual_overflow.js`'s `ROUTES` list (home/macro/trend/competition/exposure/assistance/simulator/provinces/market/branches/databook) does NOT include `live.html` (the "Live board" nav route, added after the audit was written) or the 3 deck.gl pages — so the standing "text bleeding out of its box" gate never lays out those pages. (Manually verified live.html is currently CLEAN at true mobile 390px: `document.scrollWidth==clientWidth==390`, the pulse grid resolves to 2×142px columns that fit — the harness PNG's apparent right-edge clipping is a non-`isMobile` render artifact, not a real overflow.) Adding `['live','/live.html']` to the ROUTES array would extend real overflow coverage to the 6th nav route. Test-infra (touches `tests/`, not `platform/`), sibling to `qa-visual-overflow-not-in-ci` — left for its own run.
- ux-table-scope-sweep-appjs-REOPENED — ✅ CLOSED 2026-08-12 (final slice shipped as `ux-table-scope-assist-dataroom-printtoc` below — a fresh grep finds zero real bare `<th>` in app.js). History: ⚠️ RE-OPENED 2026-08-09 — the 2026-08-02 "✅ DONE / CLOSED — every app.js SPA table header is now `scope=col`, no bare `<th>` remains in the SPA" claim (line 164) is **incorrect**: a fresh grep found **~90 bare `<th>` (no `scope`) still in `app.js`**, spanning ~15 render fns the six earlier slices never touched — `renderRivalBookImpact` (#acq contested-book, 3 tables), `drawPicoCompetitors`, `drawExitWhitespace`, `drawGapRegions`/`drawGapBoard` (#map coverage-gap lens), `drawPeerOutliers`, `drawSiegeTable`, `renderTrendBaseline` (#trend), and the #home impact-card / watchlist / decision-queue table families (lines ~8220–12088). The earlier slices only covered the tables they named; these were missed and the item was closed prematurely. **PROGRESS:** `renderRivalBookImpact`'s 3 Competition-route tables (18 `<th>`: main within-province contested table incl. the bare single-letter "X" / "90+" headers, the raw-national-gradient table, and the 12-most-outnumbered-branches table) are now `scope="col"` (see `ux-table-scope-acq-rivalbookimpact` below). Remaining fns tracked here for future surgical slices.
- ux-table-scope-acq-rivalbookimpact — ✅ FIXED 2026-08-09 (a11y, WCAG 1.3.1 Info and Relationships) — first slice of the re-opened sweep (`ux-table-scope-sweep-appjs-REOPENED`): `renderRivalBookImpact()` (the Competition `#acq` route's "Does the competition actually cost us?" contested-book section) built all three of its tables' column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`) — including the cryptic single-letter **"X"** (X-days bucket) and **"90+"** headers that most need the header→cell association for a screen reader to make sense of the abbreviations. Added `scope="col"` to all 18 column headers across the main within-province contested table, the collapsed raw-national-gradient `<details>` table, and the 12-most-outnumbered-branches `<details>` table, matching `data.html`'s already-scoped `#ptbl`/`districtTable` convention and the six earlier app.js slices. Zero visual change (scope is non-presentational — headless `#acq` render identical); `node --check` clean, `tests/run.sh check` 0-failed.
- ux-table-scope-acq-coveragegap-lens — ✅ FIXED 2026-08-10 (a11y, WCAG 1.3.1 Info and Relationships) — second slice of the re-opened sweep (`ux-table-scope-sweep-appjs-REOPENED`): the four competitor-coverage / whitespace lens tables — `drawPicoCompetitors` (`#acq` PICO-operator outnumber table), `drawExitWhitespace` (`#acq` rival-fragility / sub-scale-residual table), and `drawGapRegions` + `drawGapBoard` (the `#map` coverage-gap lens: `#gapregions` region rollup + `#gaptbl` catchment board) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so a screen reader couldn't reliably associate each data cell with its column header on these wide multi-column rival/coverage tables. Added `scope="col"` to all 25 column headers across the four tables (all are column headers — incl. the `h-opp`/`h-collat`-classed provenance-tinted headers, whose class attr is preserved), matching `data.html`'s already-scoped convention and the earlier app.js slices. Zero visual change (scope is non-presentational — the `.h-opp`/`.h-collat` classes and `title` tooltips are untouched); `node --check` clean, `tests/run.sh check` 0-failed. Remaining fns tracked under the REOPENED item: `drawPeerOutliers`, `drawSiegeTable`, `renderTrendBaseline`, and the #home impact-card / watchlist / decision-queue table families.
- ux-table-scope-trend-peer-siege — ✅ FIXED 2026-08-10 (a11y, WCAG 1.3.1 Info and Relationships) — third slice of the re-opened sweep (`ux-table-scope-sweep-appjs-REOPENED`): the three `#trend`-route peer/siege/baseline tables — `drawPeerOutliers` (`#peertbl` peer-twin outlier benchmark), `drawSiegeTable` (`#siegetbl` most-besieged rival-pressure top-10), and `renderTrendBaseline` (the three baseline tables: most-stressed provinces, worst crop-household stress, highest household leverage) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to all 28 column headers across the five tables (all are column headers — incl. the `h-risk`-classed and `no-print`-classed headers, whose class attrs are preserved), matching `data.html`'s already-scoped convention and the earlier app.js slices. Zero visual change (scope is non-presentational — classes and `title` tooltips untouched); `node --check` clean, `tests/run.sh check` 0-failed. Remaining fns tracked under the REOPENED item: the #home impact-card / watchlist / decision-queue table families.
- ux-table-scope-impactcard-drilltables — ✅ FIXED 2026-08-10 (a11y, WCAG 1.3.1 Info and Relationships) — fourth slice of the re-opened sweep (`ux-table-scope-sweep-appjs-REOPENED`): the two command-center **impact-card drill tables** — `icBranchRows` (the province→branch drill: Branch/Accounts/Book ฿m/Bucket ladder/Current/X·pre-30/NPL-live/Book status) and `icProvTable` (the region→province drill: Province/Book/Bucket ladder/Current/X·pre-30/NPL-live/Book/DTI/Crops/Rivals/Agri backdrop) — which render on `#home` plus the Assistance/Exposure/Competition impact strips (via `renderImpactStrip`), built their column-header rows as inline template literals with bare `<th>` / `<th class="ic-ladcol">` / `<th title="…">` (no `scope="col"`), so on these wide drill tables a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to all 19 column headers across the two tables (all are column headers — incl. the `ic-ladcol`-classed bucket-ladder header, whose class attr is preserved), matching `data.html`'s already-scoped convention and the earlier app.js slices. Zero visual change (scope is non-presentational — classes and `title` tooltips untouched); `node --check` clean, `tests/run.sh check` 0-failed. Remaining fns tracked under the REOPENED item: the watchlist / decision-queue / assist-occupation table families.
- ux-table-scope-assist-occ — ✅ FIXED 2026-08-10 (a11y, WCAG 1.3.1 Info and Relationships) — fifth slice of the re-opened sweep (`ux-table-scope-sweep-appjs-REOPENED`): the **assistance-occupation** (`#assist`) drill-down family — `aodOccTable` (the region/province/branch occupation table: Occupation/Accounts/OS/Current/X-days·assist/Rolling/At risk 90+/Δ income), `aodSyncHitList` (the ranked occupation hit-list: Occupation/Accounts/OS/90+/Roll/X-days/Score), `aodRenderLevel` (the region→province and province→branch drill tables) and `renderAssistOccMacro` (the national occupation-income table: Occupation/Accounts/% of book/Income·NSO/Est. impact to income) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so on these occupation-mix tables a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to all 32 column headers across the five tables (all are column headers — incl. the two empty trailing chevron `<th></th>` in the province/branch drill tables), matching `data.html`'s already-scoped `#ptbl`/`districtTable` convention and the earlier app.js slices. Zero visual change (scope is non-presentational — `title` tooltips untouched, headless `#assist` render identical, `data-errors=[]`); `node --check` clean, `tests/run.sh check` 130-passed/0-failed. Remaining fns tracked under the REOPENED item: the #home watchlist / decision-queue table families.
- ux-table-scope-market-collateral — ✅ FIXED 2026-08-11 (a11y, WCAG 1.3.1 Info and Relationships) — sixth slice of the re-opened sweep (`ux-table-scope-sweep-appjs-REOPENED`): the `#market` route's real-tape **acquisition-lens collateral-concentration** family — `renderMarketCollateral()`'s four tables (`#mkt-coll-econ` collateral-type economics: Type/Accounts/OS/Yield/Spread/90+; `#mkt-coll-branch` branch×brand concentration: Branch/Brand/Accounts/90+; `#mkt-coll-region` type×region: Type/Region/Accounts/OS/90+; `#mkt-coll-age` by age-at-origination: Age/Accounts/90+/Avg eval) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to all 19 column headers across the four tables (all are column headers — the `Spread` header's `title` tooltip is preserved), matching `data.html`'s already-scoped convention and the earlier app.js slices. Zero visual change (scope is non-presentational — headless `#market` render identical); `node --check` clean, `tests/run.sh check` 0-failed. NOTE: the REOPENED tracker's "remaining: #home watchlist / decision-queue table families" is a mis-label — those two surfaces are DIV-based (`ccRow`→`.cc-row`, `.cc-qrow`), NOT `<th>` tables, so nothing to scope there. The real remaining bare-`<th>` fns are the impact-card economics/occupation/commodity/product sub-tables (`renderSegmentEconomics`/`icOccBox` ~L8713–9014), the crop-watch + farm-book tables (~L11210–11461), the dataroom layer table (`#dr` ~L11933) and the print-TOC (~L12088) — tracked here for future surgical slices.
- ux-table-scope-impactcard-econ-commodity — ✅ FIXED 2026-08-11 (a11y, WCAG 1.3.1 Info and Relationships) — seventh slice of the re-opened sweep (`ux-table-scope-sweep-appjs-REOPENED`): the four impact-card economics/commodity/product sub-tables (~L8713–9014) — the income-impact engine region table (`renderIncome`: Region/Book income pressure/Best-off·Worst-off occupation/Top book occupations/NSO wage·LFS), the commodity-board province-spread sub-table (Province/Mean/Quotes), the commodities board (`Commodity/Borrower feels/Price trend/World YoY/Thai price/Divergence/Accounts in belt` + trailing chevron `<th>`), and the product→segment→driver map (`renderProducts`: Product/Book share/NPL-live/Borrower segments/Income drivers/Scenarios that hit it) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` (no `scope="col"`), so a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to all 23 column headers across the four tables (incl. the empty trailing chevron `<th scope="col"></th>` on the commodities board; the `title` tooltips are preserved), matching `data.html`'s already-scoped convention and the earlier app.js slices. Zero visual change (scope is non-presentational — headless `#home`/`#overview`/`#exposure` renders identical); `node --check` clean. Remaining bare-`<th>` fns tracked under the REOPENED item: the crop-watch + farm-book tables (~L11210–11461), the dataroom layer table (`#dr` ~L11933) and the print-TOC (~L12088).
- ux-table-scope-cropwatch-farmbook — ✅ FIXED 2026-08-11 (a11y, WCAG 1.3.1 Info and Relationships) — eighth slice of the re-opened sweep (`ux-table-scope-sweep-appjs-REOPENED`): the four crop-watch / farm-book tables (~L11210–11461) — the farm-household cash-income trend table (`Crop year/Farm cash income/Non-farm/Cash expense/Net cash·yr/·month/Farm share`), the falling-crop depend-map (`Crop/Farm-gate YoY/Provinces on it/Healthy accounts riding on it/Farm book/Where most of it sits`) with its by-province `<details>` sub-table (`Province/Current+X/Current·X/Farm accounts/Farm book/Crops it depends on`), the branch-exposure ranking table (`Branch/Province/Exposed ha/Share of catchment/Farm accounts here/X %/Province Current+X`), and the account-lifecycle ladder (`Where the account is in its life/Accounts/90+/NPAT per account`) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` / `<th class="sub">` (no `scope="col"`), so a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to all 25 column headers across the four tables (all are column headers — the `class="sub"` and `title` tooltips are preserved, scope inserted before the class attr), matching `data.html`'s already-scoped convention and the earlier app.js slices. Zero visual change (scope is non-presentational); `node --check` clean, `tests/run.sh check` 0-failed. Remaining bare-`<th>` fns tracked under the REOPENED item: the sibling assist crop-watch / status tables (~L9194, ~L11494–11516), the dataroom layer table (`#dr` ~L11988) and the print-TOC (~L12143).
- ux-live-meta-stale-feed-counts — ✅ FIXED 2026-08-11 (clarity, honesty-of-numbers / share-metadata) — `live.html`'s static `<head>` share/meta descriptions hardcoded VOLATILE feed counts that had drifted well out of date: the `meta name="description"` said "the stored history behind the **four** feeds that carry a real trend line" and `og:description` said "**20** live feeds … and **151** stored history points behind **four** real trend lines" — but the live board now renders **23 live feeds, 8 trend lines, 230 stored points** (its own body note correctly computes "8 of 23 feeds carry one" from data). So a link shared internally (a documented "core use of an exec tool", per `ux-share-metadata`) unfurled with stale, wrong counts, cutting against the project's honesty-of-numbers mandate. Unlike every other page's metadata — which cites only STABLE platform invariants (2,015 branches, 77 provinces) — live.html cited counts that inherently grow as feeds land, so they can only ever drift. Fix (matching the page's own `twitter:description`, which already carries no counts): reworded both descriptions to drop the volatile counts — "the stored history behind the **feeds** that carry a real trend line" / "**every live feed** with per-source cadence and freshness, and the stored history behind the feeds that carry a real trend line." Now accurate as the feed set grows, no future drift. Pure `<head>` metadata — zero body/visual change (headless render identical), no JS/CSS touched.
- ux-table-scope-assist-dataroom-printtoc — ✅ FIXED 2026-08-12 (a11y, WCAG 1.3.1 Info and Relationships) — **final slice of `ux-table-scope-sweep-appjs-REOPENED`, closing the sweep**: the last five bare-`<th>` app.js tables — `renderHomeTape` (`#cc-tape` command-center Assistance radar: Province/Tier 1 slipping/Tier 2 watch/Drought/crops), `renderAssist`'s two `#assist` tables (`#assist-radar` pre-emptive drought radar, 5 headers; `#assist-restr` restructuring-held status ladder: Status/Accounts/OS/X-days/Roll/90+/180+), the dataroom layer table (`renderHomeDataRoom`, `.dr-tbl`: Layer/Provenance/Source·builder/Vintage·size), and the print summary's table-of-contents (`buildPrintSummary`, `.pr-toc`: Section/Contents/Evidence) — built their column-header rows as inline template literals with bare `<th>` / `<th title="…">` / `<th class="num">` (no `scope="col"`), so a screen reader couldn't reliably associate each data cell with its column header. Added `scope="col"` to all 24 column headers across the five tables (all are column headers — the `class="num"` and `title` tooltips are preserved), matching `data.html`'s already-scoped convention and the earlier app.js slices. A fresh grep now finds **zero real bare `<th>` in app.js** (the one remaining hit is a code-comment mention, not a header cell) — **`ux-table-scope-sweep-appjs-REOPENED` is fully CLOSED**; every SPA + data.html table header is `scope="col"`. Zero visual change (scope is non-presentational — headless renders identical); `node --check` clean.
- ux-fb-h4-tag-nowrap-bleed-bookcheck — ✅ FIXED 2026-08-12 (mobile, layout/BLEED+PAGEX) — a second `.fb-h4` bleed the earlier `ux-fb-h4-tag-nowrap-bleed` (flex-wrap) fix could not reach: on `#overview` (Macro) at 390px the collateral **book-check** heading (`#collat-bookcheck`, `renderCollatBookCheck` in app.js) pairs its title with the long provenance pill "MEASURED · auction/retail listings × real loan tape" (~52 chars). `.tag` is `white-space:nowrap` globally, so this pill is wider than a phone line **even on its own row** — `flex-wrap:wrap` drops it below the title but it still bled **78px** past the box, pushing the document to **422px wide (sideways page scroll / PAGEX)** at 390px. Added a phones-only `@media(max-width:520px){.fb-h4 .tag{white-space:normal}}` in styles.css so an over-wide provenance tag inside a collateral-book heading wraps onto multiple lines; desktop has ample room so the tag stays one line (no-op there). Verified with the Playwright overflow audit at true mobile 390px: macro now clean (was PAGEX 1 + BLEED 1), full 11-route audit clean; CSS-only, no JS touched. `tests/run.sh check` 0-failed; node --check unaffected.
- ux-nav-live-tooltip-stale-count — ✅ FIXED 2026-08-12 (clarity, honesty-of-numbers / IA drift) — sibling of `ux-live-meta-stale-feed-counts` (which dropped the drifted feed counts from `live.html`'s OWN `<head>` share metadata): the "Live board" nav menu-item tooltip — the `title="Every live feed — freshness against each source's own cadence, plus **the four** with real stored history"` repeated verbatim on all 6 nav pages that carry the Explore menu (`index.html`/`data.html`/`status.html`/`province.html`/`rayong-catchment.html`/`branch-explorer.html`) — still hardcoded the volatile trend-line count "**the four**", but the live board now renders **8** trend lines (its own body note computes "8 of 23 feeds carry one" from data, confirmed in the headless `live.html` DOM). So hovering the Live-board affordance promised four stored series when there are eight — a stale, wrong count on an exec tool, against the honesty-of-numbers mandate. Following the `ux-live-meta-stale-feed-counts` precedent (a count that inherently grows as feeds land can only ever drift, so drop it rather than re-pin it), reworded the tooltip on all 6 pages "plus **the four** with real stored history" → "plus **the feeds that carry** real stored history" — accurate as the trend-line set grows, no future drift. `title`-attribute copy only (a hover tooltip, not rendered content) → zero visual/layout change (headless renders identical), no JS/CSS touched; `node --check` unaffected. `live.html` itself carries no self-link tooltip, so it needed no change.
- ux-navmore-keyboard-secondary-pages — ✅ FIXED 2026-08-13 (a11y, WCAG 2.1.1 Keyboard / 2.4.3 Focus Order) — `ux-nav-more-keyboard` (2026-07-17) added the full menu-button keyboard pattern (ArrowDown/Up open into the menu, Arrow/Home/End roam the items, Escape/Tab close and restore focus to the trigger) to **index.html's** "Explore ▾" nav dropdown — but that enhancement was never propagated to the secondary pages, which each carry a byte-identical *copy* of only the click+Escape subset (comment even reads "copied from index.html"). So the SAME re-parented-to-`<body>` `role="menu"` widget (`aria-haspopup`, 7 `role="menuitem"` links) was keyboard-roamable on the front door but NOT elsewhere: a keyboard user could open it (Enter on the button) yet couldn't arrow between items, focus was never moved INTO the menu on open, and Escape didn't return focus to the trigger — and because the menu is re-parented to the end of `<body>`, tabbing off the button skipped its items straight to the page body (the exact gap the index fix closed). Ported index.html's keyboard block verbatim to the three **non-3D** secondary pages that share the identical simple script — `live.html`, `data.html`, `status.html` — WITHOUT index's `place()` positioning logic (index-specific; these pages position the menu via styles.css and are untouched), so the change is purely additive keyboard handling: zero visual change, no positioning change. `tests/run.sh check` 0-failed (node --check on every page's inline JS, incl. the three edited); headless `live.html` render byte-visually identical (menu closed by default; keyboard-only behaviour has no static-paint footprint). Remaining: the three deck.gl pages (`province.html`/`rayong-catchment.html`/`branch-explorer.html`) carry the same simple script BUT style `.nav-more-menu` as `position:absolute` in their own inline CSS (see `ux-navmore-3dpages-absolute-overflow`) and mount a WebGL canvas whose pointer/gesture behaviour isn't headless-verifiable — so they need their own device-tested run, NOT an unattended auto-merge (same caution as `ux-viewport-user-scalable-3dpages`). Tracked as `ux-navmore-keyboard-3dpages` below.
- ux-navmore-keyboard-3dpages (NEW, a11y — spotted while fixing the above) — the three deck.gl pages (`province.html`/`rayong-catchment.html`/`branch-explorer.html`) still carry only the click+Escape subset of the "Explore ▾" menu-button keyboard pattern (no arrow-roaming / focus-into-menu / focus-restore), same as the secondary pages before `ux-navmore-keyboard-secondary-pages`. The keyboard block is drop-in identical, BUT these pages (a) style `.nav-more-menu` as `position:absolute;left:0` in their own inline CSS rather than the styles.css `position:fixed` (see `ux-navmore-3dpages-absolute-overflow`), and (b) mount a WebGL/deck.gl canvas whose pointer/gesture/focus behaviour is not verifiable in the headless render harness — so this needs its own device-tested run, not an unattended auto-merge.
- ux-search-input-mobile-hints — ✅ FIXED 2026-08-13 (mobile, input UX) — the four filter-as-you-type search boxes (`#search`/`#provsearch`/`#mktsearch` in index.html + `#brsearch` in data.html) carried NO mobile input-hint attributes, so on the exec's Thai handset a search over branch/province proper nouns and transliterations triggered iOS autocorrect (rewriting a partial name like "Rayo…" to an English dictionary word mid-type — actively breaking a filter box), first-letter auto-capitalization, a saved-value autocomplete dropdown covering the live results, and red spellcheck squiggles under every non-dictionary token. The scene search (`ssInput`, rayong-catchment.html) already establishes the house pattern (`autocomplete="off" spellcheck="false"`). Added `autocomplete="off" autocorrect="off" autocapitalize="none" spellcheck="false"` to all four filter inputs (the full set, since these filter proper nouns where autocorrect/autocapitalize are the real harm). Attribute-only, zero visual change (headless renders identical — the hints only affect the on-device soft keyboard); `node --check`/`tests/run.sh check` unaffected (data.html string is still valid JS).
- ux-casc-select-label-association — ✅ FIXED 2026-08-13 (a11y, WCAG 1.3.1 Info and Relationships / 4.1.2 Name-Role-Value / 3.3.2 Labels) — the left-panel cascading Region/Province/Branch `<select>` controls (`#cscRegion`/`#cscProv`/`#cscBranch`, `.casc` block) on the two deck.gl pages `province.html` + `rayong-catchment.html` paired each select with a VISIBLE `<label>` that was a bare sibling — no `for=` and not wrapping the control — so the label was not its accessible NAME: a screen reader announced each as an unnamed "combobox" and the visible "Region"/"Province"/"Branch" text wasn't a click target to focus/open the select. Unlike the deferred deck.gl backlog items (`ux-viewport-user-scalable-3dpages`, the navmore-3dpages pair), this is a STATIC HTML attribute association with no map/canvas/gesture behaviour to device-test and zero rendering effect, so it is headless-verifiable and safe. Added `for="cscRegion"`/`for="cscProv"`/`for="cscBranch"` to the three labels on both pages (6 selects total; each document's `for`→`id` pair is unambiguous). Zero visual change (a `for` attr has no paint footprint — headless renders identical); `node --check` unaffected (no JS touched). All six cascading selects now expose their visible label as their accessible name.
- ux-live-chart-aria-latest-value — ✅ FIXED 2026-08-13 (a11y, WCAG 1.1.1 Non-text Content + lead-with-the-answer) — `live.html`'s five inline-SVG trend charts (System NPL / commodity prices / real-GDP+IMF projection / household debt / accumulated daily feeds) each PRINT their latest value as an on-chart label (the `.lb-lastv` text at the line's end — "4.48%", "494", "2.5%", "87.5% GDP", "36.69 ฿/L"), the single most important number a sighted reader takes off the chart — but the SVG's `role="img"` **aria-label named only what the series IS** ("SFI system NPL ratio by quarter since 2008-Q1"), so a screen-reader user learned where the data STARTED but never where the line now SITS or which way it heads — the "answer" was invisible to AT even though it's drawn on screen. Enriched the accessible name in ONE place (`lineChart()`'s return, so all five charts benefit): appended "; latest <value><unit>, <rising/falling/flat> across the series", reusing the same `fmtNum(pts[li].y)+unit` the visible last-value label already uses and deriving direction from first-vs-last point. Now AT announces e.g. "…since 2008-Q1; latest 4.48%, falling across the series" — matching the on-screen label + each caption (household "deleveraging"→falling, diesel "▼"→falling). SVG-attr-only change → **zero visual change** (before/after PNG byte-identical, mean_diff 0.0); the existing `esc(o.aria)` single-escape of the descriptor half is preserved. `tests/run.sh check` 0-failed; node --check clean; aria-labels verified in the settled headless DOM.
- ux-print-color-adjust-dataviz — ✅ FIXED 2026-08-14 (print, exec/board brief — VERIFIED against a real PDF) — the command-center / exposure "🖨 Print / PDF" is an explicit board-facing feature, but Chrome & Safari strip element **backgrounds** from print + "Save as PDF" unless the "Background graphics" box is ticked (OFF by default) and no page declared `print-color-adjust`. So on a real default PDF the whole-book **delinquency ladder** (`.ic-lad-seg`) and its colour **legend** (`.ic-lad-leg i`) — whose ONLY encoding is the background fill — printed as **blank white boxes**, gutting the core risk readout; the sibling proportion bars (`.rz-bar`, `.bar>i`, `.conc-legend i`) share the same defect. Confirmed EMPIRICALLY by rasterising `page.pdf({printBackground:false})` (the real Chrome default): before-fix the ladder + legend were blank, after-fix they render in full green/gold/orange/red/purple. Foreground colour (the red status dot, tag borders/text) already survived — only background-encoded marks were lost. Fix: added an `@media print` rule setting `-webkit-print-color-adjust:exact;print-color-adjust:exact` on exactly those data-viz marks (`.ic-lad,.ic-lad-seg,.ic-lad-leg i,.rz-bar,.rz-bar span,.bar,.bar>i,.conc-legend i`), so the fill renders regardless of the user's background-graphics setting. Scoped to data-viz only — the print block still deliberately whitens **card fills** (`.cc-card`/`.mcard`), which are untouched. Print-only CSS, so the SCREEN render is byte-identical (verified: `#exposure` screen render unchanged, `data-errors=[]`); `tests/run.sh check` 0-failed. (NEW backlog items spotted: the decorative `h2::before`/card region accent bars and the `.tag`/`.chip` pill backgrounds are ALSO stripped in the default PDF — lower value than the data-viz, left as a possible follow-up.)
- ux-boot-error-exec-facing — ✅ FIXED 2026-08-14 (clarity, error UX) — the front-door SPA's critical-fetch failure state (`boot()`'s `catch`, app.js ~L1728) rendered a **developer-facing** message — "Couldn't load data files. Make sure `data/branches.json` and `data/meta.json` sit next to this page. (<err>)" — but the front door's reader is the owner/board, not a developer, and the message names internal file paths and a local-serving assumption the exec cannot act on (and which reads as "broken" if a transient network/CDN blip drops the fetch on the production Vercel site). Every OTHER page already degrades more calmly (`data.html` "Could not load data/branches.json.", the rayong-catchment scene's calm notice). Rewrote the front-door state to lead with a plain-language answer ("**Couldn't load the dashboard data.** This is usually a brief network hiccup — reload to try again…") + an actionable **↻ Reload** button (house `.chip`, wired to `location.reload()` — no inline JS), and demoted the raw `err` to a muted `.sub` "Technical detail:" line for whoever debugs. Error-path only, so the happy path is untouched — the normal `#home` render is byte-identical (verified). Matches CLAUDE.md's "lead with the answer, explain simply, prefer something that visibly works". `node --check` clean; `tests/run.sh check` 0-failed.
- ux-hero-card-role-button — ✅ FIXED 2026-08-14 (a11y, WCAG 4.1.2 Name/Role/Value) — the command-center front-door verdict "hero" card (`renderHomeHero`, app.js ~L11464) was rendered as `<a class="cc-hero-card" data-v="…" href="#…" role="button">` — a real navigation LINK (it routes to a `#hash` view via the `#main-content a[data-v]` click delegation, with the `href` as a graceful fallback) that `role="button"` made a screen reader announce as a "button". That mismatch is twofold: it misnames the element's nature (it navigates, it doesn't act), and it misrepresents the keyboard contract — buttons activate on **Space**, anchors do NOT (Space just scrolls), so an AT user who heard "button" and pressed Space got page-scroll instead of activation. Every sibling content jump-link (`.cc-link`, `.pill`, `.story-next`) is a plain `<a href data-v>` announced correctly as a link; the hero card was the lone outlier. Removed the `role="button"` attribute (the ONLY change) so it's announced as the link it is, matching its siblings; Enter still activates it natively (fires a click → the delegation `preventDefault`s + `showTab`s). No CSS/JS depends on the role (styling is class-based `.cc-hero-card`), so **zero visual change** and routing is untouched. `node --check` clean.
- ux-print-color-adjust-provenance-dot — ✅ FIXED 2026-08-15 (print, exec/board brief — honesty-of-provenance mandate) — follow-up to `ux-print-color-adjust-dataviz` (which forced the data-viz fills to survive Chrome/Safari's default background-strip in print/"Save as PDF"): the SAME defect silently breaks the **MEASURED provenance marker**. The leading dot of a measured pill — `.prov.m .pd` (`background:#5fd6c2`, no border) and `.cc-tag.m::before` (`background:#5fd6c2`, no border) — is a SOLID disc encoded ONLY by background fill, so on a default board PDF it drops to an invisible box, while the ESTIMATED (`.prov.e`/`.cc-tag.e` — border ring) and UNLABELLED (`.cc-tag.u` — border square) markers survive on their `border`. So a board printing the brief lost the single most important honesty signal — measured (the trustworthy number) reads as a blank dot, indistinguishable at a glance from an unmarked one — cutting directly against the project's always-state-measured-or-estimated mandate. Added `.prov,.prov .pd,.cc-tag,.cc-tag::before` to the existing `@media print` `print-color-adjust:exact` rule (styles.css, alongside the data-viz marks), so the measured dot + the pill's semantic tint render in the PDF exactly as on screen; the dot colours (`#5fd6c2` dark / `#0c6a5b` light measured) are saturated enough to read on white paper. Print-only CSS, so the SCREEN render is byte-identical (headless `#home`/`#exposure` unchanged); scoped to the provenance pills only, nothing the print block intentionally whitens (`.cc-card`/`.mcard` fills) is affected. `tests/run.sh check` expected 0-failed; `node --check` unaffected (CSS-only). (The generic semantic `.tag` variants degrade acceptably — their text + border keep the colour code — and the decorative `h2::before` accent bars are cosmetic, both left as the lower-value residual the parent fix noted.)
- ux-print-color-adjust-h2-accent — ✅ FIXED 2026-08-15 (print, exec/board brief — segment lens) — closes the print-colour-strip series: the LAST background-encoded mark, the section-heading accent bar (`h2::before` — a 3px gradient rail, `left:0`, NO border), was still stripped from the default board PDF. Its SEMANTIC variants carry the segment lens — `h2.risk` (agri red), `h2.demand` (merchant teal), `h2.collat` (collateral purple), `h2.opp` (gold), all used pervasively across app.js + index.html — so the whole brief's segment colour-coding vanished to blank section markers on paper (the prior fix mislabelled these "cosmetic", conflating the base blue bar with the semantic segment bars). Added `h2::before` to the existing `@media print` `print-color-adjust:exact` rule (covering the base + all four variants via one selector), so the on-screen segment colour-coding survives on paper. Verified EMPIRICALLY like the two prior print fixes — rasterised `page.pdf({printBackground:false})` (real Chrome default) with pymupdf: left-margin colored pixels 3051 → 3212 (+161, all in the heading-accent band; full-page delta identical, so nothing else moved), i.e. the bars now render instead of dropping to white. Print-only CSS → SCREEN render byte-identical (headless `#home` render `cmp`-equal, 116,576 B before/after). `node --check` unaffected (CSS-only); negligible ink (3px per heading), card fills stay deliberately whitened.
- ux-deckgl-toggle-aria-pressed-branchexplorer — ✅ FIXED 2026-08-15 (a11y, WCAG 4.1.2 Name/Role/Value) — `branch-explorer.html`'s left control cluster carries a single-select **basemap** segmented selector (`#b-light`/`#b-voyager`/`#b-dark`, one `.on` at a time) and two **layer** toggle buttons (`#t-ring` rings on/off, `#t-orbit` auto-orbit on/off), but every one conveyed its selected/pressed state ONLY visually (the `.on` class) with **no `aria-pressed`** — so a screen reader announced each as an undifferentiated toggle button with no cue which basemap is active or whether rings/orbit are on. This is the exact gap `ux-chip-aria-pressed` (2026-07-23) closed for the SPA `.chip` groups and `ux-live-series-switcher-group-role` (2026-08-04) for live.html's switchers; the deck.gl pages' own control clusters were never given the treatment. Like `ux-themetoggle-aria-label-3dpages` + `ux-casc-select-label-association`, this is a STATIC ARIA-attribute fix with no map/canvas/gesture behaviour to device-test (headless-verifiable, safe for the unattended loop) — NOT the deferred `ux-viewport-*`/`ux-navmore-*-3dpages` gesture class. Added `aria-pressed` to the five toggle-state buttons (initial values mirror the defaults: light basemap + rings ON = `true`, others `false`; `#t-reload` is an action, left untouched), wrapped the basemap `.seg` in `role="group" aria-labelledby="baseLbl"` (its visible "Basemap" `.glab` now `id="baseLbl"`), and synced `aria-pressed` alongside the existing `.on` toggle in all four code paths that set it (`#t-ring` click, `#t-orbit` click, `setOrbitUI()` [the auto-pause-on-interaction path], and the basemap select loop) so the accessible state always mirrors the visual `.on` state — no new desync, zero behavior change. Verified in the settled headless DOM: `#b-light`/`#t-ring` `aria-pressed="true"`, the rest `false`, all matching their `.on` class; scene + panels render intact (SR-only attrs, zero visual change). `tests/run.sh check` 137-passed/0-failed; node --check clean. (NEW backlog item below: `province.html` + `rayong-catchment.html` carry the SAME gap on their `data-b` basemap selectors + `#ringtog`/`#croptog`/`#orbittog` layer toggles + `.stb` side-panel tabs + `.lg` POI-legend toggles — a follow-up slice.)
- ux-deckgl-toggle-aria-pressed-province — ✅ FIXED 2026-08-15 (a11y, WCAG 4.1.2 Name/Role/Value) — first of the two-page slice tracked below: `province.html`'s control clusters conveyed selected/pressed state ONLY via the `.on` class with no `aria-pressed` — the basemap segmented selector (`.seg > button[data-b]` light/voyager/dark, single-select), the three layer toggles (`#ringtog` reach-rings, `#croptog` crop lens, `#orbittog` auto-orbit), the four `.stb` side-panel tab buttons (impacts/districts/near/comp), and the dynamically-built `.lg` POI-legend toggle chips. Mirrored the branch-explorer sibling fix (`ux-deckgl-toggle-aria-pressed-branchexplorer`): added `aria-pressed` to all static toggle buttons with initial values matching the defaults (`basemap='dark'`→dark `true`, `showRings=true`→ring `true`, crop/orbit `false`, `.stb` impacts `true`), wrapped the basemap `.seg` in `role="group" aria-labelledby="baseLbl"` (the visible "Basemap" `.glab` now `id="baseLbl"`), emitted `aria-pressed` in the `.lg` chip template (mirrors the `poiOn[t]` state), and synced `aria-pressed` alongside every existing `.on` toggle in all six code paths (basemap select loop, `#ringtog`/`#croptog`/`#orbittog` onclick, `setOrbitUI()` auto-pause path, `.stb` tab click, and the `.lg` chip onclick) so the accessible state always mirrors the visual state — no new desync, zero behavior change. Verified in the settled headless DOM: dark/ring/impacts/on-chips `aria-pressed="true"`, the rest `false`, every value matching its `.on` class; scene + panels render intact (SR-only attrs, `data-errors=[]`, zero visual change). `tests/run.sh check` 138-passed/0-failed; node --check clean. Remaining: `rayong-catchment.html` (below).
- ux-deckgl-toggle-aria-pressed-rayong — ✅ FIXED 2026-08-16 (a11y, WCAG 4.1.2 Name/Role/Value) — **final slice of the deck.gl toggle-state series, closing it** (siblings: branch-explorer 2026-08-15, province 2026-08-15): `rayong-catchment.html`'s control clusters conveyed selected/pressed state ONLY via the `.on` class with no `aria-pressed` — the basemap segmented selector (`.seg > button[data-b]` studio/light/voyager/dark/satellite, single-select), the layer toggles (`#ringtog` reach-rings, `#isotog` isochrone [hidden until scenery ships], `#orbittog` auto-orbit), and the dynamically-built `.lg` POI-legend toggle chips. Mirrored the province/branch-explorer fixes: added `aria-pressed` to the static toggle buttons with initial values matching the defaults (basemap `studio`→`true`; `#ringtog`/`#isotog` `class="on"`→`true`; `#orbittog`→`false`), wrapped the basemap `.seg` in `role="group" aria-labelledby="baseLbl"` (the visible "Basemap" `.glab` now `id="baseLbl"`), emitted `aria-pressed` in the `.lg` chip template (mirrors `poiOn[t]`), and synced `aria-pressed` alongside every existing `.on` toggle in all five code paths (basemap select loop, `#ringtog`/`#isotog`/`#orbittog` onclick, `setOrbitUI()` auto-pause path, and the `.lg` chip onclick). The `📸 Poster` (`#postertog`) button is a momentary capture ACTION (like branch-explorer's `#t-reload`), left untouched; this page has no `.stb` tabs (its mobile `#mtog` panel switcher toggles panel `.show`, not a selected state). Zero visual change (SR-only attrs — the `.on` styling/behaviour is untouched); every deck.gl page's basemap/layer/legend control now exposes its pressed state consistently. `node --check` clean.
- ux-deckgl-legend-group-role — ✅ FIXED 2026-08-16 (a11y, WCAG 4.1.2 Name/Role/Value / 1.3.1 Info and Relationships) — follow-up to the just-closed deck.gl toggle-state series: `province.html` + `rayong-catchment.html` build their POI-legend `#legend` container as a run of `.lg` POI-layer **toggle buttons** (each now `aria-pressed`, per `ux-deckgl-toggle-aria-pressed-province`/`-rayong`), but the CONTAINER itself carried no `role="group"`/`aria-label` — so a screen reader announced a bare sequence of pressable buttons with no cue they form one related set of map-layer toggles (a legend). This is the exact container-grouping the SPA `.chip` groups got in `ux-chip-aria-pressed` (2026-07-23), live.html's switchers in `ux-live-series-switcher-group-role` (2026-08-04), and the deck.gl basemap `.seg` selectors got in the toggle-state series (`role="group" aria-labelledby="baseLbl"`) — the POI legend was the lone ungrouped toggle cluster on these two pages. Like the sibling toggle-state fixes it is a STATIC ARIA-attribute addition with no map/canvas/gesture behaviour to device-test (headless-verifiable, safe for the unattended loop). Added `role="group" aria-label="POI layer toggles"` to the static `#legend` div on both pages (a labelled empty group is harmless when a province has no mapped POI layers; populated, it names the toggle set). `branch-explorer.html` is correctly OUT of scope — its POI legend items are read-only `<div class="pl">` display rows (no toggle, no `aria-pressed`), a pure colour key rather than a control cluster. Zero visual change (SR-only attrs — the `.lg` chips/styling/behaviour are untouched); `node --check` unaffected (no JS touched).
- ux-skip-link-branchexplorer — ✅ FIXED 2026-08-16 (a11y, WCAG 2.4.1 Bypass Blocks, Level A) — the three deck.gl pages (`province.html`/`rayong-catchment.html`/`branch-explorer.html`) were the ONLY nav pages with no "Skip to main content" bypass link: their `#nav` carries the same ~9 focusable stops (brand link + 6 primary nav links + "Explore ▾" menu button + theme toggle) repeated on every visit, with no way for a keyboard/switch/SR user to jump past it — the exact gap `ux-skip-link` (index) + `ux-skip-link-status` closed on all four `styles.css` nav pages (index/data/live/status). Unlike the deferred deck.gl gesture backlog (`ux-viewport-user-scalable-3dpages`, `ux-navmore-*-3dpages`), a skip-link is a STATIC HTML bypass + a tiny `preventDefault`+`focus()` handler that never touches the map canvas or any gesture — headless-verifiable and safe for the unattended loop, same class as the deck.gl ARIA fixes already shipped (`ux-themetoggle-aria-label-3dpages`, `ux-casc-select-label-association`, the toggle-state series). Following the established one-deck.gl-page-per-run cadence (`ux-deckgl-toggle-aria-pressed-*` shipped branch-explorer→province→rayong on separate runs), did **branch-explorer.html** this run: added the off-screen-until-focus `<a class="skip-link" href="#bar">` + its `preventDefault`+`focus()`+`scrollIntoView` handler as the first `<body>` children, gave the first content block after the nav — the `#bar` branch-breadcrumb strip (its "← National" back-link + branch name are the start of the page's unique content) — `tabindex="-1"` so it's the programmatically-focusable skip target (reused the existing `#bar` id rather than minting a new one), and inlined the `.skip-link` CSS in the page's own `<style>` (these pages don't link `styles.css`) with this page's font stack and z-index 3000 to overlay the z-2000 nav. `href="#bar"` is a graceful JS-off fallback (native fragment jump to the bar). Zero visual change for pointer users (skip-link is `transform`-hidden until keyboard focus); node --check clean. Remaining: `province.html` + `rayong-catchment.html` (same drop-in, tracked below as `ux-skip-link-deckgl-remaining`).
- ux-skip-link-province — ✅ FIXED 2026-08-16 (a11y, WCAG 2.4.1 Bypass Blocks, Level A) — second of the three deck.gl pages to get the "Skip to main content" bypass link (branch-explorer shipped 2026-08-16 via `ux-skip-link-branchexplorer`), keeping the one-deck.gl-page-per-run cadence. `province.html`'s `#nav` carries the same ~9 focusable stops (brand link + 6 primary nav links + "Explore ▾" menu button + theme toggle) repeated on every visit with no bypass. Mirrored the branch-explorer drop-in exactly: added the off-screen-until-focus `<a class="skip-link" href="#strip">` + its `preventDefault`+`focus()`+`scrollIntoView` handler as the first `<body>` children (before the sr-only h1), inlined the `.skip-link` CSS in the page's own `<style>` (this page doesn't link `styles.css`) using its font stack + accent token (`var(--blue,#5B7CFA)`) and z-index 3000 to overlay the z-2000 nav, and gave the first post-nav content block — the `#strip` province title/KPI strip (the analog to branch-explorer's `#bar` breadcrumb; the decorative `#sky`/`#map`/`#atmos`/`#vignette` canvas layers precede it but carry no unique content) — `tabindex="-1"` so it's the programmatically-focusable skip target. `href="#strip"` is a graceful JS-off fragment-jump fallback. Zero visual change for pointer users (skip-link is `transform`-hidden until keyboard focus); node --check clean. Remaining: `rayong-catchment.html` (tracked below as `ux-skip-link-rayong`).
- ux-skip-link-rayong — ✅ FIXED 2026-08-17 (a11y, WCAG 2.4.1 Bypass Blocks, Level A) — **final slice of the skip-link series, closing it across all 7 nav pages** (siblings: branch-explorer + province 2026-08-16). `rayong-catchment.html` was the last nav page without the "Skip to main content" bypass link — its `#nav` carries the same ~9 focusable stops (brand link + 6 primary nav links + "Explore ▾" menu button + theme toggle) repeated on every visit with no way to jump past them. Mirrored the province.html drop-in byte-for-byte: added the off-screen-until-focus `<a class="skip-link" href="#strip">` + its `preventDefault`+`focus()`+`scrollIntoView` handler as the first `<body>` children (before the `<noscript>`), inlined the `.skip-link` CSS in the page's own `<style>` (this page doesn't link `styles.css`) using its font stack + accent token (`var(--blue,#5B7CFA)`) and z-index 3000 to overlay the nav, and gave the `#strip` title strip (the first post-nav content block; the decorative canvas layers precede it but carry no unique content) `tabindex="-1"` so it's the programmatically-focusable skip target. `href="#strip"` is a graceful JS-off fragment-jump fallback. Zero visual change for pointer users (skip-link is `transform`-hidden until keyboard focus); node --check clean. All 7 nav pages now expose the bypass link consistently.
- ux-dataroom-truncated-title — ✅ FIXED 2026-08-17 (clarity/a11y, WCAG 1.3.1) — the command-center **dataroom** table (`renderHomeDataRoom`, `.dr-tbl` on `#home`) truncates its Layer-column cells with `overflow:hidden;text-overflow:ellipsis;white-space:nowrap` (`.dr-name` styles.css:788, `.dr-desc` :789), but — unlike the Source cell (`.dr-src span`, which already carries a `title`) — neither the `.dr-name` layer filename nor the `.dr-desc` provenance label had a `title`, so a truncated layer name or (often long, multi-source) provenance description was **unrecoverable** on hover / to screen readers. Same defect + fix as `ux-expocards-clamp-title`: added a `title` to both spans carrying the full text. IMPORTANT — several layer `label`s are JSON strings containing literal double quotes (e.g. `amphoe.json`, `crop_stress.json`, `occupation_income_individual.json`), so `dqEsc` (which escapes only `&<>`, not `"`) was insufficient — a raw `title="${dqEsc(label)}"` broke the attribute and injected junk attrs (caught in the headless DOM dump, not by the gate). Escaped the quotes too (`.replace(/"/g,'&quot;')`, the house pattern at app.js:1787/6067). Now the full layer name + label are recoverable on hover / to AT; the deliberate single-line ellipsis clamp is untouched. Zero visual change (title is hover/SR-only — headless `#home` render byte-identical, 182,114 B before/after, `data-errors=[]`, the 5 quote-bearing labels now render as valid complete `title` attrs). `tests/run.sh check` 0-failed; `node --check` clean.
- ux-live-theme-toggle-missing — ✅ FIXED 2026-08-17 (consistency/functional + mobile chrome) — `live.html` (the "Live board" nav route, added after the theme-persist sweep) was the **only** nav page with NO theme-toggle button in its `#nav` (`grep -c themeToggle`: live 0 vs index/data/status 2, the 3 deck.gl pages 4) — so a user on the Live board could not switch light/dark at all, and (the smoking gun) its `<meta name="theme-color">` was added expecting a `sync()` that never came: static `#F4F6FA`, never updated to match `data-theme`, so whenever the persisted/`?theme=dark` theme was active the mobile browser chrome bar showed the light colour against the dark page — the exact gap `ux-databook-theme-color` closed for data.html. Same "added after the sweep, missed X" class as live.html's earlier noscript / skip-link / gstatic-preconnect / navmore-keyboard / theme-color-meta fixes. Added the `#themeToggle` button before `</nav>` and its sync script byte-for-byte from data.html (the closest styles.css analog): the `sync()` flips the ☀/☾ glyph + `aria-pressed` AND updates the `theme-color` meta (light `#F4F6FA` / dark `#0a0e17` = `--bg`) on load and on every toggle. `tests/nav_consistency.py` still passes (it checks the pillar + Explore links, which are untouched — data.html carries the same toggle and passes). Now all 7 nav pages expose the theme toggle consistently and keep their chrome bar in sync. `tests/run.sh check` 0-failed; `node --check` clean; headless `live.html` render self-reviewed (toggle present, default light unchanged).
- ux-mtog-disclosure-aria-province — ✅ FIXED 2026-08-17 (a11y, WCAG 4.1.2 Name/Role/Value + 1.3.1 Info and Relationships) — on `province.html` the mobile bottom-sheet **panel switcher** (`#mtog`, the ONLY way to open the Branch-reach / Province / Layers overlay panels on a phone, since the panels are `display:none` until tapped) rendered three bare `<button data-s="left|right|ctl">` that show/hide the `#left`/`#right`/`#ctl` panels via a `.show` class — a classic disclosure control — but conveyed **no state and no relationship** to screen readers: no `aria-expanded` (so an AT user tapping "Branch reach" got no cue a panel opened, or whether it's now open/closed) and no `aria-controls` (no link from button to the panel it governs). This is the disclosure-pattern analog of the deck.gl toggle-state series (`ux-deckgl-toggle-aria-pressed-*`) — those buttons are `aria-pressed` single-selects, but the `#mtog` switcher genuinely expands/collapses a panel, so `aria-expanded`+`aria-controls` is the correct semantic. Static ARIA + a tiny sync (no map/canvas/gesture behaviour to device-test → headless-verifiable, safe for the unattended loop, same class as the shipped deck.gl ARIA fixes). Added `aria-controls="left|right|ctl" aria-expanded="false"` to the three buttons, a `syncMtogAria()` in the switcher IIFE that sets each button's `aria-expanded` from its panel's `.show` state (called after every switch and once on load), and resync in the `.xbtn` close-button handler (closing a panel via its own ✕ now flips the matching switcher button back to `aria-expanded="false"`, so the two close paths never leave stale state). Zero visual change (SR-only attrs — the panels/switcher styling and toggle behaviour are untouched); `node --check` clean, `tests/run.sh check` 0-failed. Following the one-deck.gl-page-per-run cadence; `rayong-catchment.html` carries the identical `#mtog` switcher (Analysis/Rivals/Layers) with the same gap — tracked below as `ux-mtog-disclosure-aria-rayong`. (NEW backlog item spotted while here: `province.html`'s `.xbtn` panel-close buttons are bare `✕` with no `aria-label`, so AT announces "multiplication sign" — `branch-explorer.html`'s `#panelClose` already carries `aria-label="Close info panel"`; noted below.)
- ux-mtog-disclosure-aria-rayong — ✅ FIXED 2026-08-18 (a11y, WCAG 4.1.2 Name/Role/Value + 1.3.1 Info and Relationships) — **final slice of the `#mtog` disclosure series, closing it** (sibling: province.html 2026-08-17). `rayong-catchment.html`'s mobile bottom-sheet panel switcher (`#mtog`, the only way to open the Analysis/Rivals/Layers overlays on a phone since the panels are `display:none` until tapped) rendered three bare `<button data-s="left|right|ctl">` that show/hide `#left`/`#right`/`#ctl` via a `.show` class — a disclosure control conveying no state and no relationship to screen readers (no `aria-expanded`, no `aria-controls`). Mirrored the province.html drop-in byte-for-byte: added `aria-controls="left|right|ctl" aria-expanded="false"` to the three buttons, a `syncMtogAria()` in the switcher IIFE that sets each button's `aria-expanded` from its panel's `.show` state (called after every switch and once on load), and `.xbtn` close-button resync (closing a panel via its ✕ flips the matching switcher button back to `aria-expanded="false"`, so the two close paths never leave stale state — this page has ✕ on `#left`/`#right` only; `#ctl` has none, so its resync is a harmless no-op). Zero visual change (SR-only attrs — the panels/switcher styling and toggle behaviour are untouched); `node --check` clean. Both deck.gl `#mtog` switchers now expose their disclosure state consistently.
- ux-deckgl-xbtn-close-aria-label — ✅ FIXED 2026-08-18 (a11y, WCAG 4.1.2 Name/Role/Value) — the panel-close buttons on `province.html` + `rayong-catchment.html` (`.xbtn`, content `✕`, 2 per page) had no `aria-label`, so a screen reader announced them as "multiplication sign" rather than a meaningful close control; `branch-explorer.html`'s `#panelClose` already modelled the fix (`aria-label="Close info panel"`). Static attribute, headless-verifiable (no map/canvas/gesture behaviour to device-test), so safe for the unattended loop. Gave each button an `aria-label` naming the panel it closes, matching the exact `#mtog` mobile-switcher vocabulary users see — province: "Close Branch reach panel" (`#left`) / "Close Province panel" (`#right`); rayong-catchment: "Close Analysis panel" (`#left`) / "Close Rivals panel" (`#right`). Zero visual change (SR-only attr — the `✕` glyph/styling/close behaviour are untouched); `node --check` clean. All deck.gl panel-close controls now expose a meaningful accessible name.
- ux-apple-touch-icon-missing — ✅ FIXED 2026-08-18 (mobile, home-screen branding) — every page shipped only `<link rel="icon" href="/favicon.svg">` and **no `apple-touch-icon`** — but iOS Safari **ignores** an SVG favicon for the home-screen ("Add to Home Screen") icon and, absent an `apple-touch-icon`, falls back to a **low-quality screenshot of the page** rather than a branded mark. For a Thai mobile exec (the owner's exact context — laptop + handset) who pins this dashboard to their home screen, that meant a blurry page-thumbnail instead of the AutoX mark. Added a 180×180 `platform/apple-touch-icon.png` — the iOS-standard size — **rasterised deterministically from the existing `favicon.svg`** (same dark `#11182A` ground, `#5B7CFA` accent border + bold "AX" monogram, drawn full-bleed so iOS applies its own corner-rounding cleanly) via headless chromium + a stdlib-only PNG crop (no new deps), and a `<link rel="apple-touch-icon" href="/apple-touch-icon.png">` right after the existing favicon link on all 8 pages (7 nav pages + the `rayong-province.html` redirect stub, matching the favicon-everywhere precedent). PNG is a static asset served by Vercel like `favicon.svg`; not gitignored. Zero change for desktop / normal browsing (the tag is consulted only by iOS home-screen pinning) — the on-page render is byte-identical (headless `index.html`/`live.html` renders unchanged, `data-errors=[]`); self-reviewed the 180×180 PNG (clean, on-brand, centred). `tests/run.sh check` 0-failed; `node --check` unaffected (head-only `<link>`, no JS/CSS/body touched). Now every page offers a branded iOS home-screen icon. (NEW backlog residual: still no `og:image` for link-unfurl share cards — needs a wider social-card asset, left as a separate item.)
- ux-web-app-manifest-missing — ✅ FIXED 2026-08-18 (mobile, home-screen branding) — natural Android sibling to `ux-apple-touch-icon-missing`: no page carried a `<link rel="manifest">` and no web app manifest existed (verified `grep`/`ls`), so on Android/Chrome "Add to Home Screen" the dashboard got a **generic bookmark** — page-title label, no standalone display, no branded splash — rather than an installable app entry (iOS pinning was already covered by the apple-touch-icon; Android reads a manifest instead). Added `platform/manifest.webmanifest` (name "AutoX · เงินไชโย — Credit Intelligence", `short_name` "AutoX CI", mandate-honest `description`, `start_url`/`scope` "/", `display:standalone`, `background_color`/`theme_color` `#0a0e17` = the dark-console `--bg` matching the branded icon's splash, `lang:en`/`dir:ltr`, and icons referencing the existing `favicon.svg` [`sizes:"any"`, image/svg+xml — a scalable icon satisfies Chrome installability, so no new binary asset needed] + `apple-touch-icon.png` [180×180]) and a `<link rel="manifest" href="/manifest.webmanifest">` right after the apple-touch-icon link on all 8 pages (7 nav pages + the `rayong-province.html` stub, matching the favicon/apple-touch-icon-everywhere precedent). Static JSON asset served by Vercel like `favicon.svg`; not gitignored. Head-only `<link>` — zero body/visual change (headless `index.html` render unchanged, `data-errors=[]`, manifest valid JSON); `tests/run.sh check` 143-passed/0-failed; `node --check` unaffected (no JS/CSS/body touched). Now both mobile platforms offer a branded home-screen install. (The `og:image` share-card residual from the apple-touch-icon item remains open.)
- ux-og-image-share-card — ✅ FIXED 2026-08-19 (share/link-unfurl, closes the `og:image` residual flagged by `ux-apple-touch-icon-missing` + `ux-web-app-manifest-missing`) — the share metadata (`ux-share-metadata` + `-other-pages`) gave every page an `og:title`/`og:description` but **no `og:image`**, and `twitter:card` was the small **`summary`** variant — so a link shared internally (board / colleagues, a documented core use of an exec tool) unfurled as a bare text card with no visual. Added `platform/og-card.png` — a 1200×630 (the standard OG / `summary_large_image` aspect) on-brand share card rasterised **deterministically via headless chromium** (dark-console `#0a0e17` ground + faint accent grid + `#5B7CFA`→`#7A4FE0` left rail, the AutoX "AX" monogram tile + `AutoX·เงินไชโย` brand + "CREDIT INTELLIGENCE" kicker, headline "Competitive & portfolio risk, across all 2,015 branches" with the count in accent, a one-line lead, and a `2,015 branches · 77 provinces · every number tagged measured or estimated` credibility line — all mandate-honest, IBM Plex fonts) — no new deps, self-reviewed the PNG (balanced, on-brand, Thai shaping correct, nothing clipped). Wired `og:image` (+ `og:image:type`/`:width`/`:height`/`:alt`), `twitter:image`, and upgraded `twitter:card` `summary → summary_large_image` on all 7 nav pages (the `rayong-province.html` redirect stub carries no share block, correctly excluded). The image URL is **absolute** (`https://competitive-intel-blue.vercel.app/og-card.png` — the documented production alias, verified 200/serving) so scrapers that don't resolve relative paths (Facebook/LinkedIn) still unfurl it; the asset is same-origin (served by Vercel like `favicon.svg`), not a CDN, so it stays self-contained. Head-only `<meta>` + one static PNG — zero body/visual change (headless renders identical), no JS/CSS touched; `tests/run.sh check` 0-failed. Closes the last share-metadata gap; every page now unfurls with a branded large image card. (NEW residual: `og:image`/`twitter:image` are per-brand, not per-route — a page-specific card (e.g. the province name on `province.html`) would be richer, but a single brand card is standard practice and lower-maintenance; left as a possible future enhancement, not a gap.)
- ux-og-url-canonical — ✅ FIXED 2026-08-19 (share/link-unfurl, completes the Open Graph required-property set) — the share block (`ux-share-metadata`/`-other-pages` + `ux-og-image-share-card`) gave every page `og:type`/`og:site_name`/`og:title`/`og:description`/`og:image`, but **no `og:url`** — one of Open Graph's four REQUIRED base properties (`og:title`, `og:type`, `og:image`, `og:url`). Without it, this exec tool's multiple Vercel aliases (the `-blue` production alias, the `-git-master` branch alias, and per-deployment URLs) each unfurl as a SEPARATE OG object, so shares/engagement never consolidate and unfurlers that key off `og:url` (Slack/LinkedIn/Facebook) canonicalize inconsistently. Added `og:url` after `og:title` on all 7 nav pages, each pointing to that page's **canonical clean production URL** on the same `-blue` alias the existing absolute `og:image` already uses (`vercel.json cleanUrls:true`, so `/`, `/data`, `/live`, `/status`, `/province`, `/rayong-catchment`, `/branch-explorer`; the per-province/-city/-branch query strings are deep links off each canonical, and a single brand canonical matches the per-brand og-card the last fix chose). The `rayong-province.html` redirect stub carries no share block, correctly excluded. Head-only `<meta>` — zero body/visual change (headless renders identical), no JS/CSS/body touched; `tests/run.sh check` 0-failed, `node --check` unaffected. Every page's OG object is now canonical + complete.
- ux-favicon-svg-type-raster-fallback — ✅ FIXED 2026-08-19 (tab-icon robustness, cross-browser) — every page (all 7 nav pages + the `rayong-province.html` stub) declared ONLY `<link rel="icon" href="/favicon.svg">` — an SVG favicon with **no `type` hint and no raster fallback**. Safari (desktop) does not render an SVG favicon declared this way, so on the owner's Mac the browser tab showed a blank/generic icon rather than the AutoX "AX" mark (Chrome/Firefox render the SVG fine). Followed the web.dev "Definitive guide to favicons" recommended pattern — raster fallback FIRST, then the SVG with its `type`: added `<link rel="icon" type="image/png" sizes="180x180" href="/apple-touch-icon.png">` before the SVG link (reusing the existing 180×180 `apple-touch-icon.png`, no new asset — keeps it deterministic) and appended `type="image/svg+xml"` to the SVG link. SVG-capable browsers prefer the scalable SVG via its type hint; Safari (which ignores the SVG) falls back to the PNG — so every browser now shows a branded tab icon. Head-only `<link>` change on all 8 pages → zero body/visual change (page render byte-identical), no JS/CSS/body touched; `tests/run.sh check` 0-failed, `node --check` unaffected.
- ux-twitter-image-alt — ✅ FIXED 2026-08-19 (a11y + share/link-unfurl, residual of the share-metadata series) — all 7 nav pages carried `og:image:alt` (read by Facebook/LinkedIn/Slack) and a `twitter:image` (the `summary_large_image` share card), but **no `twitter:image:alt`** — and X/Twitter does NOT fall back to `og:image:alt`, so on an X unfurl the share-card image had no accessible alt text for screen-reader users (WCAG 1.1.1 Non-text Content), the one platform in the share set left without it. Added `<meta name="twitter:image:alt">` immediately after each page's `<meta name="twitter:image">`, reusing that page's existing `og:image:alt` string verbatim (identical brand card, well under X's 420-char cap). Head-only `<meta>` — zero body/visual change (headless renders identical), no JS/CSS/body touched; `tests/run.sh check` 0-failed, `node --check` unaffected. Now every share platform (OG + Twitter) exposes alt text for the share-card image.
- ux-newtab-cue-rivalads-source — ✅ FIXED 2026-08-20 (a11y, WCAG G201 / 3.2.5 Change on Request) — a straggler `ux-newtab-link-cue` (2026-08-08) missed: `drawRivalAds` (the Competition `#acq` route's rival paid-media / ad-copy table, app.js L4051) renders a per-operator `source` **citation link** with `target="_blank"` that opens the rival's ad/rate-card in a NEW TAB, but — unlike every other external SPA link (the branch-risk method-box source, the two rival-promo title links, the two rival-social page links, all fixed by `ux-newtab-link-cue`) — it carried NO cue that the context would change, so a screen-reader/keyboard user heard only "source" with no warning the link opens elsewhere. A grep confirmed it was the LONE `target="_blank"` in app.js without the cue (the other 5 all already carry it). Appended the identical visually-hidden `<span class="sr-only"> (opens in a new tab)</span>` inside the anchor so AT announces "source, opens in a new tab"; `.sr-only` is the canonical clip utility already in styles.css:130 (print-hidden at :876), and the link lives only in the index.html SPA which loads styles.css, so it resolves. Zero visual change (SR-only span — sighted layout byte-identical); `node --check` clean. Closes the last new-tab-cue gap in the SPA.
- ux-callsheet-doc-lang — ✅ FIXED 2026-08-20 (a11y, WCAG 3.1.1 Language of Page) — the printable **call-sheet** document that `rayong-catchment.html`'s "leads by occupation" export builds via `window.open`+`document.write` (the sole generated document in `platform/`, ~L1994) emitted a bare `<!doctype html><html>` with **no `lang` attribute**, so a screen reader announced the whole sheet in the user's default language rules. But a call sheet is Thai-majority content — the branch name (`esc(bn)`), the occupation group headers (`esc(cur)`), and every lead name (`esc(r.name)`, from Overture Places) are Thai — so on a Thai exec's English-set browser every name to actually call was mispronounced. Applying the same principle as `ux-doc-lang-consistency` (declare the language of the majority of real content — English chrome there → `lang="en"`; Thai leads/names here → `lang="th"`), set `lang="th"` on the generated `<html>` so the substance (the names/occupations being called) is pronounced correctly; the few English chrome labels ("Call sheet", "leads to call by occupation", the provenance footer) are the accepted minority, the inverse of the tradeoff the index precedent took. One-token change inside the assembled doc string — zero visual/behaviour change (the base page render is untouched; the popup is print-only), `node --check` clean.
- ux-canonical-link — ✅ FIXED 2026-08-20 (share/URL hygiene, completes the share-metadata series) — every live page carries an absolute `<meta property="og:url">` (added by `ux-share-metadata` / `ux-og-url-canonical`) whose own comment states its purpose is to "consolidate unfurls/shares from the git-master + per-deployment aliases onto one" URL — but that only reaches OG unfurl scrapers; the browser/crawler-level `<link rel="canonical">` that actually governs bookmark/history/share dedup was present ONLY on the retired `rayong-province.html` stub, missing from all 7 real pages (index/data/live/status/province/rayong-catchment/branch-explorer). So a link opened via the `-git-master-` alias or a preview host looked like a distinct URL, and the query-parameterised 3D pages' many deep-link variants (`?p=` per-province, `?city=` per-city, `?lat=&lng=&n=` per-branch — the comments already call these "a deep link off this canonical") each looked unique. Added one `<link rel="canonical">` per page immediately after its `og:url` meta, href matching that page's existing absolute `og:url` value exactly (base URL for the 3D pages, so the deep-link variants consolidate onto it). Head-only, self-contained (no CDN/asset ref) — zero body/visual change (headless index render byte-identical, `data-errors=[]`); `tests/run.sh check` 0-failed. Closes the URL-canonicalisation half of the share-metadata series (og:url did the OG half).
- ux-color-scheme-3dpages — ✅ FIXED 2026-08-20 (a11y/consistency, native-UA-surface theming) — `ux-color-scheme` (2026-07-16) added the theme-tracking `color-scheme` pair to `styles.css` (`:root{color-scheme:dark}` + `html[data-theme="light"]{color-scheme:light}`), but its own note claimed it covered "5 styles.css pages (index/province/rayong-catchment/branch-explorer/status)" — a mislabel: `province.html`/`rayong-catchment.html`/`branch-explorer.html` are deck.gl pages that carry their OWN inline `<style>` and do NOT link `styles.css`, so they never actually received `color-scheme` (verified: 0 hits in all three; it lives only in styles.css:148/655). Without it, native UA surfaces on these pages render in the OS default scheme regardless of the app theme — most visibly the cascading Region/Province/Branch **`<select>` dropdowns** (per `ux-casc-select-label-association`) and the panel scrollbars: on a dark-console deck.gl page a dark-OS-preferring user was fine, but a light-OS user toggled to dark (or `?theme=dark`) got a LIGHT native select-popup/scrollbar against the dark chrome (and vice-versa). Same "deck.gl page missed a styles.css feature" class as the shipped `ux-reducedmotion-3dpages`/`ux-themetoggle-aria-label-3dpages` fixes; CSS-only, static, no map/canvas/gesture behaviour → headless-verifiable, safe for the unattended loop. Added `color-scheme:dark` inside each page's canonical `:root{}` + a `html[data-theme="light"]{color-scheme:light}` rule, byte-mirroring styles.css. Keys off the same `data-theme` attr the pre-paint script sets before paint, so it applies from the first frame and auto-follows the toggle. Zero visual change to the deck.gl scene/panels (native UA surfaces only); `tests/run.sh check` 0-failed, `node --check` clean. All 7 nav pages now declare `color-scheme` consistently.
- ux-jumpnav-chip-aria-selected-invalid — ✅ FIXED 2026-08-21 (a11y, WCAG 4.1.2 Name/Role/Value / axe `aria-allowed-attr`) — the Macro/Competition/Brand topic switchers (`#ovjump`/`#compjump`/`#brandjump`, static `<a class="chip" data-jump>` links) and their per-topic sub-switchers (the JS-built `.ovsubnav` `<button class="chip" data-ovsub>`) conveyed their single-select active state with **`aria-selected`** (`showOvPanel` app.js:1682, `ovSubApply` :1763, and the `ovSubInit` template :1787-1788) — but `aria-selected` is **not an allowed attribute on the `link` or `button` role** (only tab/option/row/gridcell/treeitem/columnheader/rowheader), so axe flags `aria-allowed-attr`, AT silently ignores it, and the active topic was NOT announced — directly contradicting the code's own comment (app.js:1647: "it carries aria-selected so the active topic is announced"). The app-wide `.chip` toggle convention (`ux-chip-aria-pressed`, 2026-07-23) never reached these jump-nav switchers; `aria-pressed` is itself only valid on `button` (so it would fix the sub-switcher buttons but STILL be invalid on the `<a>` topic links). Fixed with **`aria-current="true"`** — a GLOBAL attribute valid on both links and buttons, semantically exact ("current item in the set"), and already the app's idiom for active-in-a-set controls (the breadcrumb + map-lens buttons at app.js:10534-10671, and the main nav's `aria-current="page"`). Swapped all four JS sites (active → `aria-current="true"`, inactive → attribute removed) and the one CSS selector (`.ovsubnav .chip[aria-selected="true"]` → `[aria-current="true"]`, styles.css:1834) together so the active sub-chip's border/color highlight is byte-identical; the static topic chips' highlight is `.chip.on`-driven (unaffected). Verified in the settled headless DOM: `#ovjump` active chip `class="chip on" ... aria-current="true"`, `#compjump` active `data-jump="sec-pulse" aria-current="true"`, `.ovsubnav` active `class="chip on" data-ovsub="" aria-current="true"`; zero `aria-selected` remain; both `#overview` + `#acq` renders `data-errors=[]`, PNG self-reviewed (layout identical). `tests/run.sh check` 146-passed/0-failed; `node --check` clean.
- ux-mtog-disclosure-aria-branchexplorer — ✅ FIXED 2026-08-21 (a11y, WCAG 4.1.2 Name/Role/Value + 1.3.1 Info and Relationships) — **third and final `#mtog` switcher, closing the disclosure series across all deck.gl pages** (siblings: province.html 2026-08-17, rayong-catchment.html 2026-08-18). `branch-explorer.html`'s mobile bottom-sheet panel switcher (`#mtog`, the only way to open the Info/Layers overlays on a phone since `#panel`/`.ctl` are hidden until tapped) rendered two bare `<button data-s="panel|ctl">` that show/hide their target via a `.show` class — a disclosure control conveying no state and no relationship to screen readers. Its structure differs from the province/rayong sibling (panel/ctl, not left/right/ctl; the `.ctl` frame had no id and the close is a single `#panelClose`, not `.xbtn`), so mirrored the pattern to fit: gave `.ctl` `id="ctl"` (so `getElementById(dataset.s)` resolves both targets; the existing `querySelector('.ctl')` still works), added `aria-controls="panel|ctl" aria-expanded="false"` to the two buttons, added a `syncMtogAria()` in the switcher IIFE that sets each button's `aria-expanded` from its target's `.show` state (called after every toggle and once on load), and resync in the `#panelClose` handler (closing the panel via ✕ flips the Info button back to `aria-expanded="false"`, so the two close paths never leave stale state). Zero visual change (SR-only attrs — the panels/switcher styling and toggle behaviour are untouched); `node --check` clean. All three deck.gl `#mtog` switchers now expose their disclosure state consistently.
- ux-branchexplorer-status-live-region — ✅ FIXED 2026-08-21 (a11y, WCAG 4.1.3 Status Messages, Level AA) — closes backlog item (2) below: `branch-explorer.html`'s `#status` loading/error toast (the `status()` helper at ~L607 swaps its `textContent` through "Loading buildings…" → "No OSM buildings mapped here (rural / factory zone)" / "Building footprints unavailable — showing branch + POI layer" / "No branch coordinates in the link.") was a plain `<div id="status">` with **no `role="status"`/`aria-live`**, so every one of those state changes was silent to screen readers — a blind user got no cue whether the scene was loading, had loaded, or had degraded to the POI-only fallback. The SPA got its live regions in `ux-search-results-status-message` + `ux-sim-result-status-message`; this deck.gl toast was missed. Mirrored the app's established live-region idiom byte-for-byte — added `role="status" aria-live="polite" aria-atomic="true"` to the `#status` div (polite is correct: the note-worthy states are calm degradation notices, styled amber/`.info`, "never red/raw", and even the error variants are non-blocking; `aria-atomic="true"` re-reads the whole short message on each swap). Static single-element ARIA fix with no map/canvas/gesture behaviour to device-test → headless-verifiable, safe for the unattended loop (same class as the shipped deck.gl ARIA fixes). Zero visual change (SR-only attrs — the toast styling/positioning/behaviour untouched); `node --check` clean. Remaining backlog items (3)+(4) below.
  - NEW backlog (headless-verifiable, NOT the deferred deck.gl gesture class — each its own future surgical slice): (4) the three deck.gl pages have no `<main>` landmark (their skip-links target `#bar`/`#strip tabindex=-1` rather than a real main — borderline structural, needs its own consideration). **PROGRESS: `branch-explorer.html` shipped 2026-08-22 (`ux-deckgl-main-landmark-branchexplorer` below); `province.html` shipped 2026-08-22 (`ux-deckgl-main-landmark-province` below); remaining `rayong-catchment.html`.**
- ux-deckgl-main-landmark-branchexplorer — ✅ FIXED 2026-08-22 (a11y, WCAG 1.3.1 Info and Relationships / axe `landmark-one-main`) — first slice of backlog item (4): `branch-explorer.html` had **no `<main>` landmark** — its skip-link targeted the `#bar` breadcrumb strip (`tabindex="-1"`), the *start* of the content rather than a real main region, so a screen-reader user navigating by landmark found `<nav>` but no main, and axe flags `landmark-one-main` / `region`. The 4 `styles.css` nav pages (index/data/live/status) all wrap their content in `<main id="main-content" tabindex="-1">`; the deck.gl pages were the gap. Wrapped the scene content — the `#bar` breadcrumb through the `#status` toast (all `position:fixed`, so a bare `display:block` `<main>` with no positioning/transform/filter establishes no new containing block and the fixed children stay anchored to the viewport exactly as before) — in `<main id="main-content" tabindex="-1">`, moved the skip target from `#bar` (dropped its now-redundant `tabindex="-1"`) to the `<main>`, and repointed the skip-link `href="#bar"→"#main-content"` + its handler `getElementById('bar')→('main-content')`. The `<nav>` stays outside main (separate landmark, correct); the trailing `<script>` stays outside main. Verified no `body >`/sibling CSS combinators or JS refs (other than the skip-link) depend on `#bar` being a body child. Static structural markup with no map/canvas/gesture behaviour to device-test → headless-verifiable, safe for the unattended loop (same class as the shipped deck.gl skip-link + ARIA fixes). Zero visual change (the `<main>` box is empty-height since every child is `position:fixed` — headless render identical); `node --check` clean. Remaining: `province.html` + `rayong-catchment.html` (same drop-in, tracked above).
- ux-deckgl-main-landmark-province — ✅ FIXED 2026-08-22 (a11y, WCAG 1.3.1 Info and Relationships / axe `landmark-one-main`) — second slice of backlog item (4), keeping the one-deck.gl-page-per-run cadence (branch-explorer shipped 2026-08-22 via `ux-deckgl-main-landmark-branchexplorer`): `province.html` had **no `<main>` landmark** — its skip-link targeted the `#strip` province-title strip (`tabindex="-1"`), the *start* of the content rather than a real main region, so a screen-reader user navigating by landmark found `<nav>` but no main, and axe flags `landmark-one-main` / `region`. Mirrored the branch-explorer drop-in exactly: wrapped the scene content — the decorative `#sky`/`#map`/`#atmos`/`#vignette` canvas layers, the `#strip` title strip, `#ctl` control cluster, the `#left`/`#right` side panels, `#mtog` mobile switcher, and `#legendwrap` (all `position:fixed`, so a bare `display:block` `<main>` with no positioning/transform/filter establishes no new containing block and the fixed children stay anchored to the viewport exactly as before) — in `<main id="main-content" tabindex="-1">`, moved the skip target from `#strip` (dropped its now-redundant `tabindex="-1"`) to the `<main>`, and repointed the skip-link `href="#strip"→"#main-content"` + its handler `getElementById('strip')→('main-content')`. The `<nav>` + its scripts stay outside main (separate landmark, correct); the trailing `window.initProvince` `<script>` stays outside main. Verified no `body >`/sibling CSS combinators or JS refs (other than the skip-link) depend on `#strip`/`#sky` being a body child (`#stripTtl` is still reached by id). Static structural markup with no map/canvas/gesture behaviour to device-test → headless-verifiable, safe for the unattended loop. Zero visual change (the `<main>` box is empty-height since every child is `position:fixed` — headless render identical); `node --check` clean. Remaining: `rayong-catchment.html` (same drop-in, tracked above).
- ux-deckgl-main-landmark-rayong — ✅ FIXED 2026-08-22 (a11y, WCAG 1.3.1 Info and Relationships / axe `landmark-one-main`) — **final slice of backlog item (4), closing the `<main>` landmark series across all three deck.gl pages** (siblings: branch-explorer + province, both shipped 2026-08-22): `rayong-catchment.html` had **no `<main>` landmark** — its skip-link targeted the `#strip` city-title strip (`tabindex="-1"`), the *start* of the content rather than a real main region, so a screen-reader user navigating by landmark found `<nav>` but no main, and axe flags `landmark-one-main` / `region`. Mirrored the branch-explorer/province drop-in exactly: wrapped the scene content — `#sky`/`#map`/`#focalLabel`/`#atmos`/`#vignette` canvas layers, the `#strip` title strip, `#scenesearch` box, `#ctl` control cluster, the `#left`/`#right` side panels, `#mtog` mobile switcher, and `#legendwrap` (all `position:fixed` except `#focalLabel` which is `position:absolute` with no positioned ancestor → resolves to the viewport either way; a bare `display:block`/`static` `<main>` with no positioning/transform/filter establishes no containing block for fixed OR absolute descendants, so every child stays anchored to the viewport exactly as before) — in `<main id="main-content" tabindex="-1">`, dropped `#strip`'s now-redundant `tabindex="-1"`, and repointed the skip-link `href="#strip"→"#main-content"` + its handler `getElementById('strip')→('main-content')`. The `<nav>` + its scripts, the legend-collapse `<script>` and the trailing `window.initCatch` loader all stay outside main. Verified `#strip`/`#cityTitle` are still reached by id in the loader (`insertAdjacentHTML`, `_strip=getElementById('strip')`) — only the skip-link changed target, and no `body >`/sibling CSS combinator depends on these being body children. Static structural markup, no gesture behaviour to device-test → headless-verifiable, safe for the unattended loop. Zero visual change (the `<main>` box is empty-height since every child is out of flow — headless render identical); `tests/run.sh check` 146-passed/0-failed; `node --check` clean. Backlog item (4) now closed across all 7 nav pages.
- ux-databook-search-results-status — ✅ FIXED 2026-08-21 (a11y, WCAG 4.1.3 Status Messages, Level AA) — closes backlog item (3) above: `data.html`'s "All branches" filter box (`#brsearch`) replaces its result table's `innerHTML` on every keystroke (via `fillBranchBody`), but the change was **entirely silent to screen readers** — data.html had no `aria-live`/`role="status"` region, so the sibling SPA fix `ux-search-results-status-message` (which gave index.html's three search boxes a shared polite live region) never reached the data-book's own search on the primary "② Acquisition" nav route. An AT user typing a branch/province filter got no cue how many rows now match (or that a mistyped query dropped to zero). Mirrored the SPA precedent byte-for-idiom: added ONE visually-hidden polite live region (`<div id="brsearchstatus" class="sr-only" role="status" aria-live="polite" aria-atomic="true">`) right after the `#brsearch` input, plus a guarded `setBrStatus(n)` helper called from `fillBranchBody()` with the post-filter row count — query-aware (silent on an empty query so tab-open / a cleared box stay quiet), region-qualified when a region chip is active (" in <region>"), singular/plural ("1 branch match" / "N branches match" / "0 branches match"), and guarded (skips identical text so a re-sort / region re-filter that lands the same count doesn't re-announce). `.sr-only` is the canonical clip utility already in styles.css:130 (print-hidden at :876), and data.html links styles.css, so it resolves. Zero visual change (SR-only region — sighted layout byte-identical); `node --check` clean (the data.html inline JS string is still valid). Now the data-book search announces its result count consistently with the SPA. Remaining: backlog item (4) — the three deck.gl pages' missing `<main>` landmark.
- ux-viewport-fit-cover-branchexplorer — ✅ FIXED 2026-08-22 (mobile, notched-device parity/consistency) — `branch-explorer.html` was the **only** one of the 7 nav pages whose `<meta name="viewport">` lacked `viewport-fit=cover` (all 6 others carry it — the 2 sibling deck.gl pages `province.html`/`rayong-catchment.html` ship exactly `…, maximum-scale=1, viewport-fit=cover`). Without `cover`, on a notched iPhone (the owner's exact context — Thai exec on a handset) Safari letterboxes the page inside the safe-area insets with default-UA bars, so branch-explorer's immersive full-bleed deck.gl 3D scene did NOT reach the screen edges the way its two sibling scenes do — a visible inconsistency on the one page whose whole point is an edge-to-edge 3D map. Root cause is the "added-after/missed-one" class (like live.html's earlier theme-toggle / skip-link / gstatic-preconnect catch-ups): branch-explorer uses the same `position:fixed` nav/`#bar`/`#panel`/`.ctl` architecture (12 fixed elements) as the two siblings that already ship `cover`, so adding it brings it to exact parity — it ADOPTS behaviour already proven in production on those pages, not a novel unverified one. Surgical single-descriptor addition: `…, user-scalable=no` → `…, user-scalable=no, viewport-fit=cover`. Deliberately left `maximum-scale=1`/`user-scalable=no` untouched — that is the separate deferred a11y item `ux-viewport-user-scalable-3dpages` (removing the zoom-lock needs `touch-action` + device gesture testing, NOT unattended); `viewport-fit` is an independent viewport descriptor (safe-area coverage, not zoom) so the two don't interact. Head-only `<meta>` — zero body/JS/CSS touched; headless render self-reviewed clean (`data-errors=[]`, scene + panels intact, layout unchanged at normal widths — the notch/safe-area effect only manifests on a real notched device but matches the already-shipped sibling behaviour). `node --check` clean. All 7 nav pages now declare `viewport-fit=cover` consistently.
- ux-search-enterkeyhint — ✅ FIXED 2026-08-23 (mobile, virtual-keyboard affordance) — the five true filter search boxes (`#search`/`#provsearch`/`#mktsearch`/`#pulseadsq` in index.html + `#brsearch` in data.html) already carried the full mobile-input hardening (`type="search"` + `autocomplete="off" autocorrect="off" autocapitalize="none" spellcheck="false"`) but **no `enterkeyhint`**, so the on-screen keyboard's action key wasn't guaranteed to read "Search" across browsers (Android Chrome in particular defaults it inconsistently for a form-less `type="search"`). For the owner's exact context (Thai exec on a handset) these boxes are the primary interaction, so added `enterkeyhint="search"` right after `type="search"` on all five — the natural completing slice of the search-input hardening series. Scoped deliberately to the pure filter boxes; the `#ssInput` scene search in rayong-catchment.html is a `role="combobox"` select-a-result widget (Enter picks the highlighted match, not "search"), correctly left out. Attribute-only, zero visual/behavioural change on desktop and inside the app; `tests/run.sh check` 0-failed, `node --check` clean.
- ux-scene-search-activedescendant — ✅ FIXED 2026-08-23 (a11y, WCAG 4.1.2 Name/Role/Value — ARIA combobox pattern) — `rayong-catchment.html`'s scene search (`#ssInput`, `role="combobox"` + `#ssResults` `role="listbox"`) implements full ArrowDown/ArrowUp keyboard navigation of its result rows with a **visual** `.active` highlight (`paintActive()` toggles the class, mouse hover syncs it too), and the rows already carry `role="option"` — but the option-selection was conveyed to screen readers by **nothing**: the `.ss-row` options had no `id`, the combobox input had no `aria-activedescendant`, and the active row got no `aria-selected`, so a keyboard/SR user arrowing through matches saw the highlight move on screen while AT announced no change of active option (the exact gap `aria-activedescendant` exists to close — it points the combobox at its active descendant option without moving DOM focus). Completed the combobox contract, mirroring the existing visual state 1:1: gave each row a stable `id="ss-opt-<i>"` + `aria-selected` in `rowHTML`, added `aria-autocomplete="list"` to the input (the correct combobox suggestion-source hint), and a `syncActiveDesc()` helper that sets/removes `aria-activedescendant` from the active row's id — called from `paintActive()` (keyboard arrow + mouse hover), the fresh-render path in `paint()` (active=-1 → cleared), and cleared on the no-value / no-match / `hide()` paths so it never goes stale. `paintActive()` now also toggles each option's `aria-selected` alongside the `.active` class (no new desync). Static ARIA that mirrors already-present keyboard behaviour — no map/canvas/gesture behaviour to device-test → headless-verifiable, safe for the unattended loop (same class as the shipped deck.gl ARIA fixes). Zero visual change (the `.active` styling/keyboard/mouse behaviour is untouched — the SR-only ARIA state now tracks it); verified in the settled headless DOM (arrow-down → input `aria-activedescendant="ss-opt-0"`, that row `aria-selected="true"`, the rest `false`; input clear → attribute removed). `tests/run.sh check` 0-failed; `node --check` clean.
- ux-catchment-notice-foot-exec-facing — ✅ FIXED 2026-08-23 (clarity, exec-facing microcopy) — `rayong-catchment.html`'s CALM "buildings haven't been pulled" notice (`notPulled()`, the graceful end-state shown on the primary 3D entry path for ~74 of 77 provinces that ship no committed catchment file) rendered a **developer-facing** footnote: not-pulled showed the raw CLI `pull command: pull_overture_buildings.py --province <slug>` and the CDN-failure branch showed `reload to retry · manifest: data/catchments_r2.json` — an internal script name, the raw province slug and a repo file path surfaced to the owner/board reader, who can't and shouldn't run any of it (same owner-not-developer class as `ux-boot-error-exec-facing`, 2026-08-14, which reworded the front-door boot error). Reworded both `foot` strings to plain-language status — not-pulled: "3D building scenes are added province by province — this one is on the way."; failure: "Reload the page to try again." — no script/slug/path leaks; the district-view button (unchanged) is the real next step. Zero structural/style change (same 11px Mono dim footnote); headless render of an absent-catchment city confirms the calm notice + district-view button intact. `tests/run.sh check` 0-failed.
- ux-ios-standalone-webapp-meta — ✅ FIXED 2026-08-23 (mobile, home-screen install — closes the iOS half of the manifest/apple-touch-icon PWA series) — `ux-web-app-manifest-missing` (2026-08-18) added `manifest.webmanifest` with `"display":"standalone"`, giving Android/Chrome "Add to Home Screen" a branded, chrome-free standalone launch — but **iOS Safari ignores the web manifest's `display` mode entirely**, so a Thai exec pinning the dashboard to their iPhone home screen (the exact owner context that justified the apple-touch-icon + manifest work) still got a mini-**browser** view with Safari chrome, not the standalone app the Android side already delivered. The classic "did the Android half, missed the iOS legacy half" gap (same class as `ux-apple-touch-icon-missing` vs the manifest, and the theme-color / skip-link catch-ups). Added the iOS standalone meta set right after the existing `<link rel="manifest">` on all 8 pages (7 nav + the `rayong-province.html` redirect stub, matching the manifest/apple-touch-icon-everywhere precedent): `apple-mobile-web-app-capable=yes` (the tag iOS actually reads to launch standalone), `mobile-web-app-capable=yes` (the standardized modern name some browsers prefer), `apple-mobile-web-app-status-bar-style=default` (a solid — not `black-translucent` — status bar so the notch never overlaps the fixed top nav), and `apple-mobile-web-app-title="AutoX CI"` (iOS home-screen label = the manifest `short_name`, not the long `<title>`). Head-only `<meta>` — zero body/visual change (the tags are consulted only by an iOS home-screen launch; on-page render byte-identical), no JS/CSS/body touched; `tests/run.sh check` 0-failed. Now both mobile platforms launch the pinned dashboard as a branded standalone app.
- ux-tblwrap-inline-region-a11y — ✅ FIXED 2026-08-24 (a11y, WCAG 2.1.1 Keyboard / scrollable-region-focusable) — the SPA's `wrapTables()` gives every wide table's scroll container `role="region" tabindex="0" aria-label` so a keyboard/switch user can pan it and a screen reader names it — but it **early-returned on any table already inside a manual `<div class="tblwrap">`**, so tables pre-wrapped inline in a template string never got the treatment. A true-mobile (390px, isMobile/DSF2) audit found the gap on the two most important routes: `#home` (the exec front door — the double-pressure watchlist) and `#overview`/Macro (the commodity/vehicle/crop boards, incl. `gd-wrap`) had scroll containers with **no role/tabindex/aria-label while actually scrolling horizontally**, so a keyboard user could not reach the clipped right-hand columns and an SR user got an unnamed region — inconsistent with `#acq`/`#exposure`/`#trend`/`#provinces`/`#market` (all 0-missing, auto-wrapped). Fixed at the single source: `wrapTables()` now **reuses** an existing `.tblwrap` instead of skipping it and idempotently fills in only the missing `role`/`tabindex`/`aria-label` (a hand-set value is preserved; unique-name de-dup unchanged). Two async renderers that mount their inline-wrapped table *after* the boot-time `wrapTables()` and never re-called it — `renderHomeDoublePressure()` (fires on the PROVPRESS fetch) and `renderFarmCrops()` (fires on the crop fetch, unlike its sibling `renderFarmHousehold()` which already called it) — now call `wrapTables()` after mounting, matching the app's established post-boot convention. Verified across all 8 table-bearing routes: **0 tblwraps missing a11y** (was 7 across #home+#overview), **0 nested `.tblwrap` (no scroller-in-scroller), 0 duplicate region labels, no page-level horizontal overflow**; `#home`+`#overview` headless renders byte-identical (`#home` PNG identical size, `data-errors=[]`) — zero visual change (attribute-only on wrapper divs). `tests/run.sh check` 146-passed/0-failed; `node --check` clean.
- ux-branches-default-sort-risk — ✅ FIXED 2026-08-24 (clarity/mandate-alignment, "lead with the answer") — the `#branches` route (index.html, `renderBranchSort`/`renderBranches`) defaulted `branchSort='dwork'` (app.js:1228), so a director opening "Branches · all 2,015" got the list ordered by **Factory workers (DIW)** — a borrower-BASE metric — even though the table's FIRST data column and the FIRST sort chip are both "Portfolio risk ▲ est", and objective #1 is portfolio risk. The result was a self-inconsistent front-of-list: the risk column led the table but the rows were ranked by an unrelated metric, and the highlighted (active) chip was the 2nd one, not the 1st — against the project's "lead with the answer" + portfolio-risk-first mandate. Changed the default to `branchSort='risk'` (the one-token literal at app.js:1228), so the branches list now opens sorted by ESTIMATED portfolio-risk proxy **worst-first** (the sort is descending: `branchSortVal(b)-branchSortVal(a)`, higher risk = riskier = top), the active chip matches the leading column, and `renderBranchSort`'s `on`/`aria-pressed` highlight lands on "Portfolio risk ▲ est". `branchSort` drives ONLY these sort chips + this table's sort (grep-verified — not the map lens `curLens`), and is never read from localStorage/URL, so the literal is the clean single point of change; `riskVal(d)` already renders the risk column so no new compute. Zero style/layout change (same table, same columns — only the initial row order + which chip is pre-highlighted); a user can still switch to any other sort. Verified headless (`index.html#branches`, 390px): first chip `aria-pressed="true"`/`.on`, risk column descending from the top, `data-errors=[]`. `tests/run.sh check` 146-passed/0-failed; `node --check` clean.
- ux-databook-csv-utf8-bom — ✅ FIXED 2026-08-24 (clarity, i18n/data-integrity) — `data.html`'s own "⬇ Download CSV" export (`#brcsv` → `autox_branches.csv`, the All-branches data-book table, ~L940) built its Blob as `new Blob([lines.join('\n')],{type:'text/csv;charset=utf-8;'})` with **no UTF-8 BOM** — the exact defect `ux-csv-utf8-bom` (2026-07-31) closed for app.js's four client-side exports, but data.html has its OWN export that the app.js sweep never reached. The exported rows carry Thai text (the `branch`/`province`/`region`/`district` columns — `d.n`/`d.v`/`d.r`/`d.d`, measured Thai branch names incl. `สาขา…`), and Microsoft Excel on Windows (the exec's tool in Thailand — Kaustav/board download this branch list) does NOT auto-detect a BOM-less UTF-8 CSV, so it decodes with the legacy locale codepage and all Thai columns open as mojibake. Prepended the UTF-8 BOM as a separate leading Blob array element (`new Blob(['﻿',lines.join('\n')],{…})`, byte-identical to the app.js precedent's literal-BOM pattern at app.js:4158), so Excel now auto-detects UTF-8 and renders Thai correctly; Google Sheets / LibreOffice / `csv` parsers already strip/ignore a leading BOM so nothing else regresses. Verified: the produced Blob now begins with bytes `EF BB BF` (Node `Blob.arrayBuffer()` check) and the data.html body render is unchanged (behavioural export change only). `tests/run.sh check` 0-failed; `node --check` clean. Closes the last BOM-less CSV export across the app (all five client-side exporters — four in app.js, this one in data.html — now emit the BOM).
- ux-viewport-user-scalable-branchexplorer — ✅ FIXED 2026-08-24 (a11y, WCAG 1.4.4 Resize Text, Level AA — safe parity slice of the open `ux-viewport-user-scalable-3dpages` item) — of the three deck.gl pages, `branch-explorer.html` was the ONLY one carrying `user-scalable=no` in its `<meta name="viewport">` (alongside `maximum-scale=1`); its two sibling deck.gl pages (`province.html` + `rayong-catchment.html`) run in production with `maximum-scale=1` **alone**, no `user-scalable=no`. `user-scalable=no` is the specific token WCAG SC 1.4.4 calls out as the mobile pinch-zoom antipattern. Removed only that redundant token from branch-explorer's viewport meta, bringing it to exact parity with the two proven-working sibling pages. This does NOT touch the shared `maximum-scale=1` lock — the deferred gesture-class question tracked by `ux-viewport-user-scalable-3dpages` (whose correct full fix removes the lock AND adds `touch-action:none`, then needs real-device gesture testing outside the headless harness) — so the map's pinch-to-zoom gesture behaviour is unchanged relative to the two siblings that already work with `maximum-scale=1` only. Headless-verifiable (viewport meta grep), zero visual change; `tests/run.sh check` 0-failed; `node --check` clean. The parent `ux-viewport-user-scalable-3dpages` STAYS OPEN for the device-tested full removal on all three pages.
- ux-csv-export-filename-vintage — ✅ FIXED 2026-08-25 (clarity, exec workflow / consistency) — four of the six client-side CSV exports used STATIC filenames — `acqCSV`→`autox_catchment_coverage.csv`, the district-coverage export→`autox_district_coverage.csv`, `#mktcsv`→`autox_market_assessment.csv`, and `ccBriefCSV`→`autox_command_center_brief.csv` — so a director re-downloading the same brief across data refreshes got silent Downloads-folder overwrites (or browser `(1)`/`(2)` collisions) with no way to tell which vintage each file held. The other two exports (`autox_rate_board_<asof_card>.csv`, `autox_rival_ads_<pulled>.csv`) already stamp the filename with their DATA vintage — the established convention these four broke. Added a shared `vintageTag()` helper (app.js, next to `dqEsc`) that slugs `META.updated` (the global data vintage, e.g. `"2026M07 prices · drought 2026-06-21"`) to a filename-safe `[0-9A-Za-z-]` token (`2026M07-prices-drought-2026-06-21`), falling back to `latest` when absent, and appended `_'+vintageTag()+'` to all four filenames — so each export self-identifies its data vintage and no longer overwrites the last, matching the rate-board/rival-ads convention and the project's honesty-of-provenance ethos. Deterministic (no wall-clock — data vintage, like the two existing dated exports), function-declaration is hoisted so it resolves at every call site. Zero visual/body change (download-attribute only — headless `#home` render byte-clean, `data-errors=[]`, button intact); slug verified filename-safe + collision-free across vintages. `tests/run.sh check` 146-passed/0-failed; `node --check` clean.
- ux-status-loop-row-overlap — ✅ FIXED 2026-08-25 (mobile+tablet, layout/OVERLAP+PAGEX) — `status.html`'s "Autonomous loops" panel (`.st-loop`, styles at status.html:112–116) laid each loop out as a single 3-child flex row — `.nm` name · `.ow` owns (`flex:1;min-width:0`) · `.cd` cadence (`white-space:nowrap`) — but the cadence field carries long descriptive notes ("account-scheduled routine (no committed workflow)", "paused — on demand via workflow_dispatch") that can't share one line with the name+owns inside the `.st-two` grid's narrow ~320–460px column. So on `#status` at BOTH the default two-column desktop width (~1000px, each column ~460px) and mobile single-column (500px), `.ow` collapsed to ~0 (the member list "data · feature · deploy" stacked one word per line) and the nowrap `.cd` overflowed the panel's right edge and visually OVERLAPPED the bold name ("data" ran into "account-scheduled…" → "dataccount-scheduled…") — a real, visible text collision; only at ≥~1500px did it lay out acceptably. Root cause: a non-wrapping flex row forcing a nowrap note + a growable-but-crushable owns cell + a fixed name into one line that only fits when the column is wide. Fixed CSS-only in the `.st-loop` block: added `flex-wrap:wrap` (+ a 3px row-gap so wrapped lines breathe), changed `.ow` `flex:1`→`flex:0 1 auto` (stop it consuming/collapsing the line so the cadence can sit beside it when there's room), and `.cd` `white-space:nowrap`→`normal` (let the note drop to its own line and wrap in place instead of overflowing when cramped). Adaptive: short loops keep the cadence on line 1 (verified at 1600px — ux-improvement/committee-scout/committee-planner stay one line), longer ones drop it to a muted accent line below; the member list flows horizontally again everywhere. Verified across 500/1000/1600px headless renders — no overlap, no member-word stacking, no right-edge overflow, and the rest of the page (hero, pillar cards, recent activity, footer) is unchanged. `node --check` clean; `tests/run.sh check` 0-failed. (Only `platform/status.html`'s inline `<style>` touched — 3 property tweaks + one explanatory comment; no JS/body change.)
- ux-databook-branches-aria-sort — ✅ FIXED 2026-08-25 (a11y, WCAG 4.1.2 Name/Role/Value + 1.3.1 Info and Relationships) — `data.html` has TWO sortable tables, and only ONE announced its sort state. The national province table (`#ptbl`) already sets `aria-sort="none"`→`ascending`/`descending` on its `<th>`s (line 592 + the applySort sync at 621, the correct WAI-ARIA sortable-columnheader pattern), but the primary "All branches" table (`#brtbl`, the ② Acquisition data-book branch list — every column sortable by click or Enter/Space) rendered its headers as `<th data-c tabindex="0" role="button">` with **no `aria-sort` and no `scope="col"`**: `fillBranchBody()` already toggled the VISUAL sort arrow (`.sort-asc`/`.sort-desc` ::after ▲/▼, line 925-926) so a sighted user saw which column was sorted, but a screen-reader user got nothing — and the `role="button"` override actively suppressed the native `columnheader` role that `aria-sort` requires (aria-sort is only valid on columnheader/rowheader/gridcell/row), so the attribute could never have worked while it was there. Converged `#brtbl` onto `#ptbl`'s house pattern: replaced `role="button"` with `scope="col" aria-sort="none"` in the header template (keeping `tabindex="0"` + the existing Enter/Space keydown handler, so keyboard sorting is unchanged), and added the `aria-sort` state sync alongside the existing className toggle in `fillBranchBody()` (active column → `ascending`/`descending`, the rest → `none`), byte-mirroring `#ptbl`'s line 621. Now both data.html sortable tables announce their sort column + direction to AT, consistently. Zero visual change (aria-sort/scope are non-presentational; the `.sort-asc`/`.sort-desc` arrow logic is untouched — the `role="button"` removal has no paint footprint since the ▲/▼ indicator is class-based); `node --check` clean, `tests/run.sh check` 0-failed. (NEW backlog item spotted while here: `#ptbl`'s sortable headers are `tabindex="0"` (focusable) but wire only a `click` listener with NO `keydown` handler (data.html:606), so a keyboard user can focus a province-table header but Enter/Space does not sort it — WCAG 2.1.1 Keyboard. `#brtbl` already has the Enter/Space handler; porting it to `#ptbl`'s `doSort` is the natural follow-up slice.)
- ux-reduced-motion-data-status — ✅ FIXED 2026-08-25 (a11y, WCAG 2.3.3 Animation from Interactions) — `data.html` and `status.html` were the only two pages carrying CSS motion (data-book `.db-card` hover `transition:border-color/transform .12s`; status.html's autonomy progress bars `transition:width .5s ease` — a half-second animated width change on load) with NO `prefers-reduced-motion:reduce` guard, while every other page has one (styles.css L614 covers index/live via `*{transition:none!important}`; the three deck.gl pages each carry the same rule inline). Added the identical house one-liner `@media(prefers-reduced-motion:reduce){*{transition:none!important}}` to each page's inline `<style>` (before `</style>`), so users who set the OS "reduce motion" preference get no animated hover/progress motion — matching the deck.gl-page pattern exactly. Zero visual change for the default (motion-allowed) case — headless renders identical; `node --check` clean, `tests/run.sh check` 0-failed. Closes the last two pages missing the reduced-motion guard; the platform now honours the preference everywhere.
- ux-databook-row-nav-keyboard — ✅ FIXED 2026-08-26 (a11y, WCAG 2.1.1 Keyboard, Level A) — `data.html`'s three navigating tables — the national province table (`#ptbl`, rows → `?p=<slug>`), the per-province branch list (`fillProvinceBranches`, rows → `?b=<i>`), and the "All branches" table (`#brtbl`, rows → `?b=<i>`) — navigate to a detail page on **row click** (delegated `tbody` listeners) but the rows were bare `<tr>` (or `<tr style="cursor:pointer">`) with **no `role`, `tabindex`, or keyboard handler**, so a keyboard/switch user could neither focus nor activate them — the province/branch drill-down was mouse-only (the same gap the SPA closed via `ux-impact-prow-keyboard`). Aligned the three row templates to the house clickable-row convention (`role="link" tabindex="0"`, reusing the existing `tr[role="link"]:focus-visible` inset outline in styles.css:570 — data.html links styles.css) and replaced the three click-only `tbody` listeners with a shared `wireRowNav(tbody, href)` helper that wires both `click` and an Enter/Space `keydown` (guarded to `tr[role="link"]`, `preventDefault` to stop Space-scroll) to the same navigation. Behaviour-preserving for pointer users (identical delegated-click nav); adds keyboard operability. Verified headless (`data.html`, `data-errors=[]`, 78 `#ptbl` rows now carry `role="link" tabindex="0"`); zero visual change. `tests/run.sh check` 147-passed/0-failed; render clean. (Also verified on the way: the earlier line-253 backlog note claiming `#ptbl`'s sortable **headers** lack an Enter/Space keydown handler is STALE — `ux-databook-sort-keyboard` (2026-07-20) already added it at data.html:609; ruled out, no fix needed.)
- ux-sim-factory-caption-bleed — ✅ FIXED 2026-08-26 (mobile/tablet, layout/BLEED — found by the standing `tests/visual_overflow.js` audit at 768×1024, iPad-portrait) — on the `#sim` Simulator route the factory/manufacturing sub-card grid (`#sim-factory-out`, `renderSimFactory`) renders in the right column of the still-two-column `.sim-grid` (300px + 1fr, which only collapses to one column at ≤760px — so at 768px each `.grid.macro` `auto-fit,minmax(140px,1fr)` card is only ~140px wide, ~118px content box). The "Below income-floor · measured" card's `.n` provenance caption ends in the long unbreakable filename token `factory_income_by_province.json` (~30 chars, no default break opportunity at `_`), which at ~118px overflowed **32px past the card's right border** (BLEED) — the exact "text bleeding out of its box" class the audit exists to catch, on a very common exec device (iPad portrait). The codebase already solved this identical defect for the `#expocards` provenance captions (`#expocards .mcard .n{overflow-wrap:anywhere}`, styles.css:446). Applied the same one-line fix scoped to `#v-sim .mcard .n{overflow-wrap:anywhere}` (covers all three sim card grids — `#sim-cards`/`#sim-collat`/`#sim-factory-out` — so no future long-token caption bleeds either). `overflow-wrap:anywhere` only ADDS a break opportunity when a token would otherwise overflow (and, unlike `break-word`, is honoured in the grid track's min-content sizing so the card no longer forces itself wide); normal wrapping of breakable text is unchanged, so zero regression on non-bleeding captions. Verified: the 768×1024 overflow audit is now **clean across all 11 routes** (was BLEED 1 on simulator), the mobile 390px audit stays clean, headless `#sim` render self-reviewed (captions wrap within their card borders, layout/theme intact). `tests/run.sh check` 148-passed/0-failed; CSS-only, no JS touched, `node --check` unaffected.
- ux-status-recent-activity-clip — ✅ FIXED 2026-08-26 (clarity, `status.html` — found by running the standing `tests/visual_overflow.js` audit on the two content pages NOT in its ROUTES list, `live.html`+`status.html`; `live.html` clean, `status.html` had 1 CLIP at every width tested 360/390/768/1440) — the **Recent activity** panel (`.st-act` rows, the git-log of what the autonomous loops actually shipped — the whole reason a reviewer opens this page) clamped each commit summary (`.st-act .sm`, styles at status.html:123) to ONE line with `overflow:hidden;text-overflow:ellipsis;white-space:nowrap` and **no `title`** to recover it, so a summary like "data: ThaiWater flood + rain pulse (2026-08-26, run 32955837826)" was cut off with "…" and unreadable in full — and it clipped even at 1440px (the owner's laptop), not just on mobile. Unlike the `ux-expocards-clamp-title`/`ux-dataroom-truncated-title` precedent (which KEPT the clamp + added a `title` because the clamped text was a SECONDARY provenance caveat inside a dense scannable grid), here `.sm` is the PRIMARY content of each row, so the right fix is to show it in full: replaced the single-line clamp with `overflow-wrap:anywhere` so the summary wraps within its flex cell (the `.st-act` flex row stays `align-items:baseline`, so the date/sha/pillar-chip stay aligned to the first line and the summary flows below), and `overflow-wrap:anywhere` breaks the long unbreakable run-id/filename tokens so nothing bleeds. CSS-only, one line, scoped to `.st-act .sm`; verified the 360/390/768/1440 status.html audit is now **clean** (was CLIP 1 at every width) and the headless status.html render self-reviewed (full summaries visible, panel + hero/pillars/loops intact). `tests/run.sh check` 0-failed; no JS touched, `node --check` unaffected. (NEW backlog note: the standing `tests/visual_overflow.js` ROUTES list omits `#brand`, `#map`, `live.html`, and `status.html` — all audited clean this run EXCEPT status.html, now fixed — so a future run could add `live.html`+`status.html` to that harness's ROUTES to keep them under the standing check; harness lives in tests/, out of the platform/-only loop scope.)
- ux-brand-section-summary-tag-bleed — ✅ FIXED 2026-08-26 (mobile/tablet, layout/BLEED — found by running the standing `tests/visual_overflow.js` audit against the two SPA routes NOT in its ROUTES list, `#brand`+`#map`, at 390/600/768/1000/1440; only `#brand` @ 1000px bled) — the Brand route's `③ Paid` collapsible section summary (`details#sec-paid.compsec > summary > h2.opp`) carries the longest provenance badge in the section-title set — `<span class="tag">MEASURED · Google Ads Transparency</span>` — and `.tag` is `white-space:nowrap` (styles.css:108/449), while the summary `<h2>` is `flex:1;min-width:0` inside a flex `<summary>` (styles.css:953–956). At ~1000px (the two-column band where the SPA content column narrows), the unbreakable ~200px badge could not shrink with the flexed heading, so its ink ran **13px past the `h2`'s right border** — a real BLEED. This is the exact "a provenance pill that is a SENTENCE, not a word, has to wrap" defect the styles.css:970–977 comment documents and fixes — but that rule (`white-space:normal;display:inline-block;max-width:100%`) was applied only to the `.compsub`/`.compsub2`/`.ovsub` SUB-headings, and the collapsible **section summary headings** (`details.compsec/ovsec > summary > h2`) were missed. Extended the identical rule's selector list to cover `details.compsec>summary>h2 .tag` + `details.ovsec>summary>h2 .tag`, so a sentence-length badge wraps inside one continuous pill border when the heading is cramped and stays one line when there's room. Scoped to compsec/ovsec summaries (Macro/Competition/Brand section titles), so it also guards every other long section badge (e.g. `#sec-comp`'s "MOSTLY MEASURED"+"1 ESTIMATED PROXY", `#sec-segments`'s "MEASURED counts"+"ESTIMATED scores"). CSS-only, one selector added to an existing rule. Verified: `#brand` audit now CLEAN at 390/1000/1440, `#acq` (same compsec pattern) stays CLEAN at 1000px, headless `#brand` render self-reviewed (badges within their boxes, theme intact, `data-errors=[]`); `tests/run.sh check` 149-passed/0-failed. (NEW backlog spotted this run: `#overview` @ 1000px still bleeds 9px on `details#sec-ov-collateral.ovsec > #collat-outlook.grid.macro .mcard > .v.down` (text "↓ volatile") — a stat-card VALUE overflow, a different class from this tag-wrap fix; its own future surgical slice.)
- ux-collat-outlook-value-bleed — ✅ FIXED 2026-08-27 (mobile/tablet, layout/BLEED — the backlog item spotted by `ux-brand-section-summary-tag-bleed`; reproduced with a headless chromium probe at 1000×900) — the `#overview` collateral-outlook board (`#collat-outlook.grid.macro`) mixes numeric values (`▼ -4.3%`) with editorial WORD values (`↓ volatile`, `↓ pressure`) in the same `.mcard .v`, which renders at display scale (`--fz-display` 28px mono). At the ~1000px band each auto-fit card content box is ~121px wide, and "volatile" is a single unbreakable word ~130px at 28px mono, so it bled 9px past the card's right border (measured: `over:9` on the "↓ volatile" card only; every numeric card `over:0`). Fixed by scoping these values to `--fz-lg` (22px) — the exact house precedent already used one line above at `#v-trend .mcard .v` (styles.css:425) — so the word wraps intact at its space (2 lines) and numeric values still fit on one line, rather than `overflow-wrap:anywhere` which (tested) broke "volatile" mid-glyph into 3 lines AND shrank the grid tracks enough to force two numeric cards to wrap. CSS-only, one selector. Verified with the chromium probe: `over:0` on all six cards at 1000/390/1440px (was `over:9` at 1000px); numeric values 1 line at every width. `tests/run.sh check` 0-failed; headless `#overview` render self-reviewed.
- ux-forced-colors-dataviz — ✅ FIXED 2026-08-27 (a11y, Windows High Contrast Mode / `forced-colors:active` — the SAME background-strip defect the print-color-adjust series fixed on paper, never addressed for WHCM; `forced-colors` had 0 hits across the whole platform) — in `forced-colors:active` (Windows High Contrast Mode / a forced-colours OS theme) the browser overrides every author `background-color` with a system colour, so the data-viz marks whose ONLY encoding is that fill collapse to uniform system-coloured blocks: the whole-book **delinquency ladder** (`.ic-lad`/`.ic-lad-seg`) + its legend swatches (`.ic-lad-leg i`), the brand/segment **proportion bars** (`.rz-bar span`, `.bar>i`, `.conc-legend i`), and the **MEASURED provenance dot** (`.prov .pd` / `.cc-tag::before`) + the **segment accent bar** (`h2::before`) — exactly the risk-distribution + honesty (measured/estimated) signals the print fixes protect. Added a single `@media(forced-colors:active)` block in styles.css (right after the `@media print` block, mirroring its curated selector set) applying `forced-color-adjust:none` to those NON-TEXT colour swatches / `::before` dots+rails so their author colours (and the estimated border-ring / unlabelled border-square) survive WHCM. Deliberately MORE conservative than the print block: scoped to the swatches ONLY, NOT the text-bearing `.prov`/`.cc-tag`/`.ic-lad-leg`/heading containers, so ALL text still uses the forced palette and keeps its high-contrast legibility. The media query is inactive unless the OS forces colours, so **default rendering is byte-identical** (headless `#home`/`#exposure` renders unchanged); CSS-only, no JS touched, `node --check` unaffected. Completes the "colour IS the information" protection series across BOTH output contexts (print + forced-colors). (NEW backlog spotted: no page declares a `prefers-contrast:more` treatment — a separate, lower-priority contrast dimension from WHCM; left as its own future slice.)
- ux-prefers-contrast-more — ✅ FIXED 2026-08-27 (a11y, `prefers-contrast:more` — the backlog item spotted by `ux-forced-colors-dataviz`; `prefers-contrast` had 0 hits across the whole platform) — distinct from `forced-colors:active` (which forces a system palette): here the OS is set to "increase contrast" but keeps the app's colours, and the app never responded to it. The only marks short of that request are the deliberately-muted tiers — the microcopy tokens `--dim`/`--mid` (captions, provenance notes, chart-axis labels; the light-theme `--dim #5B6678` sits at ≈5.4:1, AA but not AAA) and the quiet hairlines `--line`/`--line2`. Added a single `@media(prefers-contrast:more)` block in styles.css (right after the `forced-colors` block) that pushes each token one step toward its high-contrast neighbour — dark `--dim #909CB2→#BFC8D8`, `--mid #A6B0C4→#CAD2E0`, `--line #2C3650→#4A5878`, `--line2 #202A40→#3A4763`; light `--dim #5B6678→#3C4557`, `--mid #57627A→#3A445C`, `--line #DCE1EB→#B4BECE`, `--line2 #EBEEF4→#CFD6E2` — mirroring the base `:root` + `html[data-theme="light"]` override structure (later in file, same specificity → wins when active). Token-only, so every surface using them lifts at once; the media query is inactive unless the OS requests more contrast, so **default rendering is byte-identical**. Covers the 4 `styles.css` nav pages (index/data/live/status); the 3 deck.gl pages carry their own inline `:root` tokens and are the natural follow-up slice (tracked below). CSS-only, no JS touched; `node --check` unaffected, `tests/run.sh check` 0-failed.
- ux-prefers-contrast-more-3dpages (a11y — spotted while fixing `ux-prefers-contrast-more`) — the three deck.gl pages (`province.html`/`rayong-catchment.html`/`branch-explorer.html`) define their muted-text + hairline tokens in their OWN inline `:root`, so the styles.css `@media(prefers-contrast:more)` block does not reach them. A drop-in per-page `@media(prefers-contrast:more){:root{…}}` mirroring the styles.css values (adjusted to each page's own token names) extends the treatment; CSS-only + headless-verifiable (no map/canvas/gesture behaviour), safe for the unattended loop — a one-page-per-run slice, same cadence as the `color-scheme`/`reduced-motion` deck.gl catch-ups.
  - ✅ `rayong-catchment.html` FIXED 2026-08-27 (the primary 3D entry point per CLAUDE.md — highest-value deck.gl page). Added `@media(prefers-contrast:more){:root{--dim:#BFC8D8;--mid:#CAD2E0;--cardln:#4A5878}}` right after this page's own `:root`/light-theme token block, lifting the muted microcopy tiers (`--dim #6a7589`, `--mid #8a94a8`) toward `--txt` and the card hairline (`--cardln #222c41`) to a clearly-readable line, mirroring the styles.css values. Token-only + inactive unless the OS requests more contrast, so **default rendering is byte-identical** (headless render unchanged); CSS-only, no JS touched, `node --check` unaffected. **REMAINING slice:** `province.html` + `branch-explorer.html` (same drop-in, adjusted to each page's own token names) — next runs.
  - ✅ `province.html` FIXED 2026-08-28 (second deck.gl slice). Added `@media(prefers-contrast:more){:root{--dim:#BFC8D8;--mid:#CAD2E0;--cardln:#4A5878}html[data-theme="light"]{--dim:#3C4557;--mid:#3A445C}}` right after this page's `color-scheme:light` line. Extended the rayong dark-only lift to be theme-aware because province.html — like rayong — has NO light-theme token redefinition, so `var(--dim)`/`var(--mid)` still drive some LIGHT-default text with no per-element override (`.foot`, `.crop .sub`, `.hhinc`, `.wbox .cap`); a dark-only `:root` lift would wash those out on the light default, so the light block DARKENS them toward `--txt` (the exact styles.css `ux-prefers-contrast-more` light values `#3C4557`/`#3A445C`), raising contrast in BOTH themes with no regression. Token-only + inactive unless the OS requests more contrast → **default rendering byte-identical** (headless render unchanged); CSS-only, no JS touched, `node --check` unaffected. **REMAINING slice:** `branch-explorer.html` (next run — same theme-aware drop-in adjusted to its own token names; note the earlier rayong entry's dark-only block shares the same latent light-default wash-out for any token-driven muted text, worth revisiting).
  - ✅ `branch-explorer.html` FIXED 2026-08-28 (**final deck.gl slice — closes `ux-prefers-contrast-more-3dpages` across all three deck.gl pages**). Added `@media(prefers-contrast:more){:root{--dim:#BFC8D8;--mid:#CAD2E0;--line:#4A5878}html[data-theme="light"]{--dim:#3C4557;--mid:#3A445C}}` right after this page's `color-scheme:light` line — substituting this page's hairline token name `--line` (dark `#1d2738`→`#4A5878`, mirroring styles.css's `--line` lift) for province/rayong's `--cardln`. Made it theme-aware like the province slice: although branch-explorer carries far more per-element light overrides than its siblings, a handful of muted consumers (`#bar .sub`, `.pxbtn`, `.ctl .poileg .pl-grp`/`.pl-empty`, `.lead .ln .ds`) use `var(--dim)`/`var(--mid)` with NO light override, so the light block DARKENS those toward `--txt` (`#3C4557`/`#3A445C`) while the dark `:root` lift brightens the dark-default muted tiers + hairline — raising contrast in both themes with no regression (the per-element light hex overrides, being higher specificity, are untouched). Token-only + inactive unless the OS requests more contrast → **default rendering byte-identical** (headless render unchanged); CSS-only, no JS touched, `node --check` unaffected. The `ux-prefers-contrast-more` series now covers all 7 nav pages (4 styles.css + 3 deck.gl).
- ux-clamp-more-btn-ambiguous-label — ✅ FIXED 2026-08-28 (a11y, WCAG 4.1.2 Name/Role/Value + 2.4.4 Link/Button Purpose) — the SPA clamps every long method/caveat `p.lead` to two lines behind a `.clampbtn` whose visible text is the bare word **"more"** (→ "less" when open); a content-dense tab spawns dozens of them (headless-measured: **75 on `#overview` alone**), so a screen-reader user navigating by button heard "more, button, collapsed" 75 times with **no way to tell which paragraph each expands** — all 75 had the identical accessible name "more". Added a `clampLabel(p,expanded)` helper in `app.js` that derives a stable, content-specific `aria-label` from the paragraph's first 60 chars ("Show full text: <lead>…" collapsed / "Collapse text: <lead>…" expanded), set on button creation in `clampLeads()` and re-set on toggle in the `.clampbtn` click handler; the visible "more"/"less" text is untouched (aria-label wins the accessible-name computation) and open/closed state stays on `aria-expanded`. Verified headless: `#overview` PNG **byte-identical** before/after (zero visual change), all **75/75 clampbtn now carry unique labels** (was 75× "more"), `data-errors=[]`. `tests/run.sh check` **151 passed, 0 failed**; node --check clean. (NEW backlog spotted: the long-table fold buttons `.foldbtn` ("＋ N more rows" / "－ show fewer") carry the row-count but NOT which table they belong to — on a page with several folded tables of the same length their accessible names collide; a `clampLabel`-style table-context suffix is the sibling fix — its own future slice.)
- ux-foldbtn-ambiguous-label — ✅ FIXED 2026-08-28 (a11y, WCAG 4.1.2 Name/Role/Value + 2.4.4 Link/Button Purpose — the sibling backlog item spotted by `ux-clamp-more-btn-ambiguous-label`) — `foldLongTables()` (app.js) collapses any table over 8 rows behind a `.foldbtn` whose accessible name is JUST the row count ("＋ N more rows" / "－ show fewer"); on a content-dense tab several folded tables can share the same length, so the buttons collide on the identical name with no way to tell which table each expands (headless-measured on `#exposure`: **18 fold buttons, three of them "Show 5 more rows"** — indistinguishable to a screen reader navigating by button). Added a `foldLabel(tbl,n,expanded)` helper (mirroring `clampLabel`) that suffixes a stable, content-derived context from the table's own column-header `<th>` texts ("Show 5 more rows — Collateral class · Age · Accounts · …"); set on button creation in `foldLongTables()` (escaped via the house `dqEsc` for the double-quoted attr, matching the `title="…${dqEsc()}"` pattern) and re-set on toggle in the `.foldbtn` click handler. Visible text untouched (aria-label wins the accessible-name computation → zero visual change); a header-less bare table degrades to the plain row count as before. Verified headless: `#exposure` all 18 fold buttons now carry distinct labels (the three "5 more rows" now disambiguated by column context), `data-errors=[]`, render self-reviewed (Risk pillar intact). `tests/run.sh check` **152 passed, 0 failed**; node --check clean.
- ux-forced-colors-3dpages-rayong — ✅ FIXED 2026-08-29 (a11y, Windows High Contrast Mode / `forced-colors:active`) — first slice of the deck.gl parity catch-up flagged by `ux-forced-colors-dataviz`: styles.css opts its data-viz swatches out of WHCM's system-palette override with `forced-color-adjust:none`, but the three deck.gl pages define their own inline `:root` and DON'T load styles.css, so their legend colour-swatches + proportion bars (`.sw`/`.bl-bar`/`.occ .bar i`/`.lg-dot`/`.bchip .d` — each built to MATCH its map layer / building-type colour) collapsed to uniform system-coloured blocks under WHCM, breaking the legend→scene colour mapping. Added a `@media(forced-colors:active)` block to `rayong-catchment.html` (the primary 3D entry point per CLAUDE.md → highest-value first) that scopes `forced-color-adjust:none` to those NON-TEXT swatch/bar marks ONLY (never the text rows, which keep the forced high-contrast palette). CSS-only + inactive unless the OS forces colours → default rendering byte-identical; headless-verifiable (no map/gesture behaviour). `province.html` + `branch-explorer.html` remain as sibling slices (same cadence as the color-scheme/prefers-contrast deck.gl catch-ups). `tests/run.sh check` 0-failed; node --check clean; headless render self-reviewed unchanged.
  - ✅ `province.html` FIXED 2026-08-29 (second deck.gl slice). Added a `@media(forced-colors:active)` block right after this page's `prefers-contrast:more` line scoping `forced-color-adjust:none` to its NON-TEXT colour-fill marks only — `.bchip .d`/`.krow .sw`/`.imp .dot` (legend + impact dots), `.occ .bar i` (occupation proportion-bar fill), `#bldglegend .bl-bar`/`#bldglegend .bl-acc .sw` (building-type gradient legend + accent swatch), `#croplegend .cl-k .sw` (crop legend swatch) and `.lg .sw` (generic map legend swatch) — so each mark's author colour (which matches its map layer) survives WHCM's system-palette override; text-bearing rows are untouched and keep the forced high-contrast palette. CSS-only + inactive unless the OS forces colours → default rendering byte-identical (headless render unchanged); no JS touched, `node --check` unaffected. **REMAINING slice:** `branch-explorer.html` (next run — same drop-in adjusted to its own swatch/legend class names).
  - ✅ `branch-explorer.html` FIXED 2026-08-29 (**final deck.gl slice — closes `ux-forced-colors-3dpages` across all three deck.gl pages**; siblings: rayong-catchment + province 2026-08-29). Added a `@media(forced-colors:active)` block right after this page's `prefers-contrast:more` line scoping `forced-color-adjust:none` to its NON-TEXT colour-fill marks only — `.sw` (POI-legend category swatches, krow swatches, and the bl-acc landmark dot, each `background:${rgb(c)}`/`rgb(91,124,250)`), `.bl-bar` (the low→tall building-height gradient legend), and `.bar i` (the occupation-mix proportion-bar fill). In WHCM these collapsed to uniform system-coloured blocks and stopped mapping to the scene; text-bearing rows keep the forced high-contrast palette. Selector set is `.sw,.bl-bar,.bar i` (this page's `.sw` class already covers all three swatch flavours). Inactive unless the OS forces colours → default render byte-identical. Closes the WHCM parity catch-up the print-color-adjust + `ux-forced-colors-dataviz` series flagged for the deck.gl pages.
- ux-route-change-focus — ✅ FIXED 2026-08-29 (a11y, WCAG 2.4.3 Focus Order — the SPA route-change focus-management gap) — `showTab(v)` (app.js) swaps the `.view`, updates `document.title`, `aria-current` and the active nav pill, and `scrollTo(0,0)`, but never MOVED focus into the newly-shown view: after a nav-link click, a content "→" jump-link, or a hashchange, keyboard/SR focus stayed on the old link (or the previous view) with the entire content swap silent — the classic single-page-app route-change defect (a sighted user sees the new tab; an AT user hears nothing and is stranded). The page already had the exact hook — `<main id="main-content" tabindex="-1">` added by `ux-skip-link` — but only the skip-link used it. Added a `moveFocus` flag to `showTab(v,moveFocus)`: when set it focuses `#main-content` (`focus({preventScroll:true})` so the existing `scrollTo(0,0)` stays authoritative), so the new view is the reading start and AT announces the main landmark + the updated title. Passed `true` only from the three USER-initiated paths (nav click, `hashchange`, content `#main-content a[data-v]` link); the boot call (`showTab` on load, app.js:1917) omits it, so first paint never steals focus from the top. The specialised "view on map" jump (`showTab('map')`) is left untouched (the map manages its own focus). Pointer users unaffected (focusing a `tabindex="-1"` container shows no outline and does not scroll); node --check clean, headless renders unchanged. (NEW backlog spotted: the same "view on map" branch-detail jump and the Region→Province→Branch drill re-render content in place without moving focus either — a smaller, in-view variant of this gap; its own future slice.)
- ux-prefers-contrast-more-rayong-light-override — ✅ FIXED 2026-08-30 (a11y, `prefers-contrast:more` — the revisit line 264's province slice explicitly flagged: "the earlier rayong entry's dark-only block shares the same latent light-default wash-out for any token-driven muted text, worth revisiting") — `rayong-catchment.html`'s 2026-08-27 `prefers-contrast:more` block (line 263) was dark-only: `:root{--dim:#BFC8D8;--mid:#CAD2E0;--cardln:#4A5878}` with NO `html[data-theme="light"]` override, unlike its two sibling deck.gl pages (province.html + branch-explorer.html, both theme-aware). This page DEFAULTS to the light theme (per CLAUDE.md the primary 3D entry point) and, like province, has NO per-theme redefinition of `--dim`/`--mid` — so `var(--dim #6a7589)`/`var(--mid #8a94a8)` drive light-default muted microcopy directly. Under OS "increase contrast" the dark-only lift set `--dim:#BFC8D8` — a LIGHT grey — on the light ground, an INVERSION that made muted text LESS readable, the opposite of the request. Added `html[data-theme="light"]{--dim:#3C4557;--mid:#3A445C}` to the block (the exact styles.css `ux-prefers-contrast-more` light values, matching province.html/branch-explorer.html) so light-default muted text DARKENS toward `--txt` under high contrast, raising contrast in BOTH themes with no regression. Token-only + inactive unless the OS requests more contrast → **default rendering byte-identical** (headless render unchanged); CSS-only, no JS touched, `node --check` unaffected. All three deck.gl `prefers-contrast:more` blocks are now theme-aware.
- ux-provenance-note-pending-stale — ✅ FIXED 2026-08-30 (clarity, honesty-of-provenance / factual accuracy) — the SPA's global "One platform, live data." note (`.note` in index.html, shown under `#provinces` and the other content routes) still ended its provenance list with _"Pending the data.go.th run: DIW factories, DLT vehicle registrations."_ — a **factually stale** claim: per CLAUDE.md's ✅ BREAKTHROUGH (2026-07-09) + RETIRED (2026-08-08) notes the departments' OWN CKAN catalogs (`diw-dataset.diw.go.th` factories, `gdcatalog.dlt.go.th` vehicles) are NOT geoblocked and have refreshed **weekly from CI** (`.github/workflows/data-gov-census.yml`, last success ~2026-08-03) with no Thai IP needed, and the committed layers are present (`vehicle_fleet/registry/collateral/mix/brands.json`, factory census) and MEASURED. The very `#provinces` table directly above this note renders populated FACTORIES (e.g. Bangkok 4,847) + VEHICLES (12,373k) columns from those exact sources — so the footer told the exec reader the data was still "pending" while the table showed it live, directly cutting against the project's state-whether-measured-or-estimated honesty principle. Replaced the false "pending" sentence by folding both into the MEASURED provenance list: _"…, IMF/OECD/BoT/MOTS (macro), DIW factories + DLT vehicle registrations (departments' own CKAN catalogs, refreshed weekly)."_ Text-only, one line in index.html; zero layout change (headless `#provinces` render self-reviewed — note now reads accurately, table/theme intact). `node --check` unaffected (no JS touched).
- ux-icscroll-mobile-scroll-cue — ✅ FIXED 2026-08-30 (mobile, discoverability) — the impact-card drill tables (`.ic-scroll` → `.ic-tbl` min-width 760px, `.ic-drilltbl` 960px) that front all four pillars (Home/Assist/Risk/Competition) plus the commodity board render 1047–1548px wide inside a 390px phone viewport (MEASURED via headless isMobile layout), and mobile browsers hide the scrollbar — so their comparative right-hand columns (Crops, Rivals, Agri backdrop, DTI…) sit off-screen with **no cue they exist**. Confirmed NOT a page-x/overflow bug (`document.scrollWidth==clientWidth==390` on every route; each table sits in a working `overflow-x:auto` scroller) — the gap is purely discoverability. Added a theme-aware `--scrollcue` token (dark `rgba(8,11,20,.55)`, light `rgba(24,32,54,.13)`) and a `@media(max-width:760px)` right-edge fade on `.ic-scroll` (`linear-gradient(to left,var(--scrollcue),transparent) right center/26px 100% no-repeat`), painted BEHIND the transparent cells so text stays crisp on top and pinned to the visible right edge via the default `background-attachment:scroll`. Gated to ≤760px where the 760/960px-min tables are guaranteed wider than the viewport, so it NEVER shows a spurious fade on a table that fits (desktop >760px is byte-identical — computed `background-image:none` confirmed). CSS-only, no JS/HTML touched; `node --check` unaffected. Verified: cue present + pinned right at 390px in BOTH themes (computed bg + element screenshots read), absent at 1440px.
- ux-status-grid-minmax-mobile-bleed — ✅ FIXED 2026-08-30 (mobile, layout/BLEED — found by running the standing `tests/visual_overflow.js` audit against the pages NOT in its ROUTES list: the 3 deck.gl pages [clean] + `live.html` [clean] + `status.html` [1 BLEED @ 390×844], confirmed with a headless chromium probe) — `status.html`'s two `auto-fit` card grids used a fixed-px min track — `.st-grid` (pillar cards) `repeat(auto-fit,minmax(300px,1fr))` and `.st-two` (Autonomous loops + Recent activity) `repeat(auto-fit,minmax(320px,1fr))` — so on a 390px phone, where the grid container settles to ~298px, the `minmax()` MIN forced each single-column track to 300/320px (min wins over the 1fr max), overflowing the container by 2px / **22px** respectively: the "Autonomous loops"/"Recent activity" cards' right edge bled past the `.st-two` box (measured card 320px in a 298px track). The classic fixed-px-`minmax`-on-a-narrow-viewport bug. Applied the canonical clamp `minmax(min(300px,100%),1fr)` / `minmax(min(320px,100%),1fr)` to both grids: when the container is narrower than the px floor the min collapses to `100%` (single full-width column, no overflow), and at any width ≥ the floor `min()` resolves to the px value so the multi-column desktop/tablet layout is byte-identical (verified headless: 390px → 1 col / 0 bleed on both; 768px → st-grid 2 cols; 1440px → 3 cols each — unchanged from before). Zero desktop visual change; no page-x introduced. `tests/run.sh check` 0-failed; `node --check` clean. (NEW backlog item spotted while here: the deferred deck.gl-gesture items `ux-viewport-user-scalable-3dpages` + `ux-navmore-3dpages-absolute-overflow` remain open — they need real-device gesture testing, still NOT for the unattended loop.)
- ux-cc-hero-foot-narrow-wrap — ✅ FIXED 2026-08-31 (mobile, layout/BLEED — found by running the standing `tests/visual_overflow.js` audit at the common-Android narrow widths 360×740 + 320×700, below the audit's default 390px [which is clean]) — the command-center hero card's foot (`.cc-hero-foot` in styles.css, the `▶ Watching…` card on `#home`) is a `display:flex; justify-content:space-between` row with NO `flex-wrap`, whose two children are BOTH `white-space:nowrap` — the provenance pill `.cc-hero-tag` ("measured + estimated") and the CTA `.cc-hero-cta` ("Map view →"). On a single-column narrow card (≤~360px) their combined min-content widths exceed the card, and with no wrap and no shrink the CTA bled past the card's right edge (measured 27px over at 360px, 67px at 320px — on the FRONT-DOOR hero, the first card an exec sees on a phone). Added `flex-wrap:wrap` (+ `gap:6px 8px` so a wrapped row gets vertical breathing room) so the CTA drops to its own line when the two don't fit; at any width where they DO fit, `flex-wrap` is inert, so tablet/desktop renders are byte-identical (verified headless: 1100px desktop render shows the foot still a single "measured + estimated … Map view →" row, unchanged). Verified with the Playwright overflow audit: the `.cc-hero-foot` BLEED that fired at 360 + 320 before the change is GONE after it, and no `.cc-hero-*` finding remains. CSS-only, one rule; `tests/run.sh check` 155-passed/0-failed. (NEW backlog spotted while running the narrow-width audit — a broader sub-390px content-bleed cluster, each its own future surgical slice: (i) `#home` `.cc-grid` decision-queue "This week — do these first" table bleeds ~32px at 360 [and the page picks up a PAGEX at 320]; (ii) `#overview` `#macrostrip` Co-pay chip runs its label+value+note together (~15px @360); (iii) `#overview` `.macro-book .mb-head` tab strip + `.rz-panel` collateral-resilience cards bleed a few px @360; (iv) `#acq` `.method li` bullets + the regulatory-shakeout `.insight` block (~24px @360); (v) `#sim` `.scn-grid` scenario cards (~12px @360); (vi) `status.html` `.st-act` activity rows carry an unbroken `run <id>` token that bleeds ~9px @360. The `#home` hero foot was the highest-value [front door] + cleanest [one nowrap-pair] of the set and was taken first; the rest are pre-existing, independent of this change, and NOT introduced by it.)
- ux-cc-grid-minmax-mobile-bleed — ✅ FIXED 2026-08-31 (mobile, layout/BLEED — the front-door sub-390 cluster item (i) flagged by the 2026-08-31 `ux-cc-hero-foot-narrow-wrap` run; reproduced authoritatively with the standing `tests/visual_overflow.js` audit at 360×740 [32px BLEED on `.cc-grid`] and 320×700 [2056px PAGEX + 1782px `.cc-grid` BLEED]; 390px — the audit's default width — is clean, which is why every prior 390-only pass missed it) — the command-center pillar grid `.cc-grid` (styles.css) fronting `#home` used `grid-template-columns:repeat(auto-fit,minmax(300px,1fr))` with a `@media(max-width:340px){…1fr}` guard. On a phone the grid's content box is narrower than the fixed 300px min track (≈268px @360, ≈228px @320 after `main` padding), so (a) @360 the 300px MIN overflowed the 268px container by exactly 32px, and (b) @≤340 the `1fr` fallback (=`minmax(auto,1fr)`, whose `auto` floor resolves to a card child's min-content) blew the single column out to ~2010px → a 2056px page-x on a 320px viewport. Applied the canonical clamp `minmax(min(300px,100%),1fr)` (same fix pattern as `ux-status-grid-minmax-mobile-bleed`): the explicit `min(300px,100%)` track-min collapses to the container width when it's narrower — one full-width column, no overflow at 360 AND 320 — and resolves to 300px at any width ≥300, so the ≥340 auto-fit and the `@media(min-width:1000px)` 2-col desktop layout are byte-identical (verified headless: 320/360/390 → 1 col / PAGEX 0 / 0 grid findings; 768 → 2 cols; 1440 → 2 cols; desktop + 500px-mobile PNGs self-reviewed clean, `data-errors=[]`). The now-redundant+harmful `@media(max-width:340px){.cc-grid{1fr}}` rule (it re-introduced the 320 blowout) was removed — the clamp subsumes it. CSS-only, one rule; no JS touched (`node --check` unaffected); `tests/run.sh check` 0-failed. (NEW backlog spotted while here — the `.cc-grid` grid bleed is gone but 4 smaller SEPARATE content bleeds remain at 320×700 on `#home`, each pre-existing and on its own element, its own future surgical slice: `.cc-card-h` card-header row ~8px, the pillar-card `.l`/`span.l` label ~13-14px, and `span.s` sub-label ~4px — all label/nowrap microcopy inside the now-correctly-sized single column, unrelated to the grid track.)
- ux-acq-insight-arrow-token-bleed — ✅ FIXED 2026-08-31 (mobile, layout/BLEED — the front-door-cluster sibling item (iv) `#acq` flagged by the 2026-08-31 `ux-cc-hero-foot-narrow-wrap` run; reproduced with the standing `tests/visual_overflow.js` audit at 360×740, 24px BLEED on the `#sec-comp` regulatory-shake-out `.insight`) — the Competition `#acq` "Rival fragility" insight callout (`.insight`, static prose in index.html:956) cites Heng's SET-filing decline as arrow-joined figure sequences — `฿13,206.2m→฿9,412.7m→฿8,002.8m`, `1,018→743→450`, `Dec 2024→Dec 2025→Jun 2026` — where the `→` (U+2192 rightwards arrow) is **not** a Unicode line-break opportunity, so `฿13,206.2m→฿9,412.7m→฿8,002.8m` is one ~260px unbreakable run that, in the ~224px content box of a single-column card on a 360px phone, ran **44px of ink past the card's right border** (measured maxTextRight 337 vs padR 293; `scrollWidth−clientWidth` 24). The insight opens when a user expands the "Competitor coverage" section, so it is a real user-reachable bleed. Fixed with the documented house long-token guard `overflow-wrap:anywhere` (styles.css `.insight` rule — same pattern as `#expocards .mcard .n`, `.cc-hero-big`, `#v-sim .mcard .n`, `ux-sim-factory-caption-bleed`): it adds a break opportunity inside an otherwise-unbreakable token only when it would overflow, so the arrow-figure sequence now wraps within the box (on a 360px phone the extreme ฿-sequence breaks mid-figure — the accepted tradeoff vs. text cut off screen; normal prose wrapping unchanged). `overflow-wrap` is an INHERITED property, so the fix also resolved the sibling 16px `details.method .mb li` bleed nested inside the same insight (the peer-scoreboard method note), clearing `#acq` entirely. CSS-only, one property; verified: post-fix the insight's widest text run sits −4px inside the padding box (`dx:0`), the 360px audit dropped 6→4 findings with `#acq` fully clean, the standard 1440+390 audit is **clean across all 11 routes × 2 viewports** (no regression), and the wrapped insight was PNG self-reviewed (all text contained within the rounded border). `tests/run.sh check` 0-failed; `node --check` unaffected (no JS touched). Remaining sub-390 cluster items from the 2026-08-31 run (each its own future surgical slice): (ii) `#overview` `#macrostrip` Co-pay chip label+value+note run-together (~15px @360); (iii) `#overview` `.rz-panel` collateral-resilience card (~3px) + `.macro-book .mb-head` tab strip (~5px) @360; (v) `#sim` `.scn-grid` scenario cards (~12px @360); (vi) `status.html` `.st-act` activity-row `run <id>` token (~9px @360); plus the line-277 `#home` sub-320 microcopy cluster.
- ux-live-kpi-zero-alarm-color — ✅ FIXED 2026-08-31 (clarity, semantic colour — `live.html`) — the "Live board" pulse KPI row (`#lb-pulse`, rendered in `drawPulse`) paints its Aging value in amber (`var(--opp)`) and its Stale value in alarm-red (`var(--agri)`) UNCONDITIONALLY — so a healthy **0 stale feeds** (the current production state) rendered as a red "0", and a 0-aging count would render amber. The card is a traffic-light readout (Current green · Aging amber · Stale red), so a zero glowing in the warning colour reads as a problem where none exists and undercuts the honest-signal ethos ("lead with the answer"). Made the warning colour conditional on the count being non-zero: `style="color:" + (tally.lb_aging ? 'var(--opp)' : 'var(--dim)')` for Aging and `(tally.lb_stale ? 'var(--agri)' : 'var(--dim)')` for Stale, so a healthy 0 shows in neutral `--dim` (matching the already-neutral Reference count) while any real aging/stale count keeps its amber/red alarm. `platform/live.html` only, two inline-style expressions; node --check clean; headless render self-reviewed (Stale "0" now neutral-grey, Aging "2" still amber). Zero change when a count is non-zero.
- ux-macrostrip-gloss-maxwidth-mobile-bleed — ✅ FIXED 2026-09-01 (mobile, layout/BLEED — the front-door-cluster sibling item (ii) `#overview` flagged by the 2026-08-31 `ux-cc-hero-foot-narrow-wrap` run) — the `#overview` "Macro overlay" compact strip's glossed chips (`.mcell.has-gloss` in styles.css — the six jargon chips: Co-pay, Informal work, Self-employed, Agri jobs, Household debt, Policy rate) carried a fixed `max-width:340px`, but on a 360px phone the strip's content box is under 340px (≈325px after `main` padding), so each glossed pill overflowed the strip by ~15px at the right edge (the co-pay chip was the one the cluster named). Applied the canonical house clamp `max-width:min(340px,100%)` (same pattern as `ux-status-grid-minmax-mobile-bleed` / `ux-cc-grid-minmax-mobile-bleed`): the cap collapses to the container width when it's narrower than 340px (single full-width chip, no bleed) and resolves to 340px wherever there's room, so the desktop/tablet strip is byte-identical (`min()` = 340px at any container ≥340px). One rule fixes all six glossed chips at once. CSS-only, one property; verified headless (360px render — co-pay + all glossed chips contained within the strip, gloss wraps cleanly, `data-errors=[]`; 1100px desktop render unchanged, `data-errors=[]`); `tests/run.sh check` 151-passed/0-failed. Remaining sub-390 cluster items from the 2026-08-31 run (each its own future surgical slice): (iii) `#overview` `.rz-panel` collateral-resilience card (~3px) + `.macro-book .mb-head` tab strip (~5px) @360; (v) `#sim` `.scn-grid` scenario cards (~12px @360); (vi) `status.html` `.st-act` activity-row `run <id>` token (~9px @360); plus the line-277 `#home` sub-320 microcopy cluster. (NEW backlog spotted while here: the `#overview` full macro card grid [the commodity/collateral `.mcard` cards, e.g. RICE/RUBBER/HOUSEHOLD DEBT] clips its right column at 360px inside its own `overflow-x:auto` region — a discoverability/scroll-cue candidate like `ux-icscroll-mobile-scroll-cue`, not a page-x bleed; its own future slice.)
- ux-sim-scngrid-minmax-mobile-bleed — ✅ FIXED 2026-09-01 (mobile, layout/BLEED — the front-door-cluster sibling item (v) `#sim` flagged by the 2026-08-31 `ux-cc-hero-foot-narrow-wrap` run; the last of that cluster's grid-track-floor slices, matching the `ux-cc-grid-minmax-mobile-bleed`/`ux-status-grid-minmax-mobile-bleed` fixes) — the `#sim` simulator's "Current scenarios" cards grid `.scn-grid` (styles.css) used `grid-template-columns:repeat(auto-fill,minmax(280px,1fr))` with an UNCLAMPED 280px min track: on a phone whose `#sim` content box drops below ~280px (measured in isolation against the real styles.css — a 260px box ≈ a 320px handset after `main` padding), the single column is forced to 280px and each `.scn-card` BLEEDS ~22px past the container's right edge. Applied the canonical house clamp `minmax(min(280px,100%),1fr)` (identical to the two prior grid fixes) so the track can shrink to the container on a narrow phone while staying byte-identical at every width ≥280px (`min(280px,100%)`=280px there, so desktop multi-column layout is unchanged). Verified in isolation with the real stylesheet: 260px box — card 280px→256px, over-box +22px→−2px (fits); 300px box unchanged. `tests/run.sh check` 0-failed; headless `#sim` render clean. Closes cluster item (v); the sibling (iii) `#overview .macro-book .mb-head` + `.rz-panel` @360 remains open.
- ux-sim-baseline-zero-empty-state — ✅ FIXED 2026-09-01 (clarity, "lead with the answer" — `#sim`) — the simulator's front-and-centre baseline verdict (`renderSimVerdict`, shown ABOVE the sliders on load) read "⚙️ Baseline: 0 provinces in high agri-stress today — 0 branches (0.0% of the network)" whenever `baseHiP===0`, which is the ACTUAL current state every day: measured `crop_stress.json` agri_stress peaks at ~0.21 (×100 = 20.6), well under the `SIM_HI=45/100` high-stress bar, so no province crosses it at baseline (verified: 0 provinces ≥45 today; a max crop-price −30% + drought −30% shock DOES tip 9 provinces over 45, so the tool is not inert). The triple-zero headline reads as an empty/failed-to-load state rather than the real, reassuring "book is clear today" reading — a weak lead-with-the-answer on the one line the page opens with. Added a `baseHiP===0` branch to the no-shock path that reframes it: "Baseline: the book is clear today — no province crosses the high agri-stress bar (agri-stress ≥45/100)" + a sub-line pointing at the crop-price/drought sliders ("see which provinces tip in first"), keeping the ILLUSTRATIVE/estimated caveat + `${TAG_E}` tag. The naming of the bar makes the 0 legible as "under a defined threshold", not "no data". Copy-only, one function; the non-zero baseline and shocked paths are untouched. `tests/run.sh check` 0-failed (node --check on app.js); headless `#sim` render self-reviewed — the new baseline line reads correctly, no visible regression.
- ux-mbnpl-caption-narrow-bleed — ✅ FIXED 2026-09-01 (mobile, layout/BLEED — the front-door-cluster sibling item (iii) `#overview` `.macro-book .mb-head` flagged by the 2026-08-31 `ux-cc-hero-foot-narrow-wrap` run; reproduced with the standing `tests/visual_overflow.js` audit at 360×740, a BLEED on `#macro-book .mb-head`) — the "Conditions at our grain" macro-book heading row (`.mb-head`, styles.css) is a `flex-wrap:wrap; justify-content:space-between` row of two children: the lens-pill group `.mb-navwrap` (which wraps) and the one-line system-NPL backdrop caption `.mb-npl` (`renderMacroBook` in app.js: "System NPL <b>N%</b> (period, state banks) [sparkline] <span class=sub>Q quarters · range X–Y% · low P, high H</span>"). `.mb-npl` was itself a `display:flex` row with NO `flex-wrap`, so on a 360px phone — where `.mb-navwrap` fills the ~268px content box and `.mb-npl` drops to its own line — the caption's own content (272.7px, the trailing `.sub` "N quarters · range…" span the widest) could not fit the box and its last span bled **~4.7px past the right edge** (measured: `.mb-npl` childMaxRight 318.7 vs box right 314; visible as a clipped caption fragment next to the "Collateral value" lens pill). Fixed with the documented house pattern (`ux-fb-h4-tag-nowrap-bleed`, `ux-cc-hero-foot-narrow-wrap`): added `flex-wrap:wrap` to `.mb-npl` (+ `gap:4px 7px` so a wrapped caption line gets a small row-gap) so the trailing caption span drops below the sparkline when the row can't fit, staying within the box; every individual caption item is < the phone content box so wrapping fully contains it. `flex-wrap` is inert when the row fits, so desktop is byte-identical (verified headless: 360/390 → `over:0` / fits; 1100px desktop → `.mb-npl` stays one line, `over:-325`). CSS-only, one rule. Verified: the standing overflow audit is **clean across all 11 routes × 2 viewports (1440+390)** (no regression), the 360px `.mb-head` BLEED is gone, and the mb-head region was PNG self-reviewed (caption contained). `tests/run.sh check` 0-failed; `node --check` unaffected (no JS touched). NOTE — the two OTHER 360px macro bleeds the audit still reports are NOT this item: (a) `#macrostrip` (the compact strip container) is a SEPARATE residual distinct from the now-fixed `.mcell.has-gloss` chip cap (`ux-macrostrip-gloss-maxwidth-mobile-bleed`) — its own future slice, added to the backlog below; (b) `#rz-panels .rz-panel` is a **false positive** — the collateral-resilience bar's minor-segment label ("everyone else") is DELIBERATELY clipped by `.rz-bar span{overflow:hidden;text-overflow:ellipsis}` (the CSS comment: "Labels clip rather than wrap so a narrow panel never grows a second line"), and the audit flags the clipped inner `<b>`'s layout box even though no ink reaches the panel border (confirmed by PNG: "everyon" cut at the bar's right edge, well inside the panel) — investigated, not a bug, ruled out.
- ux-macrostrip-container-narrow-bleed (NEW, mobile — spotted while fixing `ux-mbnpl-caption-narrow-bleed`) — at 360px the `#overview` compact "Macro overlay" strip CONTAINER (`#macrostrip.mstrip`) still reports a BLEED in the standing overflow audit even after `ux-macrostrip-gloss-maxwidth-mobile-bleed` (2026-09-01) capped the glossed `.mcell.has-gloss` chips to the container width — so the residual is a DIFFERENT element than the gloss chip (measured: a non-gloss `.mcell` child at childMaxRight ~329 vs strip box right 314). A separate surgical slice at ≤360px; clean at the 390px default audit width, so lower priority than the sub-390 front-door cluster already cleared.
- ux-macrostrip-mcell-minmax-mobile-bleed — ✅ FIXED 2026-09-02 (mobile, layout/BLEED — closes the `ux-macrostrip-container-narrow-bleed` item spotted on 2026-09-01) — the `#overview` compact "Macro overlay" strip's residual 360px bleed was the NON-gloss `.mcell` FX chip (`USD/THB 33.165 ▲0.78 [sparkline] 2026-08-31`): base `.mcell` is a `white-space:nowrap` single-line pill with NO width cap or wrap, so on a <340px phone strip (measured strip content box ~268px at 360) that chip's intrinsic width (~283px) ran ~15px past the `#macrostrip` right edge (confirmed live: worst `.mcell` over=+15 @360). The earlier `ux-macrostrip-gloss-maxwidth-mobile-bleed` only capped `.mcell.has-gloss` cells; the plain FX/rate chips were uncapped. Applied the same house remedy to the base `.mcell` rule — `max-width:100%;flex-wrap:wrap` — so the trailing segment (the period tag) drops to a second line and the chip stays inside the strip when (and only when) the row can't fit; individual segments keep their own nowrap. Inert at any width where the chip already fits, so desktop/tablet are byte-identical (verified worst-over + wrapped-count identical at 390/768/1440; `.mcell` is used ONLY by index.html's app.js-rendered macro strip, so no other styles.css page is touched). Verified: live measure over +15→0 @360, PNG self-review (USD/THB chip wraps its `2026-08-31` period cleanly onto line 2, contained, reads consistently with the surrounding chips); `tests/run.sh check` 156-passed/0-failed; standing overflow audit clean across 11 routes × 2 viewports (no regression). NOTE — a separate `#macrostrip` container residual reported at ≤360 in the 2026-09-01 note is now resolved by this cell-level cap (the offending cell WAS the container residual). Remaining sub-390 backlog: the line-277 `#home` sub-320 microcopy cluster and the `og:image` share-card residual (line 225).
- ux-cc-card-h-header-narrow-wrap — ✅ FIXED 2026-09-02 (mobile, layout/BLEED — the `.cc-card-h` slice of the line-277 `#home` sub-320 microcopy cluster; reproduced with the standing `tests/visual_overflow.js` audit at 320×700 [8px BLEED on `#cc-whitespace .cc-card-h` "Competitive pressure Competition →"]; 360/390 — the audit's default widths — are clean, which is why prior passes missed it) — the command-center card header `.cc-card-h` (styles.css) fronts every `#home` pillar/queue card (`#cc-queue`/`#cc-tape`/`#cc-whitespace`/`#cc-risk`/`#cc-macro`/`#cc-defend`) as a `display:flex; align-items:center` row with **no `flex-wrap`**: an uppercase title text node + a right-floated `.cc-link` ("Competition →" / "Data book →", `margin-left:auto`). On a single-column card at 320px (the small-Android / iPhone-SE-1 / Fold-cover width) the title + link min-content exceed the card, and with no wrap the link ran 8px past the card's right border. Fixed with the documented house pattern (`ux-cc-hero-foot-narrow-wrap`, `ux-mbnpl-caption-narrow-bleed`): added `flex-wrap:wrap` (+ `gap:4px 8px` so a wrapped line gets a small row-gap) so the `.cc-link` drops to its own line when the row can't fit, staying inside the card; `flex-wrap` is inert at any width where the row fits so tablet/desktop are byte-identical (verified headless: the standing overflow audit is **clean across all 11 routes × 2 viewports [1440+390]** — no regression — and the 320px `.cc-card-h` BLEED is gone). One rule fixes the header row across all four `styles.css` nav pages at once. Desktop `#home` render self-reviewed (pillar-card headers still single-line, "→" links right-aligned — layout unchanged). CSS-only, one rule; `tests/run.sh check` 156-passed/0-failed. Remaining line-277 sub-320 slices (each its own future surgical slice): `#cc-risk .cc-row .l` (~13px), `#cc-defend span.l` (~14px) + `span.s` (~4px) — pillar-card label/nowrap microcopy inside the now-correctly-sized single column.
- ux-ccrow-narrow-stack — ✅ FIXED 2026-09-02 (mobile, layout/BLEED — closes the remaining line-277 `#home` sub-320 microcopy cluster: `#cc-risk .cc-row .l` [~13px] + `#cc-defend span.l` [~14px]/`span.s` [~4px], flagged by `ux-cc-card-h-header-narrow-wrap`; reproduced with the standing `tests/visual_overflow.js` audit at 320×700; 360/390 clean, which is why prior passes missed it) — the command-center rows `.cc-row` (styles.css) are `display:flex; justify-content:space-between` label/value pairs where the value `.r` is `white-space:nowrap` with default `min-width:auto`, so it refuses to shrink below its full text width and STARVES the label `.l`: on a 320px single-column card the `.r` claims most of the ~186px row, leaving `.l` too little to hold its content floor — the 85px `.prov.m` "measured" provenance badge in `#cc-risk` (85px nowrap in a 77px box) and `#cc-defend`'s `.l` crushed to ~7px by the 169px "Hardest to defend" value — so the label bled past the card's right edge. `overflow-wrap:anywhere` on `.l` (already present) can't help a fixed-width badge, and `flex-wrap:wrap` never fires because `.l` can shrink to ~1 char, so the row never wraps — it just squeezes `.l`. Fixed by STACKING the pair below 340px: a `@media(max-width:340px)` rule sets `#v-home .cc-row{flex-direction:column;align-items:stretch;gap:2px}` (+ `.r{text-align:left}`), giving both the full card width (badge 85px < 186px fits; `.r` 169px < 186px fits). Scoped to `#v-home` so `#exposure`'s `#expo-occ` cc-rows (which share the class) are untouched — verified byte-identical exposure probe output with/without the change (git-stash A/B at 320px). Inert ≥341px where the row already fits one line (verified: the standing overflow audit + a focused `#home` probe are **0-bleed / 0-pagex at 320/360/390/768/1440** — home fully clean, no regression). CSS-only, one media block; 320px `#home` render self-reviewed (contained, theme intact); `tests/run.sh check` 156-passed/0-failed. Closes the line-277 sub-320 `#home` cluster. (NEW backlog spotted while here — PRE-EXISTING, NOT introduced by this change [proven byte-identical A/B]: `#exposure` `#expo-occ .cc-row` bleeds at 320px — its occupation-mix rows render in a flex-column where `.l` collapses to `clientWidth:0` and the label text ["Food service5,857,207 establishments…"] bleeds ~11px, plus the `.r` share % ["30.8%"/"21.2%"] bleeds ~10px; ~11 findings at 320, clean at 360+; its own future surgical slice.)
- ux-expo-occ-ccrow-narrow-stack — ✅ FIXED 2026-09-02 (mobile, layout/BLEED — closes the `#exposure` `#expo-occ .cc-row` item spotted 2026-09-02 by `ux-ccrow-narrow-stack`; reproduced with the standing `tests/visual_overflow.js` audit at 320×700 [2 BLEED: `#expo-occ > .cc-card-b > .cc-row` and its `> .r`]; 360/390 — the audit's default width — are clean, which is why prior passes missed it) — the Risk-tab **Borrower-base concentration** bar rows (`#expo-occ .cc-row`, `renderOccConcentration` in app.js) pair a label `.l` (occupation + establishment count) with a value `.r` that carries an **inline `min-width:230px`** wrapping a **180px `.bar`** + a `white-space:nowrap` `%` share. On a ~228px single-column phone card (measured at 320px) the 230px min-width alone forced `.r` 12px past the card's right edge and starved `.l` to `clientWidth:0` (its label then bled), and even once stacked the nowrap 180px bar + ~50px `%` (=230px) still overflowed the 228px card. Unlike the sibling `#v-home` rows the `ux-ccrow-narrow-stack` (2026-08-31) fix stacked, these `.r` cells carry that inline min-width + fixed-width bar, so the plain stack was insufficient. Extended that same `@media(max-width:340px)` block to also stack `#v-exposure #expo-occ .cc-row` (label over value, each full card width) AND, for its `.r` only, `min-width:0!important` (defeats the inline 230px so the stretched value column fits the card) + `white-space:normal` (lets the `%` share drop below the 180px bar rather than run off the edge). Scoped to `#v-exposure #expo-occ` so no other `.cc-row` surface is touched; inert ≥341px where the row already fits one line, so tablet/desktop are byte-identical (verified: 1440px render unchanged — single-line label · bar · right-aligned %). Verified headless at 320px: the two `#expo-occ` BLEEDs are GONE (`.cc-row`/`.r` now within the card box — `.r` right 274=card right, the `%` wraps to a second line at left within `.r`), the element screenshot self-reviewed clean (each row stacks readably, bars + % contained), and the standard **390+1440 audit stays clean across all 11 routes × 2 viewports** (no regression). CSS-only, one media block; `tests/run.sh check` 156-passed/0-failed. (The other 320px findings the audit still reports — `#overview` macro ×2, `#exposure #exposure-products p.lead.cc-provenance`, the `.story-next` block, and `data.html` — are SEPARATE pre-existing sub-320 items, independent of this change and each its own future surgical slice.)
- ux-story-next-lab-narrow-wrap — ✅ FIXED 2026-09-03 (mobile, layout/BLEED — the `.story-next` slice of the sub-320 cluster the 2026-09-02 `ux-expo-occ-ccrow-narrow-stack` note left open; reproduced with the standing `tests/visual_overflow.js` audit at 320×700 [2 BLEED: `#v-exposure .story-next` 15px + `data.html .story-next` 7px]; 360/390 — the audit default widths — are clean, which is why prior passes missed it) — the "Next in the story" tab-footer (`.story-next`, styles.css) is a `flex-wrap:wrap` row of a `.story-next-lab` label + a forward `<a>` link. The label was `flex:none` (= `0 0 auto`), so even though its `white-space` is already `normal` it was pinned to its max-content width on one line and could not wrap: on a ≤~340px single-column phone the longest label ("Still ④ Risk · the time dimension", ~243px) exceeded the ~228px content box and bled ~15px past the footer's right edge (also `data.html`'s "Next in the story · ③ of ⑤ …", ~7px). Root cause: an unshrinkable flex child, not a long token. Fixed by letting the label shrink+wrap — `flex:none`→`flex:0 1 auto;min-width:0` — so its normal white-space wraps in place when the line is too narrow; flex-shrink only engages when the row overflows, so at any width where label+link already fit one line no shrink applies and tablet/desktop are byte-identical (verified with a playwright probe: 1100px → label+link stay one line same-baseline, `.story-next` height 30px unchanged; 320px → label wraps to 2 lines, its right edge 274 = footer box right, contained). One rule fixes `.story-next` across every SPA route + `data.html` that carries the footer. CSS-only, one property pair; verified: the two 320px `.story-next` BLEEDs are GONE (audit 5→3 findings at 320, both story-next cleared) and the standard **390+1440 audit stays clean across all 11 routes** (no regression). `tests/run.sh check` 0-failed. Remaining sub-320 items (each its own future surgical slice): `#overview` `#ov-imfweo > h2` (~12px) + `.rz-panel` collateral-resilience card (~29px), and `#exposure` `#exposure-products p.lead.cc-provenance` (~26px) at 320×700.
